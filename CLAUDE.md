# AICopilot 规则入口 (Claude Code)

本文件是 Claude Code 的接入点，不持有规则内容。规则事实源是本目录的 `AGENTS.md`（它再指向 `资料/AICopilot业务规则.md` 和各专题契约）；改规则只改 `AGENTS.md` 和对应契约文档，不要把规则写进本文件。

@AGENTS.md

修改前端 `src/vues/AICopilot.Web` 时，该目录下的嵌套规则会自动加载。
