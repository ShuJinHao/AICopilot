# 改动复盘与规则沉淀

本文档是 `AICopilot` 的项目滚动复盘入口，最新记录放在最前。每次代码改动完成前必须更新本文件，并判断是否需要把长期结论沉淀到 `AGENTS.md`、`资料/AICopilot业务规则.md`、专题契约或工作区 `../docs/总规则.md` / `../docs/历史核心记录.md`。

默认只更新本滚动文档，不为每个任务新增单独流水文档。只有形成可长期复用的业务规则、技术契约、部署口径或验收口径时，才新增专题文档。

没有形成长期规则时，复盘记录必须写明固定结论“无新增长期规则”及原因。

## 2026-07-17 AICopilot 多服务发布 SDK artifacts 隔离

- 改动范围：修改 `deploy/enterprise-ai/build-and-push.sh` 为每个 .NET 服务设置统一输出目录下私有的 `service-build/<service>` 并传入 `dotnet publish --artifacts-path`；工作区 `deploy/Deploy.ps1` 把源码 detached worktree 从系统 `TMPDIR` 移到 invocation artifacts 根；扩展两级部署门禁并同步测试治理冻结摘要、AICopilot 项目规则、业务规则、部署指南、安全部署契约以及工作区总规则/部署总览。
- 改动原因：正式统一入口先 publish DataWorker 再 publish HttpApi 时，最初两个入口点复用默认 `bin/obj`；第一版隔离每服务 artifacts、第二版把 artifacts 移出 source worktree 后，正式运行仍显示源码项目同时使用 `/private/var/folders/...` 与 `/var/folders/...`。根因是统一入口本身仍通过 macOS `TMPDIR` 创建 detached source worktree，HttpApi 把同一项目识别为不同路径输入并误用错误的 Infrastructure/SharedKernel 依赖图，出现 `Microsoft.IdentityModel.Tokens 8.0.1/8.14.0` 冲突和 MediatR/SharedKernel/JwtBearer/OpenTelemetry 类型缺失。最终把源码快照和每服务 artifacts 一并固定到 `/Users/.../artifacts/deploy/routine/<invocation>`，同时消除共享目录和源码路径别名两类污染。
- 影响面：只改变 AICopilot 本地后端镜像构建的 SDK 中间产物位置；镜像名、服务闭包、源码 SHA、最终 container publish 目录、Harbor 推送、远端 Runner、migration 和运行时业务契约不变。
- 验证命令：`pwsh -NoProfile -File deploy/enterprise-ai/tests/TestDeploymentPolicy.ps1`；在干净 detached worktree 执行 DataWorker 后再执行 HttpApi 的 `dotnet publish --artifacts-path <service-private-root>`；`dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore`；`git diff --check`；工作区 `pwsh ./deploy/Deploy-Changed.ps1 -EdgeReleaseNotes <显式说明>`。
- 验证结果：部署策略测试、测试治理静态门禁、shell 语法和 `git diff --check` 均通过；从 `/private/tmp` 源码快照出发，把两个服务的 SDK artifacts 写入 `/Users/.../artifacts/verification/private-var/service-build/<service>`，按 DataWorker → HttpApi 顺序 publish 均成功。两轮正式统一入口进一步证明 macOS `/private/var/folders/...` source worktree 才是仍存的路径别名来源并保留失败证据；`bash deploy/tests/TestRoutineDeploy.sh` 已覆盖 invocation artifacts 内源码快照、Cloud/AI 远端快照、单 SSH、migration backup、rollback、resume、dry-run 和不兼容候选 fail-fast 并全过。源码快照迁入 invocation artifacts 根后的最终生产发布结果随本批部署记录收口。
- 规则提炼：形成长期部署规则：同一候选内的源码 detached worktree 与多 .NET 入口点私有 SDK artifacts 根都必须位于工作区统一 deployment artifacts 根，禁止落入 macOS `TMPDIR=/private/var/folders/...`、混用 `/private/var` 与 `/var` 路径别名、共享 `bin/obj` 或依赖构建顺序。已沉淀至 `docs/总规则.md`、`docs/上传部署总览.md`、`AICopilot/AGENTS.md`、`资料/AICopilot业务规则.md`、`AICopilot 项目部署与维护指南.md` 和 `docs/AICopilot安全部署契约.md`。

## 2026-07-17 AICopilot 架构升级总计划 v2.1 补充复核

- 改动范围：仅更新 `docs/AICopilot后续AI架构升级计划.md` 和本滚动复盘，将 Kimi 补充指出的 Text-to-SQL/Direct DB 二级读路径、现有 IntentRouting 资产和 `PredictionReadNode` 第一版执行矛盾完整合入 v2.1；未修改生产源码、测试、API、数据库 schema、配置、Cloud、Edge 或部署脚本。
- 改动原因：v2.0 把“八个 typed GET”概括得过宽，遗漏现行契约允许的治理型自由探索；把 `PredictionReadNode` 放进第一版 NodeKind 却没有执行器和验收；同时未把已运行的 IntentRouting Executor/Parser/Fallback 和分散目录写入源码事实，容易让 P0 被误解为从零建设。
- 方案收口：已覆盖正式 Cloud-only intent 固定映射 `CloudReadNode → QuerySemanticAsync → 八个 typed GET` 且严禁 fallback；未覆盖的低频探索显式映射 `GovernedDataReadNode → GovernedSql/TextToSql`，两条路径使用不同权限和 Evidence，Unknown 不得自动进入 SQL。`QuerySemanticAsync` 明确为内部门面而非第九个 endpoint。`PredictionReadNode` 从第一版活动 allowlist 移除，在 PlanCompiler/Validator/Runtime 三层拒绝；当前健康评估只能是 `DerivedFact`。P0 盘点并适配现有 IntentRouting，P6 才统一 Registry。
- 影响面：只影响未来架构评审与实施拆解口径；当前 `Cloud只读数据分析契约.md`、Agent 工作流契约、业务规则和生产行为均未改变。v2.1 仍不是实施授权。
- 验证命令：`wc -l docs/AICopilot后续AI架构升级计划.md`；`awk '/^```/{count++} END{print count; if (count % 2 != 0) exit 1}' docs/AICopilot后续AI架构升级计划.md`；`rg -n '[[:blank:]]+$' docs/AICopilot后续AI架构升级计划.md docs/改动复盘与规则沉淀.md`；`rg -n "CloudReadNode|GovernedDataReadNode|QuerySemanticAsync|IntentRoutingExecutor|PredictionReadNode|KnownButUnavailable|DerivedFact" docs/AICopilot后续AI架构升级计划.md`；逐项检查附录 A 的 33 个 AICopilot 源码引用；`git diff --check -- docs/AICopilot后续AI架构升级计划.md docs/改动复盘与规则沉淀.md`。
- 验证结果：计划共 2807 行，60 个 Markdown code fence 成对闭合，无尾随空格；附录 A 的 33 个源码引用全部存在；Cloud/Governed 双读路径、QuerySemantic 内部门面、现有 IntentRouting 继承、Prediction 三层拒绝、当前健康结果保持 DerivedFact 等核心语义均可检索；`git diff --check` 通过。因本批只修改文档，未运行 solution build、业务测试、数据库 migration 或部署验证。
- 规则提炼：无新增长期规则。原因是本批只修正待审批未来计划的完整性，尚未改变已验收系统。现行长期规则和专题契约继续有效；只有相应阶段获批、实现并通过测试后，才把稳定结论写入正式规则。

## 2026-07-17 AICopilot 后续 AI 架构升级总计划 v2.0

- 改动范围：完整重写 `docs/AICopilot后续AI架构升级计划.md` 为 2550 行 v2.0 总计划，纳入 Kimi 二次评估、AICopilot 当前源码事实和六个开源 Agent 框架固定提交的源码证据；补齐一套节点契约/两种生命周期编排、P0–P7 阶段、R1–R7 运行时地基、Evidence 授权、有限 DAG、受控 Agent Node、Skill 后置物理退役、设备预测独立路线、迁移/回滚和验收矩阵。本批未修改生产源码、测试、API、数据库 schema、配置或部署脚本。
- 改动原因：纠正早期方案中“先删 Skill”“Phase 0 建完整 IntentRegistry”和把 Chat/长任务做成两套节点执行面的风险；明确先修原子 claim、fencing、checkpoint、幂等、取消和不可变快照，再统一 Node/Evidence 平面，最后单轨切换 Plan v2 并删除 Skill。
- 影响面：仅影响未来架构评审和实施拆解材料；文档不是当前执行入口，不授权 Cloud 写入、PLC 直连、模型自主授权、跨项目实施、数据库变更或生产发布。现行 `AGENTS.md`、业务规则和专题契约继续有效。
- 验证命令：`wc -l docs/AICopilot后续AI架构升级计划.md`；`awk '/^```/{count++} END{print count; if (count % 2 != 0) exit 1}' docs/AICopilot后续AI架构升级计划.md`；`rg -n '[[:blank:]]+$' docs/AICopilot后续AI架构升级计划.md docs/改动复盘与规则沉淀.md`；`rg -n "Cloud 永久只读|永不直连 PLC|PlanDraft|EvidenceSetDigest|Skill.*物理退役|fencing|exactly-once|SelectedPluginIds|RequestedCapabilityCodes" docs/AICopilot后续AI架构升级计划.md`；逐项检查附录 A 的 22 个 AICopilot 源码引用；在六个本地只读仓库执行 `git rev-parse HEAD` 和 `git ls-files --error-unmatch` 核对固定提交及附录 B 关键源码；`git diff --check -- docs/AICopilot后续AI架构升级计划.md docs/改动复盘与规则沉淀.md`。
- 验证结果：计划共 2550 行，48 个 Markdown code fence 成对闭合，无非预期尾随空格；22 个 AICopilot 源码引用全部存在；OpenCode、Codex、Cline、Gemini CLI、Goose、Grok Build 的本地 HEAD 全部与文档固定提交一致，附录 B 的关键源码均由 Git 跟踪；核心红线、阶段顺序、EvidenceSetDigest、Skill 物理退役、fencing、非 exactly-once 边界和前端能力选择新语义均可检索；`git diff --check` 通过。因本批只修改文档，未运行 solution build、业务测试、数据库 migration 或部署验证。
- 规则提炼：无新增长期规则。原因是 v2.0 仍是待 Kimi 与人工审批的未来架构评审稿，尚未形成实施事实；当前执行边界继续以 `AGENTS.md`、`资料/AICopilot业务规则.md` 和现行专题契约为准。只有具体阶段获批、实现并通过相应门禁后，才把稳定结论沉淀到正式规则或专题契约。

## 2026-07-16 AICopilot 后续 AI 架构升级计划评审稿

- 改动范围：仅新增 `docs/AICopilot后续AI架构升级计划.md`，整理 Skill 物理退役、无 Skill Plan/Node Contract、耐久工作流、Evidence Context、有限 DAG、受控专业 Agent、设备健康与预测预留、阶段顺序、风险和验收矩阵；未修改 AICopilot 源码、测试、API、数据库 schema、配置、部署脚本或生产环境，也未修改 CloudPlatform 和 EdgeClient。
- 改动原因：将 2026-07-16 关于企业数据分析平台、多插件、子任务并行、长工作流稳定性、Skill 去留和设备预测边界的讨论沉淀为可交给其它 AI 或人工继续审核的完整 Markdown 评审稿，避免后续讨论把 Coding Agent 模式、企业工作流、Agent Profile、独立子会话和远程 Agent 混成同一概念。
- 影响面：文档明确提出“Skill 职责迁移后物理退役、先修串行 checkpoint/恢复再增加 DAG、Evidence 作为统一节点输出但不新增聚合根、设备预测走 Edge→Cloud→AICopilot 只读链”的候选方向；同时明确它不是实施授权，当前 `AGENTS.md`、业务规则和专题契约继续生效。因此本批不改变任何运行时行为、权限、Cloud 只读表面、前端页面或数据结构。
- 验证命令：检查 `AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/Agent工作流与异常契约.md`、`docs/Cloud只读数据分析契约.md`、`docs/DDD聚合根边界.md` 均存在；`awk '/^```/{count++} END{print count; if (count % 2 != 0) exit 1}' docs/AICopilot后续AI架构升级计划.md`；`rg -n "Cloud 永久只读|不直接连接 PLC|PlanDraft|EvidenceSetDigest|Skill 物理退役|Phase 0|跨项目" docs/AICopilot后续AI架构升级计划.md`；`git diff --check -- docs/AICopilot后续AI架构升级计划.md docs/改动复盘与规则沉淀.md`。
- 验证结果：五个正式规则/契约入口均存在；计划文档共 925 行，Markdown code fence 数量为 22、成对闭合；Cloud 永久只读、AI 不直连 PLC、PlanDraft 确认、Skill 物理退役、EvidenceSetDigest、阶段顺序和跨项目授权边界均可检索；独立只读复审进一步确认了评审稿生命周期、现行 Skill 契约差异、替代权限先于删除、ArtifactWorkspace 文件集对账、设备身份和非 exactly-once 语义，相关修正已写入计划；`git diff --check` 通过。由于本批只新增文档，未运行 solution build、后端测试、前端测试、数据库 migration 或部署验证，不据此宣称任何计划功能已实现。
- 规则提炼：无新增长期规则。原因是本文件记录的是待 Kimi/人工继续评审的未来架构候选方案，尚未成为当前执行契约；当前仍以 `AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/Agent工作流与异常契约.md`、`docs/Cloud只读数据分析契约.md` 和 `docs/DDD聚合根边界.md` 为准。后续只有在用户明确批准具体阶段并完成源码、测试和迁移闭环后，才把相应结论写入长期规则或专题契约。

## 2026-07-12 前端会话水合与 resolved-session 动作边界（AI-TEST-UI-001A）

- 改动范围：只修改 AICopilot Web 的 Chat composer/session store、Agent workbench 会话门、前端 Unit/Playwright 测试，以及 AICopilot 测试治理 policy/workflow/行为负例和项目规则文档；未修改 CloudPlatform、EdgeClient、后端 API、数据库 schema、生产配置或生产环境，未部署。
- 根因与复核：`ChatView` 在 `onMounted` 才异步初始化 session list，但 `ChatComposer` 已可交互；旧 watcher 直接观察从 `sessionStorage` 恢复的 raw `currentSessionId`，列表返回后的 `null/raw -> initial` 写入会清空用户刚输入的草稿、Plan 模式和高级面板。深入源码复核又确认 raw stale ID 会被 Chat/Plan/Upload 当成可操作会话，clean null 下 Enter 可能先清空输入再被 store 静默拒绝；初始化最后调用普通 `selectSession` 还会清空刚选择的 Skill/Tool/知识库。此前 Windows smoke 偶发失败、随后同链通过只说明时序不稳定，不代表缺陷消失。
- 生产修复：新增唯一纯谓词 `shouldResetComposerForSessionChange`，composer 观察稳定的 `composerSessionId`，动作授权单独使用 `resolvedSessionId`；`null -> A` 与 `A -> A` 保留草稿、模式、面板、高级选择和同会话附件，只有真实 `A -> B` / `A -> null` / 新建会话才重置。`currentSessionId` 继续只承担持久化候选/列表选择，Chat、Plan、Upload、历史分页、attestation、stream approval、任务、产物、建议问题和刷新入口均以 loaded roster 中实际存在的 resolved session 为目标；未知 session/task/artifact ID 与“合法 artifact ID + 篡改 URL/name”在发请求前被拒绝，产物下载只使用当前 workspace canonical metadata。统一 `isSessionTransitionBlocked` 临界区覆盖初始化、流、任务 mutation、上传、预览、下载、在岗声明、轮询、历史分页和删除；task/workspace/approval 读取按同一代快照原子提交，任一步失败保留上一代可信投影。Function approval 使用 per-session authority-unknown，Agent approval 使用 latest-task scoped authority-unknown；未知时允许编辑草稿，但 Chat/Plan/审批/任务/最终输出 mutation 在 UI 与 store 双层 fail-closed。DELETE 异常先读取 session list 对账：目标仍在才恢复，已消失按成功收口，对账也失败则保持 unresolved。普通 JSON 请求有 30/60/120 秒分级 timeout，SSE 有 10 分钟上限且明确禁止无 idempotency key 的自动 reconnect。
- 测试分类：纯 `composerSession.spec.ts` 以 5 条表驱动 Unit 固化 `null/null`、`null/A`、`A/A`、`A/B`、`A/null`；`chatService.spec.ts` 固化 JSON timeout 和 SSE 断流不自动重连；store/Application guard 以正反例覆盖 stale raw ID、unknown roster/facade、canonical artifact、task-workspace-approval 原子投影、function/agent approval unknown、DELETE ACK-lost、同会话附件恢复、catalog source error 隔离、合法 function approval reactive proxy 和 Plan/Chat 双入口门禁。Playwright 使用受 runner timeout 管理且始终在 `finally` 释放的 session-list/history/approval Promise 屏障，不使用 sleep/重试/扩大 timeout；12 个逻辑场景分别在 desktop/mobile 执行，共新增 24 个 runner case，证明未激活前无 chat/plan/upload POST、初始化期间不能离开 Chat、草稿/高级选择被保留、真实切换会重置、失败恢复不串会话且旧 runtime 不会短暂获得动作权限。
- 测试治理：Vitest 精确基线从 29 文件/132 条升级为 31 文件/184 条，canonical reconcile 同步为 31/184；Playwright 从 22 升为 46 runner case，最终 43 passed/3 个既有 viewport Skip。更新 Vitest roster/content、Playwright source/Skip manifest、canonical workflow/job 和九-workflow manifest SHA；composer regression 与 workflow 旧计数回退继续由治理负例阻断，治理自测保持 4 正/65 负。
- 验证命令：`npm run type-check`；`npm run test:unit -- --reporter=json --outputFile=/tmp/aicopilot-vitest-ui-hydration.json`；`npx playwright test --config=playwright.smoke.config.ts --list`；`npm run test:smoke`；`npm run build`；`dotnet build AICopilot.slnx -c Release --no-restore --disable-build-servers --nologo -v:minimal`；测试治理 behavior/static/repository/discovery；`Test-AgentWorkbenchReleaseCandidateScope.ps1 -IncludeWorkingTree`；`git diff --check`。
- 验证结果：type-check 通过；Vitest 31/31 文件、184/184 tests；Playwright list 46，实跑 43 passed/3 个既有 viewport Skip；Web production build 成功，仅保留第三方 `@vueuse/core` 两条既有 pure annotation 非阻断提示；Release solution 用时 40.48 秒，0 warning/0 error；治理 static 通过，behavior 4 正/65 负全部符合预期，repository 为 AiEval/Architecture/Backend/CloudLive 声明 1/84/699/1，discovery runner 为 6/91/924/1；Agent Workbench RC scope 检查 37 个 working-tree 文件通过，`git diff --check` 通过。独立提交 `f345f94` 推送 Draft PR #59 后，canonical run `29179011823` 的 `build-test` 用时 12 分 04 秒并对账 Architecture 91/91、Eval 6/6、Backend 924/924、deployment 33/33、Vitest 184/184 与 Web build；Simulation run `29179011826` 的 `backend-full-tests` 和 `simulation-rc` 同时成功，三个 job annotations 均为 0。Playwright 当前仍由旧 Simulation workflow 执行，不在 canonical required reconcile，三个 viewport Skip 也未消除，因此 `AI-TEST-UI-001` 仍为 Partial。
- 规则提炼：形成 AICopilot 前端长期规则，已写入根 `AGENTS.md` 与 `src/vues/AICopilot.Web/AGENTS.md`：raw session 不得作为动作目标；初始水合/A→A 不得破坏 composer/附件；真实跨会话必须清理；所有 ID 必须验证 roster/ownership 并使用 canonical projection；任务/工作区/审批必须原子刷新，authority unknown 必须 fail-closed；DELETE ACK-unknown 必须对账；非幂等 SSE 禁止自动重连；初始化错误只能由同源成功重试清除。未写入 `资料/AICopilot业务规则.md` 或工作区 `../docs/总规则.md`，因为这是 AICopilot Web 的会话状态技术契约，不改变三项目业务边界。
- 收口边界：生产代码与测试已提交为 `f345f94` 并由 run `29179011823/29179011826` 证明当前 SHA 的 canonical/Simulation 全绿；本记录随后只补充远端证据，不改变生产源码、测试源码、workflow 或冻结 SHA。create/upload/Chat/Plan/approval SSE 的 durable client operation id、唯一 receipt 与结果回放已登记 `AI-SEC-054`，在完成前 ACK-unknown 不得自动重放或宣称确定失败。PR #59 仍为 Draft，未 merge、未部署、未修改生产数据。

## 2026-07-12 Linux 部署恢复与 canonical 证据失败传播（AI-TEST-001A）

- 改动范围：只修改 AICopilot `deploy-release.sh` 的 trap 恢复成功返回、deployment behavior fake 外部依赖场景、`aicopilot-ci/build-test` 证据 pipeline 以及测试治理 policy/behavior；同步项目规则、安全部署契约、AI 治理清单和本复盘。未修改 CloudPlatform、EdgeClient、业务服务、数据库 schema、真实配置或生产环境，未部署。
- 首次远程证据：修复跨 OS discovery 后，GitHub canonical run `29172644694` 的 Architecture 91/91、AiEval 6/6、Backend 924/924、Vitest 132/132、build 均成功；deployment behavior 在原生 Ubuntu 的“可逆容器失败”场景把期望的原始 `58` 变成 unsafe-partial `86`。workflow 原使用 `bash ... | tee ...` 且未开启 `pipefail`，该 step 被 `tee=0` 局部假绿；末尾 reconcile 因仅看到 24/33 条 `TEST` 而正确将整个 job 拉红。因此准确定性是“step 失败传播错误+最终对账成功拦截”，不夸大为整个 CI 已假绿。
- 根因：`deploy-release.sh` 在 `EXIT` trap 中带着原始失败码 `58` 执行 `restore_previous_runtime`。Ubuntu Bash 5.2 中，`wait_for_compose_service_healthy` 成功分支的 bare `return` 会继承 trap 保留的 `58`；所以日志虽打印 `postgres is healthy`，恢复链却立即中断，全部应用 runtime 未重建。macOS Bash 3.2 同一代码返回 0，因而本机 33/33 无法暴露。`probe_web` 的 200 分支也有同类潜伏问题。独立复审进一步确认，`finish_deploy_release` 先解除 trap，恢复链内遗留的健康/探针 `exit` 会直接终止父 Shell，绕过 `86`、blocked evidence 和 invocation state。逐项比对 `.env`、current/previous/staged release state、summary 与 transaction backup 全部一致，排除了文件 state restore、Docker 和 Linux 文件系统猜测。
- 修复与测试：健康等待的 `healthy/running` 和 Web 200 三个提前成功分支改为显式 `return 0`；`restore_previous_runtime` 整体改为可捕获的子 Shell，使内部直接 `exit` 也能回到父 Shell 统一收口。对账查询 fake timeout 从不可覆盖的 2 秒改为可注入、默认 10 秒，不改生产 20 秒默认。现有可逆成功场景必须复建全 runtime，执行 runtime inspect、Web probe、HTTP-only 安全头和 release attestation，保留 `58`且不留状态；既有恢复失败场景改为真实安全头探针内部 `exit`，必须得到 `86`、blocked state/lock/backup 和 `unsafe-partial` terminal invocation state。两者不增加 33 场景计数。canonical workflow 改为显式 Bash + `set -euo pipefail` + `2>&1 | tee`；治理行为测试新增删除 `pipefail` 和删除 stderr 捕获两条负例。
- 验证结果：治理 static 通过，behavior 为 4 个正例/63 个负例；macOS Bash 和原生 Ubuntu 24.04/Bash 5.2 均完整通过 deployment behavior 33/33，Ubuntu 同时证明恢复成功全链保留原始 `58`、恢复内部探针失败统一收口为 `86`。`bash -n`、`git diff --check` 通过；Release build 用时 53.66 秒，0 warning/0 error，四个旧 xUnit 程序集门禁全部通过。新提交的 GitHub canonical/Simulation 结果在本记录收口前补齐。
- 规则提炼：形成两条长期规则，已写入 `AGENTS.md` 和 `docs/AICopilot安全部署契约.md`：`EXIT` trap 恢复链禁止 bare `return`，提前成功必须 `return 0`，整体在子 Shell 中可捕获并由 native Linux 同时验收成功/内部失败；经 `tee` 保存的 required 证据流必须开启 `pipefail` 并收集 stderr。未提升到工作区总规则，原因是当前实现和证据链只约束 AICopilot 部署脚本/canonical workflow。

## 2026-07-12 enabled Admin 并发不变量闭环（AI-SEC-051）

- 改动范围：只修改 AICopilot Identity 管理员禁用/角色降级、migration identity seed、PostgreSQL advisory transaction lock、初始化状态 DTO 及对应 Backend/Architecture/Web smoke 测试；抽取测试专用 Identity manager/persistence helper，补充管理员不变量真实 PostgreSQL 竞争测试；同步 `AGENTS.md`、AI 治理清单和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 只读边界、数据库 schema、生产配置或部署环境。
- 源码审核：`PostgresIdentityEnabledAdminInvariantGuard` 只允许在当前 `IdentityStoreDbContext` transaction 和同一已打开 connection 上调用 `pg_advisory_xact_lock`；`PersistenceCommitEngine` 的官方 execution strategy 每个 attempt 都重新执行 handler callback，因此 Disable、Demote、Seed 每次都先重新加锁，再查用户、角色和 enabled Admin。多角色 Admin 按真实 Admin membership 判断，不依赖排序后的第一个角色；非成功 Result 回滚业务事务后，Rejected audit 通过 `IdentityRejectedAuditCommitter` 另开事务提交。
- 行为闭环：真实 PostgreSQL barrier 覆盖 disable+disable、disable+demote、demote+demote、seed+disable、seed+demote和多角色最后 Admin；新增 transient retry 交错让第一次 attempt 在取得锁并读取后失败，竞争事务随后禁用另一 Admin，第二 attempt 必须重新取得同一锁、重读竞争提交并拒绝自身修改。waiter 查询已限定当前 database、固定 `ResourceKey` 的 `classid/objid` 和 `objsubid=1`，不再用数据库内任意 advisory waiter 冒充目标锁。
- Seed/契约：seed 不会重新启用被人工禁用的 bootstrap Admin；有另一 enabled Admin 时保留 disabled bootstrap，最终仍为零 enabled Admin 时明确失败并整体回滚。初始化 DTO 从 `HasAdminUser` 收紧为 `HasEnabledAdminUser`，后端、前端、Fresh DB、runtime smoke 和 fake-server consumer 已同步，旧名称全仓归零。
- 删除边界：当前生产树没有 DeleteUser API。Architecture 门禁扫描整个生产 `src` 并冻结现有三条减员表面；新增删除或其它减员入口必须显式评审并接入同一锁。该门禁仍属于源码词法门禁，变量别名、helper 或直接 DbContext 写入最终必须由 `AI-ARCH-001` Roslyn/MSBuild Analyzer 接管，不能冒充编译语义保证，也不能为满足清单制造不存在的 DeleteUser 空壳 API。
- 相邻债务：`UpdateRole` 仍可能移除 Admin 的关键恢复权限；它不减少 enabled Admin 数量，因此登记为独立 `AI-SEC-052`，不混入 AI-SEC-051 人数不变量。
- 失败诊断：第一次完整 Backend 为 898/924、0 Skip，26 个失败全部来自同一个共享 fixture 在 `Start distributed application` 两分钟超时，业务测试体未进入。TRX/DCP 证明 migration/seed 已完成，真正阻塞是本机 RabbitMQ 缓存以 `linux/amd64` 在 Apple Silicon 上运行，AMQP `connection.start` 约 112 秒未返回；其余五个 fixture 均在约 23–26 秒启动。拉取同 tag 的原生 `linux/arm64/v8` 镜像、确认无残留 DCP/容器后，按 canonical 顺序单独复跑 Backend 924/924、0 Skip、4 分 56 秒。首次红测和根因保留，未用盲目重跑覆盖。
- 规则提炼：形成两条长期规则，已写入 `AGENTS.md`：所有减少 enabled Admin 的路径必须在同一 Identity transaction 内先取得固定全局 PostgreSQL transaction advisory lock，并在 execution-strategy retry 中重新加锁/重读；Admin 人数存在与最小恢复权限是两个不变量，后者由 AI-SEC-052 单独治理。未提升到工作区总规则，原因是两项均为 AICopilot Identity 内部边界。
- 验证命令：`dotnet build AICopilot.slnx -c Release --no-restore --nologo -v:minimal`；`dotnet test ...BackendTests.csproj --filter 'FullyQualifiedName~IdentityEnabledAdminInvariantTests|FullyQualifiedName~IdentityPersistenceCommitTests'`；真实 HTTP last-admin/FreshDatabaseSeed 定向测试；完整 Architecture/AiEval/Backend TRX；测试治理 behavior/static/repository/discovery；deployment behavior；Web Vitest JSON/build；required-result reconciliation；`git diff --check`。
- 验证结果：Release build 0 warning/0 error；管理员不变量新增 retry 用例单独 1/1，HTTP last-admin 禁用/降级加 Fresh DB 3/3，Architecture 91/91、legacy Eval 6/6、Backend 最终 924/924、Vitest 29 文件/132 条、deployment behavior 33 场景通过，均为 0 Skip；repository 785/785、discovery 1022/1022，测试治理 3 个正例/61 个负向变异通过。CloudAiReadLive 按 Manual/Release 规则未运行。独立提交 `88a9687` 已推送 Draft PR #59；GitHub run `29170924668` 的 `backend-full-tests` 与 `simulation-rc` 均成功，Architecture 91/91、Backend 924/924，两个 job annotations 均为 0。旧 Simulation 的 Linux Docker/PostgreSQL suite 因 Windows runner 分支仍被跳过，只能证明 skip-profile acceptance 12/12，不冒充真实 Linux Simulation；另记录 1 个 ECharts moderate vulnerability。未 merge、未部署、未修改生产数据。

## 2026-07-12 AICopilot 测试架构 Phase 0 止血与资产冻结（AI-TEST-001）

- 改动范围：只新增 AICopilot 测试治理 policy/behavior/baseline/waiver、统一 `failSkips` runner、CODEOWNERS、LF `.gitattributes` 和 `aicopilot-ci` required 候选 workflow；扩展根 `Directory.Build.targets`，让 Release build 触发静态、测试程序集和 runner 配置门禁；同步本项目规则、AI 治理清单和本滚动复盘。未修改 CloudPlatform、EdgeClient、生产业务行为、Cloud 只读边界、数据库 schema、真实配置或部署环境；以已独立审核、提交并远端绿测的 AI-SEC-051 `88a9687` 作为冻结前置边界。
- 改动原因：现有 `BackendTests` 把 Unit/Aggregate/Application/Workflow/Contract/Persistence/HTTP/E2E/Deployment 混在 120 个文件中，`ArchitectureTests` 仍以 3189 行源码字符串/Regex 为主，独立 AiEval 没进普通 PR，部署 33 个行为场景没进 PR，Windows Simulation 在 Docker 不可用时会成功 Skip；测试项目、声明、数据行、runner 数、Skip、CI 和 CODEOWNERS 都没有一套 fail-closed 资产边界。
- 资产冻结：当前 Release 基线包含 4 个 xUnit 项目、785 个声明、785 个 execution template、976 个静态投影和 1022 个真实 runner case；Backend 为 699/883/924、Architecture 84/91/91、AiEval 1/1/6、CloudAiReadLive 1/1/1。7 个 MemberData 来源、245 个 InlineData payload 使用真实 `CustomAttributeData` 签名。门禁同时精确固定 32 个 csproj、32 个 solution project、2 个 `Directory.Build` 文件、9 个 workflow、4 个测试 csproj、support host 源码、Backend 120 个测试源文件及内容哈希、Vitest 29 个源文件/package/config/lock、Playwright legacy smoke/config、deployment policy 和 33 场景 behavior 脚本。基线 provenance 已从临时 worktree overlay 收口为 `Reviewed`；`sourceHead=88a9687` 明确表示被独立审核的源码快照，不冒充随后产生的治理提交 SHA。`.gitattributes` 将所有字节 SHA 资产固定为 LF。
- CI 与执行：新增 `aicopilot-ci/build-test` 为无 path-filter 的 25 分钟 Ubuntu job，Checkout 后在 restore/npm ci 前先运行 behavior + static governance，再固定执行部署静态策略、Linux Docker preflight、Release solution build、repository/discovery 对账、Architecture 91、legacy Eval 6、Backend 924、TRX 计数/Skip 对账、deployment behavior 33 日志对账、Vitest 29 文件/132 条 JSON 对账和 Web build；CloudAiReadLive 不进入普通 PR。整个 workflow/job/step 顺序、全部 workflow identity、CODEOWNERS、根 targets、共享 props 和 runner 内容均受精确 SHA/清单保护。平台盘点确认 `main` 当前无 branch protection/ruleset，仓库只有 `ShuJinHao` 一个管理员协作者；CODEOWNERS 文件本身不构成审批，直接要求 Code Owner review 会让唯一作者无法自批而永久锁死。先验证并要求 status context；加入独立 reviewer 后才能启用 required Code Owner review，届时才闭环同 PR 自改 policy/workflow 的风险。
- 证据边界：现有 AiEval 中 tool safety 与 formatter 走生产实现，但 approval/prompt-injection 两类仍是输入自证，本轮只能称 legacy eval continuity，不能称 Golden quality gate。现有 ArchitectureTests 仍混合反射、XML、字符串和 Regex，并在 build 后由 CI `dotnet test` 执行；它不是“架构违规导致编译失败”。跨层、循环依赖、直接 DbContext/SQL、绕聚合根和插件/Cloud-readonly 边界必须由 `AI-ARCH-001` Roslyn/MSBuild analyzer 以 compiler error 落地。旧 Simulation PR workflow 仍重复 full suite、Batch filter 和 Docker-aware skip，远端退役前 `AI-TEST-001/003` 不能关单。本轮 canonical `aicopilot-ci` 的 checkout/setup-dotnet/setup-node/upload-artifact 已 pin 40 位 commit SHA；其他 8 个既有 workflow 仍有 major tag，属部署供应链和 `AI-TEST-003` 后续债务，不得宣称全仓已 pin。生产/test fixture/case clone ratchet 保留给 `AI-TEST-DUP-001`。
- 冷启动诊断：完整 Backend 首轮 922/923、0 Skip，唯一失败为共享 Aspire fixture 在 `Start distributed application` 两分钟超时；当时进程证据显示 Docker Desktop Linux VM 刚被唤起。Docker 预热后精确失败用例 1/1 通过，随后同一完整命令 923/923、0 Skip、4 分 58 秒通过。该结果支持本机冷启动归因，但不得冒充 GitHub hosted runner 或远端 25 分钟 required job 已验证。
- 首次远端红测与修复：Phase 0 提交 `2d06efd` 的 canonical run `29171520017` 在 `ValidateDiscovery` 失败，墙钟 6 分 08 秒，不是超时。Repository 的 785 声明与四项目 runner 数 6/91/924/1 均一致，但 AiEval/Backend digest 跨 OS 漂移；后续 Architecture/Eval/Backend/deployment/Web/reconcile 均未执行，annotations 为 1 failure + 1 missing-artifact warning。独立复现证明 AiEval 来自 xUnit 按不同绝对路径长度先截断 display name，Backend 来自 PowerShell `Sort-Object` 文化排序。修复没有降级成 count-only：仅将已截断且位于当前 workspace 根下的绝对路径字符串参数规范为 `<ABSOLUTE_PATH>`，用 `StringComparer.Ordinal` 稳定排序，并用硬编码跨 OS 顺序和重复项夹具证明根相对 API、JSON Pointer、协议相对 URL、外部绝对值、相对业务数据和 case 重数不会被擦除。使用 Linux .NET SDK 10.0.301、GitHub 相同工作目录从 `2d06efd` 干净重建后，AiEval 6、Architecture 91、Backend 924、CloudLive 1 的 digest 全部与 macOS 同一 baseline 通过。原 baseline 还写入但从未验证 raw assembly SHA，且该字节值受构建平台/路径影响；本修复物理删除该伪保证，以声明/参数/trait 元数据、冻结源码内容和跨 OS runner digest 作为权威证据。
- 规则提炼：形成长期规则，已写入 `AGENTS.md` 的 `Test Architecture Governance`：四个旧桶冻结、typed project 迁移、support host、唯一 runner/baseline/workflow、Required Skip=0、CloudLive 分流、legacy Eval 不冒充 Golden、Architecture 字符串门禁不冒充 Analyzer，以及生产/测试重复代码分开 ratchet。工作区级执行顺序和三项目目标架构继续以 `../docs/三项目测试架构治理总计划.md` 为准。
- 验证命令：
  - `pwsh -NoLogo -NoProfile -File scripts/tests/TestAICopilotTestGovernanceBehavior.ps1`
  - `pwsh -NoLogo -NoProfile -File scripts/tests/TestAICopilotTestGovernancePolicy.ps1 -Mode ValidateStatic -Configuration Release`
  - `dotnet build AICopilot.slnx -c Release --no-restore`
  - `pwsh -NoLogo -NoProfile -File scripts/tests/TestAICopilotTestGovernancePolicy.ps1 -Mode ValidateRepository -Configuration Release`
  - `pwsh -NoLogo -NoProfile -File scripts/tests/TestAICopilotTestGovernancePolicy.ps1 -Mode ValidateDiscovery -Configuration Release`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj -c Release --no-build --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.AiEvalTests/AICopilot.AiEvalTests.csproj -c Release --no-build --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --no-build --no-restore --logger "console;verbosity=minimal"`
  - `bash deploy/enterprise-ai/tests/deployment-behavior.sh`
  - `cd src/vues/AICopilot.Web && npm run test:unit && npm run build`
  - `git diff --check`
- 验证结果：治理 4 个正例与 61 个负向变异通过（新增跨 OS runner path/ordinal 规范化正例；原 11 个 snapshot、49 个 static、1 个 output runner 负例保持）；Release solution build 0 warning/0 error，并实际触发 4 个程序集和 4 份输出 runner 校验；repository 785/785、macOS discovery 1022/1022，独立 Linux 干净重建 discovery 也为 6/91/924/1；Architecture 91/91、legacy Eval 6/6、Backend 最终 924/924、前端 29 文件/132 条、deployment behavior 33 场景此前本地均通过且 0 Skip（Playwright smoke 未进入 required，本轮内容冻结其 3 个既有 viewport Skip）。前端 build 保留第三方 `@vueuse/core` pure annotation warning但退出码为 0。首次 canonical 远端红测已保留，修复 commit 尚未 push；未部署、未执行 CloudAiReadLive、未修改生产数据，也未验证修复后 GitHub 25 分钟全链、branch protection 或 Code Owner required review。

## 2026-07-11 Identity 与文件提交结果对账收口（AI-PERSIST-01c）

- 改动范围：仅修改 AICopilot Identity 事务、AiGateway/RAG 数据库绑定文件上传、持久化文件对账、commit marker retention、DataWorker/compose 配置、相关 migration、Backend/Architecture 测试和持久化/部署规则文档；未修改 CloudPlatform、EdgeClient、Cloud 只读边界、Agent 工作流业务语义或生产环境，未执行 merge、Harbor push、SSH 或部署。
- Identity 事务：物理删除 `EfTransactionalExecutionService`，`IdentityTransactionalExecutionService` 复用唯一 `PersistenceCommitEngine` 和官方 execution strategy。`IdentityStoreDbContext` 记录一次 attempt 内 UserManager/RoleManager 的中间保存；所有返回 `Result` 的 Identity 写命令统一走 `ExecuteResultAsync`，非成功结果抛内部回滚信号，事务回滚后再把 DeleteRole、DisableUser、Cloud OIDC 的拒绝审计通过唯一 `IdentityRejectedAuditCommitter` 独立提交。通用 `ExecuteAsync<TResult>` 对 `IResult` 类型 fail-fast，防止后续绕回“失败 Result 仍提交”；创建用户不再对刚创建且必然无角色的用户执行冗余角色查询/清空。
- Identity 单命令完整性：Create/Update/Delete/Enable/Disable/Reset/Cloud OIDC 路径检查所有 IdentityResult；角色权限同步改为差量增删并去掉重复 claim，不再全删全加；用户角色未变化时不重复移除/添加角色或刷新 security stamp；migration seed 把角色、权限和 BootstrapAdmin 收口到同一事务并保持幂等。本项只关单 Result 回滚、IdentityResult 检查和差量更新，不代表跨命令“至少一个 enabled Admin”并发不变量已闭环；该既有 P1 已登记 `AI-SEC-051 Partial`。
- 文件提交边界：新增 `IPersistenceFileStorageService`、scoped commit id 和 durable reconciliation journal。RAG `UploadDocument` 与 AiGateway `SessionTemp` / `AgentInput` `UploadRecord` 固定为“先原子落 journal、再以 `WriteThrough` 写物理文件、最后让 repository marker 复用同一 commit id”；没有真实业务行变化时禁止确认文件，同一 scope 禁止叠加未完成 stage，路径拒绝 `..`、绝对路径、大小写旁路和既有 symlink/reparse traversal。Windows fakepath/反斜杠先转安全 basename，控制字符拒绝、UTF-8 名称受限，拒绝审计也不落原始客户端路径。commit 已确认只删 journal，明确回滚先删文件再删 journal，结果未知则保留二者等待对账。
- 对账与部署：请求侧、RAG 删除 consumer 和 DataWorker 复用唯一 PostgreSQL advisory-lock helper，并用 namespaced commit id lease 防止活跃上传被后台或删除事件清理。删除事件按 storage path 查 journal、锁内复查、先持久退休 journal 再删文件；不可读/active 让 MassTransit 重试。DataWorker 只处理超过安全延迟且成功取得 lease 的记录；有 marker 且文件存在时保留文件，无 marker 时删除文件；journal 损坏或“marker 已提交但文件缺失”均保留证据并 fail-closed。journal/file 删除后的目录项即使已不可见，也要再次完成父目录 durability barrier 才能继续；单条失败只保护对应 marker，不阻断无关 marker GC。HttpApi/DataWorker/RagWorker 共享 `/var/lib/aicopilot` 为硬部署契约，两个文件根固定在共享卷内且不能被 `.env` 绕开。默认每 300 秒扫描、延迟 10 分钟、单批 100、marker 保留 30 天；新增 `ix_commit_markers_created_at_utc` migration，存在 pending journal 的 marker 不删除，禁止 cron/手工 `rm`/第二套清理器。
- 冗余与垃圾清理：删除旧 Identity 双轨事务实现、公开但零生产调用的 `IFileStorageService.SaveAsync`、零调用 `UploadRecord.MarkFailed()`、三组重复 `CapturingFileStorage`、四组重复 `FixedDocumentFormatPolicy`、ToolRegistry 的 no-op 文件服务、Identity 集成测试中被真实 PostgreSQL 测试替代的重复回滚用例，以及 Backend 中与 ArchitectureTests 完全相同的两条源码字符串门禁；RAG/AiGateway 重复的 unknown/rollback/confirm catch 收口为唯一窄职责 `PersistenceFileCommitProtocol`，callback 漏保存时由 commit id 消费状态 fail-closed 回滚。PostgreSQL session lock 与文件对账 lease 共用唯一参数化 advisory-lock 实现，没有复制 SQL/key 算法。没有新增万能基类、反射式 handler、兼容壳或第二套清理状态机。
- KnowledgeBase 影子链止写：源码消费者审计确认知识库前端只调用 `/api/rag/document`，AiGateway `KnowledgeBase` upload 生成的 `UploadRecord` 没有可读 `StoragePath`、不进入 Session/Task list，若作为 `uploadId` 传给 Agent 也只能得到无内容元数据。继续为这条同步 RAG→AiGateway 双写补 saga 会把垃圾链复杂化，因此本批从 Controller/Command/前端类型删除 `knowledgeBaseId`，只允许 SessionTemp/AgentInput；物理删除 `IRagDocumentUploadBridge`、实现和 DI；领域构造器拒绝 KB scope，所有 UploadRecord 查询 spec 过滤为 active scope。知识库文档唯一写入事实源是 RAG Document。
- 测试架构：`Suite=PersistenceFileStorage` 单独承载 7 个纯本地文件系统用例，不再错误标记 Docker；`Suite=PersistenceCommit` 承载 37 个真实 PostgreSQL 用例，覆盖 Identity 无操作/失败 Result/真实 UserManager 中间保存回滚/pre-commit retry/ACK lost、RAG/UploadRecord 文件 journal 未知结果恢复、无业务行拒绝确认、活跃 lease、损坏 journal、缺失已提交文件、删除事件竞争和 marker retention。MigrationSafety 验证真实索引，ArchitectureTests 锁定唯一事务/文件/host 注册边界及 KB bridge 不得回潮；测试文件服务与格式策略只保留一份共享实现。
- 首轮失败与修正：首次 AppHost 定向测试发现 `PersistenceFileMaintenanceService` 被错误注册到通用 EF 模块，导致不拥有文件 journal 的 MigrationWorkApp DI 校验失败；已把 maintenance 只注册到 DataWorker 并增加反向架构门禁。全量首轮又命中仍检查旧 `ExecuteAsync` 字符串的重复 Backend 门禁；没有维持两份同义测试，而是删除 Backend 副本、由 ArchitectureTests 唯一负责结构规则。独立复核随后消除了 Cloud OIDC retry 读取旧实体、JIT 补偿删除、commit-id 参数校验前泄漏、错误 stage/lease 配对、文件/目录 durability 缝隙、marker GC 全局饥饿、Windows 客户端路径泄漏、删除事件绕过 pending journal、compose 根目录可绕共享卷、固定 Identity 文件数组、KB shadow 双写和 Failed/Deleted 历史行回流。Identity 门禁改为动态发现全部 Commands Result handler；真实 UserManager、SessionTemp/AgentInput UploadRecord 与 unknown-commit 后删除事件均补 PostgreSQL/文件系统测试。前端单测首跑误传 Vitest 不支持的 `--runInBand`，改为项目原生脚本后 132/132；AgentInput PostgreSQL theory 首跑仍固定发送 SessionTemp scope，保留 37/38 红测证据并改为 `scope.ToString()` 后 38/38。
- migration 与部署验证：`AiCopilotDbContext`、`RagDbContext`、`AiGatewayDbContext`、`IdentityStoreDbContext` 均无 pending model changes；从 `20260711082644_AddPersistenceCommitMarkers` 到 `20260711101753_AddPersistenceCommitMarkerRetentionIndex` 的增量 SQL 仅包含 `CREATE INDEX ix_commit_markers_created_at_utc ...` 和 migration history insert。Compose 展开、全部 enterprise-ai shell `bash -n`、DeploymentPolicy、Baseline、Architecture、TextEncoding 和 Agent Workbench RC scope 均通过；Simulation scope 按设计拒绝本批前端协议文件，本批不是 Simulation batch，因此记为不适用且不伪造 changed-file 集合。本轮只验证配置，没有连接或改变生产服务。
- 规则提炼：形成长期技术与部署规则，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/DDD聚合根边界.md`、`docs/Agent工作流与异常契约.md`、`docs/AI架构治理清单.md`、`AICopilot 项目部署与维护指南.md`、`deploy/enterprise-ai/README.md` 和工作区 `../docs/上传部署总览.md`。不提升 `../docs/总规则.md`，原因是工作区总规则已有“发布前读部署文档/变更闭环”红线，本批新增内容只约束 AICopilot 内部持久化和其共享卷部署契约。
- 验证命令：`dotnet build AICopilot.slnx -c Release --no-restore --disable-build-servers --nologo -v:minimal`；完整 Release Architecture/AiEval/Backend；Backend `--filter 'Suite=PersistenceCommit'` 与 `--filter 'Suite=PersistenceFileStorage'`；四个受影响 context 的 `dotnet ef migrations has-pending-model-changes` 与 root 增量 migration SQL；Web `npm run type-check`、`npm run test:unit`、`npm run build-only`；changed-file `dotnet format whitespace --verify-no-changes`；`TestDeploymentPolicy.ps1`、Baseline、Architecture、TextEncoding、Simulation/RC scope；Compose config、`bash -n`、`rg` 旧类型/重复 fake/dead Save API、`git diff --check`。
- 验证结果：Release build 0 warning/0 error；ArchitectureTests 90/90、AiEvalTests 6/6、最终 BackendTests 911/911（5 分 1 秒，0 skip）、PersistenceCommit 38/38 + PersistenceFileStorage 7/7（合计 45/45）；Web type-check 通过、unit 29 files / 132 tests 全过、production build 成功。四个受影响 context 均无 pending model changes，增量 SQL 人工核对通过；DeploymentPolicy/Baseline/Architecture/TextEncoding/RC、Compose 和 shell 门禁通过，Simulation scope 对非 Simulation 前端变更按设计拒绝并记为不适用；changed-file whitespace 与 `git diff --check` 通过。EF CLI 10.0.8 低于 runtime 10.0.9 仍只输出工具版本提示，本批未越权升级全局工具；Vite 只对第三方 `@vueuse/core` 的 pure annotation 输出两条非阻断提示，本批未修改该依赖。
- 收口状态：`AI-SEC-046` 对 Identity、RAG 上传和 active AiGateway SessionTemp/AgentInput 上传由 01a/01b/01c 组合源码与非生产全量验收关单。01c 将提交并推送既有 draft 分支接受 GitHub 审核；GitHub 未绿前不评估 merge，本轮没有部署授权，因此无论 CI 结果如何都不执行生产发布。
- 接受风险与后续：共享卷只允许受信任的 AICopilot 后端容器写入；现有逐段 symlink/reparse 检查防既有链接，但不是同 UID 恶意进程竞态下的 `openat/O_NOFOLLOW` 沙箱。若扩大威胁模型，必须先做容器权限隔离或 dirfd 级原子路径操作。`ArtifactWorkspace` 多文件/覆盖/归档/final 复制仍是 `AI-PERSIST-01d / AI-SEC-047 Partial`。历史 KnowledgeBase shadow upload 行、`knowledge_base_id` / `rag_document_id` 列和枚举字符串仍是 `AI-PERSIST-01e / AI-SEC-048 Partial`；单独维护窗口必须先只读盘点并导出校验，再 drain 旧 HttpApi、删除 shadow 行、删索引/列并增加 active scope/目标二选一约束，不能在 migration-first 滚动窗口直接 drop 旧 runtime 仍映射的列。
- 后续去重债务：AiGateway/RAG 上传 policy 的业务 allowlist 与限额必须分离，但 seekable stream 归一化、header/MZ/NUL 技术探测仍有重复，已登记 `AI-SEC-049 Partial`，后续只能抽窄技术 helper 并明确临时流释放所有权，不能合并业务规则。全仓 whitespace 门禁另发现 13 个本批未修改的既有格式债务文件；本批只对 changed C# 文件执行并通过 format gate，旧债务进入后续代码卫生批次，不混入 01c 提交。
- 既有并发债务：RAG 同 hash 预查没有数据库唯一约束，并发请求仍可能产生重复 Document/文件，已登记 `AI-SEC-050 Partial`；本批只证明单次提交与文件对账一致，不宣称 RAG 业务幂等。Identity last-admin 检查也尚未跨 demote/disable/delete/seed 使用同一串行化边界，已登记 `AI-SEC-051 Partial`；本批单命令事务绿测不得冒充管理员生存并发验收。
- 平台边界与垃圾清理：复核确认 Windows `MoveFileEx(path, null, WRITE_THROUGH)` 不能证明立即耐久删除，父目录 flush 原实现又是空操作；已删除整条伪 Windows 分支，durable local backend 在 Windows 明确 `PlatformNotSupportedException`，标准生产继续限定 Linux 容器，macOS 仅用于本地验证。`UploadRecordStatus.Failed/Deleted` 字符串历史值仍可能存在于已落库数据，暂仅作为持久化枚举兼容保留；01e 清库前不得先删 enum member，否则旧行 materialize 会失败。

## 2026-07-11 普通持久化提交结果确认收口（AI-PERSIST-01b）

- 改动范围：仅修改 AICopilot 普通 repository/audit/outbox 持久化链、RAG Document ID 分配、HttpApi commit-unknown 错误边界、两份 migration、Backend/Architecture 测试和长期规则文档；未修改 CloudPlatform、EdgeClient、Identity commands/custom stores、`ITransactionalExecutionService` / `EfTransactionalExecutionService`、部署脚本、生产配置或生产环境。
- 改动原因：01a 已删除无生产者 Outbox 重复所有权，但 AiGateway/RAG 原保存路径仍无法区分 commit 前 transient failure 与 COMMIT 已成功但 ACK 丢失；RAG delayed factory 还可能在提交确认前清空。仅用 `SaveChanges(false)`、可选 Outbox 或可选 audit 推断成功不成立，也不能在结果未知时按普通失败删除上传文件。
- 持久化实现：新增唯一 `PersistenceCommitEngine` / `RepositoryPersistenceCommitter`，普通 repository 每个 EF 官方 `ExecuteInTransactionAsync(... verifySucceeded ...)` attempt 只调用一次业务 `SaveChangesAsync(false)`，随后在同一 PostgreSQL transaction 用短生命周期 Context 写 Outbox、audit 和 durable marker；只有 fresh marker verification 确认提交后才 `AcceptAllChanges`、清 AiGateway domain events 或清 RAG factory buffer。业务代码没有 retry loop，marker 写入后 caller cancellation 不再中断 commit/verification。
- commit-unknown 契约：fresh verifier 使用独立 Context、独立 execution strategy 和 10 秒 timeout。EF 外层 strategy 的 AsyncLocal 会抑制 verify callback 内的嵌套 strategy，因此只在 verifier 边界隔离 `ExecutionContext`；真实 transient verifier 测试证明隔离后 verifier 可重试且业务 attempt 保持一次。无法确认 marker 时抛出 `PersistenceCommitOutcomeUnknownException`，HttpApi 返回稳定 503 `persistence_commit_outcome_unknown`、trace id 和非敏感 commit id，不自动重放业务、不记录 raw message；RAG 上传保留文件等待对账。
- RAG 主键：`DocumentId` 改为在进入可重放事务前通过 PostgreSQL `rag.documents_id_seq` 分配，领域构造器要求显式正整数，EF 将 Document ID 视为 application-assigned；migration `20260711082654_CalibrateRagDocumentIdSequence` 在校准事务内用 table lock 阻断旧 runtime 并发插入，只单调校准 sequence 到现有最大 ID，不在 Down 降低生产序列。数据库生成的 `DocumentChunk` identity 仍由真实 PostgreSQL replay 测试覆盖，证明业务 Save 后 transient failure 可重放且最终只有一行。
- 数据库所有权：root migration `20260711082644_AddPersistenceCommitMarkers` 创建 `persistence.commit_markers`；`AiCopilotDbContext` 是 Outbox 与 marker 的唯一 migration owner，运行时 `OutboxDbContext` / `PersistenceCommitMarkerDbContext` 均 `ExcludeFromMigrations`。root/RAG 均通过 `has-pending-model-changes`，增量 SQL 只包含 marker DDL、sequence 校准和各自 history insert。
- 冗余与垃圾清理：物理删除旧 `AuditTransactionCoordinator`、`RagIntegrationEventStager`、AiGateway/RAG 业务 Context 的 Outbox mapping/Save override；删除全员 no-op 的 `PrepareAttemptAsync` hook、attempt counter、无消费者的公开 context 状态和 async wrapper；repository 原子提交已消费并 detach staged audit 后，删除三组 coordinator 中 6 次必然返回 0 的 trailing audit save 及 2 个失去用途的注入参数。共享唯一 PostgreSQL fixture/scratch database/options helper，旧源码字符串事务断言改为反射/真实行为门禁。repository 无业务变更、无 staged audit、无 pending Outbox 时直接返回 0，不生成垃圾 marker；Docker fixture 在启动后任一步失败都清理容器，只忽略有证据的 `No such container`，权限/daemon/cleanup 失败必须让测试失败。
- 测试架构：`Suite=PersistenceCommit` 统一承载真实 PostgreSQL 用例，覆盖普通/无操作 save、AiGateway Session domain event、RAG 上传/删除、业务+audit+outbox+marker 原子性、audit failure rollback、pre-commit retry、commit ACK lost、fresh verifier transient/persistent failure、caller cancellation 与 ACK lost 组合、RAG 文件保留、显式 Document ID 与数据库生成 chunk identity replay；MigrationSafety 另覆盖已有高位 Document ID 但 sequence 落后的真实升级校准，MigrationOwnership/Architecture 使用 EF metadata/反射，不复制生产 SQL 或源文件实现文本。
- 首轮失败与修正：fresh verifier 初版直接继承外层 execution-strategy AsyncLocal，transient verifier 没有重试，增加仅限 fresh verifier 的 ExecutionContext 隔离后定向用例通过；MigrationSafety 的 PostgreSQL bool 文本大小写期望由 `True` 修为实际 `true`；旧 `SecurityHardeningTests` 仍读取已删除 coordinator 源码，已改为 runtime metadata/ChangeTracker 行为；删除 trailing audit save 后，Architecture 首轮仍要求 command coordinator 注入 `IAuditLogWriter`，已改为反向禁止该冗余依赖并全量复跑。以上均在修正后完成定向和全量复跑，没有隐藏或删除有效业务断言。
- 明确未完成/禁止合并部署：`AI-SEC-046` 仍为 `Partial`。`AI-PERSIST-01c` 必须继续收口 Identity 事务、commit-unknown 文件对账/清理和 marker retention/lifecycle，并与 01b 共同完成全量验收；01b 可以推送当前 draft 分支接受 CI 审核，但 01c 完成前禁止 merge 或 deploy，不得宣称整条持久化链已完成。
- 规则提炼：形成长期技术规则，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/DDD聚合根边界.md`、`docs/Agent工作流与异常契约.md`、`docs/frontend-integration-contract-package-2026-05-17.md` 和 `docs/AI架构治理清单.md`。不提升工作区总规则，原因是本项只约束 AICopilot 内部 EF/Outbox/audit/文件对账边界。
- 验证命令：`dotnet build AICopilot.slnx -c Release --no-restore --disable-build-servers --nologo -v:minimal`；完整 Release `ArchitectureTests`、`AiEvalTests`、`BackendTests`；`--filter 'Suite=PersistenceCommit'`；root/RAG `dotnet ef migrations has-pending-model-changes` 与增量 migration SQL；`TestDeploymentPolicy.ps1`、Baseline、Architecture、Encoding、Simulation scope、RC scope；`rg` 旧类型/重复 fixture/Save override；`git diff --check`。
- 验证结果：Release build 0 warning/0 error；ArchitectureTests 88/88、AiEvalTests 6/6、最终 BackendTests 878/878（4 分 50 秒）、真实 PostgreSQL PersistenceCommit 18/18、最终 Persistence+Migration 组合 31/31；root/RAG 均无 pending model changes，migration SQL 人工核对通过；部署策略、基线、架构、编码、Simulation 和 RC scope 门禁全部通过；旧协调器/暂存器仅剩“禁止出现/类型不存在”的架构门禁引用，重复 PostgreSQL helper 为 0。EF CLI 10.0.8 低于 runtime 10.0.9 只输出工具版本提示，不影响 model/migration 检查结果，本批未越权升级全局工具。

## 2026-07-11 Outbox 持久化所有权第一批收口（AI-PERSIST-01a）

- 改动范围：仅修改 AICopilot 的四个 EF Core context、Outbox 接口/实现与 DI、两个 snapshot-only migration、相关 Architecture/Backend 测试和四处规则文档；未修改 CloudPlatform、EdgeClient、AiGateway `Session` 事件生产、生产配置、部署脚本或生产环境。
- 改动原因：源码复核确认 `AiCopilotDbContext`、DataAnalysis、MCP 的 `SaveChangesAsync` 事件扫描没有真实生产者，DataAnalysis/MCP 仍映射共享 Outbox，两个零调用 publisher 抽象及 `Stage(message)` 即时重载也已无调用方；这些路径制造了多重持久化所有权和伪兼容层。RAG 只允许保留索引完成后的延迟工厂事件，不应继续扫描通用 `IHasDomainEvents`。
- 代码与行为影响：`AiCopilotDbContext` 只保留 Outbox 表与 migration 唯一所有权；DataAnalysis/MCP 删除 Outbox mapping 和无生产者 Save override；RAG 删除通用 domain-event 扫描、stage、clear，只保留既有延迟 factory、两次保存与事务分支；AiGateway context 与 `Session` 不变。物理删除零调用 `IIntegrationEventPublisher`、Outbox/MassTransit 实现和两处 DI 注册，`IIntegrationEventStager` 只保留 factory overload；连同 EF 生成文件和复盘在内，本批合计新增 504 行、删除 579 行，净删 75 行。
- 数据库与部署影响：两个 migration 均由 `dotnet ef migrations add` 真实生成，`Up/Down` 操作为 0；从上一 migration 到本 migration 的 SQL 仅写各自 history 表，不含 table/column/outbox DDL。按项目引用闭包，后续发布会影响 HttpApi、DataWorker、RagWorker 与 Migration；本轮未 push、未部署、未连接生产数据库。
- 测试架构与去重：删除受影响的源码字符串断言和即时 stager fake 分支，改由 EF design-time metadata、migration operations、反射类型/方法集合和真实 PostgreSQL 原子性测试验证；保留所有无关安全断言，没有引入新的 coordinator、buffer、receipt、兼容壳或共享 PostgreSQL helper。
- 明确保留债务：本批不修 Outbox retry、RAG staged factory 过早 clear、事务提交结果未知；`AI-PERSIST-01b / AI-SEC-046` 必须以持久化幂等标记、fresh-context 查询和真实 PostgreSQL commit-ACK fault 注入闭环，不能以 `SaveChanges(false)` 冒充解决。
- 失败与复现：EF 10.0.8 首次生成的 DataAnalysis/MCP designer 和 snapshot 因同名 namespace/type 产生 `DbContext` 属性歧义，改为 `global::` 精确类型后 Architecture build 通过；首次 `dotnet ef --no-build` SQL 检查读取陈旧 Debug 输出并报 migration not found，重建 Debug 后两条 SQL 均仅含 history insert。完整 Backend 首轮 866/869，三条均在共享 `AICopilotAppFixture` 启动分布式应用时 120 秒超时、未进入业务断言；未改测试/fixture，隔离重跑 3/3（4 秒），完整重跑 869/869（4 分 50 秒）。
- 规则提炼：形成长期规则，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/DDD聚合根边界.md` 和 `docs/AI架构治理清单.md`：无真实事件生产者不得持有 Outbox mapping/Save override；commit-unknown 必须持久化幂等闭环。不提升工作区总规则，因为本项只约束 AICopilot 内部 Outbox 所有权。
- 验证命令：`dotnet build AICopilot.slnx -c Release --no-restore --disable-build-servers --nologo -v:minimal`；完整 ArchitectureTests、AiEvalTests、BackendTests；受影响 Backend 组合；真实 PostgreSQL migration/seed/ownership/RAG atomicity/audit/security 组合；DataAnalysis/MCP `dotnet ef migrations has-pending-model-changes` 与 migration SQL；`TestDeploymentPolicy.ps1`、Baseline、Architecture、Encoding、Simulation scope、RC scope；`git diff --check`。
- 验证结果：Release build 0 warning/0 error；ArchitectureTests 88/88、AiEvalTests 6/6、受影响组合 111/111、真实 PostgreSQL 组合 25/25、最终 BackendTests 869/869；两个 context 均无 pending model changes，最终 scope/架构/编码/部署策略门禁通过。旧 `Test-AICopilot55TotalReviewScope.ps1` 仍因依赖已退役的 5.5 历史文档和旧 comparison scope 失败，本批不恢复过期流水、不把该脚本结果包装为通过。

## 2026-07-11 DataAnalysis 最终上下文字段与值边界收口

- 改动范围：仅修改 AICopilot `DataAnalysisFinalContextFormatter`、Backend 测试分类和 Agent workflow / Cloud 只读 / AI 治理三份专题文档。未修改 CloudPlatform、EdgeClient、Widget 生成、HTTP route、Cloud API、数据库 schema、部署脚本、生产配置或生产环境。
- 改动原因：源码复核确认旧 formatter 会把 metadata description 直接作为最终 JSON property key，逐行单独处理重名，危险 description、Text-to-SQL alias 或返回 row key 可能进入最终 prompt；旧值入口还会对任意 CLR object 调用自定义 `ToString()`，可能透出嵌套 key、凭据或对象内部文本。相关行为测试散落在大型 `AiEvalBehaviorGuardrailTests`，标签映射、标量白名单和单次枚举没有独立责任边界。
- 代码与行为影响：Semantic/FreeForm 现在先生成唯一 `OrdinalIgnoreCase` 字段标签映射，metadata name/description 与 preview key 共用该映射；内部字段和 `CloudReadOnlyGovernedSchema.BlockedFieldFragments` 敏感 raw field 整项丢弃，description 不安全时依次回退 raw field 和固定“业务字段”，控制字符、指令文本、超过 80 字符与重名只在唯一入口处理。FreeForm rows 只枚举一次并收集前三个可识别 dictionary row。值只走既有 `SanitizeValue/SanitizeTextValue` 链：JSON 标量与显式 CLR 标量白名单保留稳定类型，其余 JSON object/array、CLR object/collection 统一脱敏，不递归、不调用自定义 `ToString()`。删除旧 `ResolveBusinessFieldName`、逐行 `DeduplicateLabel`、无效 schema description 字典，以及被 fragment 判定完全覆盖的 exact 常量；没有新增第二份 blacklist、value sanitizer 或 Widget 映射。
- 测试架构与去重：新增独立 `DataAnalysisFinalContextFormatterTests` 并归类 `Suite=AiEval`，数据矩阵统一覆盖 Semantic/FreeForm 的合法中文、大小写重名、空/超长/控制字符/指令标签、敏感字段整项丢弃、80 字符后缀、JSON/CLR 标量、嵌套对象/集合、恶意自定义 `ToString()`、前三个有效 row 与单次枚举；从混合 `AiEvalBehaviorGuardrailTests` 删除两组 formatter 所有权用例，只保留并增强真实 final prompt/compact consumer；`CloudReadOnlyTextToSqlFallbackRunnerTests` 增加真实 alias 消费回归。没有复制 fake connector、Widget sanitizer 或同义源码字符串断言。
- 失败与复现记录：最终 CLR sanitizer 后第一次完整 Backend 为 868/869，唯一失败 `AgentSimulationAcceptanceTests.OfflineSimulation_ShouldKeepProductionCloudReadonlyToolProtected` 停在共享 `AICopilotAppFixture` 的 `Start distributed application` 120 秒超时，未进入 simulation 业务断言。未修改该测试或 fixture；随后孤立重跑 1/1 通过（Aspire/PostgreSQL/RabbitMQ/Qdrant/Redis 正常启动），完整重跑 869/869；删除等价冗余常量后的最终源码再次完整重跑 869/869。现有证据支持一次性共享集成环境启动抖动，首次红测仍原样保留，未用局部通过冒充全量通过。
- 规则提炼：形成长期技术规则，已写入 `docs/Agent工作流与异常契约.md`、`docs/Cloud只读数据分析契约.md`，并在 `docs/AI架构治理清单.md` 以 `AI-SEC-045` 关单：最终 DataAnalysis JSON key 属于不可信消费边界，metadata/preview 必须共用唯一映射，flat preview 只能输出显式标量白名单。`AGENTS.md` 与 `资料/AICopilot业务规则.md` 已有 Cloud 只读、最终回答不得暴露内部字段和禁止伪造红线，本轮不重复增加同义条款；不提升工作区总规则，原因是该约束只属于 AICopilot final-context formatter。
- 验证命令：
  - `dotnet build AICopilot.slnx -c Release --no-restore --disable-build-servers --nologo -v:minimal`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --no-build --no-restore --disable-build-servers --filter "FullyQualifiedName~DataAnalysisFinalContextFormatterTests|FullyQualifiedName~AiEvalBehaviorGuardrailTests|FullyQualifiedName~CloudReadOnlyTextToSqlFallbackRunnerTests|FullyQualifiedName~SemanticAnalysisRunnerTests|FullyQualifiedName~SessionPolicySemanticsTests" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj -c Release --no-build --no-restore --disable-build-servers --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.AiEvalTests/AICopilot.AiEvalTests.csproj -c Release --no-build --no-restore --disable-build-servers --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --no-build --no-restore --disable-build-servers --logger "console;verbosity=minimal"`
  - `pwsh -NoProfile -File scripts/Test-AICopilotBaselineFreezeScope.ps1`；`pwsh -NoProfile -File scripts/Test-ArchitectureBoundaries.ps1`；`pwsh -NoProfile -File scripts/Test-TextEncoding.ps1`；`git diff --check`
- 验证结果：formatter/final prompt/Text-to-SQL/Semantic/session 组合 66/66、ArchitectureTests 85/85、AiEvalTests 6/6、最终 BackendTests 869/869；Release solution build 0 warning/0 error；baseline scope、architecture boundary、text encoding 与 diff 检查全部通过。未运行 Cloud live test，因为本批未修改 typed HTTP client/provider 契约且没有当前授权的非生产 token；未 push、未部署、未连接生产 Cloud、数据库或模型服务，不据此宣称线上已发布或生产已验收。

## 2026-07-11 正式语义 Runner Cloud-only 边界与冗余清理

- 改动范围：仅修改 AICopilot `SemanticAnalysisRunner`、对应 Backend/Architecture/Security 测试和 Cloud 只读、Agent workflow、AI 治理三份专题文档。未修改 CloudPlatform、EdgeClient、HTTP route、配置键、physical mapping / semantic source status 运维诊断及其消费者、`SemanticSqlGenerator` 独立实现与测试、数据库 schema、部署脚本或生产环境。
- 改动原因：源码复核确认六类正式语义在成功规划后已优先进入 Cloud-only 分支，但 Recipe 在 planner 失败时仍可能进入 Text-to-SQL fallback；同时七值目标枚举已经覆盖全部可达目标，Runner 后半段 Direct DB、mapping、SQL generator、数据库审计与 fallback 代码实际不可达，却持续携带六个无效构造依赖。旧 Runner 测试还复制 Direct SQL 专用桩和五组已失去执行对象的 SQL 防护用例，安全测试则用过期 `FailureCode` 源码字符串锁定已删除实现。
- 代码与行为影响：Recipe 现在在 planner 前返回固定禁读边界，即使 planner 本会失败也保持 0 planner/0 Cloud 调用；Device、DeviceLog、Capacity、ProductionData、Process、ClientRelease 六类在合法空集、规划失败、Cloud 关闭和 Cloud 错误时均不进入 Direct DB、Text-to-SQL 或 Simulation。Runner 构造依赖从九个收敛为 `ICloudAiReadClient`、`ISemanticQueryPlanner`、logger 三个，删除无 sink 便利重载和不可达执行分支，保留 Cloud AiRead 正式执行、设备日志 Widget、free-form 与 Agent 受治理 Text-to-SQL 独立链。
- 测试架构与去重：`SemanticAnalysisRunnerTests` 从 1071 行收敛为 413 行，六类 `(intent,target)` 只在唯一 `TheoryData` 定义一次，Recipe 单独验证 planner 前拒绝；四组矩阵覆盖合法空集、planning failure、disabled、Cloud error，保留错误码脱敏和 DeviceLog Widget 行为。ArchitectureTests 不再读取 Runner 源码或断言 token 顺序/不存在字符串，改为反射验证唯一 public constructor 的精确依赖集合；SecurityHardeningTests 删除 Direct DB 时代的 Runner 源码特例，通用生产日志门禁与真实错误行为测试继续覆盖脱敏。生产与测试代码合计净删 968 行。
- 验证过程：第一次完整 Backend 运行暴露 `ProviderWorkflowAndWorkerLogs_ShouldNotAttachRawExceptions` 仍要求已删除 Runner 分支含 `FailureCode`；确认是过期实现细节断言后物理删除该 Runner 特例，没有新增同义字符串断言。重新编译并完整复跑后全部通过。
- 规则提炼：形成长期规则，已写入 `docs/Cloud只读数据分析契约.md`、`docs/Agent工作流与异常契约.md`，并以 `docs/AI架构治理清单.md` 的 `AI-SEC-044` 关单：Recipe 必须在 planner 前拒绝，六类正式语义的空集、规划失败、关闭和 Cloud 错误都不得 fallback，正式语义 Runner 不得重新依赖 Direct DB/SQL 链。`AGENTS.md` 与 `资料/AICopilot业务规则.md` 已有六类 Cloud-only、Recipe 禁读和无 fallback 红线，本轮不重复堆叠同义规则；不提升到工作区总规则，原因是本项只收口 AICopilot 消费方内部执行结构。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter FullyQualifiedName~SemanticAnalysisRunnerTests`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore --filter 'FullyQualifiedName~SemanticAnalysisRunner_ShouldExposeOnlyCloudAiReadPlannerAndLoggerDependencies|FullyQualifiedName~CloudReadOnlyDirectDb_ShouldKeepReadonlyGuardsAndRejectSimulationFallback'`
  - `dotnet build AICopilot.slnx -c Release --no-restore --disable-build-servers --nologo -v:minimal`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj -c Release --no-build --no-restore --disable-build-servers --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --no-build --no-restore --disable-build-servers --logger "console;verbosity=minimal"`
  - `pwsh -NoProfile -File scripts/Test-AICopilotBaselineFreezeScope.ps1`；`pwsh -NoProfile -File scripts/Test-ArchitectureBoundaries.ps1`；`pwsh -NoProfile -File scripts/Test-TextEncoding.ps1`；`git diff --check`
- 验证结果：Runner 定向 31/31、边界定向 2/2、ArchitectureTests 85/85、BackendTests 864/864；Release solution build 0 warning/0 error；baseline scope、architecture boundary、text encoding 和 diff 检查全部通过。未运行真实 Cloud live test，因为本批未改 typed HTTP client/provider 契约且没有当前授权的非生产 Cloud token；未 push、未部署、未连接生产数据库或模型服务，不据此宣称线上已发布或生产已验收。

## 2026-07-11 AICopilot PR CI、Simulation 默认值与部署测试分层修复

- 改动范围：仅修改 AICopilot 五个 GitHub workflow、HttpApi Development 默认配置、两份 Simulation/RC scope 脚本、ArchitectureTests、BackendTests、`deploy/enterprise-ai` Shell Git mode、部署指南/README 及三份专题治理文档。未修改 AI 业务执行链、Cloud/EdgeClient、数据库 schema、生产 `.env`、服务器文件或线上容器。
- 改动原因：PR run `29136917057` 暴露四类独立问题：Backend full job `86502831304` 的部署文案断言已落后于 `Deploy-Changed.ps1`、`check-release-state-access.sh` 在 Linux checkout 后不可执行、Controller 测试用大小写错误路径读取源码；Simulation job `86502831311` 因 `appsettings.Development.json` 默认开启 Simulation 违反 scope 红线；同一 run 还显示 checkout/setup action 的 Node 20 runtime 退役 warning。旧审计把行为 preflight、Controller 结构和部署文案字符串都堆在 `SecurityHardeningTests`，并由两份 PowerShell 脚本重复解析 JSON，测试职责与真实运行行为不一致。
- 配置与 CI 影响：`appsettings.json + appsettings.Development.json` 叠加后默认保持 `CloudReadonly.Mode=Disabled`、`Simulation.Enabled=false`、`CloudAiRead.Enabled=false`；专用 Simulation fixture 仍显式开启模拟，既有 Production 拒绝测试未放宽。Simulation workflow 的 PR 使用事件 base SHA，手动运行只接收严格校验的分支名并解析成 commit SHA；scope 与 whitespace 统一使用 `baseSha...HEAD`，workflow expression 只经环境变量进入脚本。六处 checkout 升到 v7、三处 setup-dotnet 升到 v5、一处 setup-node 升到 v6，并把 ArchitectureTests 纳入 PR backend job。
- 测试架构与冗余收敛：真实配置绑定行为测试替代两份 scope 脚本的重复 JSON 检查；部署 `--validate-only` preflight 和全部 tracked Shell mode 归入独立 `Suite=DeploymentBehavior`；Controller/`ISender` 组合根改用 ArchitectureTests 反射验证，不再读源码字符串；旧部署入口文案断言删除，不用新的同义字符串断言替代。进程测试辅助类统一项目根、异步并行收集 stdout/stderr、15 秒超时、kill process tree 和 best-effort temp cleanup，不保留 sync-over-async 或重复 process helper。
- Shell mode：`deploy/enterprise-ai` 当前 20 个 tracked `.sh` 被唯一分类为 17 个直执行 `100755` 与 3 个 bash/source-only `100644`；本批修正其中 12 个错误 `100644`。数据驱动测试对 Git 索引做全量集合相等校验，新增脚本未分类、分类交叉或 mode 漂移都会失败。
- Runner 兼容历史证据：下列既有成功日志首行均为 `Current runner version: '2.335.1'`：routine inspect run `29134479181` / job `86496009044`（2026-07-11）、provision run `28345850382` / job `83968896541`（2026-06-29）、image run `29132999328` / job `86491885024`（2026-07-11）、deploy run `28361295575` / job `84016316019`（2026-06-29）。这些仅用于证明 Node 24 action 要求的 runner 前置已满足；本批未重新触发生产 workflow，也不声称升级后的 action 已在生产类 job 执行。
- 文档与规则提炼：形成长期规则，已写入 `docs/Cloud只读数据分析契约.md` 的 Development 默认禁用/显式 opt-in，`docs/AICopilot安全部署契约.md` 的不可变 base SHA、Node 24 action/runner 与 Shell mode 分类，`docs/AI架构治理清单.md` 的 `AI-SEC-042/043`。部署指南和 README 统一为 `Deploy-Changed.ps1` 日常唯一入口，`Deploy.ps1` 只保留内部执行、Doctor/DryRun 和显式恢复。`AGENTS.md` 与 `资料/AICopilot业务规则.md` 已有日常唯一入口、Simulation/Cloud 只读和禁止伪造红线，本轮不重复增加同义条款。
- 验证命令：
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj -c Release --disable-build-servers --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --disable-build-servers --filter "FullyQualifiedName~SecurityHardeningTests|Suite=DeploymentBehavior|FullyQualifiedName~CloudReadonlySimulationTests" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --no-build --no-restore --disable-build-servers --logger "console;verbosity=minimal"`
  - `pwsh -NoProfile -File scripts/Run-AgentSimulationAcceptance.ps1 -SkipDockerAcceptance`
  - `bash deploy/enterprise-ai/tests/deployment-behavior.sh`
  - `dotnet build AICopilot.slnx -c Release --no-restore --disable-build-servers --nologo -v:minimal`
  - `pwsh -NoProfile -File deploy/enterprise-ai/tests/TestDeploymentPolicy.ps1`；`pwsh -NoProfile -File scripts/Test-AICopilotBaselineFreezeScope.ps1`；`pwsh -NoProfile -File scripts/Test-ArchitectureBoundaries.ps1`；`pwsh -NoProfile -File scripts/Test-TextEncoding.ps1`
  - `pwsh -NoProfile -File scripts/Test-AgentSimulationScope.ps1 -IncludeWorkingTree`；`pwsh -NoProfile -File scripts/Test-AgentWorkbenchReleaseCandidateScope.ps1 -IncludeWorkingTree`
  - Ruby 解析全部 `.github/workflows/*.yml`；PowerShell parser 解析 Simulation workflow 的 base resolver/diff step；本地执行 resolver 的 PR SHA、合法手动 branch 和恶意 `main;echo-pwned` 三组用例
  - `git ls-files -s 'deploy/enterprise-ai/**/*.sh' 'deploy/enterprise-ai/*.sh'`；全部 tracked Shell `bash -n`；`git diff --check` 与 `git diff --cached --check`
- 验证结果：ArchitectureTests 84/84、最终安全/配置/部署定向组合 88/88、完整 BackendTests 854/854 通过；Simulation 非 Docker 单元 12/12 且生成的临时离线报告未纳入仓库；完整非生产部署行为回归通过；solution build 0 warning/0 error；DeploymentPolicy、Baseline、Architecture、Encoding、Simulation scope、RC scope、全部 Shell 语法、全部 workflow YAML、内嵌 PowerShell 解析和 diff 检查通过。Simulation scope 首次把 ArchitectureTests 的 LINQ 误判为 SQL，已只补测试目录排除并重跑通过，未放宽生产源码扫描。合法 PR SHA/手动 branch 均解析到 `156fbd31713fe4b26f7e2e8b4009a6f61ccb30d2`，恶意 ref 在 fetch 前失败。
- 未执行及原因：未 push、未部署、未连接真实 Cloud/生产数据库/模型服务，未触发 routine/provision/image/deploy 等生产类 workflow；升级后的 Simulation PR workflow 必须由后续 push 后的 PR run 验证，本批本地结果不得冒充 GitHub hosted Windows/Ubuntu 双 job 已通过。

## 2026-07-11 Agent 并行分支失败语义与路由日志脱敏

- 改动范围：仅修改 AICopilot `AgentWorkflowPipeline`、`BranchResult`、四个既有并行 executor、`ContextAggregatorExecutor` 和 `IntentRoutingExecutor`；新增 `AgentWorkflowBranchSemanticsTests`，把既有 Text-to-SQL 日志捕获器提取为共享测试辅助类，避免为新日志行为测试复制同类假实现；同步 `docs/Agent工作流与异常契约.md`、`docs/AI架构治理清单.md`。未修改 CloudPlatform、EdgeClient、Cloud AiRead 客户端/端点、Text-to-SQL fallback、数据库 schema、Plan/Tool/MCP/HITL 执行链、部署脚本或生产配置。
- 改动原因：旧 workflow 把分支异常统一变成空结果，最终模型无法区分“本次不相关”“合法零结果”和“真实失败”，存在基于缺失上下文继续回答的风险；意图路由还会把模型原始响应写入 debug 日志，可能携带 prompt、query、intent reasoning 或敏感内容。
- 影响面：`BranchResult` 现在显式区分 `Skipped/Empty/Succeeded/Failed`，分支 `Required` 由本次 routing intents 与对应 executor 的同一相关性判断产生；必需分支失败在 fan-in 后、上下文聚合和 final agent 前返回稳定脱敏 Error chunk，合法 empty 可以继续，可选失败和所有非 `Succeeded` 载荷不得进入最终上下文。调用方取消继续向上传播。四个分支仍由 `Task.WhenAll + AgentWorkflowSink` 并行运行；PlanDraft 仍只做能力发现，不执行真实 Tool/MCP/Cloud/Worker。意图路由日志只记录 response length、SHA-256、类型和解析状态，不记录原始响应。
- 规则提炼：形成长期规则，已写入 `docs/Agent工作流与异常契约.md` 的“分支完成状态与合流门禁”和日志脱敏条款，并在 `docs/AI架构治理清单.md` 以 `AI-SEC-040/041` 关单。`AGENTS.md` 与 `资料/AICopilot业务规则.md` 已有四分支 fan-out/fan-in、Cloud 只读、无 fallback 和安全日志红线，本轮不重复堆叠同义规则。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --no-restore --disable-build-servers --filter "FullyQualifiedName~AgentWorkflowBranchSemanticsTests|FullyQualifiedName~ClaudeFollowupClosureTests|FullyQualifiedName~IntentRoutingFallbackClassifierTests|FullyQualifiedName~IntentRoutingResultParserTests|FullyQualifiedName~SemanticAnalysisRunnerTests|FullyQualifiedName~TextToSqlReadOnlyTests" --logger "console;verbosity=minimal"`
  - `dotnet build AICopilot.slnx -c Release --no-restore --disable-build-servers`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj -c Release --no-build --no-restore --disable-build-servers --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --no-build --no-restore --disable-build-servers --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --no-build --no-restore --disable-build-servers --filter "FullyQualifiedName~RagUploadOutboxAtomicityTests" --logger "console;verbosity=normal"`
  - `pwsh -NoProfile -File scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh -NoProfile -File scripts/Test-ArchitectureBoundaries.ps1`
  - `pwsh -NoProfile -File scripts/Test-TextEncoding.ps1`
  - `git diff --check`
- 验证结果：本批 workflow/intent/semantic 与共享日志捕获器定向回归 62/62 通过（其中核心 workflow/intent/semantic 集合 54/54）；Release solution build 0 warning/0 error；ArchitectureTests 83/83 通过；三项项目规则/架构/编码脚本均通过。完整 BackendTests 命令退出码为 `1`，结果 811/853：1 项是本轮未修改的部署 README 与 `SecurityHardeningTests.DeploymentScriptsAndDocs_ShouldKeepSingleStandardReleasePath` 仍要求旧 `Deploy.ps1 -Target AICopilot ...` 字符串之间的既有漂移；其余 41 项共享同一个 `AICopilotAppFixture` 的 `Start distributed application` 两分钟超时，没有进入各自业务断言。完整套件结束后单独重跑其中真实分布式 `RagUploadOutboxAtomicityTests`，命令退出码为 `0`，Aspire/PostgreSQL/RabbitMQ/Qdrant/Redis 成功启动且 3/3 通过，确认该组为共享运行时瞬态初始化失败而非本批业务回归。未通过的完整套件结果原样保留，未用排除项冒充全量通过。
- 未执行及原因：未连接真实 Cloud、数据库或模型服务，未做生产部署、线上日志验收、push 或发布；本批只收口仓库内分支事实、日志边界、行为测试和文档契约，因此不得据此宣称生产已发布或线上已验收。

## 2026-07-11 AICopilot 自动增量 CI/CD 与编译级防回退门禁

- 改动范围：为 `aicopilot-routine-request.yml` 增加只读生产状态 inspect；新增 `deploy/enterprise-ai/tests/TestDeploymentPolicy.ps1`、`Directory.Build.targets` 并接入 simulation CI；同步 AGENTS、业务规则、安全部署契约、部署指南/README 和工作区统一增量入口。未修改 AI 业务源码、模型配置、数据库 schema、Cloud 只读边界或生产 `.env`，本轮未再次发布生产容器。
- 改动原因：AI 多宿主和共享项目不能再用“共享目录变化就全部构建”的粗粒度规则，也不能由操作者逐项选择。标准流程必须自动 push Git、读取生产 SHA，通过 `ProjectReference` 闭包只发布真正受影响镜像，并保留 HttpApi/DataWorker/RagWorker 命中时的 migration 安全闭包。
- 影响面：self-hosted workflow 只读导出 allowlist 当前状态，inspect 不 checkout 仓库，避免只读规划被 Git 拉取卡住；策略架构测试在 HttpApi build/CI 前执行，删除 inspect、显式服务或 migration 闭包会让构建失败。工作区影响测试证明 Rag 共享项目只命中 RagWorker，并自动加入 migration，Web 和其他后端不跟发。
- 规则提炼：形成长期规则，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/AICopilot安全部署契约.md`、部署指南/README 和工作区总规则/部署总览：AI 日常禁止全量 fallback，只发布影响镜像并保留迁移闭包。
- 验证命令：`pwsh -File deploy/enterprise-ai/tests/TestDeploymentPolicy.ps1`；工作区两套增量策略/影响测试；`dotnet build src/hosts/AICopilot.HttpApi/AICopilot.HttpApi.csproj -c Release --no-restore -p:BuildInParallel=false --disable-build-servers`；Ruby YAML 解析；`git diff --check`。
- 验证结果：AICopilot 策略测试通过；依赖/迁移影响测试通过；Release build 0 warning/0 error且实际触发策略门禁。只读生产 inspect run `29134258023` 成功导出 `GIT_SHA=e844b53...` / `STATUS=healthy`，部署请求步骤明确 skipped。增量入口尚未用新的单服务生产改动执行真实发布，因此本条不冒充增量生产 E2E 已验收。

## 2026-07-11 AICopilot 一键发布与 Runner 投递兜底

- 改动范围：新增 `.github/workflows/aicopilot-routine-request.yml`，修正 `aicopilot-image.yml` / `aicopilot-deploy.yml` 的安全检查脚本调用并恢复该脚本 Git executable 位；更新工作区 `deploy/Deploy.ps1`、新增 `deploy/Deploy-All.ps1`，同步 AICopilot AGENTS、业务规则、部署指南/README 和工作区总规则/部署总览。未修改 AI 业务源码、模型配置、数据库 schema、Cloud 只读边界、生产 `.env` 或服务器稳定 Runner。
- 改动原因：生产 SSH TCP 不可达，但 `ubt-aicopilot` self-hosted Runner 在线；首次灾备镜像构建还因仓库中文件模式为 `0644`、workflow 直接执行脚本而在编译前返回 `126`。发布不应要求操作者找 SSH、手工编译或逐镜像推送。
- 影响面：`RemoteTransport=Auto` 在 SSH 不可达时把同一份 digest-bound 请求交给稳定 Runner；目标 Runner 完全缺失时，新 workflow 从当前提交一次性安装 pinned 副本，已有 Runner 永不覆盖；除此之外不同步 support、不覆盖 `.env`。安全 attestation workflow 改为显式 `bash` 调用并保留 Git `100755`，避免 archive/checkout 丢失工作树本地 chmod 后再次失败。
- 规则提炼：形成长期部署规则，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md`、`AICopilot 项目部署与维护指南.md`、`deploy/enterprise-ai/README.md` 和工作区规则/总览：统一入口可在 SSH 与生产 self-hosted transport 间选择，但候选 SHA、服务闭包、OCI digest、请求 digest、non-root 稳定 Runner 和迁移前备份不变。
- 验证命令：PowerShell parser；Ruby YAML 解析 AICopilot 三个相关 workflow；`bash deploy/enterprise-ai/scripts/check-runner-security-attestation.sh --dry-run`；`git diff --check`；真实 `aicopilot-image`；真实 `Deploy.ps1 -Target AICopilot -All -Deploy`；Web/OIDC/容器与远端 routine state 验收。
- 验证结果：PowerShell parser、YAML 解析、Shell 语法、真实 Docker info attestation 和 diff 检查通过；`SecurityHardeningTests` 重新编译后 76/76，新增 production workflow 已进入同一最小权限白名单；`TestRoutineDeploy.sh` 走完 AI backend 自动闭包 migration、备份、rollout 与健康探针。前三次真实 image run `29131957229` / `29132574726` / `29132796522` 依次暴露 executable、不可遍历 Docker 数据目录和现场 Docker Root 漂移，均在构建前 fail-fast；修正后 image run `29132999328` 全量成功。生产 deploy run `29133322059` 完成 non-root/工作目录/Docker Root/部署根 attestation、pinned Runner bootstrap、PostgreSQL 备份、migration、DataWorker/HttpApi/RagWorker/Web rollout，服务器本机探针 200 并写入 `releases/routine-current.env`；外部 Web、Cloud OIDC 状态均为 200，CSP、nosniff、DENY frame 和 no-referrer 安全头存在。

## 2026-07-10 AICopilot 可信工作站日常发布链收敛

- 改动范围：本轮只新增工作区 `deploy/Deploy.ps1`、稳定 Runner、隔离回归，并更新 AICopilot 部署文档、规则、安全契约与部署门禁口径；未修改 AI 业务源码、数据库 schema、模型配置或 Cloud 只读链，未连接 Harbor/SSH/生产服务器，未 commit/push。
- 改动原因：旧日常链把 clean/pushed 门禁、support 同步、reservation、复杂锁、断联对账、深度 attestation 和 cleanup 叠入一次应用发布，既拖慢执行，也会因为其它窗口持续修改业务工作树而反复阻断。
- 影响面：日常入口改为取 `origin/main` tip 创建 detached worktree，只构建选中镜像并以一次 SSH 请求稳定 non-root Runner。HttpApi/DataWorker/RagWorker 自动闭包 migration；Runner 在迁移前生成 PostgreSQL dump/checksum，随后 `--no-deps` 更新应用、健康检查、失败镜像回滚。支持文件升级和 cleanup/GC/深度巡检独立维护；远端失败可按 invocation 续传，不重建镜像。
- 规则提炼：形成长期部署规则，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/AICopilot安全部署契约.md`、部署指南和工作区总规则/总览。本地工作树允许脏但未推送内容不得发布；日常应用发布不得替换服务器执行代码；root 只允许一次性修 owner/mode。
- 验证命令：工作区执行 `bash -n deploy/server/iiot-release-runner.sh deploy/tests/TestRoutineDeploy.sh`；`bash deploy/tests/TestRoutineDeploy.sh`；`dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --no-build --no-restore --filter "FullyQualifiedName~SecurityHardeningTests" --disable-build-servers --logger "console;verbosity=minimal"`；`git diff --check`。
- 验证结果：隔离回归已通过 AICopilot 脏工作树/远端 SHA/一次 SSH/不可变镜像、migration 自动闭包、迁移前备份、失败回滚、同一请求无重建续传和 dry-run 零 SSH。真实生产闭环尚未执行，不能称已完成生产验收。

## 2026-07-10 AICopilot 标准部署事务与低能力 AI 交接收敛

- 改动范围：只修改 `deploy/enterprise-ai/**` 的本地/远端发布脚本、support installer/cancel/query 工具、行为测试和部署说明，并同步项目部署规则、安全契约、部署指南与本复盘；未修改业务源码、数据库 schema、Cloud、EdgeClient 或生产环境。
- 改动原因：旧链路仍把候选 SHA、服务闭包、support 接管、SSH ACK 丢失、配置漂移、基础设施身份和失败恢复交给执行 AI 临场判断，存在旧 token 删除新锁、部分发布写入全局 fingerprint、恢复失败证据过期、可变 infra tag 冒充旧运行态等风险。
- 影响面：正式运行绑定 workspace plan/profile、固定 SHA、显式 migration 闭包、per-run manifest 和 immutable OCI；support install/adopt/cancel 使用原子 transition，live identity 优先 TTL。SSH 断联按 invocation token 对账；active/unknown 为 `87`。不安全恢复或证据落盘不确定为 `86`，残留 backup/blocked lock 持续 fail-closed。事务前冻结 PostgreSQL/RabbitMQ/Qdrant RepoDigest/runtime image id，失败时连同 support、compose、state 与全部常驻 runtime 一起恢复并验证。
- 规则提炼：形成长期部署规则，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/AICopilot安全部署契约.md` 和 `AICopilot 项目部署与维护指南.md`；核心是 immutable candidate、全局配置漂移只允许全量、原子 reservation、`86/87` fail-closed、真实 infra identity 和全 runtime 恢复。
- 验证命令：`bash -n deploy/enterprise-ai/*.sh deploy/enterprise-ai/scripts/*.sh deploy/enterprise-ai/tests/*.sh`；`bash deploy/enterprise-ai/tests/deployment-behavior.sh`；`dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --no-build --no-restore --disable-build-servers --filter "FullyQualifiedName~SecurityHardeningTests"`；`git diff --check -- deploy/enterprise-ai`。
- 验证结果：最终文件树隔离 fake 回归退出码 `0`，`SecurityHardeningTests` 76/76，Shell 语法和 diff 检查通过；`Microsoft.OpenApi 2.7.5` 与 solution 漏洞扫描清零结论仍有效，本文后续记录中关于 2.0.0/NU1903 的旧状态已被修复记录取代。未连接真实 Harbor、SSH 或生产容器，不能称生产 E2E 验收完成。

## 2026-07-10 Microsoft.OpenApi 高危循环引用漏洞修复

- 改动范围：仅修改 `src/hosts/AICopilot.HttpApi/AICopilot.HttpApi.csproj` 和本滚动复盘；为 `Microsoft.AspNetCore.OpenApi 10.0.9` 的可传递依赖显式固定 `Microsoft.OpenApi 2.7.5`。未修改 Cloud、EdgeClient、OpenAPI 路由/Schema、业务代码、数据库、部署脚本或生产环境。
- 改动原因：solution restore/build 报告 `NU1903`，确认依赖链为 `Microsoft.AspNetCore.OpenApi 10.0.9 -> Microsoft.OpenApi 2.0.0`。该版本受 `CVE-2026-49451` / `GHSA-v5pm-xwqc-g5wc` 影响，解析包含循环 schema 引用的 OpenAPI JSON/YAML 时可能因无限递归和 stack overflow 终止进程；2.x 官方修复下限为 2.7.5。
- 影响面：保持 Microsoft.OpenApi 2.x 主版本，不跨到可能破坏 ASP.NET Core OpenAPI/Swashbuckle 兼容性的 3.x；完整 solution restore 后所有项目统一解析 2.7.5，`/openapi/v1.json` 生成和现有关键 Schema/ProblemDetails 契约保持不变。源码检索未发现生产代码读取用户提供的 OpenAPI 文档，本轮仍按发布阻断级依赖漏洞处理。
- 验证过程：只 restore HttpApi 后首次 `--no-restore` solution build 暴露 BackendTests 的旧 `project.assets.json` 仍持有 2.0.0，产生 MSB3277；执行完整 `dotnet restore AICopilot.slnx` 后冲突消失。BackendTests 第一次全量重跑因共享 `AICopilotAppFixture` 在 “Start distributed application” 阶段统一超时、未进入业务断言；终止并清理该次 orphan DCP/worker/httpapi 测试进程后，原命令重跑通过。
- 规则提炼：无新增长期规则。原因是本次属于具体 CVE 的最小安全版本修复，现有依赖安全扫描和“测试/构建后才能发布”规则已经覆盖；不把单个 NuGet 版本写入长期业务规则，后续由漏洞扫描和父包升级继续治理。
- 验证命令：
  - `dotnet restore AICopilot.slnx --disable-build-servers`
  - `dotnet nuget why src/hosts/AICopilot.HttpApi/AICopilot.HttpApi.csproj Microsoft.OpenApi --framework net10.0`
  - `dotnet list AICopilot.slnx package --vulnerable --include-transitive`
  - `dotnet build AICopilot.slnx -c Release --no-restore --disable-build-servers --nologo -v:minimal`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --no-build --no-restore --disable-build-servers --filter 'FullyQualifiedName~OpenApiContractTests'`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --no-build --no-restore --disable-build-servers`
  - `git diff --check`
- 验证结果：依赖树统一为 `Microsoft.OpenApi 2.7.5`；solution 级漏洞扫描对全部 32 个项目均报告“没有易受攻击的包”；solution build 0 warning、0 error；真实 `/openapi/v1.json` 契约 3/3、BackendTests 全量 844/844 通过。已创建下述本地源码提交，未 push、部署或访问生产环境。
- X0 重新配对：因本轮修改 `src/` 使旧联合证据失效，已重跑真实 Cloud Aspire provider ↔ 当前 AI typed client；`CloudCandidateSourceDigest=sha256:51f58843aff8b4f8616c6c4560e39aa54b299aedff14553174f2d15fa50a2afc`，`AICopilotCandidateSourceDigest=sha256:f6faca901b01519bcd798cad07fa8605e3bc75bc5d6e064d7ad536e9c7db789b`，`ExecutedAtUtc=2026-07-10T08:48:58Z`，`CloudEnvironment=local-aspire-isolated`，`Result=Passed`（1/1，约 50 秒）。
- 权威源码提交对：Cloud `bb4184ca107b0e35a4293748594364686f864f18`；AICopilot `b66e472685cffb4c65fa30ce54d1fca1b589dd3a`。两笔提交在上述 X0 通过后由同一候选工作树创建，未改变已配对的 `src/` 字节或源码 digest；本条复盘回写属于纯文档提交。


## 2026-07-10 AICopilot ↔ Cloud 整体契约对齐与真实 live 验收

- 改动范围：在当前 AICopilot 部署/UI 候选工作树上完成 Cloud AiRead typed client、严格信封/DTO、参数构造、HTTP 错误映射、设备解析、业务语义、Agent 工具、Cloud-only no-fallback、JWT/bootstrap 配置和测试对齐；删除无生产引用的 Pilot readiness 残留，新增独立 `AICopilot.CloudAiReadLiveTests` 并纳入 solution；更新项目规则、业务规则、Cloud 只读契约、Agent 工作流契约和本复盘。未修改 EdgeClient、Cloud migration、生产环境或工作区部署调度器；未提交、未推送、未部署。
- 改动原因：Cloud C1a/C1b 已把设备主数据、运行状态、严格参数、信封元数据和软件状态进一步正式化，AI 原消费方仍存在字段别名、自然语言降级、状态/日志混用和已覆盖域 fallback 风险；单仓 Stub 也不能证明当前 typed client 与当前 Cloud 真实兼容。
- 影响面：八端点只发送正式参数，未知/误导过滤在 HTTP 前 fail-closed；设备编码只在需要 `deviceId` 的域通过 `/devices` 唯一解析，`Device.Status` 直接访问状态端点。adapter 只接受正式信封并保留 provider 元数据，不用本机时间或字段别名补值。Device、DeviceLog、Capacity、ProductionData、Process、ClientRelease 六类已覆盖语义成功、空集或任意 Cloud 错误均不进入 Direct DB、Text-to-SQL、Simulation、MCP 或隐藏适配器；`Analysis.Capacity.ByProcess` 不路由。状态汇总区分 Missing/Stale/有效心跳，生产字段不互相冒充；bootstrap auto-bind 默认 false，本地 emergency admin 保留。
- 配对候选：`CloudBaselineSha=e7835a62e7ee06c7a180c879ce4a8db0af8643be`；`CloudCandidateSourceDigest=sha256:51f58843aff8b4f8616c6c4560e39aa54b299aedff14553174f2d15fa50a2afc`；`AICopilotBaselineSha=9164d869cd2f58794eb3e3a2b5fb8320576a0ac3`；`AICopilotCandidateSourceDigest=sha256:4185c4abc0ec0e0478ae57d6721aabd887620cbd4c3674bead185d381fe13419`；`ExecutedAtUtc=2026-07-10T08:16:19Z`；`CloudEnvironment=local-aspire-isolated`；`Result=Passed`。两仓仍是 baseline HEAD + 未提交候选工作树；任一侧 `src/` 变化后结果失效。
- 规则提炼：形成长期规则，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/Cloud只读数据分析契约.md` 和 `docs/Agent工作流与异常契约.md`：六类正式语义 Cloud-only 且 no-fallback；`softwareStatus`/Missing/Stale 是 Cloud 权威状态；生产字段不得冒充；显式 live-test 缺环境必须失败，token 只经子进程环境传递；单仓 Stub 不等于跨仓验收。无需提升工作区总规则，原因是该规则属于 AICopilot Cloud consumer 与 Agent 执行边界。
- 验证命令：ArchitectureTests、BackendTests 全量、HttpApi/AppHost/solution Release build、Web unit/build、shell syntax、`git diff --check`；live 防伪门禁 `dotnet test src/tests/AICopilot.CloudAiReadLiveTests/AICopilot.CloudAiReadLiveTests.csproj -c Release --no-build --no-restore`；真实联合验收由 Cloud E2E 显式入口启动。
- 验证结果：ArchitectureTests 83/83、BackendTests 844/844、Web unit 132/132 通过，Web production build 通过；HttpApi、AppHost 和 solution build 成功。solution 仍报告 1 个已知 NU1903：`Microsoft.OpenApi 2.0.0` / GHSA-v5pm-xwqc-g5wc，必须在发布前单独闭环，不能写成零 warning；Web build 另有 2 条 `@vueuse/core` PURE annotation 依赖告警但退出码为 0。live-test 缺 BaseUrl/token 时按设计 1/1 失败、0 skip；真实 Cloud Aspire provider 注入临时环境后联合矩阵 1/1 通过。shell 语法和 diff 检查通过。未执行真实生产发布、SSH、Harbor、push 或 commit。


## 2026-07-10 AICopilot 部署行为测试入口目录无关化

- 改动范围：只修改 `deploy/enterprise-ai/tests/deployment-behavior.sh`、部署 README、安全部署契约和本复盘；未修改 Cloud、EdgeClient、AICopilot 业务实现、测试业务断言、生产配置或服务器。
- 改动原因：全新低能力 AI 按工作区部署文档从工作区根执行 `bash AICopilot/deploy/enterprise-ai/tests/deployment-behavior.sh` 时，脚本用调用者当前目录执行 `git rev-parse --show-toplevel`，因工作区根不是 Git 仓库而返回 `128`。这会让正确遵守边界、不会自行猜测 `cd` 的 AI 无法完成非生产交接测试。
- 影响面：脚本现在先通过 `BASH_SOURCE[0]` 解析自身绝对目录，再用 `git -C <script-dir> rev-parse --show-toplevel` 定位 AICopilot 仓库；从工作区根或 AICopilot 仓库根调用都进入同一隔离 clone、本地临时提交、本地 bare origin 和 fake Docker/SSH/remote 行为链，不连接生产。
- 规则提炼：形成长期部署测试规则，已写入 `docs/AICopilot安全部署契约.md`：部署行为测试必须根据脚本自身位置解析项目根，不得依赖 AI 当前终端目录，也不得把工作区根不是 Git 仓库误报成部署链失败。
- 验证命令：`bash -n AICopilot/deploy/enterprise-ai/tests/deployment-behavior.sh`；在工作区根执行 `bash AICopilot/deploy/enterprise-ai/tests/deployment-behavior.sh`；`dotnet test AICopilot/src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --no-build --no-restore --filter "FullyQualifiedName~SecurityHardeningTests" --disable-build-servers`；`git -C AICopilot diff --check -- deploy/enterprise-ai docs/AICopilot安全部署契约.md docs/改动复盘与规则沉淀.md`。
- 验证结果：`bash -n` 通过；从非 Git 的工作区根直接执行原失败命令后，行为回归完整通过，覆盖成功、同 SHA 幂等跳过、普通失败 `42`、真实超时 `124` 且子孙清零、digest mismatch `65`、support drift 恢复、`.env`/release state 回滚、active/stale lock、并发互斥、cleanup TERM `143` 解锁和 watchdog orphan 为零；SecurityHardeningTests 76/76，`git diff --check` 通过。真实 Harbor、SSH、生产容器和 `current-production.json` 均不在本测试范围。

## 2026-07-10 AICopilot 固定提交、digest 绑定与原子部署链加固

- 改动范围：先在 `/tmp/iiot-ai-deploy-validation` 隔离仓库修改 AICopilot `deploy/enterprise-ai/build-and-push.sh`、`local-release.sh`、`deploy-release.sh`、`post-release-cleanup.sh`，新增 `scripts/release-common.sh`、`scripts/install-support-release.sh`、`tests/deployment-behavior.sh` 并更新部署 README、安全部署契约和本复盘；行为验证后逐文件确认正式工作区对应脚本与测试快照一致，再机械同步部署文件并增量合并文档，没有覆盖正在进行的 AI 业务代码。未连接 Harbor、SSH 生产宿主机、真实 `.env`、真实数据库或真实容器。
- 改动原因：原标准链存在会让多个 AI 重复部署、误诊或串任务的 P0：clean/pushed 只做瞬时检查却从可变工作树构建；dry-run 和正式任务共享 `aicopilot-built-services.txt`；support sync 遗漏 compose、无 hash/原子 staging/锁；远端无 release 全局锁且在校验前改写 `.env`；普通失败被重写成 `124`；timeout 只杀直接 PID；cleanup lock 无 stale recovery；信号 trap 可能只删锁不退出；同 SHA 会重复 build/migration/cleanup。
- 影响面：本机入口现在为固定 Git SHA 创建 detached worktree，镜像和 support release 只读取该快照；每次 run id 使用私有 services/image/support manifest。support release 包含 `docker-compose.yaml`、受控脚本、`scripts/` 和 `cloud-readonly/`，远端先 staging + SHA256 逐文件校验，再以 reservation token 在全局 release lock 内安装，`deploy-release.sh` 接管同一 token/digest。release manifest/summary 记录 support digest；digest mismatch 在任何持久状态变更前返回 `65`。
- 事务和失败语义：`deploy-release.sh` 在容器变更前备份 `.env`、current/previous/staged manifest 和 current summary，健康/attestation 前失败恢复原状态；健康 release 提交后 cleanup 失败保留 current release 并返回真实 cleanup code。普通 git/dotnet/Docker/SSH/support/attestation 错误保留原码，只有 watchdog 实际触发时返回 `124`。active lock 立即返回 `75`，stale PID/process-start/reservation 可安全回收；HUP/INT/TERM 显式退出并释放本任务锁。
- 幂等和速度：本地 build 前通过只读 `--check-current` 核对同 SHA、同 support digest、目标服务镜像和真实健康/attestation；已部署健康时跳过 build/push/support sync。远端再次执行相同 release 也不会重复 migration、容器重建或 cleanup。
- 测试过程异常与收口：第一版行为回归暴露 timeout timer 的 `sleep 120/900/1800` 在测试父 shell 异常结束后成为 PPID=1 orphan。已清理全部测试专属进程组，将 timer cleanup 改为递归终止 timer 进程树，并加入测试专属 watchdog PPID=1 零残留断言；修复后行为回归通过且进程扫描无残留。同步到仍有业务改动的正式工作树后，测试最初从 committed HEAD clone 到旧脚本并返回 `126`；测试夹具现只把当前 `deploy/enterprise-ai` 复制到临时仓库、生成本地临时提交和本地 bare origin，再验证 clean+pushed 固定 SHA，既覆盖当前部署改动又不提交、推送或混入并行业务修改。
- 规则提炼：形成长期部署规则，已写入 `docs/AICopilot安全部署契约.md` 第 7.1 节，并同步 `AGENTS.md`、`资料/AICopilot业务规则.md`、部署 README 和工作区总规则/总览：固定提交隔离构建、运行私有控制文件、support digest/token/lock 绑定、持久状态失败回滚、active/stale 托管锁、真实退出码、受监督进程树超时清理和同 SHA 健康幂等均为标准发布硬门禁。
- 验证命令：`bash -n deploy/enterprise-ai/*.sh deploy/enterprise-ai/scripts/*.sh deploy/enterprise-ai/tests/*.sh`；`./deploy/enterprise-ai/tests/deployment-behavior.sh`；`dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --filter "FullyQualifiedName~SecurityHardeningTests" --disable-build-servers`；`git diff --check`；测试后 `ps` 检查专属 watchdog PPID=1 残留。
- 验证结果：隔离部署行为回归通过，真实走完固定 clone、fake build/push、fake SSH support staging、fake remote release、health/attestation、cleanup/history，覆盖成功发布、同 SHA build 前跳过、普通失败 `42`、真超时 `124` 且子孙清零、digest mismatch `65`、compose 失败恢复 `.env/current`、active `75`、stale 回收、并发仅一个进入、cleanup TERM `143` 解锁和 watchdog orphan 为零。当前部署文件行为回归再次通过；SecurityHardeningTests 从旧断言导致的 74/76 收口为 76/76，并把两个灾备 workflow 的提示统一到工作区 `Invoke-WorkspaceDeploy.ps1`。工作区统一入口先对未推送隔离 HEAD 正确返回 `64`，把同一提交推到 `/tmp` 本地 bare origin 后，正式 fake deploy 从 preview、固定 SHA、fake build/push、support digest/token 安装走到 fake SSH release，入口与摘要均返回 `0`；没有连接 GitHub 或生产。`Microsoft.OpenApi 2.0.0` 的既有 NU1903 高危依赖警告仍是发布阻断项；真实统一入口到 Harbor/SSH/生产容器的 E2E 未执行。

## 2026-07-10 AI-only 契约、安全、Agent 与前端修复

- 改动范围：严格只修改 `AICopilot/**`，未写入 `IIoT.CloudPlatform`、`IIoT.EdgeClient` 或工作区根目录。后端覆盖 Cloud AiRead typed GET transport、生产记录字段真实性、JWT 运行时校验、Device/DeviceStatus/DeviceLog 语义分离、Process/ClientRelease/DeviceStatus Cloud-only 语义与 Agent 能力、PlanDraft 确认门、OpenAPI/ProblemDetails 契约和无引用 readiness DTO 删除；测试 Simulation 设备主数据补入独立 `process_id`。前端覆盖登录页、Shell、Chat 空态和访问页，删除虚构在线/KPI/READY 状态，增加真实能力边界、低高度响应式样式和 UI 真实性门禁测试。同步更新 `AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/Cloud只读数据分析契约.md`、`docs/Agent工作流与异常契约.md`、`docs/AI架构治理清单.md`、前端 `AGENTS.md` 和本复盘。
- 改动原因：AI 消费方此前仍存在任意 Cloud 传输/POST 配置残留、生产记录字段代填、设备主数据/状态/日志混义、JWT 只依赖部署脚本、三个正式 Cloud 只读 Agent 能力缺口、Plan 草案与执行边界缺少源码门禁、OpenAPI 错误响应快照不足，以及登录页用硬编码 KPI 暗示运行状态等问题。修复必须在不修改 Cloud 提供方、不放开 Cloud 写入、不碰用户原有部署脏改动的前提下完成。
- 影响面：Cloud AiRead 公共面现在只有八个固定 typed GET，非 GET 在请求发送前拒绝；`production-records` 缺少 `processName/stationName` 时保持空。`Analysis.Device.List/Detail` 只返回设备主数据，`Analysis.Device.Status` 只返回 `/device-client-states` 的最后上报状态/心跳且零条不解释为离线；`Analysis.Process.List/Detail` 和 `Analysis.ClientRelease.List` 复用同一 Cloud AiRead 语义实现。上述三类 Cloud-only 能力在 Cloud 不可用、拒绝、截断、零命中或多命中时均不回退 Direct DB、Text-to-SQL 或 Simulation。Process 详情只能唯一精确命中；ClientRelease 不生成 Cloud 未返回的版本、hash、下载地址、说明或状态。PlanDraft 确认前不创建可执行意图、不查询、不调用工具、不入队。HttpApi 统一校验非空 issuer/audience、至少 64 字符 secret 和正数 token lifetime，错误不回显 secret。REST/OpenAPI 关键端点具备稳定请求 schema 与 ProblemDetails 响应 schema 快照。`CloudReadonlyPilotReadinessOptions` 在无绑定、反射、序列化和测试引用证据后删除。
- 前端影响：采用项目既有 SaaS/HMI 视觉规则，把登录页从装饰性演示面板改为静态能力和安全边界说明；Shell 明示“Cloud 写入禁用”，Chat 建议只使用当前正式只读能力。运行时状态仍只能来自后端、stream 和 session 事实。浏览器实际检查覆盖登录页和 Chat 的 1920×1080、1366×768、1024×768，横向/纵向溢出均为 0；1024 宽度按设计隐藏非必要预览但保留登录和主要工作区可用。
- 规则提炼：形成长期规则，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/Cloud只读数据分析契约.md`、`docs/Agent工作流与异常契约.md` 和 `src/vues/AICopilot.Web/AGENTS.md`。核心结论是：Process/ClientRelease/DeviceStatus 只走唯一 Cloud AiRead 语义实现且不得跨源降级；PlanDraft 确认前禁止真实查询和执行；前端不得硬编码在线、就绪、命中率、数据源数量或业务结果。`docs/AI架构治理清单.md` 的 AI-SEC-029/030/031/032/034/035/036/037/038/039 已关单，AI-SEC-033 保持 Deferred。
- 验证命令：
  - `dotnet build AICopilot.slnx -c Release --no-restore --disable-build-servers`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj -c Release --no-restore --disable-build-servers --logger 'console;verbosity=minimal'`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --no-restore --disable-build-servers --filter "FullyQualifiedName!=AICopilot.BackendTests.SecurityHardeningTests.DeploymentScriptsAndDocs_ShouldKeepSingleStandardReleasePath" --logger 'console;verbosity=minimal'`
  - `dotnet test src/tests/AICopilot.AiEvalTests/AICopilot.AiEvalTests.csproj -c Release --no-restore --no-build --disable-build-servers --logger 'console;verbosity=minimal'`
  - `cd src/vues/AICopilot.Web && npm run test:unit && npm run build && npm run test:smoke`
  - `git diff --check`
  - `rg -n "Analysis\\.Process\\.(List|Detail)|Analysis\\.ClientRelease\\.List|Analysis\\.Device\\.Status|Product Truthfulness|AI-SEC-03[4-9]" AGENTS.md 资料/AICopilot业务规则.md docs/Cloud只读数据分析契约.md docs/Agent工作流与异常契约.md docs/AI架构治理清单.md src/vues/AICopilot.Web/AGENTS.md`
  - `rg -n "CloudReadonlyPilotReadinessOptions" src --glob '!**/bin/**' --glob '!**/obj/**'`
- 验证结果：解决方案 Release build 0 警告、0 错误；ArchitectureTests 83/83；本轮范围 BackendTests 823/823；AiEvalTests 6/6；FrontendIntegrationContractTests 11/11；JWT 定向测试 8/8；前端 unit 132/132，生产 build 通过，Playwright smoke 19 通过、3 个按设备类型设计跳过；真实浏览器三档视口无页面溢出；`git diff --check` 和规则/残留检索通过。第一次不排除任何项的 Backend 全集为 820/824：其中 3 项由本轮测试基线/Simulation schema 更新修复并专项复验通过，剩余 1 项 `SecurityHardeningTests.DeploymentScriptsAndDocs_ShouldKeepSingleStandardReleasePath` 是执行前已存在的用户部署指南与用户部署测试脏改动不一致（测试要求“标准发布走操作者本机”，指南当前未包含该原句）；本轮未修改或还原该部署链脏改动，并以精确排除该项的 823/823 证明其余全集通过。
- 未执行及原因：未做真实 Cloud 提供方 HTTP E2E、真实 token/权限联调、生产部署、服务器验证、SSH、数据库写入、commit、push 或发布，因此不能宣称 Cloud 已发布或生产已验收。Bootstrap admin 默认值 AI-SEC-033 需要同步修改工作区部署总览/profile，超出本轮 AI-only 写入授权；OpenAPI 客户端生成器需要新增依赖，未获授权，本轮只完成现有依赖可支持的 stage 1 契约增强。

## 2026-07-09 工作区唯一部署入口与 AICopilot 对外口径收口

- 改动范围：新增工作区 `../deploy/Invoke-WorkspaceDeploy.ps1`、`../deploy/README.md`、`../deploy/profiles/template-rules.json`、`../deploy/profiles/current-production.json`，并更新工作区 `../AGENTS.md`、`../docs/总规则.md`、`../docs/上传部署总览.md`、`../docs/历史核心记录.md`，同时修改 `AICopilot 项目部署与维护指南.md`、`deploy/enterprise-ai/README.md`、`AGENTS.md`、`资料/AICopilot业务规则.md` 和本复盘。未修改 AICopilot 业务 API、数据库 schema、运行时业务流程或服务器生产 `.env`。
- 改动原因：AICopilot 虽然已有标准 `local-release.sh` / `deploy-release.sh`，但外部操作者和 AI 仍然要自己记住 `REGISTRY`、`CLOUD_PLATFORM_URL`、SSH 目标、服务选择和 support-files 同步语义，缺少统一外部入口和固定结果摘要。
- 影响面：AICopilot 的对外标准入口现在统一收口到工作区根 `deploy/Invoke-WorkspaceDeploy.ps1 -Target AICopilot ...`；项目内 `deploy/enterprise-ai/local-release.sh`、`build-and-push.sh` 和 `deploy-release.sh` 继续作为被调度实现脚本。统一入口已产出 AICopilot dry-run 摘要 `../artifacts/deploy/workspace/latest-aicopilot.md` 和 validate-only 摘要 `../artifacts/deploy/workspace/history/20260709-162638-aicopilot-validate-only.md`，让后续 AI 直接读取 support-files sync、SSH 目标、deploy 根和 non-root validate 结果。
- 规则提炼：形成长期部署规则，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md`、工作区 `../docs/总规则.md`、`../docs/上传部署总览.md` 和 `../docs/历史核心记录.md`：工作区唯一对外标准执行入口是 `deploy/Invoke-WorkspaceDeploy.ps1`；`deploy/profiles/*.json` 是机器可读部署口径源；AICopilot 当前真实部署根、runner work root、Docker Root Dir、support files sync 目标和统一入口参数变更时，必须先同步更新这些入口与文档，再允许继续改脚本或发布。
- 验证命令：
  - `pwsh -NoProfile -File ../deploy/Invoke-WorkspaceDeploy.ps1 -Target AICopilot -Services httpapi,web -DryRun`
  - `pwsh -NoProfile -File ../deploy/Invoke-WorkspaceDeploy.ps1 -Target AICopilot -Services httpapi,web -ValidateOnly`
  - `rg -n "Invoke-WorkspaceDeploy\\.ps1|current-production\\.json|template-rules\\.json|latest-summary\\.md" "AICopilot 项目部署与维护指南.md" deploy/enterprise-ai/README.md AGENTS.md 资料/AICopilot业务规则.md ../docs/上传部署总览.md ../deploy/README.md`
- 验证结果：通过。统一入口 dry-run 成功调度 AICopilot `local-release.sh --dry-run`，并输出标准 support-files sync 与 `ssh iiot-server-runner` 发布命令；validate-only 成功通过真实服务器 `/srv/enterprise-ai/deploy` 的 `check-release-state-access.sh` 和 `deploy-release.sh --validate-only`。检索确认 AICopilot 项目部署文档、项目规则、工作区总览和机器可读 profile 已统一指向唯一入口。

## 2026-07-09 同机双部署根口径显式化与 AICopilot support-file 门禁补强

- 改动范围：更新 `deploy/enterprise-ai/local-release.sh`、`deploy/enterprise-ai/README.md`、`AICopilot 项目部署与维护指南.md`、`AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/AICopilot安全部署契约.md`、`docs/AI架构治理清单.md`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本复盘；同时修复同机 AICopilot 真实部署根 `/srv/enterprise-ai/deploy` 的 `scripts/`、`cloud-readonly/` 和 `.env` owner/mode，并同步最新 `deploy-release.sh` 与 `scripts/check-release-state-access.sh` 到服务器。未修改 CloudPlatform、EdgeClient、AICopilot 业务 API、数据库 schema 或运行时业务流程。
- 改动原因：上一轮虽然给 AICopilot 补了 `releases/*` 门禁，但这轮按同机真实环境回归时发现两个缺口：一是项目文档没有把“与 Cloud 同机、共享标准发布人、独立部署根”说透；二是 `local-release.sh` 在 support files sync 前没有检查远端目标目录可写性，导致同机 `scripts/` 被 root 留成只读后，会在同步阶段直接失败，而 `--validate-only` 还没机会执行。
- 影响面：AICopilot 当前部署文档、项目规则和安全契约现在都明确写出“与 Cloud 同机但部署根独立”的口径，并把共享宿主机事实统一指向工作区 `../docs/上传部署总览.md`。`local-release.sh` 新增 support target preflight，会在同步前显式检查 `REMOTE_DEPLOY_DIR`、`scripts/` 和 `cloud-readonly/` 是否对标准 non-root 用户可读可写；同机真实服务器上的 `scripts/`、`cloud-readonly/` 和 `.env` 已恢复到 `github-runner` 可访问状态，`./deploy-release.sh --validate-only` 已重新通过。
- 规则提炼：形成长期部署规则，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/AICopilot安全部署契约.md` 和工作区 `../docs/上传部署总览.md`：当 AICopilot 与 Cloud 共用同一台宿主机时，必须同时写清共享宿主机事实、共享标准发布人和两个独立部署根；AICopilot 标准 non-root 发布不只要验证 `releases/*`，还必须保证 support files sync 目标目录可写。
- 验证命令：
  - `bash -n deploy/enterprise-ai/local-release.sh deploy/enterprise-ai/deploy-release.sh deploy/enterprise-ai/scripts/check-release-state-access.sh`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~DeploymentScriptsAndDocs_ShouldKeepSingleStandardReleasePath" --no-restore`
  - `REGISTRY=harbor.local:80 CLOUD_PLATFORM_URL=http://cloud.local:81 ./deploy/enterprise-ai/local-release.sh --services web --ssh-target github-runner@<shared-host> --dry-run`
  - `ssh root@10.98.90.154 'chown -R github-runner:github-runner /srv/enterprise-ai/deploy/scripts /srv/enterprise-ai/deploy/cloud-readonly && chown github-runner:github-runner /srv/enterprise-ai/deploy/.env && chmod 600 /srv/enterprise-ai/deploy/.env'`
  - `ssh iiot-server-runner 'cd /srv/enterprise-ai/deploy && ./scripts/check-release-state-access.sh && ./deploy-release.sh --validate-only'`
- 验证结果：通过。AICopilot 定向 `SecurityHardeningTests` 1/1 通过；`local-release.sh --dry-run` 已输出新的 `verify AICopilot deploy support target access` 步骤和 `github-runner@<shared-host>` 标准示例；同机真实服务器上的 `scripts/`、`cloud-readonly/` 和 `.env` 权限恢复后，`check-release-state-access.sh` 与 `deploy-release.sh --validate-only` 均通过，说明 AICopilot 已不再停留在“release state 能过、support files 仍会卡死”的半收口状态。

## 2026-07-09 AICopilot 部署文档与 non-root release-state 门禁收口

- 改动范围：仅修改 `AICopilot` 的部署脚本、部署文档和项目规则，并同步更新工作区统一入口。覆盖 `deploy/enterprise-ai/deploy-release.sh`、`deploy/enterprise-ai/local-release.sh`、新增 `deploy/enterprise-ai/scripts/check-release-state-access.sh`、`AICopilot 项目部署与维护指南.md`、`deploy/enterprise-ai/README.md`、`AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/AICopilot安全部署契约.md` 和本复盘。未修改 CloudPlatform、EdgeClient、业务 DTO、数据库 schema、真实服务器 `.env`、真实发布目录 owner 或运行时业务流程。
- 改动原因：AICopilot 的部署真实慢路径常被误解成“上传限速”，但它根本没有对应的 HTTP 上传节流参数；同时 release state owner/mode、support files sync、当前生产根目录、runner work root 和 Docker Root Dir 分散在多份文档里，没有形成当前现场口径。root/non-root 混跑后，`releases/*` 同样可能在下一轮标准 non-root 发布时卡死。
- 影响面：`deploy-release.sh --validate-only` 现在除了 `.env` / HTTP-only / Cloud readonly 校验外，还会 fail-fast 检查 `releases/current-release*`、`staged-release*`、`previous-release*`、`current-release.summary.md` 的 owner/mode 是否仍适合标准 non-root 路径；`local-release.sh` 会把新的 release-state gate 脚本同步到服务器；项目规则和部署文档明确区分“长期模板/规则”与“当前生产现场口径”，并写死当前真实根目录 `/srv/enterprise-ai/deploy`、runner work root `/data/github-runner/aicopilot`、Docker Root Dir `/data/docker` 以及 AI 的真实慢路径。
- 规则提炼：形成长期部署规则，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/AICopilot安全部署契约.md` 和工作区 `../docs/上传部署总览.md`：如果当前真实部署根目录、runner work root、Docker Root Dir、support files sync 目标或标准部署用户与模板不同，必须先更新“当前生产现场口径”文档，再允许继续改脚本或发布；root 应急路径不得留下 root-owned release state 后直接收口。
- 验证命令：
  - `bash -n deploy/enterprise-ai/deploy-release.sh deploy/enterprise-ai/local-release.sh deploy/enterprise-ai/scripts/check-release-state-access.sh`
  - `REGISTRY=harbor.local:80 CLOUD_PLATFORM_URL=http://cloud.local:81 ./deploy/enterprise-ai/local-release.sh --services web --ssh-target deploy@aicopilot.local --dry-run`
  - `rg -n "check-release-state-access|当前生产现场口径|support files|releases/current-release|1000M" deploy/enterprise-ai AGENTS.md 资料/AICopilot业务规则.md docs/AICopilot安全部署契约.md "AICopilot 项目部署与维护指南.md"`
  - `git diff --check -- deploy/enterprise-ai/deploy-release.sh deploy/enterprise-ai/local-release.sh deploy/enterprise-ai/scripts/check-release-state-access.sh "AICopilot 项目部署与维护指南.md" deploy/enterprise-ai/README.md AGENTS.md 资料/AICopilot业务规则.md docs/AICopilot安全部署契约.md docs/改动复盘与规则沉淀.md`
- 验证结果：通过。`deploy-release.sh`、`local-release.sh` 和新增 `scripts/check-release-state-access.sh` 的 `bash -n` 通过；`local-release.sh --dry-run` 明确输出新的 support files sync 清单和 `check-release-state-access.sh`，说明标准发布会把该门禁带到服务器；规则检索命中新的“当前生产现场口径”、release-state gate 和 support files sync 口径。远端只读 SSH 验证显示当前 `/srv/enterprise-ai/deploy/releases/*` 仍由 `github-runner` 持有且可写，说明本轮新增门禁不会立即阻断现有标准路径，而是用于防止后续 root 混跑回退。`git diff --check` 通过。本轮未直接修改真实服务器 owner。

## 2026-07-07 Cloud AiRead 全量只读端点消费对齐补漏

- 改动范围：AICopilot `CloudAiReadContracts`、`CloudAiReadClient`、`CloudAiReadDocumentAdapter`、`CloudAiReadQueryParameterBuilder`、`CloudAiReadClientTests`、受影响测试桩、`AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/Cloud只读数据分析契约.md`、`docs/frontend-integration-contract-package-2026-05-17.md`、`docs/AI架构治理清单.md` 和本复盘。未修改 `IIoT.CloudPlatform`、`IIoT.EdgeClient`、Cloud 业务写接口、Cloud 数据库、真实服务器 `.env`、生产部署或 Text-to-SQL 执行器。
- 改动原因：复核时发现此前把“高频 DeviceLog/Capacity/ProductionData 已优先走 Cloud AiRead”错误概括成“Cloud-AI 接口已全部对齐”。Cloud `AI只读接口契约.md` 当前正式开放 8 个 `GET /api/v1/ai/read/*` 表面，但 AICopilot 只消费了 `devices`、`capacity/summary`、`capacity/hourly`、`device-logs`、`production-records`，漏了 `processes`、`client-releases`、`device-client-states`。
- 影响面：AICopilot `CloudAiReadEndpointPolicy` GET allowlist 现在覆盖 Cloud 正式 8 个只读端点；`ICloudAiReadClient` 和 `CloudAiReadClient` 增加 `GetProcessesAsync`、`GetClientReleasesAsync`、`GetDeviceClientStatesAsync`；新增 DTO 和 JSON mapping；参数构建只发送 Cloud 声明参数，设备客户端状态搜索把 `clientCode/deviceCode` 转成 `keyword`，不把 `deviceCode` 当正式业务参数透传。语义 intent 路由范围未扩大，高频优先路径和 CloudReadOnly Text-to-SQL fallback 语义保持不变。
- 规则提炼：形成长期规则，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/Cloud只读数据分析契约.md` 和 `docs/AI架构治理清单.md`：AICopilot `CloudAiReadEndpointPolicy` 和 `ICloudAiReadClient` 必须逐项覆盖 Cloud `AI只读接口契约.md` 已批准的正式 `GET /api/v1/ai/read/*` 表面；高频端点接通不等于 Cloud-AI 接口全量对齐。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~CloudAiReadClientTests" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~CloudAiReadClientTests|FullyQualifiedName~SemanticAnalysisRunnerTests|FullyQualifiedName~ToolRegistryGovernanceTests|FullyQualifiedName~CloudReadonlySimulationTests|FullyQualifiedName~FrontendIntegrationContractTests" --no-restore`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore`
  - `rg -n "processes|client-releases|device-client-states|GET allowlist|CloudAiReadEndpointPolicy|高频 DeviceLog/Capacity/ProductionData 接通不等于全量接口对齐" AGENTS.md 资料/AICopilot业务规则.md docs/Cloud只读数据分析契约.md docs/frontend-integration-contract-package-2026-05-17.md docs/AI架构治理清单.md src/services/AICopilot.Services.Contracts/Contracts/CloudAiReadContracts.cs src/infrastructure/AICopilot.Infrastructure/CloudRead src/tests/AICopilot.BackendTests/CloudAiReadClientTests.cs`
  - `git diff --check`
- 验证结果：通过。`CloudAiReadClientTests` 41 条通过，覆盖 8 个 GET allowlist、三个补漏端点路径/参数/DTO 映射以及既有高频端点；组合后端回归 128 条通过，覆盖 CloudAiRead、SemanticAnalysisRunner、ToolRegistryGovernance、CloudReadonlySimulation 和前端契约；ArchitectureTests 82 条通过；静态检索确认规则、文档、客户端、allowlist 和测试均包含三个补漏端点；`git diff --check` 通过。未运行真实 Cloud 服务联调、生产部署或全量 BackendTests，原因是本批只补 AICopilot 对 Cloud 已有只读端点的客户端消费和本地契约门禁。

## 2026-07-07 私有模型 64k seed 和生产部署资料对齐

- 改动范围：AICopilot `MigrationWorkerAiGatewaySeeder`、`Worker`、`.env.example`、`docker-compose.yaml`、`deploy/enterprise-ai/README.md`、`AICopilot 项目部署与维护指南.md`、`AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/AICopilot安全部署契约.md`、`docs/AI架构治理清单.md`、`FreshDatabaseSeedTests`、`SecurityHardeningTests` 和本复盘。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、真实生产数据库、GitHub runner、Vault/OIDC 配置或 HTTPS/TLS/443/HSTS。
- 改动原因：用户确认生产清空重部署后需要把私有模型写入 AI 新库，模型为 `MiniMax-M3-AWQ-INT4`，OpenAI-compatible，最大上下文 64k；同时真实模型网关地址和 API key 不能进入 git，只能通过服务器 `.env` 或本机私密部署手册保留。此前 seed 只用固定占位模型，不能表达生产 `.env` 注入。
- 影响面：fresh DB seed 默认仍创建禁用的私有模型，占位 URL 为 `model.internal.example`，空 API key，不泄露真实内网地址；生产通过 `AICOPILOT_PRIVATE_MODEL_*` 注入 provider、model、base URL、API key、context window、max output、temperature 和 enabled。context window 默认/标准为 `65536`，max output 默认 `4096`。API key 入库前走 `SecretStringEncryptor.Encrypt`，已存在同 provider/model 时只修复密钥格式，不强行覆盖现场 URL、启用状态或参数。
- 规则提炼：形成长期模型部署规则，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/AICopilot安全部署契约.md`、`docs/AI架构治理清单.md`、部署 README 和维护指南：真实私有模型地址/API key 不进 git；生产 `.env` 使用 `AICOPILOT_PRIVATE_MODEL_*`；私有模型 context window 为 64k；API key 必须以 `encv2:` 保护格式入库。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --filter "FullyQualifiedName~FreshDatabaseSeedTests" --no-restore --disable-build-servers`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj -c Release --filter "FullyQualifiedName~SecurityHardeningTests.DeploymentConfig" --no-restore --disable-build-servers`
  - `bash -n deploy/enterprise-ai/*.sh deploy/enterprise-ai/scripts/*.sh`
  - `git diff --check`
  - `rg -n "REAL_PRIVATE_MODEL_IP|REAL_ADMIN_PASSWORD|local-minimax-key" IIoT.CloudPlatform IIoT.EdgeClient AICopilot -g '!**/bin/**' -g '!**/obj/**' -g '!**/artifacts/**' -g '!**/node_modules/**'`
- 验证结果：通过。FreshDatabaseSeedTests 1/1 通过；DeploymentConfig 门禁 1/1 通过；部署脚本 `bash -n` 通过；`git diff --check` 通过；真实私有模型地址和本机私密值未进入可执行配置或仓库文档，架构测试只保留通用内网 IP 模式负向断言。本轮未连接真实私有模型网关、未执行生产 migration、未执行线上模型 smoke；这些必须在生产部署窗口按 `.env` 和 `check-model-provider-openai.sh` 验收。

## 2026-07-07 过期 A助理阶段文档入口删除

- 改动范围：AICopilot 删除 `docs/A助理已知问题清单.md`、`docs/A助理Simulation模式说明.md`、`docs/A助理前后端Integration分支说明.md`、`docs/A助理真实CloudReadonly准备说明.md`、`docs/A助理错误码与前端提示说明.md`、`docs/A助理权限矩阵.md`，并更新 `src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、运行代码、真实服务器 `.env`、生产数据库、GitHub runner、Vault/OIDC 配置或发布状态；未引入 HTTPS、HSTS、nginx 443 或证书流程。
- 改动原因：执行第 2 节静态审计时发现旧 `A助理*.md` 文档仍保留在 `docs/`。其中已知问题清单包含“生产建议升级 AES-GCM”“npm audit 当前不阻断”等过期或容易与当前规则冲突的说法；Simulation/Integration/CloudReadonly 准备说明虽然部分带历史提示，但继续存在会污染安全扫描，也可能被误当作当前执行入口；错误码草稿和静态权限矩阵也容易与当前 `frontend-integration-contract-package-2026-05-17.md`、`PermissionCatalog` 漂移。
- 影响面：过期阶段材料、历史联调说明、草稿错误码说明和静态权限矩阵从 AICopilot 当前文档入口移除。长期结论已经沉淀在 `AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/AI架构治理清单.md`、`docs/Cloud只读数据分析契约.md`、`docs/Agent工作流与异常契约.md`、`docs/AICopilot安全部署契约.md` 和 `docs/frontend-integration-contract-package-2026-05-17.md`。`SecurityHardeningTests.DeploymentScriptsAndDocs_ShouldKeepSingleStandardReleasePath` 新增断言，防止这些旧文档恢复为执行入口。
- 计划收口：推进第四轮“收文档和历史入口”，减少过期材料对安全扫描和执行入口的干扰。`AI-SEC-010` 和 `AI-SEC-012` 状态不变，仍分别需要真实平台和真实生产库发布窗口验收。
- 规则提炼：无新增长期规则，原因是“阶段计划、批次验收报告、草稿和旧执行入口不得继续作为执行入口”的规则已经写入 `AGENTS.md`、`资料/AICopilot业务规则.md` 和 `docs/AI架构治理清单.md`；本批只是删除已被当前规则/专题契约替代的旧文档，并增加测试门禁。
- 验证命令：
  - `rg -n "A助理已知问题清单|A助理Simulation模式说明|A助理前后端Integration分支说明|A助理真实CloudReadonly准备说明|A助理错误码与前端提示说明|A助理权限矩阵|docs/A助理" . -g '!docs/改动复盘与规则沉淀.md' -g '!docs/AI架构治理清单.md' -g '!src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs'`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests.DeploymentScriptsAndDocs_ShouldKeepSingleStandardReleasePath" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `rg -n '旧 A助理|过期计划和旧入口|A助理.*\\.md|A助理已知问题清单|A助理权限矩阵' docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`
  - `git diff --check -- docs/A助理已知问题清单.md docs/A助理Simulation模式说明.md docs/A助理前后端Integration分支说明.md docs/A助理真实CloudReadonly准备说明.md docs/A助理错误码与前端提示说明.md docs/A助理权限矩阵.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
- 验证结果：通过。旧文档名在排除本复盘、治理清单和测试删除门禁后无命中；治理清单、复盘和测试中只保留旧入口删除说明与 `File.Exists(...).Should().BeFalse()` 门禁；`DeploymentScriptsAndDocs_ShouldKeepSingleStandardReleasePath` 定向通过；完整 `SecurityHardeningTests` 通过；目标文件 `git diff --check` 通过；目标文件尾随空白检索无命中。未运行真实服务器、生产库、GitHub runner 或发布验收，原因是本批只做 AICopilot 文档入口收口和测试门禁。

## 2026-07-07 AI-SEC-010 平台记录 hosted runner 和 secret 限制证据补强

- 改动范围：AICopilot `deploy/enterprise-ai/scripts/check-platform-attestation-record.sh`、`deploy/enterprise-ai/README.md`、`docs/AI架构治理清单.md`、`docs/AICopilot安全部署契约.md`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实 GitHub production environment、真实 runner 机器、Vault/OIDC 配置、生产服务器 `.env` 或真实发布状态；未引入 HTTPS、HSTS、nginx 443 或证书流程。
- 改动原因：继续审计 `AI-SEC-010` 时确认，仓库内 workflow 静态测试已能防止 production/secrets workflow 回退到 GitHub hosted runner，但平台验收记录 linter 只要求 `required reviewers`、`contents: read` 和 `self-hosted + iiot-linux-prod` 等关键词。手写记录理论上可以缺少“production environment secrets 已限制”和“生产/secret workflow 无 GitHub hosted runner”的外部证据说明，却仍通过 linter。
- 影响面：`check-platform-attestation-record.sh` 新增两项必填证据：production environment secret 限制证据，以及生产/secret workflow 无 GitHub hosted runner 证据。`SecurityHardeningTests.PlatformAttestationRecordCheck_ShouldRejectIncompleteSignOffsAndWeakEvidenceWords` 增加缺少 secret 限制证据和缺少 hosted runner 否定证据的失败样例；README、AI 治理清单和部署安全契约同步说明新的 linter 能力边界。
- 计划收口：进一步补强 `AI-SEC-010` 的仓库内证据质量门禁，避免平台侧手工记录只靠泛化关键词通过。`AI-SEC-010` 仍保持 `Partial`，因为真实 GitHub environment secret 权限、required reviewers、runner 机器权限、Vault/OIDC 或等价短期凭据仍需平台侧真实验收。
- 规则提炼：形成长期部署证据口径，已写入 `docs/AICopilot安全部署契约.md` 和 `docs/AI架构治理清单.md`：平台验收记录必须覆盖 production environment secret 限制、required reviewers、least privilege、self-hosted runner、无 GitHub hosted runner、runner 本机脚本结果，以及 OIDC/Vault 已落地或已批准基础设施例外。无需写入工作区总规则，原因是这是 AICopilot 灾备 workflow 和平台记录专项门禁。
- 验证命令：
  - `bash -n deploy/enterprise-ai/scripts/check-platform-attestation-record.sh`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests.PlatformAttestationRecordCheck_ShouldRejectIncompleteSignOffsAndWeakEvidenceWords" --no-restore`
  - `bash -n deploy/enterprise-ai/*.sh deploy/enterprise-ai/scripts/*.sh`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `rg -n "production environment secret restriction evidence|no hosted runner evidence|Environment secrets.*restricted|No production or secret-touching workflow uses GitHub hosted runners|无 GitHub hosted runner|secret 限制" deploy/enterprise-ai/scripts/check-platform-attestation-record.sh deploy/enterprise-ai/runner-platform-attestation.template.md deploy/enterprise-ai/README.md docs/AI架构治理清单.md docs/AICopilot安全部署契约.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`
  - `rg -n "HTTP-only|不得把 HTTPS|Strict-Transport-Security|listen 443|ssl_certificate|UseHttpsRedirection|证书" docs/AI架构治理清单.md docs/AICopilot安全部署契约.md AGENTS.md 资料/AICopilot业务规则.md`
  - `git diff --check -- deploy/enterprise-ai/scripts/check-platform-attestation-record.sh deploy/enterprise-ai/README.md docs/AI架构治理清单.md docs/AICopilot安全部署契约.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" deploy/enterprise-ai/scripts/check-platform-attestation-record.sh deploy/enterprise-ai/README.md docs/AI架构治理清单.md docs/AICopilot安全部署契约.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
- 验证结果：通过。脚本语法检查通过；平台验收记录行为测试 1 条通过，覆盖有效 OIDC/Vault、有效批准例外、空签署、弱证明词、缺少 secret 限制证据和缺少 hosted runner 否定证据；完整 `SecurityHardeningTests` 76 条通过；全部部署脚本 `bash -n` 通过；静态检索确认脚本、模板、README、治理清单、部署安全契约和测试都包含新增证据口径；HTTP-only 红线检索仍只命中禁止项、扫描命令和现有 HTTP-only 规则，未引入 HTTPS/HSTS/443/证书流程；目标文件 `git diff --check` 通过；目标文件尾随空白检索无命中。未运行真实 GitHub environment、runner 机器、Vault/OIDC 或生产发布验收，原因是这些仍是 `AI-SEC-010` 外部基础设施待验收项。

## 2026-07-07 AI-SEC-012 模型密钥迁移共享事务补强

- 改动范围：AICopilot `src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实生产数据库、真实服务器 `.env`、GitHub runner、Vault/OIDC 配置或生产发布状态；未引入 HTTPS、HSTS、nginx 443、证书申请/续期或 `UseHttpsRedirection`。
- 改动原因：继续审计 `AI-SEC-012` 时发现，治理清单写着避免 `LanguageModel.ApiKey` 与 `EmbeddingModel.ApiKey` 数据库级部分迁移，但当前 `MigrateAsync` 实际是 `AiGatewayDbContext` 和 `RagDbContext` 分别 `SaveChangesAsync`。这只能靠写入前预检降低失败概率，不能证明两类模型密钥数据库写入同提交/同回滚。
- 影响面：`MigrationWorkerSecretMigrator.MigrateAsync` 现在先在 `AiGatewayDbContext` 上开启事务，再用同一个 PostgreSQL connection 创建临时 `RagDbContext`，并通过 `UseTransactionAsync(transaction.GetDbTransaction())` 绑定同一事务；`LanguageModel` 与 `EmbeddingModel` 密钥迁移保存后统一 commit，任一保存、自检或提交前异常都会 rollback。`RagDbContext.SaveChangesAsync` 已有 `Database.CurrentTransaction` 分支，复用外部事务时不会再创建嵌套事务。
- 计划收口：补强 `AI-SEC-012` 的仓库内原子性证据，治理清单同步区分“同一集合内预检避免内存级部分改写”和“共享事务避免数据库级部分迁移”。`AI-SEC-012` 仍保持 `Partial`，原因是真实生产库 migration、管理员重录入和发布窗口验收仍必须在外部环境执行并留痕。
- 规则提炼：形成长期实现门禁，已写入 `docs/AI架构治理清单.md`：模型密钥迁移跨 `AiGatewayDbContext` 与 `RagDbContext` 写入必须使用共享事务，测试必须锁定 `BeginTransactionAsync -> UseTransactionAsync -> MigrateInCurrentTransactionAsync -> CommitAsync` 结构。无需新增工作区总规则，原因是这是 AICopilot 模型密钥迁移的专项约束。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~MigrationWorkerSecretMigratorTests|FullyQualifiedName~SecurityHardeningTests.MigrationWorker_ShouldMigrateAllModelSecretFormatsBeforeRuntimeStarts" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `rg -n "HTTP-only|不得把 HTTPS|Strict-Transport-Security|listen 443|ssl_certificate|UseHttpsRedirection|证书" docs/AI架构治理清单.md docs/AICopilot安全部署契约.md AGENTS.md 资料/AICopilot业务规则.md`
  - `rg -n "MigrateInCurrentTransactionAsync|Database.BeginTransactionAsync|UseTransactionAsync|transaction.GetDbTransaction\\(\\)|Database.CurrentTransaction|共享事务结构|数据库级部分迁移" src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs src/infrastructure/AICopilot.EntityFrameworkCore/RagDbContext.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md`
  - `git diff --check -- src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
- 验证结果：通过。定向 migration/helper 与新增结构门禁 11 条通过；完整 `SecurityHardeningTests` 76 条通过；HTTP-only 红线检索确认仍明确禁止把 HTTPS/HSTS/443/证书流程作为当前硬门槛；事务结构检索确认 migrator 使用 `BeginTransactionAsync`、`UseTransactionAsync(transaction.GetDbTransaction())` 和 `MigrateInCurrentTransactionAsync`，且 `RagDbContext` 仍保留外部事务分支；目标文件 `git diff --check` 通过；目标文件尾随空白检索无命中。未运行真实生产库 migration 或发布验收，原因是这些仍属于 `AI-SEC-012` 外部发布窗口待验收项。

## 2026-07-07 历史复盘真实内网地址脱敏

- 改动范围：AICopilot `docs/改动复盘与规则沉淀.md`、`docs/AI架构治理清单.md`、`docs/AICopilot安全部署契约.md`、`AGENTS.md` 和 `资料/AICopilot业务规则.md`。未修改 CloudPlatform、EdgeClient、运行代码、真实服务器 `.env`、真实生产数据库、GitHub runner、Vault/OIDC 配置或生产发布状态。
- 改动原因：继续执行 AI 完整修复计划的 secret/IP 审计时发现，当前生产源码、部署模板和 seed 已有门禁，但历史滚动复盘仍保留真实生产服务器、模型网关和 Harbor 地址，以及可复制的 root SSH 形式命令。这些历史记录不是当前配置默认值，但会污染真实 IP 扫描，也不符合“文档示例不得泄露真实内网地址”的治理口径。
- 影响面：历史诊断记录中的真实服务器、模型网关、Harbor 地址改为 `<aicopilot-prod-host>`、`<model-gateway-host>`、`<ssh-user>@<aicopilot-prod-host>` 等角色化占位；治理清单中的地址审计命令从特定现场网段改为通用私网地址模式；专题部署契约、项目规则和业务规则新增“滚动复盘/历史诊断记录也不得携带真实内网 IP、弱 secret、dummy key 或默认 root 发布目标”的长期口径。
- 计划收口：补强 `AI-SEC-004`、`AI-SEC-006` 和部署安全契约的文档证据质量，避免后续靠 `rg` 审计时把历史记录误判为当前源码/模板泄露，或反过来因为历史记录噪声忽略真实泄露。`AI-SEC-010` 和 `AI-SEC-012` 状态不变，仍需要外部 runner/OIDC/Vault 和真实生产库发布窗口验收。
- 规则提炼：形成长期文档脱敏规则，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md` 和 `docs/AICopilot安全部署契约.md`：部署模板、文档示例、滚动复盘和历史诊断记录都不得携带真实内网 IP、弱 secret、`CHANGE_ME`、`dummy-key` 或默认 `root@` 发布目标。
- 验证命令：
  - `rg -n "[1]0\\.98\\.|[1]0\\\\\\.98|http://[1]0\\.98|http://[1]0\\\\\\.98|root@[1]0\\.|[r]oot@<aicopilot-prod-host>" docs/改动复盘与规则沉淀.md docs/AI架构治理清单.md docs/AICopilot安全部署契约.md AGENTS.md 资料/AICopilot业务规则.md`
  - `rg -n "<aicopilot-prod-host>|<model-gateway-host>|<ssh-user>@<aicopilot-prod-host>|滚动复盘、历史诊断记录|滚动复盘和历史诊断记录|历史复盘真实内网地址脱敏" docs/改动复盘与规则沉淀.md docs/AICopilot安全部署契约.md AGENTS.md 资料/AICopilot业务规则.md`
  - `git diff --check -- docs/改动复盘与规则沉淀.md AGENTS.md 资料/AICopilot业务规则.md`
  - `rg -n "[ \t]+$" docs/改动复盘与规则沉淀.md docs/AI架构治理清单.md docs/AICopilot安全部署契约.md AGENTS.md 资料/AICopilot业务规则.md`
- 验证结果：通过。真实现场网段、对应转义网段、真实 HTTP 地址和 `root@` 生产主机形式在目标文档中无命中；占位主机、占位 SSH 用户、滚动复盘/历史诊断记录长期规则和本复盘条目均可检索；已跟踪目标文件 `git diff --check` 通过；全部目标文件尾随空白检索无命中。未运行 build/test，原因是本批只做 AICopilot 文档脱敏和规则口径收口，未改运行代码。

## 2026-07-07 AI-SEC-016 raw exception logger 变量名门禁补强

- 改动范围：AICopilot `src/services/AICopilot.AiGatewayService/Workflows/Executors/ToolsPackExecutor.cs`、`src/services/AICopilot.Services.CrossCutting/Behaviors/TelemetryBehaviors.cs`、`src/services/AICopilot.RagService/Commands/Documents/UploadDocument.cs`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`、`docs/AI架构治理清单.md`、`docs/Agent工作流与异常契约.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、真实服务器 `.env`、真实生产数据库、GitHub runner、Vault/OIDC 配置或生产发布状态。
- 改动原因：继续按治理清单跑生产源码静态扫描时发现，旧门禁主要拦 `LogError(ex, ...)` / `LogWarning(ex, ...)`，但 `ToolsPackExecutor` 使用 `logger.LogError(e, ...)`，`TelemetryBehaviors` 使用 `logger.LogWarning(exception, ...)`，`UploadDocument` 清理失败使用 `logger.LogWarning(cleanupException, ...)` 并记录 `originalException.Message`。这些变量名绕过了旧静态测试，但仍会把 raw exception 或原始异常 message 交给日志系统。
- 影响面：`ToolsPackExecutor` 工具包加载失败仍返回空工具分支，不阻断主流程，但日志只记录 `ErrorType` 和 `OriginalMessage=hidden_by_security_policy`。MediatR telemetry 仍记录请求类型、kind、耗时和异常类型，不附带 exception 对象。RAG 上传事务失败后的文件清理失败仍记录文件路径和两类异常类型，不记录 cleanup exception 对象或原始上传失败 message。`SecurityHardeningTests.ProductionLogs_ShouldNotAttachRawExceptionObjects` 改为扫描 `LogCritical/LogError/LogWarning/LogInformation/LogDebug/LogTrace` 中任意变量首参，覆盖 `ex`、`exception`、`e`、`cleanupException` 等命名。
- 计划收口：补强 `AI-SEC-016` 的静态门禁和文档证据，避免通过改异常变量名绕过“不得记录 raw exception”的安全口径。当前 `AI-SEC-016` 仓库内仍保持 Done；本批是门禁补强和漏网修复。
- 规则提炼：形成长期异常日志门禁，已写入 `docs/Agent工作流与异常契约.md` 和 `docs/AI架构治理清单.md`：生产日志不得把异常变量作为 logger 首参，必须记录异常类型、固定错误码或安全摘要。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests.ProviderWorkflowAndWorkerLogs_ShouldNotAttachRawExceptions|FullyQualifiedName~SecurityHardeningTests.ProductionLogs_ShouldNotAttachRawExceptionObjects" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `rg -n "Log(Critical|Error|Warning|Information|Debug|Trace)\\(\\s*[a-zA-Z_][a-zA-Z0-9_]*\\s*," src/hosts src/infrastructure src/services src/shared src/core -g '!**/bin/**' -g '!**/obj/**'`
  - `rg -n "OriginalMessage=hidden_by_security_policy|CleanupErrorType|OriginalErrorType|LoggerFirstArgumentVariablePattern|ToolsPackExecutor|TelemetryBehaviors|UploadDocument" src/services/AICopilot.AiGatewayService/Workflows/Executors/ToolsPackExecutor.cs src/services/AICopilot.Services.CrossCutting/Behaviors/TelemetryBehaviors.cs src/services/AICopilot.RagService/Commands/Documents/UploadDocument.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md docs/Agent工作流与异常契约.md docs/改动复盘与规则沉淀.md`
  - `git diff --check -- src/services/AICopilot.AiGatewayService/Workflows/Executors/ToolsPackExecutor.cs src/services/AICopilot.Services.CrossCutting/Behaviors/TelemetryBehaviors.cs src/services/AICopilot.RagService/Commands/Documents/UploadDocument.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md docs/Agent工作流与异常契约.md docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" src/services/AICopilot.AiGatewayService/Workflows/Executors/ToolsPackExecutor.cs src/services/AICopilot.Services.CrossCutting/Behaviors/TelemetryBehaviors.cs src/services/AICopilot.RagService/Commands/Documents/UploadDocument.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md docs/Agent工作流与异常契约.md docs/改动复盘与规则沉淀.md`
- 验证结果：通过。定向 `ProviderWorkflowAndWorkerLogs_ShouldNotAttachRawExceptions` 和 `ProductionLogs_ShouldNotAttachRawExceptionObjects` 2 条通过；完整 `SecurityHardeningTests` 76 条通过；生产源码 `src/hosts`、`src/infrastructure`、`src/services`、`src/shared`、`src/core` 中 `LogCritical/LogError/LogWarning/LogInformation/LogDebug/LogTrace` 以变量作为首参的扫描无命中；静态检索确认 `ToolsPackExecutor`、MediatR telemetry 和 RAG 上传清理失败均只记录异常类型和 `OriginalMessage=hidden_by_security_policy`；目标文件 `git diff --check` 通过；目标文件尾随空白检索无命中。未运行真实服务器日志、线上 trace 或完整发布验收，原因是本批只修复本地生产源码日志门禁。

## 2026-07-07 AI 专题契约拆分补齐

- 改动范围：AICopilot 新增 `docs/AICopilot安全部署契约.md`、`docs/Cloud只读数据分析契约.md`、`docs/Agent工作流与异常契约.md`，并更新 `AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、运行代码、真实服务器 `.env`、真实生产数据库、GitHub runner、Vault/OIDC 配置或生产发布状态。
- 改动原因：继续按“完整 AI 修复计划”做完成审计时发现，总治理清单已经存在，但用户最初点名的三个 AI 端专题契约文件尚未落盘。只靠总清单会让部署安全、Cloud 只读数据分析、Agent workflow/异常边界混在一个入口里，后续执行容易按印象跳过对应源码和测试。
- 影响面：三份专题契约分别把 HTTP-only 部署、Cloud 只读/Text-to-SQL、Agent workflow/异常/前端错误拆成独立长期规则入口，并写明源码归属、禁止项、验收命令和外部依赖。`AGENTS.md` 和 `资料/AICopilot业务规则.md` 新增专题契约入口；`docs/AI架构治理清单.md` 引用三份契约并要求关单时与测试口径一致。
- 计划收口：补齐 AI 端文档交付物，避免把专题契约缺失伪造成“清单已覆盖”。HTTP-only 红线保持不变，仍明确不引入 HTTPS、HSTS、443 或证书流程。`AI-SEC-010` 和 `AI-SEC-012` 仍保持 Partial，因为外部 runner/OIDC/Vault 和真实生产库发布窗口验收不能由文档替代。
- 规则提炼：形成长期专题契约入口，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md` 和 `docs/AI架构治理清单.md`。后续触碰部署、Cloud 只读、Text-to-SQL、Agent workflow、MCP/Tool、异常或前端错误时，必须先读对应专题契约。
- 验证命令：
  - `rg -n "AICopilot安全部署契约|Cloud只读数据分析契约|Agent工作流与异常契约|专题契约入口|HTTP-only|不得强制引入 HTTPS" AGENTS.md 资料/AICopilot业务规则.md docs/AI架构治理清单.md docs/AICopilot安全部署契约.md docs/Cloud只读数据分析契约.md docs/Agent工作流与异常契约.md docs/改动复盘与规则沉淀.md`
  - `rg -n "Strict-Transport-Security|listen 443|ssl_certificate|UseHttpsRedirection|证书申请" docs/AICopilot安全部署契约.md docs/AI架构治理清单.md AGENTS.md 资料/AICopilot业务规则.md`
  - `rg -n "production-records|PreviousSqlForRepair|CloudReadOnlyGovernedSchema|Simulation|MockOnly|DeviceLog" docs/Cloud只读数据分析契约.md docs/AI架构治理清单.md`
  - `rg -n "AgentWorkflowPipeline|PlanDraft|ExecutablePlan|ProblemDetails|hidden_by_security_policy|runtimeDetails|catch" docs/Agent工作流与异常契约.md docs/AI架构治理清单.md`
  - `git diff --check -- AGENTS.md 资料/AICopilot业务规则.md docs/AI架构治理清单.md docs/AICopilot安全部署契约.md docs/Cloud只读数据分析契约.md docs/Agent工作流与异常契约.md docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" AGENTS.md 资料/AICopilot业务规则.md docs/AI架构治理清单.md docs/AICopilot安全部署契约.md docs/Cloud只读数据分析契约.md docs/Agent工作流与异常契约.md docs/改动复盘与规则沉淀.md`
- 验证结果：通过。专题契约入口、三份契约文件、HTTP-only 红线和“不强制引入 HTTPS/HSTS/443/证书”的口径均可检索；HTTPS/HSTS/443/证书相关词只出现在禁止项、扫描命令或不得输出的安全检查中；Cloud 只读契约中的 `production-records`、`PreviousSqlForRepair`、`CloudReadOnlyGovernedSchema`、Simulation/MockOnly 和 DeviceLog 口径能与总治理清单对应；Agent 契约中的 `AgentWorkflowPipeline`、`PlanDraft`、`ExecutablePlan`、ProblemDetails、`hidden_by_security_policy`、运行详情和 catch 口径能与总治理清单对应；目标文件 `git diff --check` 通过；目标文件尾随空白检索无命中。本批只改文档和规则入口，未运行 build/test，未连接真实服务器、真实生产数据库、GitHub runner 或 Vault/OIDC。

## 2026-07-07 模型密钥发布验收增加当前主密钥解密检查

- 改动范围：AICopilot `src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs`、`src/hosts/AICopilot.MigrationWorkApp/Worker.cs`、`deploy/enterprise-ai/scripts/check-model-secret-migration.sh`、`deploy/enterprise-ai/README.md`、`AICopilot 项目部署与维护指南.md`、`docs/AI架构治理清单.md`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实生产库、真实服务器 `.env` 或生产发布状态。
- 改动原因：继续复核 `AI-SEC-012` 时发现，`check-model-secret-migration.sh` 只用 SQL 统计 `encv1` 和非 `encv2` 前缀，不能证明已有 `encv2:` 能用当前 `AICOPILOT_API_KEY_ENCRYPTION_KEY` 解密。如果发布时主密钥配错，前缀检查会通过，运行时 provider 才失败；这不符合“发布前 fail-fast”的治理口径。
- 影响面：`MigrationWorkerSecretMigrator` 新增 `VerifyAsync`，只查询现有 `LanguageModel.ApiKey` 和 `EmbeddingModel.ApiKey` 并复用 `EnsureMigratedSecrets` 验证非空值必须是 `encv2:` 且当前主密钥可解密。`Worker` 新增 `MigrationWorker:CheckSecretsOnly` 分支，执行只读密钥验收后停止宿主，不跑 migrations/seed。`check-model-secret-migration.sh` 在 SQL 前缀统计通过后，用 `docker compose run --rm --no-deps -e MigrationWorker__CheckSecretsOnly=true aicopilot-migration` 进行当前主密钥解密验收；dry-run 也会打印该检查计划。
- 计划收口：补强 `AI-SEC-012` 发布前和发布后 attestation 证据，避免只凭 `encv2:` 前缀误判生产库已可用。`AI-SEC-012` 仍保持 `Partial`，原因是真实生产库 migration 和真实主密钥验收仍需要发布窗口执行。
- 规则提炼：无新增长期规则，原因是“`encv2:` 必须可用当前主密钥解密、真实生产库必须发布窗口验收”的长期规则已经沉淀在 `docs/AI架构治理清单.md`、`AGENTS.md` 和 `资料/AICopilot业务规则.md`；本批只是把独立验收脚本提升到同一强度。
- 验证命令：
  - `bash -n deploy/enterprise-ai/scripts/check-model-secret-migration.sh`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~MigrationWorkerSecretMigratorTests" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `rg -n "CheckSecretsOnly|VerifyAsync|MigrationWorker__CheckSecretsOnly=true|decryptability attestation|当前主密钥|AICOPILOT_API_KEY_ENCRYPTION_KEY" src/hosts/AICopilot.MigrationWorkApp deploy/enterprise-ai/scripts/check-model-secret-migration.sh deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md" docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
  - `git diff --check -- src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs src/hosts/AICopilot.MigrationWorkApp/Worker.cs deploy/enterprise-ai/scripts/check-model-secret-migration.sh deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md" docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs src/hosts/AICopilot.MigrationWorkApp/Worker.cs deploy/enterprise-ai/scripts/check-model-secret-migration.sh deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md" docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
- 验证结果：通过。`check-model-secret-migration.sh` 脚本语法检查通过；`MigrationWorkerSecretMigratorTests` 定向 10 条通过；`SecurityHardeningTests` 定向 76 条通过；静态检索确认 migration worker、脚本、README、部署指南、治理清单、测试和复盘均包含 `CheckSecretsOnly`、`VerifyAsync`、`MigrationWorker__CheckSecretsOnly=true` 和当前主密钥解密验收口径；目标文件 `git diff --check` 通过；目标文件尾随空白检索无命中。真实生产 PostgreSQL、真实 `AICOPILOT_API_KEY_ENCRYPTION_KEY` 和真实 Docker Compose 未连接，原因是需要发布窗口和真实服务器环境。

## 2026-07-07 deploy-release validate-only 环境门禁

- 改动范围：AICopilot `deploy/enterprise-ai/deploy-release.sh`、`deploy/enterprise-ai/README.md`、`AICopilot 项目部署与维护指南.md`、`docs/AI架构治理清单.md`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、真实 Docker Compose 服务或生产发布状态。
- 改动原因：继续复核 `AI-SEC-005/006` 时发现，`validate_deploy_environment` 只能在真实发布主路径里触发；操作者如果只想验证 HTTP-only URL、Cloud OIDC 内网 issuer、弱 secret、占位符和 `.env` 权限，必须进入完整发布流程。这样仓库内证据仍偏静态，真实配置问题也容易到发版时才暴露。
- 影响面：`deploy-release.sh` 新增 `--validate-only`，该模式不需要 release tag，不改写镜像 tag，不执行 Docker/Docker Compose，只加载 `.env` 并执行 `validate_deploy_environment`。部署 README、部署维护指南和治理清单第 9 节均补充发布前环境校验命令。`SecurityHardeningTests.DeployReleaseValidateOnly_ShouldValidateHttpOnlyOidcWithoutReleaseTagOrDocker` 用临时 600 权限 `.env` 真实执行脚本，断言保留内网 DNS OIDC 通过、公共 HTTP OIDC 失败。
- 计划收口：把 `AI-SEC-005/006` 从静态描述推进到可执行 preflight-only 证据，发布前即可验证 HTTP-only/OIDC/secret 红线。没有引入 HTTPS、HSTS、443 或证书流程。
- 规则提炼：无新增长期规则，原因是部署前必须 fail-fast 校验 HTTP-only/OIDC/secret 的长期规则已经沉淀在 `AGENTS.md`、`资料/AICopilot业务规则.md`、部署文档和 `docs/AI架构治理清单.md`；本批只是补充不发布的执行入口。
- 验证命令：
  - `bash -n deploy/enterprise-ai/deploy-release.sh`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests.DeployReleaseValidateOnly_ShouldValidateHttpOnlyOidcWithoutReleaseTagOrDocker" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `rg -n "validate-only|DeployReleaseValidateOnly|AICopilot deploy environment validation passed|发布前环境校验|ENV_FILE=<server-.env>" deploy/enterprise-ai/deploy-release.sh deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md" docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
  - `git diff --check -- deploy/enterprise-ai/deploy-release.sh deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md" docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" deploy/enterprise-ai/deploy-release.sh deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md" docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
- 验证结果：通过。`deploy-release.sh` 脚本语法检查通过；新增 `DeployReleaseValidateOnly_ShouldValidateHttpOnlyOidcWithoutReleaseTagOrDocker` 行为测试通过，证明 `--validate-only` 不需要 release tag、可用临时 600 权限 `.env` 运行、保留内网 DNS OIDC 通过且公共 HTTP OIDC 失败；`SecurityHardeningTests` 完整定向 76 条通过；静态检索确认脚本、README、部署指南、治理清单、测试和复盘均包含 validate-only 入口；目标文件 `git diff --check` 通过；目标文件尾随空白检索无命中。真实服务器 `.env`、Docker Compose、Cloud OIDC 和生产发布未运行，原因是本批只补发布前环境门禁入口和仓库内行为测试。

## 2026-07-07 Cloud OIDC 公共 HTTP 域名发布前拦截

- 改动范围：AICopilot `deploy/enterprise-ai/deploy-release.sh`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、真实 Cloud OIDC 服务或生产发布状态。
- 改动原因：上一批已把 runtime `CloudOidcOptions` 的内网 HTTP OIDC host 判定补齐，但继续复核发现 `deploy-release.sh` 只检查 `CLOUD_OIDC_ISSUER` 是 HTTP、`ALLOW_INTRANET_HTTP_OIDC=true`、`CLOUD_OIDC_REQUIRE_HTTPS_METADATA=false`，没有提前拒绝 `http://cloud.example.com` 这类公共 HTTP 域名。这样会让错误配置通过发布前 preflight，直到 HttpApi runtime 启动校验才失败。
- 影响面：`deploy-release.sh` 新增 `require_intranet_http_oidc_issuer`，发布前解析 HTTP issuer host，只允许 `localhost`、`127.*`、`::1`、`10.*`、`192.168.*`、`172.16-31.*` 和保留内网 DNS 后缀 `.internal.example`、`.internal`、`.lan`、`.local`；公共 HTTP 域名直接 fail-fast。`SecurityHardeningTests` 增加静态门禁，锁定该函数、后缀列表和错误文案。
- 计划收口：补强 `AI-SEC-005/006` 的部署 preflight 证据，使发布脚本和 runtime 的 HTTP-only OIDC 边界一致。没有引入 HTTPS、HSTS、443 或证书流程。
- 规则提炼：无新增长期规则，原因是“Cloud OIDC HTTP issuer 只允许显式内网 HTTP、私网/保留内网 DNS，公共 HTTP 域名拒绝”的长期规则已在上一条复盘写入 `AGENTS.md`、`资料/AICopilot业务规则.md` 和 `docs/AI架构治理清单.md`；本批只是把同一规则接入发布 preflight。
- 验证命令：
  - `bash -n deploy/enterprise-ai/deploy-release.sh`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests.DeploymentConfig_ShouldNotCarryKnownWeakSecrets" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `rg -n "require_intranet_http_oidc_issuer|is_allowed_intranet_http_oidc_host|reserved intranet DNS suffix|公共 HTTP OIDC|Cloud OIDC issuer 仅限" deploy/enterprise-ai/deploy-release.sh src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `git diff --check -- deploy/enterprise-ai/deploy-release.sh src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" deploy/enterprise-ai/deploy-release.sh src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
- 验证结果：通过。`deploy-release.sh` 脚本语法检查通过；`DeploymentConfig_ShouldNotCarryKnownWeakSecrets` 定向 1 条通过；`SecurityHardeningTests` 定向 75 条通过；静态检索确认发布脚本、测试、治理清单和复盘均包含 OIDC issuer host fail-fast 口径；目标文件 `git diff --check` 通过；目标文件尾随空白检索无命中。真实服务器 `.env` 和 Cloud OIDC 未连接，原因是需要真实内网环境和发布/运维窗口。

## 2026-07-07 HTTP-only Cloud OIDC 内网 DNS 判定补强

- 改动范围：AICopilot `src/hosts/AICopilot.HttpApi/Infrastructure/CloudOidcOptions.cs`、`src/tests/AICopilot.BackendTests/CloudOidcLoginTestsControllerSecurity.cs`、`AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实 Cloud OIDC 配置、真实服务器 `.env` 或生产发布状态。
- 改动原因：回扫 HTTP-only 红线时发现，`.env.example` 和部署文档用 `http://cloud.internal.example:81` 表示内网 HTTP OIDC，但 `CloudOidcOptions` 只允许 loopback 和私网 IPv4；这会导致文档/模板口径和运行时校验不一致。当前红线是 HTTP-only，不能把问题改成强制 HTTPS，但也不能放宽成任意公共 HTTP 域名。
- 影响面：`CloudOidcOptions` 在显式 `AllowIntranetHttpOidc` 开启时，除 loopback 和私网 IPv4 外，允许保留内网 DNS 后缀 `.internal.example`、`.internal`、`.lan`、`.local`；`http://cloud.example.com` 这类公共 HTTP 域名仍拒绝。新增单测覆盖 `cloud.internal.example`、`.internal`、`.lan`、`.local` 通过，既有公共 HTTP 拒绝测试保留。
- 计划收口：补强 `AI-SEC-001/006` 的 HTTP-only OIDC 可执行边界，保证“必须 HTTP”不被误修成“必须 HTTPS”，也不被误解成“任意 HTTP 域名可用”。
- 规则提炼：形成长期规则，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md` 和 `docs/AI架构治理清单.md`：Cloud OIDC HTTP issuer 必须显式启用内网 HTTP OIDC，只允许 loopback、私网 IPv4 或保留内网 DNS 后缀，公共 HTTP 域名必须拒绝。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~CloudOidcLoginTestsControllerSecurity" --no-restore`
  - `rg -n "IntranetDnsSuffixes|cloud\\.internal\\.example|cloud\\.factory\\.internal|cloud\\.lan|cloud\\.local|公共 HTTP 域名|Cloud OIDC HTTP issuer" src/hosts/AICopilot.HttpApi/Infrastructure/CloudOidcOptions.cs src/tests/AICopilot.BackendTests/CloudOidcLoginTestsControllerSecurity.cs AGENTS.md 资料/AICopilot业务规则.md docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `git diff --check -- src/hosts/AICopilot.HttpApi/Infrastructure/CloudOidcOptions.cs src/tests/AICopilot.BackendTests/CloudOidcLoginTestsControllerSecurity.cs AGENTS.md 资料/AICopilot业务规则.md docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" src/hosts/AICopilot.HttpApi/Infrastructure/CloudOidcOptions.cs src/tests/AICopilot.BackendTests/CloudOidcLoginTestsControllerSecurity.cs AGENTS.md 资料/AICopilot业务规则.md docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
- 验证结果：通过。`CloudOidcLoginTestsControllerSecurity` 定向 24 条通过；静态检索确认内网 DNS 后缀、`.internal.example` / `.internal` / `.lan` / `.local` 样例、公共 HTTP 域名拒绝口径和规则沉淀均可检索；目标文件 `git diff --check` 通过；目标文件尾随空白检索无命中。真实 Cloud OIDC 服务和服务器 `.env` 未连接，原因是需要真实内网 Cloud 服务器和发布/运维窗口。

## 2026-07-07 AI-SEC-017 ProblemDetails detail/title 展示补强

- 改动范围：AICopilot `src/vues/AICopilot.Web/src/stores/chatErrorStore.ts`、`src/vues/AICopilot.Web/tests/unit/chatErrorStore.spec.ts`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、后端错误契约、真实服务器配置、发布脚本或生产发布状态。
- 改动原因：继续抽样验真 `AI-SEC-017` 时发现，前端虽然已经没有裸 `catch {}`，但 `toFriendlyMessage()` 对普通 API/SSE open 返回的 ProblemDetails `detail` / `title` 会退成“请求失败，请稍后重试”。这与 `AICopilot.Web/AGENTS.md` 中“stream open 要解析 response body 的 detail/errors，不能只按 status code 泛化”的前端错误契约不一致。
- 影响面：`extractErrorDetail()` 在 `userFacingMessage` 和 validation errors 之后补充解析 `detail`、`title`，让后端已经脱敏并结构化的 ProblemDetails 诊断进入用户可见错误栏；`resolveChatErrorMessage()` 对未知 Chat Error code 仍保持安全兜底，不直接展示 raw `detail`，避免 stream 内未知错误码泄露内部信息。
- 计划收口：补强 `AI-SEC-017` 的行为证据，从“无裸 catch”提升到“普通 API/SSE open ProblemDetails `detail/title` 可见、未知 Chat Error code 不泄露 detail”。`AI-SEC-017` 仍保持仓库内 `Done`，本批是证据补强和契约一致性修正。
- 规则提炼：无新增长期规则，原因是 ProblemDetails `detail/errors` 必须展示、未知错误码不得泄露 raw detail 的长期口径已沉淀在 `src/vues/AICopilot.Web/AGENTS.md` 和 `docs/AI架构治理清单.md`；本批只是修正实现与测试。
- 验证命令：
  - `npm run test:unit -- chatErrorStore`
  - `npm run test:unit -- frontendErrorHandling`
  - `rg -n "detail\\)|title\\)|ProblemDetails.*detail/title|未知 Chat Error code|catch\\s*\\{" src/vues/AICopilot.Web/src/stores/chatErrorStore.ts src/vues/AICopilot.Web/tests/unit/chatErrorStore.spec.ts src/vues/AICopilot.Web/tests/unit/frontendErrorHandling.spec.ts docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `git diff --check -- src/vues/AICopilot.Web/src/stores/chatErrorStore.ts src/vues/AICopilot.Web/tests/unit/chatErrorStore.spec.ts docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" src/vues/AICopilot.Web/src/stores/chatErrorStore.ts src/vues/AICopilot.Web/tests/unit/chatErrorStore.spec.ts docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
- 验证结果：通过。`chatErrorStore` 定向 7 条通过，`frontendErrorHandling` 定向 1 条通过；静态检索确认 `detail/title` 解析、未知 Chat Error code 安全兜底和无裸 `catch {}` 证据已进入源码、测试、治理清单和复盘；目标文件 `git diff --check` 通过；目标文件尾随空白检索无命中。未运行完整前端单测、type-check、smoke、后端测试或真实服务器验证，原因是本批只改前端统一错误消息解析和对应单测。

## 2026-07-07 AI-SEC-012 migration 批量预检防部分改写

- 改动范围：AICopilot `src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs`、`src/tests/AICopilot.BackendTests/MigrationWorkerSecretMigratorTests.cs`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、真实生产数据库、发布脚本行为或生产发布状态。
- 改动原因：上一批已把已有 `encv2:` 的可解密校验前移到单条迁移时机，但继续审计发现，如果不可读 `encv2:` 排在后面，前面的旧密文/明文模型仍可能先被内存改写，随后才失败。虽然 `MigrateAsync` 不会保存这些变更，但迁移失败路径仍有内存级部分改写风险，不够干净。
- 影响面：`MigrateLanguageModelApiKeys` 和 `MigrateEmbeddingModelApiKeys` 现在先把输入物化为数组，并在任何实体改写前调用 `EnsureReadableEncryptedSecrets` 批量预检已有 `encv2:` 可解密；预检通过后才迁移 `encv1:` 和明文。新增两条测试覆盖不可读 `encv2:` 排在旧密文后面的场景，断言旧密文模型仍保持原始 API key 和状态。
- 计划收口：进一步补强 `AI-SEC-012` 的迁移失败原子性，避免不可读 `encv2:` 与待迁移旧密文混在一起时留下内存级或数据库级部分迁移副作用。`AI-SEC-012` 仍保持 `Partial`，原因是真实生产库 migration 和 `check-model-secret-migration.sh` 仍需要发布窗口验收。
- 规则提炼：无新增长期规则，原因是“已有/新写 `encv2:` 必须可用当前密钥解密、生产库迁移必须发布窗口验收”的长期口径已沉淀在 `docs/AI架构治理清单.md`、`AGENTS.md` 和 `资料/AICopilot业务规则.md`；本批只是把校验提升为批量预检。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~MigrationWorkerSecretMigratorTests" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests|FullyQualifiedName~MigrationWorkerSecretMigratorTests|FullyQualifiedName~SecretStringEncryptorTests|FullyQualifiedName~ModelSecretRuntimeBoundaryTests" --no-restore`
  - `rg -n "EnsureReadableEncryptedSecrets|PreflightUnreadableProtectedSecretsBeforeChangingEarlierModels|批量预检|内存级部分改写" src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs src/tests/AICopilot.BackendTests/MigrationWorkerSecretMigratorTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `git diff --check -- src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs src/tests/AICopilot.BackendTests/MigrationWorkerSecretMigratorTests.cs docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs src/tests/AICopilot.BackendTests/MigrationWorkerSecretMigratorTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
- 验证结果：通过。`MigrationWorkerSecretMigratorTests` 定向 10 条通过；`SecurityHardeningTests|MigrationWorkerSecretMigratorTests|SecretStringEncryptorTests|ModelSecretRuntimeBoundaryTests` 组合定向 97 条通过；静态检索确认 migration worker、测试和复盘均包含批量预检口径；已跟踪目标文件 `git diff --check` 通过；目标文件尾随空白检索无命中。真实生产 PostgreSQL 未连接，原因是需要生产服务器、真实数据库和发布窗口。

## 2026-07-07 AI-SEC-012 migration 写入前 encv2 可解密校验

- 改动范围：AICopilot `src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs`、`src/tests/AICopilot.BackendTests/MigrationWorkerSecretMigratorTests.cs`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、真实生产数据库、发布脚本行为或生产发布状态。
- 改动原因：继续审计 `AI-SEC-012` 的迁移失败路径时发现，migration worker 原本会先迁移实体并保存，再调用 `EnsureMigratedSecrets` 验证已有 `encv2:` 是否可解密。如果同批数据同时包含旧密文/明文和不可读 `encv2:`，理论上可能先写入一部分迁移结果，然后才因不可读 `encv2:` 失败。虽然 runtime 仍会拒绝不可读密文，但迁移任务的失败路径不够干净。
- 影响面：`MigrateStoredSecret` 遇到已有 `encv2:` 时立即调用 `EnsureMigratedSecrets([storedValue], fieldName)` 做当前密钥可解密校验；不可读时在 `SaveChangesAsync` 前失败。新增 LanguageModel 和 EmbeddingModel 两条测试，构造第一个模型为伪 `encv2:`、第二个模型为明文，断言迁移失败且后续明文模型仍保持原值，证明不会在失败前改写后续模型。
- 计划收口：补强 `AI-SEC-012` 的仓库内失败路径可靠性，避免不可读 `encv2:` 和待迁移旧密文混在一起时留下部分迁移副作用。`AI-SEC-012` 仍保持 `Partial`，原因是真实生产库 migration 和 `check-model-secret-migration.sh` 仍需要发布窗口验收。
- 规则提炼：无新增长期规则，原因是“旧 `encv1:` 只能迁移重加密、已有/新写 `encv2:` 必须可用当前密钥解密、生产库迁移必须发布窗口验收”已沉淀在 `docs/AI架构治理清单.md`、`AGENTS.md` 和 `资料/AICopilot业务规则.md`；本批只是把校验提前到写入前并补失败路径测试。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~MigrationWorkerSecretMigratorTests" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests|FullyQualifiedName~MigrationWorkerSecretMigratorTests|FullyQualifiedName~SecretStringEncryptorTests|FullyQualifiedName~ModelSecretRuntimeBoundaryTests" --no-restore`
  - `rg -n "ShouldRejectUnreadableProtectedSecretsBeforeChangingLaterModels|EnsureMigratedSecrets\\(\\[storedValue\\]|写入前 encv2 可解密|部分迁移" src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs src/tests/AICopilot.BackendTests/MigrationWorkerSecretMigratorTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `git diff --check -- src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs src/tests/AICopilot.BackendTests/MigrationWorkerSecretMigratorTests.cs docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs src/tests/AICopilot.BackendTests/MigrationWorkerSecretMigratorTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
- 验证结果：通过。`MigrationWorkerSecretMigratorTests` 定向 8 条通过；`SecurityHardeningTests|MigrationWorkerSecretMigratorTests|SecretStringEncryptorTests|ModelSecretRuntimeBoundaryTests` 组合定向 95 条通过；静态检索确认 migration worker、测试、治理清单和复盘均包含写入前 `encv2` 可解密校验口径；已跟踪目标文件 `git diff --check` 通过；目标文件尾随空白检索无命中。真实生产 PostgreSQL 未连接，原因是需要生产服务器、真实数据库和发布窗口。

## 2026-07-07 AI-SEC-013 endpoint pool 环境变量明文门禁补强

- 改动范围：AICopilot `src/tests/AICopilot.BackendTests/ModelSecretRuntimeBoundaryTests.cs`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、runtime provider 实现、真实服务器 `.env`、真实模型网关或生产发布状态。
- 改动原因：继续沿 `AI-SEC-012/013` 的密钥运行时链路审计时确认，`ModelEndpointPoolScheduler` 对 endpoint pool `ApiKey` 和 `ApiKeyEnvironmentVariable` 值都会要求 `encv2:`。已有测试覆盖了配置项明文失败和环境变量受保护值通过，但缺少“环境变量里放明文时必须失败”的直接负例。
- 影响面：新增 `ModelEndpointPoolScheduler_ShouldRejectPlaintextEnvironmentApiKey`，将测试环境变量设置为 `sk-pool-plain`，断言 `SelectEndpoint("AnswerPool")` 抛出包含 `environment variable` 和 `encv2:` 的错误。治理清单 `AI-SEC-013` 同步说明测试同时覆盖配置项明文和环境变量明文失败。
- 计划收口：补强 `AI-SEC-013` 的仓库内测试证据，防止 endpoint pool 环境变量密钥绕过 `encv2:` 受保护格式要求。`AI-SEC-013` 仍保持仓库内 `Done`；真实生产 `.env` 中的 endpoint pool 密钥值仍需发布环境配置验收。
- 规则提炼：无新增长期规则，原因是“endpoint pool 覆盖密钥和环境变量值必须是 `encv2:`，不能放明文”的长期口径已沉淀在 `docs/AI架构治理清单.md`、`AGENTS.md` 和 `资料/AICopilot业务规则.md`；本批只是补齐负例测试。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~ModelSecretRuntimeBoundaryTests" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests|FullyQualifiedName~ModelSecretRuntimeBoundaryTests|FullyQualifiedName~SecretStringEncryptorTests|FullyQualifiedName~MigrationWorkerSecretMigratorTests" --no-restore`
  - `rg -n "ShouldRejectPlaintextEnvironmentApiKey|sk-pool-plain|环境变量明文|配置项明文和环境变量明文失败" src/tests/AICopilot.BackendTests/ModelSecretRuntimeBoundaryTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `git diff --check -- src/tests/AICopilot.BackendTests/ModelSecretRuntimeBoundaryTests.cs docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" src/tests/AICopilot.BackendTests/ModelSecretRuntimeBoundaryTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
- 验证结果：通过。`ModelSecretRuntimeBoundaryTests` 定向 7 条通过；`SecurityHardeningTests|ModelSecretRuntimeBoundaryTests|SecretStringEncryptorTests|MigrationWorkerSecretMigratorTests` 组合定向 93 条通过；静态检索确认测试、治理清单和复盘均包含 endpoint pool 环境变量明文失败口径；已跟踪目标文件 `git diff --check` 通过；目标文件尾随空白检索无命中。真实生产 `.env` 和模型网关未连接，原因是需要生产环境和发布窗口。

## 2026-07-07 AI-SEC-012 发布顺序测试门禁补强

- 改动范围：AICopilot `src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、生产数据库、GitHub runner、Vault/OIDC 配置、生产发布状态或 `deploy-release.sh` 行为。
- 改动原因：继续审计 `AI-SEC-012` 时发现，`deploy-release.sh` 源码已经在全量发布和按需发布中先运行 `aicopilot-migration`，再执行 `check_model_secret_migration_preflight`，最后启动 runtime；但原测试用 `IndexOf("check_model_secret_migration_preflight")` 会优先命中函数定义，不能有力证明实际发布分支里的命令顺序。
- 影响面：`SecurityHardeningTests.DeploymentScriptsAndDocs_ShouldKeepSingleStandardReleasePath` 改为断言全量发布分支包含 `aicopilot-migration -> check_model_secret_migration_preflight -> runtime` 三行实际命令序列，并断言按需发布分支包含 `aicopilot-migration -> check_model_secret_migration_preflight` 序列，同时保留 preflight 早于 selected runtime 启动的检查。治理清单同步说明发布脚本测试已锁定该顺序。
- 计划收口：补强 `AI-SEC-012` 的仓库内证明链，避免后续维护中把 SQL 前缀 preflight 移到 migration 前面，绕过 migration worker 的可解密自检。`AI-SEC-012` 仍保持 `Partial`，原因是真实生产库迁移和脚本执行仍需发布窗口验收。
- 规则提炼：无新增长期规则，原因是“后端 runtime 启动前必须先跑 migration 并完成模型密钥迁移 preflight”的长期口径已沉淀在 `docs/AI架构治理清单.md` 和部署文档；本批只是加强测试门禁。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests.DeploymentScriptsAndDocs_ShouldKeepSingleStandardReleasePath" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `rg -n "fullDeployMigrationThenPreflight|selectedDeployMigrationThenPreflight|aicopilot-migration -> check_model_secret_migration_preflight|发布顺序测试门禁" src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `git diff --check -- src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
- 验证结果：通过。`DeploymentScriptsAndDocs_ShouldKeepSingleStandardReleasePath` 定向 1 条通过；`SecurityHardeningTests` 定向 75 条通过；静态检索确认测试、治理清单和复盘均包含发布顺序门禁口径；已跟踪目标文件 `git diff --check` 通过；目标文件尾随空白检索无命中。真实生产 PostgreSQL 未连接，原因是需要生产服务器、真实数据库和发布窗口。

## 2026-07-07 AI-SEC-010 平台例外结构化字段门禁

- 改动范围：AICopilot `deploy/enterprise-ai/scripts/check-platform-attestation-record.sh`、`deploy/enterprise-ai/runner-platform-attestation.template.md`、`deploy/enterprise-ai/README.md`、`docs/AI架构治理清单.md`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实 GitHub environment、真实 runner 机器、Vault/OIDC 配置、生产服务器或真实发布状态。
- 改动原因：继续审计 `AI-SEC-010` 时发现，README 和治理清单已经要求 OIDC/Vault 未落地时必须提供 ticket、owner、due date 和 current mitigation，但 `check-platform-attestation-record.sh` 只检查记录里是否出现 `OIDC/Vault` 或 `short-lived credentials`，没有强制“已批准基础设施例外”的结构化证据字段。这样会让只写“approved infrastructure exception”但没有负责人、期限和缓解措施的记录误通过。
- 影响面：平台记录 linter 新增 `require_credential_strategy_evidence`：记录声明 `approved infrastructure exception` 时，必须提供非空 `Ticket or change id`、`Exception owner`、`Due date` 和 `Current mitigation`；否则必须明确写成 OIDC/Vault 或等价短期凭据已 implemented/enabled/active。平台验收模板改为结构化字段，README 和 AI 治理清单同步说明字段级门禁。`SecurityHardeningTests.PlatformAttestationRecordCheck_ShouldRejectIncompleteSignOffsAndWeakEvidenceWords` 增加有效批准例外和空泛批准例外失败样例。
- 计划收口：进一步补强 `AI-SEC-010` 的仓库内证据质量门禁，防止外部基础设施例外被空话绕过。`AI-SEC-010` 仍保持 `Partial`，真实 runner 权限、GitHub production environment、required reviewers、Vault/OIDC 或等价短期凭据仍需平台侧真实验收。
- 规则提炼：形成平台例外记录的结构化字段验收口径，已沉淀到 `docs/AI架构治理清单.md` 和 `deploy/enterprise-ai/README.md`；`AGENTS.md` 与 `资料/AICopilot业务规则.md` 既有 HTTP-only 和外部基础设施边界无需变更。
- 验证命令：
  - `bash -n deploy/enterprise-ai/scripts/check-platform-attestation-record.sh`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests.PlatformAttestationRecordCheck_ShouldRejectIncompleteSignOffsAndWeakEvidenceWords" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `rg -n "Ticket or change id|Exception owner|Due date|Current mitigation|approved exception ticket|approved exception mitigation|approved infrastructure exception" deploy/enterprise-ai docs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`
  - `git diff --check -- deploy/enterprise-ai/scripts/check-platform-attestation-record.sh deploy/enterprise-ai/runner-platform-attestation.template.md deploy/enterprise-ai/README.md docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" deploy/enterprise-ai/scripts/check-platform-attestation-record.sh deploy/enterprise-ai/runner-platform-attestation.template.md deploy/enterprise-ai/README.md docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
- 验证结果：通过。脚本语法检查通过；平台验收记录行为测试 1 条通过，覆盖 OIDC/Vault 已实现记录、已批准基础设施例外记录、空泛批准例外失败、空签署人失败和弱证明词失败；`SecurityHardeningTests` 定向 75 条通过；静态检索确认脚本、模板、README、治理清单、复盘和测试均包含结构化例外字段口径；已跟踪目标文件 `git diff --check` 通过；目标文件尾随空白检索无命中。真实 GitHub environment、runner 机器、Vault/OIDC 或等价短期凭据仍未验收，原因是需要外部平台环境和发布/基础设施窗口。

## 2026-07-07 AI-SEC-020 生产 MockOnly 配置噪音清理

- 改动范围：AICopilot `src/hosts/AICopilot.HttpApi/appsettings.json`、`deploy/enterprise-ai/docker-compose.yaml`、`docs/AI架构治理清单.md`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、Development appsettings、真实服务器 `.env`、真实 MCP server 或生产发布状态。
- 改动原因：继续按治理清单回扫生产 Simulation/Mock 边界时发现，生产基础 `appsettings.json` 和 production compose 仍携带 `MockOnly=true` 相关配置。源码实际读取的是 `Mcp:Runtime:Enabled` 和 Development-only 的 `AiGateway:MockMcp`，`Mcp:Runtime:MockOnly` 没有运行时选项承接，但保留在生产配置里会和“生产禁 mock/simulation fallback”的红线产生歧义。
- 影响面：生产基础 `appsettings.json` 去掉 `Mcp:Runtime:MockOnly`，production compose 去掉 `Mcp__Runtime__MockOnly`；Development appsettings 保留 `MockOnly=true`，不影响本地 mock/simulation 验收。`SecurityHardeningTests.DeploymentConfig_ShouldNotCarryKnownWeakSecrets` 增加断言：生产 compose 和生产 appsettings 不携带 mock-only 默认值，Development 仍保留 mock-only 测试口径。`docs/AI架构治理清单.md` 在 `AI-SEC-020` 增加生产配置不得携带 `MockOnly=true` 的关单证据。
- 计划收口：进一步补强 `AI-SEC-020` 的仓库内生产配置证据，消除无效 mock-only 配置对生产边界的干扰。`AI-SEC-020` 仍是仓库内 Done；真实线上 Cloud AiRead/HTTP 探针仍按发版门禁另行验收。
- 规则提炼：无新增长期规则，原因是“生产路径禁止 Simulation/Mock fallback，Development 可保留专用 mock/simulation 路径”的长期口径已沉淀在 `docs/AI架构治理清单.md`；本批只是清理生产配置噪音。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests.DeploymentConfig_ShouldNotCarryKnownWeakSecrets" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `if rg -n "Mcp__Runtime__MockOnly|\\\"MockOnly\\\": true" src/hosts/AICopilot.HttpApi/appsettings.json deploy/enterprise-ai/docker-compose.yaml; then exit 1; fi`
  - `rg -n "\\\"MockOnly\\\": true|Production.*MockOnly|生产基础配置和 compose 不携带|Mcp__Runtime__MockOnly" src/hosts/AICopilot.HttpApi/appsettings.Development.json docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`
  - `git diff --check -- src/hosts/AICopilot.HttpApi/appsettings.json deploy/enterprise-ai/docker-compose.yaml docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
- 验证结果：通过。`DeploymentConfig_ShouldNotCarryKnownWeakSecrets` 定向 1 条通过；`SecurityHardeningTests` 定向 75 条通过；生产 `appsettings.json` 和 production compose 对 `MockOnly=true` / `Mcp__Runtime__MockOnly` 无命中；Development appsettings 仍保留 `MockOnly=true`；AI 治理清单和测试断言已同步生产配置不携带 mock-only 口径；目标文件 `git diff --check` 通过。真实线上 Cloud AiRead/HTTP 探针未运行，原因是需要外部生产环境和发布窗口。

## 2026-07-07 AI-SEC-010 平台例外记录口径修正

- 改动范围：AICopilot `deploy/enterprise-ai/runner-platform-attestation.template.md`、`deploy/enterprise-ai/README.md`、`docs/AI架构治理清单.md`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实 GitHub environment、真实 runner 机器、Vault/OIDC 配置、生产服务器或真实发布状态。
- 改动原因：继续按治理清单做源码和文档一致性回扫时发现，上一批把 `not implemented` 作为弱证明词纳入 linter 拒绝范围，但 `runner-platform-attestation.template.md` 的 Credential strategy 仍写着 “OIDC/Vault is not implemented yet...”。这会导致模板允许的“已接受外部基础设施例外”路径被 linter 拦掉，形成计划、模板和门禁前后不一致。
- 影响面：平台验收模板不再使用 `not implemented yet` 作为合法选项，改为 `OIDC/Vault rollout is tracked as an approved infrastructure exception`；README 和 AI 治理清单同步说明：linter 继续拒绝弱证明词，但 OIDC/Vault 或等价短期凭据尚未落地时，只能填写“已批准的基础设施例外”，并提供 ticket、owner、due date 和当前 mitigation。`SecurityHardeningTests` 增加受控例外记录通过样例，并锁定 README/模板中的 `approved infrastructure exception` 口径。
- 计划收口：补齐 `AI-SEC-010` 的文档/脚本/测试一致性，避免一边允许受控例外、一边用弱证明词导致 linter 拒绝。`AI-SEC-010` 仍保持 `Partial`，真实 runner 权限、GitHub production environment、required reviewers、Vault/OIDC 或等价短期凭据仍需平台侧真实验收。
- 规则提炼：无新增长期规则，原因是“外部基础设施未完成不能伪造成完成；如暂未完成，必须以受控例外留痕”的长期口径已经沉淀在 `docs/AI架构治理清单.md` 和部署 README；本批只是修正模板措辞和测试证据。
- 验证命令：
  - `bash -n deploy/enterprise-ai/scripts/check-platform-attestation-record.sh`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests.PlatformAttestationRecordCheck_ShouldRejectIncompleteSignOffsAndWeakEvidenceWords" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `if rg -n "not implemented yet" deploy/enterprise-ai/runner-platform-attestation.template.md; then exit 1; fi`
  - `rg -n "approved infrastructure exception|弱证明词|受控例外|PlatformAttestationRecordCheck" deploy/enterprise-ai docs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`
  - `git diff --check -- deploy/enterprise-ai/runner-platform-attestation.template.md deploy/enterprise-ai/README.md docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
- 验证结果：通过。脚本语法检查通过；平台验收记录行为测试 1 条通过，覆盖 OIDC/Vault 已实现记录、已批准基础设施例外记录、空签署人失败和弱证明词失败；`runner-platform-attestation.template.md` 不再出现 `not implemented yet`；`SecurityHardeningTests` 定向 75 条通过；静态检索确认 README、AI 治理清单、模板、复盘和测试均包含受控例外口径；目标文件 `git diff --check` 通过。真实 GitHub environment、runner 机器、Vault/OIDC 或等价短期凭据仍未验收，原因是需要外部平台环境和发布/基础设施窗口。

## 2026-07-06 AI-SEC-010 平台验收记录 linter 补强

- 改动范围：AICopilot `deploy/enterprise-ai/scripts/check-platform-attestation-record.sh`、`deploy/enterprise-ai/README.md`、`docs/AI架构治理清单.md`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实 GitHub environment、真实 runner 机器、Vault/OIDC 配置、生产服务器或真实发布状态。
- 改动原因：继续审计 `AI-SEC-010` 时发现，平台侧 attestation linter 只检查 `Platform owner:`、`Reviewer:`、`Release owner:` 等标签是否出现，没有要求冒号后存在实际签署内容，也没有拦截 `pending`、`not implemented`、`N/A` 等弱证明词。这样会让形式完整但证据不足的手工记录误作为发布证据。
- 影响面：`check-platform-attestation-record.sh` 新增非空签署字段校验，拒绝空签署人、`N/A`、`pending`、`unsigned` 等无效签署值，并把 `pending`、`unverified`、`not implemented`、`not available`、`unknown`、`N/A` 纳入 unresolved placeholder wording。新增后端测试真实执行脚本，覆盖有效记录、空 `Platform owner` 和弱证明词失败场景。部署 README 和 AI 治理清单同步说明 linter 的能力边界。
- 计划收口：进一步补强 `AI-SEC-010` 的仓库内证明链，降低平台侧留痕被空签署或弱证明词绕过的风险。`AI-SEC-010` 仍保持 `Partial`，原因是真实 runner 机器权限收敛、GitHub production environment 权限、required reviewers、OIDC/Vault 或等价短期凭据仍需平台侧真实验收。
- 规则提炼：无新增长期规则，原因是“平台 attestation 只能作为外部验收留痕，不能替代真实 GitHub/Vault/OIDC/runner 验收”的长期口径已沉淀在 `docs/AI架构治理清单.md` 和部署 README；本批只是增强 linter 执法强度。
- 验证命令：
  - `bash -n deploy/enterprise-ai/scripts/check-platform-attestation-record.sh`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests.PlatformAttestationRecordCheck_ShouldRejectIncompleteSignOffsAndWeakEvidenceWords" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `rg -n "missing required sign-off value|invalid sign-off value|pending\\|not|空签署人|弱证明词|PlatformAttestationRecordCheck" deploy/enterprise-ai docs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`
  - `git diff --check -- deploy/enterprise-ai/scripts/check-platform-attestation-record.sh deploy/enterprise-ai/README.md docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
- 验证结果：通过。shell 语法检查通过；新增脚本行为测试 1 条通过，覆盖有效记录、空 `Platform owner` 和弱证明词失败场景；`SecurityHardeningTests` 定向 75 条通过；静态检索确认脚本、README、AI 治理清单、复盘和测试均包含空签署/弱证明词门禁；目标文件 `git diff --check` 通过。真实 GitHub environment、runner 机器、Vault/OIDC 或等价短期凭据仍未验收，原因是需要外部平台环境和发布/基础设施窗口。

## 2026-07-06 后端 runtime 按需发布强制包含 migration

- 改动范围：AICopilot `deploy/enterprise-ai/build-and-push.sh`、`deploy/enterprise-ai/deploy-release.sh`、`deploy/enterprise-ai/README.md`、`AICopilot 项目部署与维护指南.md`、`docs/AI架构治理清单.md`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、生产数据库、GitHub runner、Vault/OIDC 配置或真实发布状态。
- 改动原因：上一批已把 migration worker 的 `encv2:` 自检提升到可解密级，但继续审计发布链路发现：按需发布 `httpapi`、`dataworker` 或 `ragworker` 且不包含 `migration` 时，`deploy-release.sh` 只能运行 SQL 前缀 preflight，无法验证 AES-GCM tag 和当前密钥可解密。这会让伪 `encv2:` 或旧密钥加密值绕过发布前可解密自检，直到 runtime provider 才失败。
- 影响面：`build-and-push.sh` 在选择 `httpapi`、`dataworker` 或 `ragworker` 时自动把 `migration` 加入构建/部署服务清单；服务器端 `deploy-release.sh` 对后端 runtime 按需发布增加硬门禁，缺少 `migration` 时直接失败并提示 `Web-only deploys may omit migration`。部署 README、项目部署指南和 AI 治理清单同步说明：后端 runtime 按需发布必须包含 `migration`，web-only 可以不带。
- 计划收口：进一步补强 `AI-SEC-012` 的仓库内发布链路，避免后端 runtime-only 发布绕过 migration worker 的可解密自检。`AI-SEC-012` 仍保持 `Partial`，因为真实生产库 migration 和 `check-model-secret-migration.sh` 仍需要发布窗口验收。
- 规则提炼：无新增长期规则，原因是“后端 runtime 启动前必须完成密钥迁移/验收、生产库迁移必须发布窗口留痕”已经沉淀在部署文档和 `docs/AI架构治理清单.md`；本批只是把后端按需发布的执行门禁补齐。
- 验证命令：
  - `bash -n deploy/enterprise-ai/build-and-push.sh deploy/enterprise-ai/deploy-release.sh`
  - `deploy/enterprise-ai/deploy-release.sh sha-abcdef --services httpapi`（期望失败并输出 `backend runtime deploys must include migration`）
  - `REGISTRY=harbor.internal.example:80 CLOUD_PLATFORM_URL=http://cloud.internal.example:81 deploy/enterprise-ai/build-and-push.sh --services httpapi --dry-run`
  - `REGISTRY=harbor.internal.example:80 CLOUD_PLATFORM_URL=http://cloud.internal.example:81 deploy/enterprise-ai/build-and-push.sh --services web --dry-run`
  - `REGISTRY=harbor.internal.example:80 CLOUD_PLATFORM_URL=http://cloud.internal.example:81 deploy/enterprise-ai/local-release.sh --services httpapi --ssh-target deploy@aicopilot.internal.example --dry-run`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `rg -n "backend_runtime_selected=true|backend runtime deploys must include migration|migration,httpapi,web|后端 runtime 按需发布必须包含|Web-only deploys may omit migration" deploy/enterprise-ai "AICopilot 项目部署与维护指南.md" docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`
  - `git diff --check -- deploy/enterprise-ai/build-and-push.sh deploy/enterprise-ai/deploy-release.sh deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md" docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
- 验证结果：通过。shell 语法检查和目标文件 `git diff --check` 通过；直接服务器发布 `--services httpapi` 被拒绝并输出必须包含 `migration` 的固定提示；`build-and-push.sh --services httpapi --dry-run` 自动构建 `httpapi` 与 `migration`，`--services web --dry-run` 仅构建 Web；`local-release.sh --services httpapi --dry-run` 最终传给服务器的命令为 `--services 'httpapi,migration'`；`SecurityHardeningTests` 定向 74 条通过。真实生产库 migration、真实 HTTP/OIDC/Web 容器和 runner/OIDC/Vault 仍未验收，原因是需要外部生产环境和发布窗口。

## 2026-07-06 AI-SEC-012 encv2 密文可解密自检补强

- 改动范围：AICopilot `src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs`、`src/tests/AICopilot.BackendTests/MigrationWorkerSecretMigratorTests.cs`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、生产数据库、GitHub runner、Vault/OIDC 配置或真实发布状态。
- 改动原因：继续审计 `AI-SEC-012` 时发现，migration worker 的 `EnsureMigratedSecrets` 只检查 `encv2:` 前缀，测试名写着 authenticated cipher，但实际测试传入的是 `encv2:protected` 假字符串。这会让伪 `encv2:` 或用旧密钥加密但当前密钥不可解密的密文通过 migration 阶段，直到 runtime provider 才失败。
- 影响面：`EnsureMigratedSecrets` 现在对每个非空 `encv2:` 值调用 `SecretStringEncryptor.Decrypt`，验证 payload/base64/AES-GCM authentication tag 和当前密钥可用；发现非 `encv2:` 值或不可解密 `encv2:` 值时抛出包含 `non-encv2 secret value` 和 `unreadable encv2 secret value` 的固定错误。测试改为使用真实 `SecretStringEncryptor.Encrypt` 生成可通过密文，并新增伪 `encv2:` 和不同密钥加密值失败场景。`docs/AI架构治理清单.md` 同步说明 migration worker 会拒绝伪 `encv2:` 或当前密钥不可解密的旧密文。
- 计划收口：补强 `AI-SEC-012` 仓库内迁移自检强度。`AI-SEC-012` 仍保持 `Partial`，原因是真实生产库还未在发布窗口运行 migration 和 `check-model-secret-migration.sh` 留痕；SQL preflight 仍只能做前缀/旧格式计数，真正的 tag/密钥验证由 migration worker 和 runtime provider 执行。
- 规则提炼：无新增长期规则，原因是“旧 `encv1:` 只能迁移重加密、runtime provider 不得兼容旧格式、生产库迁移必须发布窗口验收”已沉淀；本批只是让 migration worker 自检从前缀级提升到可解密级。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~MigrationWorkerSecretMigratorTests|FullyQualifiedName~SecurityHardeningTests|FullyQualifiedName~SecretStringEncryptorTests" --no-restore`
  - `rg -n "SecretStringEncryptor\\.Decrypt\\(storedValue\\)|unreadable encv2 secret value|different key|encv2 密文可用当前密钥解密|伪 encv2" src/hosts/AICopilot.MigrationWorkApp src/tests/AICopilot.BackendTests docs/AI架构治理清单.md`
  - `git diff --check -- src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs src/tests/AICopilot.BackendTests/MigrationWorkerSecretMigratorTests.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore`
- 验证结果：`MigrationWorkerSecretMigratorTests|SecurityHardeningTests|SecretStringEncryptorTests` 定向 85 条通过；静态检索确认 migration worker 调用 `SecretStringEncryptor.Decrypt(storedValue)` 并输出 `unreadable encv2 secret value`，测试覆盖伪 encv2 和 different key 场景，治理清单同步当前密钥可解密口径；目标文件 `git diff --check` 通过；BackendTests 全量 767 条通过。真实生产库未运行 migration 和脚本，原因是需要生产服务器、真实 PostgreSQL 和发布窗口。

## 2026-07-06 AI 修复计划仓库内宽验证

- 改动范围：本轮只做 AICopilot 仓库内验证并更新本滚动复盘。未修改 CloudPlatform、EdgeClient、业务代码、部署脚本、真实服务器 `.env`、生产数据库、GitHub runner、Vault/OIDC 配置或真实发布状态。
- 改动原因：继续执行完整 AI 修复计划时，需要把仓库内可证明的安全/架构/前端门禁做一次宽验证，避免只凭前面局部定向测试判断计划状态。
- 影响面：验证覆盖 shell 脚本语法、ArchitectureTests、BackendTests 全量、前端 type-check、前端 unit tests、前端生产 build、docker compose 配置、Baseline freeze、Architecture boundary 和 Text encoding 脚本。前端 build 中 `@vueuse/core` 产生 Rolldown `INVALID_ANNOTATION` 依赖包注释位置警告，但 build 退出码为 0。
- 计划收口：仓库内主要验证门禁通过，支撑当前 `Done` 项的本地证据；`AI-SEC-010` 和 `AI-SEC-012` 仍保持 `Partial`，原因是真实 runner/GitHub environment/Vault/OIDC 和真实生产 PostgreSQL 迁移验收仍未执行。
- 规则提炼：无新增长期规则，原因是本轮只补本地验证证据，没有产生新的业务、部署或架构约束。
- 验证命令：
  - `bash -n deploy/enterprise-ai/*.sh deploy/enterprise-ai/scripts/*.sh scripts/*.sh`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore`
  - `npm run type-check`（工作目录：`src/vues/AICopilot.Web`）
  - `npm run test:unit`（工作目录：`src/vues/AICopilot.Web`）
  - `npm run build`（工作目录：`src/vues/AICopilot.Web`）
  - `docker compose --env-file deploy/enterprise-ai/.env.example -f deploy/enterprise-ai/docker-compose.yaml config -q`
  - `pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh ./scripts/Test-ArchitectureBoundaries.ps1`
  - `pwsh ./scripts/Test-TextEncoding.ps1`
- 验证结果：shell 语法检查通过；ArchitectureTests 82 条通过；BackendTests 765 条通过；前端 type-check 通过；前端 unit tests 28 个文件 / 129 条通过；前端生产 build 通过并仅有 `@vueuse/core` Rolldown annotation 警告；compose config 通过；Baseline freeze、Architecture boundary 和 Text encoding 脚本均通过。真实 HTTP header 探针、Cloud OIDC 状态接口、生产 Web 容器非 root、生产 PostgreSQL API key 迁移和 runner/OIDC/Vault 仍未验收，原因是需要外部生产环境和发布窗口。

## 2026-07-06 模型密钥迁移验收查询失败提示补强

- 改动范围：AICopilot `deploy/enterprise-ai/scripts/check-model-secret-migration.sh`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、生产数据库、GitHub runner、Vault/OIDC 配置或真实发布状态。
- 改动原因：继续审计 `AI-SEC-012` 新独立验收脚本时发现，`psql` 查询失败会在 `set -e` 下直接中断，虽然 stderr 可能保留 PostgreSQL 错误，但缺少稳定的 AICopilot 发布语义提示。发布窗口里如果是 postgres 未启动、schema/table 未迁移或连接配置错误，操作者需要明确知道这是模型密钥迁移验收查询失败，并且 runtime 服务不能继续启动。
- 影响面：`check-model-secret-migration.sh` 现在显式捕获 PostgreSQL 查询失败，输出固定错误 `failed while querying PostgreSQL`，提示确认 postgres 服务、`aigateway/rag` schema 已由 migration 创建，并重新执行包含 `--services migration` 的部署后再启动 runtime。`SecurityHardeningTests` 锁定该错误提示，避免后续回退为裸 `set -e` 中断。
- 计划收口：补强 `AI-SEC-012` 发布窗口失败诊断质量。`AI-SEC-012` 仍保持 `Partial`，原因是真实生产库迁移和脚本执行仍未在生产 PostgreSQL 上完成。
- 规则提炼：无新增长期规则，原因是生产库迁移必须发布窗口验收、旧 `encv1:` 不得 runtime 兼容的长期规则已经沉淀；本批只是让独立验收脚本在查询失败时输出稳定发布诊断。
- 验证命令：
  - `bash -n deploy/enterprise-ai/scripts/check-model-secret-migration.sh`
  - `deploy/enterprise-ai/scripts/check-model-secret-migration.sh --dry-run`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `rg -n "failed while querying PostgreSQL|rerun a deploy including --services migration|ON_ERROR_STOP=1" deploy/enterprise-ai/scripts/check-model-secret-migration.sh src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`
  - `git diff --check -- deploy/enterprise-ai/scripts/check-model-secret-migration.sh src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
- 验证结果：脚本语法检查通过；`check-model-secret-migration.sh --dry-run` 通过并输出 PostgreSQL 只读验收计划；`SecurityHardeningTests` 74 条通过；静态检索确认脚本保留 `ON_ERROR_STOP=1` 并输出 `failed while querying PostgreSQL` 与 `rerun a deploy including --services migration`；目标文件 `git diff --check` 通过。真实生产 PostgreSQL 未连接，原因是需要生产服务器和发布窗口。

## 2026-07-06 灾备 deploy 同步 scripts 可执行位补强

- 改动范围：AICopilot `.github/workflows/aicopilot-deploy.yml`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实 GitHub environment secrets、runner 服务器、Vault/OIDC 配置、生产 `.env`、生产数据库或真实发布状态。
- 改动原因：继续按当前工作区审计完整 AI 修复计划时发现，标准本机发布 `local-release.sh` 已同步并 chmod `scripts/check-model-secret-migration.sh`，但灾备 `aicopilot-deploy.yml` 同步整个 `deploy/enterprise-ai` 后只对根目录 `*.sh` 执行 `chmod +x`，没有显式处理 `scripts/*.sh`。虽然 checkout/rsync 通常会保留文件模式，但灾备入口应该显式保证发布前验收脚本可执行，避免 `deploy-release.sh` 因 `scripts/check-model-secret-migration.sh` 不可执行而失败在非业务原因上。
- 影响面：灾备 `aicopilot-deploy.yml` 在同步部署目录后新增 `find "$DEPLOY_TARGET_DIR/scripts" -maxdepth 1 -type f -name '*.sh' -exec chmod +x {} +`；`SecurityHardeningTests` 锁定该命令，确保灾备路径也会让脚本目录下的发布验收脚本可执行。
- 计划收口：补强 `AI-SEC-012` 新独立验收脚本在灾备发布路径下的可执行性，同时也支持 `AI-SEC-010` 灾备 workflow 门禁一致性。`AI-SEC-010` 和 `AI-SEC-012` 仍保持 `Partial`，原因是真实 runner/GitHub environment/Vault/OIDC 和真实生产数据库迁移仍未在外部环境验收。
- 规则提炼：无新增长期规则，原因是“灾备 workflow 只保留应急入口、runner/OIDC/Vault 属于外部基础设施边界、生产库迁移必须发布窗口验收”已经沉淀在部署文档和 `docs/AI架构治理清单.md`；本批只是修正灾备同步执行位细节。
- 验证命令：
  - `ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f); puts "YAML ok: #{f}" }' .github/workflows/aicopilot-deploy.yml`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `rg -n "DEPLOY_TARGET_DIR/scripts|chmod \\+x \\{\\}" .github/workflows/aicopilot-deploy.yml src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`
  - `git diff --check -- .github/workflows/aicopilot-deploy.yml src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
- 验证结果：`aicopilot-deploy.yml` YAML 解析通过；`SecurityHardeningTests` 74 条通过；静态检索确认灾备 workflow 和测试都包含 `DEPLOY_TARGET_DIR/scripts` 的 `chmod +x` 门禁；目标文件 `git diff --check` 通过。真实灾备 runner 和生产部署目录未执行，原因是需要 GitHub production environment、self-hosted runner 和发布窗口。

## 2026-07-06 AI-SEC-012 模型密钥迁移验收脚本收敛

- 改动范围：AICopilot `deploy/enterprise-ai/scripts/check-model-secret-migration.sh`、`deploy/enterprise-ai/deploy-release.sh`、`deploy/enterprise-ai/scripts/check-release-security-attestation.sh`、`deploy/enterprise-ai/local-release.sh`、`deploy/enterprise-ai/README.md`、`AICopilot 项目部署与维护指南.md`、`docs/AI架构治理清单.md`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、生产数据库、GitHub runner、Vault/OIDC 配置或真实发布状态。
- 改动原因：继续执行完整 AI 修复计划时发现，`AI-SEC-012` 的模型密钥迁移 SQL 同时存在于 `deploy-release.sh`、发布后验收脚本和文档示例中。虽然逻辑一致，但三处复制容易漂移，后续发布窗口可能出现“preflight 验的是一套 SQL、手工复验看的是另一套 SQL”的问题。
- 影响面：新增 `check-model-secret-migration.sh` 作为模型/Embedding API key 迁移验收的唯一可执行脚本，检查 `aigateway.language_models` 和 `rag.embedding_models` 的 `legacy_count=0`、`unprotected_count=0`；`deploy-release.sh` runtime 启动前 preflight 改为调用该脚本；`check-release-security-attestation.sh` 发布后总验收复用该脚本；`local-release.sh` 同步并 chmod 该脚本；部署 README、部署指南和 AI 治理清单改为引用脚本而不是复制 SQL。
- 计划收口：`AI-SEC-012` 仍保持 `Partial`，原因是真实生产库还没有在发布窗口运行 migration 和脚本并留痕。本批只把仓库内 SQL 验收入口从多处复制收敛为单一脚本，减少外部验收漏项。
- 规则提炼：无新增长期规则，原因是“旧 `encv1:` 只能迁移重加密、runtime provider 不得兼容旧格式、生产库迁移必须由发布窗口验收”已经沉淀在 `AGENTS.md`、`资料/AICopilot业务规则.md`、部署文档和 `docs/AI架构治理清单.md`。本批只是把既有规则转成独立脚本入口。
- 验证命令：
  - `bash -n deploy/enterprise-ai/*.sh deploy/enterprise-ai/scripts/*.sh`
  - `deploy/enterprise-ai/scripts/check-model-secret-migration.sh --dry-run`
  - `deploy/enterprise-ai/scripts/check-release-security-attestation.sh --dry-run`
  - `REGISTRY=harbor.internal.example:80/enterprise-ai CLOUD_PLATFORM_URL=http://cloud.internal.example:81 deploy/enterprise-ai/local-release.sh --services httpapi,migration,dataworker,ragworker,web --ssh-target deploy@aicopilot.internal.example --dry-run`
  - `test -x deploy/enterprise-ai/scripts/check-model-secret-migration.sh`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `rg -n "check-model-secret-migration.sh|aigateway\\.language_models|rag\\.embedding_models|legacy_count=0|unprotected_count=0" deploy/enterprise-ai docs/AI架构治理清单.md "AICopilot 项目部署与维护指南.md" src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`
  - `git diff --check -- deploy/enterprise-ai/scripts/check-model-secret-migration.sh deploy/enterprise-ai/deploy-release.sh deploy/enterprise-ai/scripts/check-release-security-attestation.sh deploy/enterprise-ai/local-release.sh deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md" docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
- 验证结果：脚本语法检查通过；`check-model-secret-migration.sh --dry-run` 输出 PostgreSQL 只读验收计划；`check-release-security-attestation.sh --dry-run` 复用同一模型密钥迁移脚本并通过；`local-release.sh --dry-run` 输出会同步并 chmod `scripts/check-model-secret-migration.sh`；新脚本可执行位确认通过；`SecurityHardeningTests` 74 条通过；静态检索确认独立脚本、部署脚本、总验收脚本、部署 README、部署指南、AI 治理清单和测试都引用同一入口；目标文件 `git diff --check` 通过。真实生产库迁移 SQL 未运行，原因是需要生产服务器、真实 PostgreSQL 和发布窗口。

## 2026-07-06 模型 smoke API key header 输入校验补强

- 改动范围：AICopilot `deploy/enterprise-ai/scripts/check-model-provider-openai.sh`、`src/tests/AICopilot.ArchitectureTests/ArchitectureBoundaryTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、生产数据库、GitHub runner、Vault/OIDC 配置或真实发布状态。
- 改动原因：继续按源码审计模型 smoke 诊断脚本时发现，base URL 和 `dummy-key` 已经有门禁，但 `MODEL_API_KEY` 会直接拼进 `Authorization: Bearer ...` header，缺少对空白、控制字符、引号和反斜杠的输入边界校验。
- 影响面：`check-model-provider-openai.sh` 新增 `validate_header_value`，模型 API key 现在禁止包含空格、tab、换行、回车、引号或反斜杠；`dummy-key` 的显式例外语义不变，仍必须通过 `AICOPILOT_MODEL_SMOKE_ALLOW_DUMMY_KEY=true` 或 `--allow-dummy-key` 放行。
- 计划收口：补强 `AI-SEC-006` / 模型 smoke weak secret fail-fast 的脚本输入边界。真实生产 `.env` 未修改；如果现场模型网关使用特殊 API key 格式，需要在发布窗口先确认网关是否真实要求该格式，不能绕过脚本直接发布。
- 规则提炼：无新增长期规则，原因是“模型 smoke 弱值必须显式例外”和“部署脚本默认不得携带弱 secret”的长期规则已写入 `AGENTS.md` 和 `资料/AICopilot业务规则.md`；本批只是把该规则在 header 输入边界上落实。
- 验证命令：
  - `bash -n deploy/enterprise-ai/*.sh deploy/enterprise-ai/scripts/*.sh`
  - `if deploy/enterprise-ai/scripts/check-model-provider-openai.sh --base-url http://model.internal.example:40034/v1 --model smoke-model --api-key 'bad key' --dry-run; then echo 'unexpected api key whitespace success' >&2; exit 1; else echo 'expected api key whitespace rejection'; fi`
  - `if bash -c 'deploy/enterprise-ai/scripts/check-model-provider-openai.sh --base-url http://model.internal.example:40034/v1 --model smoke-model --api-key $'\''bad\nkey'\'' --dry-run'; then echo 'unexpected api key newline success' >&2; exit 1; else echo 'expected api key newline rejection'; fi`
  - `deploy/enterprise-ai/scripts/check-model-provider-openai.sh --base-url http://model.internal.example:40034/v1 --model smoke-model --api-key explicit-test-key --dry-run`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~ModelProviderSmokeCheck_ShouldStayAvailableAsServerSideDiagnostic" --no-restore`
  - `git diff --check -- deploy/enterprise-ai/scripts/check-model-provider-openai.sh src/tests/AICopilot.ArchitectureTests/ArchitectureBoundaryTests.cs docs/改动复盘与规则沉淀.md`
- 验证结果：脚本语法检查通过；带空格的 API key dry-run 正确失败；带换行的 API key dry-run 正确失败；正常 `explicit-test-key` dry-run 通过且输出 bearer token masked；`ModelProviderSmokeCheck_ShouldStayAvailableAsServerSideDiagnostic` 1 条通过；目标文件 `git diff --check` 通过。

## 2026-07-06 模型 smoke dummy-key 显式例外门禁

- 改动范围：AICopilot `deploy/enterprise-ai/deploy-release.sh`、`deploy/enterprise-ai/scripts/check-model-provider-openai.sh`、`deploy/enterprise-ai/.env.example`、`deploy/enterprise-ai/README.md`、`AICopilot 项目部署与维护指南.md`、`docs/AI架构治理清单.md`、`AGENTS.md`、`资料/AICopilot业务规则.md`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`、`src/tests/AICopilot.ArchitectureTests/ArchitectureBoundaryTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、生产数据库、GitHub runner、Vault/OIDC 配置或真实发布状态。
- 改动原因：源码审计发现 `deploy-release.sh` 对 `AICOPILOT_MODEL_SMOKE_API_KEY=dummy-key` 有无条件放行。虽然仓库默认值已经为空，且真实模型网关可能确实需要 dummy key，但无条件放行会削弱 “weak secret fail-fast” 门禁，使真实 `.env` 中的弱值不需要显式例外即可通过。
- 影响面：`.env.example` 新增 `AICOPILOT_MODEL_SMOKE_ALLOW_DUMMY_KEY=false`；`deploy-release.sh` 默认把 `dummy-key` 视为弱值，只有同时设置 `AICOPILOT_MODEL_SMOKE_ALLOW_DUMMY_KEY=true` 才允许；独立 `check-model-provider-openai.sh` 也默认拒绝 `dummy-key`，只有真实 `.env` 设置该开关或手工传 `--allow-dummy-key` 才放行。模型 smoke base URL 进一步收紧为无 userinfo、query、fragment、控制字符、引号或反斜杠的基础 URL，避免凭据或畸形 URL 进入诊断日志。部署 README、部署指南和 AI 治理清单同步说明该例外必须显式开启。
- 计划收口：补强 `AI-SEC-006` 的弱 secret fail-fast 语义。真实生产 `.env` 未修改；如果现场模型网关确实只接受 dummy key，需要发布/运维窗口在真实 `.env` 中显式加开关并留痕。
- 规则提炼：形成长期规则，已写入 `AGENTS.md` 和 `资料/AICopilot业务规则.md`：模型 smoke 的 `AICOPILOT_MODEL_SMOKE_API_KEY=dummy-key` 只允许作为真实模型网关的显式兼容例外，必须同时设置 `AICOPILOT_MODEL_SMOKE_ALLOW_DUMMY_KEY=true` 或手工 smoke 命令传 `--allow-dummy-key`；默认 preflight 必须拒绝该弱值。
- 验证命令：
  - `bash -n deploy/enterprise-ai/*.sh deploy/enterprise-ai/scripts/*.sh`
  - `if deploy/enterprise-ai/scripts/check-model-provider-openai.sh --base-url http://model.internal.example:40034/v1 --model smoke-model --api-key dummy-key --dry-run; then echo 'unexpected dummy success' >&2; exit 1; else echo 'expected dummy rejection'; fi`
  - `deploy/enterprise-ai/scripts/check-model-provider-openai.sh --base-url http://model.internal.example:40034/v1 --model smoke-model --api-key dummy-key --allow-dummy-key --dry-run`
  - `if deploy/enterprise-ai/scripts/check-model-provider-openai.sh --base-url 'http://user:secret@model.internal.example:40034/v1' --model smoke-model --api-key explicit-test-key --dry-run; then echo 'unexpected userinfo success' >&2; exit 1; else echo 'expected userinfo rejection'; fi`
  - `if deploy/enterprise-ai/scripts/check-model-provider-openai.sh --base-url 'http://model.internal.example:40034/v1?token=secret' --model smoke-model --api-key explicit-test-key --dry-run; then echo 'unexpected query success' >&2; exit 1; else echo 'expected query rejection'; fi`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~ModelProviderSmokeCheck_ShouldStayAvailableAsServerSideDiagnostic" --no-restore`
- 验证结果：脚本语法检查通过；`check-model-provider-openai.sh --api-key dummy-key --dry-run` 默认正确失败；显式 `--allow-dummy-key` 时 dry-run 通过；带 userinfo 的 base URL 正确失败；带 query string 的 base URL 正确失败；`SecurityHardeningTests` 74 条通过；`ModelProviderSmokeCheck_ShouldStayAvailableAsServerSideDiagnostic` 1 条通过。真实生产 `.env` 未修改，如果现场模型网关需要 dummy key，仍需发布/运维窗口显式设置 `AICOPILOT_MODEL_SMOKE_ALLOW_DUMMY_KEY=true` 并留痕。

## 2026-07-06 测试样例现场 IP 去噪

- 改动范围：AICopilot `src/tests/AICopilot.BackendTests/CloudOidcLoginTestsControllerSecurity.cs`、`src/tests/AICopilot.BackendTests/MigrationWorkerCloudReadOnlySeederTests.cs` 和本滚动复盘。未修改生产代码、CloudPlatform、EdgeClient、真实服务器 `.env`、生产数据库、GitHub runner、Vault/OIDC 配置或真实发布状态。
- 改动原因：继续执行 AI 修复计划的源码审计时，生产源码和部署脚本已无真实内网地址段默认值，但测试样例仍包含现场 IP 占位对应的真实地址。这些不是生产默认值，但会污染后续私网地址扫描审计结果，容易和真实配置泄露混淆。
- 影响面：OIDC 内网 HTTP 允许列表测试把 `<aicopilot-prod-host>` 改为通用私网样例 `10.0.0.10`；Cloud readonly seed 测试把连接串 host 改为 `cloud-postgres.internal.example` 常量。测试语义不变：仍覆盖私网 HTTP OIDC 和 Cloud readonly 配置校验。
- 计划收口：不新增编号，归入 `AI-SEC-006` / `AI-SEC-028` 的审计去噪和测试证据质量补强。真实生产配置未修改。
- 规则提炼：无新增长期规则，原因是“部署模板、文档示例、workflow 默认值、脚本默认值、migration seed 和 fresh DB seed 不得携带真实内网 IP”已沉淀在 `AGENTS.md` 和 `资料/AICopilot业务规则.md`。本批只是把测试样例同步为占位口径，降低误报。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~CloudOidcLoginTestsControllerSecurity|FullyQualifiedName~MigrationWorkerCloudReadOnlySeederTests|FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `rg -n "(10\\.[0-9]{1,3}\\.|172\\.(1[6-9]|2[0-9]|3[01])\\.|192\\.168\\.|dummy-key)" src/tests/AICopilot.BackendTests src/tests/AICopilot.ArchitectureTests`
- 验证结果：`CloudOidcLoginTestsControllerSecurity|MigrationWorkerCloudReadOnlySeederTests|SecurityHardeningTests` 共 99 条通过；测试目录检索中 `10.98` / `dummy-key` 只剩 `Should.NotContain` 类安全断言，不再作为测试连接串、OIDC issuer 或默认样例值；生产源码/部署脚本/规则清单检索中 `dummy-key` 只剩部署 preflight 的拒绝规则、规则文本和测试断言。

## 2026-07-06 AI fresh DB 默认模型弱凭据和真实 IP 下线

- 改动范围：AICopilot `src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerAiGatewaySeeder.cs`、`src/tests/AICopilot.BackendTests/FreshDatabaseSeedTests.cs`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`、`AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、生产数据库、GitHub runner、Vault/OIDC 配置或真实发布状态。
- 改动原因：继续按源码核对 `AI-SEC-006`/弱 secret/真实 IP 治理时发现，虽然 `.env.example` 和部署脚本已清掉 `dummy-key` 与真实内网地址默认值，但 fresh DB seed 仍会创建 `MiniMax Private` 模型，写入真实模型网关地址，并把 `dummy-key` 加密后作为默认 API key，同时默认启用模型。这个不是部署模板，但会让新库自带可用弱凭据和现场 IP，不符合当前治理目标。
- 影响面：fresh DB 新建默认模型改为 `http://model.internal.example:40034/v1` 占位 URL、`ApiKey=null`、`IsEnabled=false`；管理员必须在 UI 或受控命令中录入真实 URL/API key 并启用模型。已有同名模型不再被 seed 强行覆盖 URL、参数或启用状态；seed 只保留对已有 key 的旧 `encv1:`/明文迁移保护，避免破坏已部署现场真实配置。`FreshDatabaseSeedTests` 改为验证无默认 key 和默认禁用；`SecurityHardeningTests` 锁定 seeder 源码不再包含真实内网地址或 `dummy-key`。
- 计划收口：新增 `AI-SEC-028`，状态 `Done`。它补的是仓库内 fresh DB seed 默认值治理；真实生产库如果历史上已经存在旧默认模型，仍需管理员在发布窗口核对并按实际生产配置处理，不能仅凭源码改动声称线上已清理。
- 规则提炼：形成长期规则，已写入 `AGENTS.md` 和 `资料/AICopilot业务规则.md`：部署模板、文档示例、workflow 默认值、脚本默认值、migration seed 和 fresh DB seed 都不得写入真实内网 IP、弱 secret、`CHANGE_ME`、`dummy-key` 或默认 `root@` 发布目标；真实生产值只能来自服务器 `.env`、GitHub secrets、管理员录入或命令行环境变量。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~FreshDatabaseSeedTests|FullyQualifiedName~SecurityHardeningTests|FullyQualifiedName~MigrationWorkerSecretMigratorTests" --no-restore`
  - `rg -n "dummy-key|<model-gateway-host>|model.internal.example|apiKey: null|isEnabled: false" src/hosts/AICopilot.MigrationWorkApp src/tests/AICopilot.BackendTests/FreshDatabaseSeedTests.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md AGENTS.md 资料/AICopilot业务规则.md`
  - `bash -n deploy/enterprise-ai/*.sh deploy/enterprise-ai/scripts/*.sh`
- 验证结果：`FreshDatabaseSeedTests|SecurityHardeningTests|MigrationWorkerSecretMigratorTests` 共 79 条通过；静态检索确认 seeder 源码只保留 `model.internal.example` 占位、`apiKey: null`、`isEnabled: false`，不再包含真实模型网关地址或 `dummy-key`；规则和治理清单已同步 migration seed / fresh DB seed 红线；脚本语法检查通过。真实生产库历史模型配置未修改，原因是需要生产服务器、真实数据库和发布/运维窗口。

## 2026-07-06 AI-SEC-012 migration worker 迁移后自检补强

- 改动范围：AICopilot `src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs`、`src/tests/AICopilot.BackendTests/MigrationWorkerSecretMigratorTests.cs`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、生产数据库、GitHub runner、Vault/OIDC 配置或真实发布状态。
- 改动原因：`AI-SEC-012` 已通过 migration worker 做 `encv1:`/明文重加密，并通过 `deploy-release.sh` 在 runtime 启动前做数据库 SQL preflight；但如果有人单独运行 migration worker 或后续改坏迁移逻辑，worker 自身此前没有保存后的“只剩 `encv2:`”自检。为把失败时机继续前移，本批在 worker 内部补迁移后自检。
- 影响面：`MigrationWorkerSecretMigrator.MigrateAsync` 在保存 LanguageModel 和 EmbeddingModel 变更后调用 `EnsureMigratedSecrets`，只允许空值或 `encv2:`；发现 `encv1:` 或明文等非 `encv2:` 非空密钥时直接抛出固定错误，提示重新运行 migration 或由管理员重录 API key。`MigrationWorkerSecretMigratorTests` 覆盖空值/`encv2:` 通过、`encv1:`/明文失败；`SecurityHardeningTests` 锁定 worker 仍在 seed 前迁移并包含迁移后自检。
- 计划收口：`AI-SEC-012` 的仓库内链路变为三层：migration worker 迁移并自检、`deploy-release.sh` runtime 启动前 SQL preflight、runtime provider 拒绝旧格式。状态仍保持 `Partial`，因为真实生产库还没有在发布窗口运行 migration 并用真实 SQL 结果证明全量迁移完成。
- 规则提炼：无新增长期规则，原因是“旧 `encv1:` 只能迁移重加密，runtime provider 不得兼容旧格式；生产库迁移必须由发布窗口验收”已经沉淀在 `AGENTS.md`、`资料/AICopilot业务规则.md`、部署文档和 `docs/AI架构治理清单.md`。本批只是把 migration worker 内部失败时机前移。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~MigrationWorkerSecretMigratorTests|FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `rg -n "EnsureMigratedSecrets|non-encv2 secret value|MigrationWorkerSecretMigrator.MigrateAsync|check_model_secret_migration_preflight" src/hosts/AICopilot.MigrationWorkApp src/tests/AICopilot.BackendTests docs/AI架构治理清单.md deploy/enterprise-ai/deploy-release.sh`
  - `bash -n deploy/enterprise-ai/*.sh deploy/enterprise-ai/scripts/*.sh`
- 验证结果：`MigrationWorkerSecretMigratorTests|SecurityHardeningTests` 共 78 条通过；静态检索确认 migration worker 在 seed 前运行、`EnsureMigratedSecrets` 和 `non-encv2 secret value` 自检已落地，`deploy-release.sh` 仍保留 runtime 启动前 `check_model_secret_migration_preflight`；脚本语法检查通过。真实生产库 API key 迁移 SQL 未运行，原因是需要生产服务器、真实 PostgreSQL 和发布窗口。

## 2026-07-06 AI-SEC-010 平台侧验收记录模板补齐

- 改动范围：AICopilot `deploy/enterprise-ai/runner-platform-attestation.template.md`、`deploy/enterprise-ai/scripts/check-platform-attestation-record.sh`、`deploy/enterprise-ai/scripts/check-runner-security-attestation.sh`、`deploy/enterprise-ai/local-release.sh`、`deploy/enterprise-ai/README.md`、`AICopilot 项目部署与维护指南.md`、`docs/AI架构治理清单.md`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实 GitHub environment secrets、runner 服务器、Vault/OIDC 配置、生产 `.env` 或真实发布状态。
- 改动原因：`AI-SEC-010` 已有 runner 机器侧脚本和 workflow 门禁，但 GitHub production environment secrets、required reviewers、OIDC/Vault 或等价短期凭据仍只能靠外部平台验收。为了避免后续继续靠口头确认，本批补充平台侧验收记录模板和记录 linter，把“谁验收、验什么、证据摘要是什么”固定成可复核产物。
- 影响面：新增 `runner-platform-attestation.template.md`，要求平台 owner 记录 runner 非 root、label、工作目录、Docker Root Dir、部署目录、GitHub production environment required reviewers、secret inventory、least-privilege workflow 权限以及 OIDC/Vault 或已接受的短期例外。新增 `check-platform-attestation-record.sh` 只做静态完整性校验：拒绝未替换占位符、未勾选 checklist、TODO/TBD/unknown 等明显未完成记录，并要求包含 `AI-SEC-010`、runner 脚本、GitHub production environment、required reviewers、`contents: read`、`self-hosted + iiot-linux-prod`、OIDC/Vault/short-lived credential 和签核字段。`local-release.sh` 会同步模板和 linter 到服务器部署目录；runner 脚本和部署文档会提示平台侧必须另行留痕。
- 计划收口：`AI-SEC-010` 仍保持 `Partial`，因为模板和 linter 只能提高外部验收证据质量，不能证明真实 GitHub environment、runner 机器权限或 Vault/OIDC 已经完成。
- 规则提炼：无新增长期规则，原因是“runner/OIDC/Vault 是外部基础设施边界，不能由仓库代码伪造成完成”已经沉淀在部署指南、部署 README 和 `docs/AI架构治理清单.md`。本批只是给该规则增加平台侧记录模板和静态记录校验入口。
- 验证命令：
  - `chmod +x deploy/enterprise-ai/scripts/check-platform-attestation-record.sh`
  - `bash -n deploy/enterprise-ai/*.sh deploy/enterprise-ai/scripts/*.sh`
  - `./deploy/enterprise-ai/scripts/check-platform-attestation-record.sh --help`
  - `if ./deploy/enterprise-ai/scripts/check-platform-attestation-record.sh --record deploy/enterprise-ai/runner-platform-attestation.template.md; then echo 'unexpected template success' >&2; exit 1; else echo 'expected template rejection'; fi`
  - `tmp_record="$(mktemp "${TMPDIR:-/tmp}/aicopilot-platform-attestation.XXXXXX.md")"; sed -e 's/- \[ \]/- [x]/g' -e 's/<[^>][^>]*>/record/g' deploy/enterprise-ai/runner-platform-attestation.template.md > "$tmp_record"; ./deploy/enterprise-ai/scripts/check-platform-attestation-record.sh --record "$tmp_record"; rm -f "$tmp_record"`
  - `./deploy/enterprise-ai/scripts/check-runner-security-attestation.sh --dry-run`
  - `REGISTRY=harbor.internal.example:80/enterprise-ai CLOUD_PLATFORM_URL=http://cloud.internal.example:81 ./deploy/enterprise-ai/local-release.sh --services web --ssh-target deploy@127.0.0.1 --remote-dir /srv/enterprise-ai/deploy --dry-run | rg "runner-platform-attestation|check-platform-attestation-record|check-runner-security-attestation|sync AICopilot"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
- 验证结果：脚本语法检查通过；平台记录 linter `--help` 正常输出；对未填写模板运行 linter 会因占位符正确失败；由模板替换占位符和勾选项生成的临时记录可通过 linter；runner 验收脚本 dry-run 通过并提示平台侧仍需单独留痕；`local-release.sh --dry-run` 输出确认会同步 `runner-platform-attestation.template.md`、`check-platform-attestation-record.sh` 和 `check-runner-security-attestation.sh`；`SecurityHardeningTests` 74 条通过；目标文件 `git diff --check` 通过；目标文件尾随空白检索无命中；HTTP/HTTPS 关键词复查只命中 HTTP-only 禁止项、扫描命令和测试断言。真实 GitHub environment secrets、required reviewers、runner 服务器权限和 Vault/OIDC 状态仍未验收，需要平台权限和运维窗口。

## 2026-07-06 AI-SEC-012 runtime 启动前密钥迁移 preflight

- 改动范围：AICopilot `deploy/enterprise-ai/deploy-release.sh`、`deploy/enterprise-ai/README.md`、`AICopilot 项目部署与维护指南.md`、`docs/AI架构治理清单.md`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、生产数据库、GitHub runner、Vault/OIDC 配置或真实发布状态。
- 改动原因：`AI-SEC-012` 已有 migration worker 和发布后 `check-release-security-attestation.sh`，但旧 `encv1:` 或明文 API key 只有在 runtime 服务启动后才会被发布后验收发现。为了避免 HttpApi/DataWorker/RagWorker/Web 在旧密钥未迁移时短暂启动，本批把模型和 Embedding API key 迁移检查前移到 runtime 启动前。
- 影响面：`deploy-release.sh` 新增 `check_model_secret_migration_preflight`，直接查询 `aigateway.language_models` 和 `rag.embedding_models`，要求 `legacy_count=0` 且 `unprotected_count=0`。全量发布改为先启动基础服务、运行 `aicopilot-migration`、执行密钥迁移 preflight，再启动 HttpApi/DataWorker/RagWorker/Web；按需发布如果包含 runtime 服务但不包含 `migration`，也会先执行同一 preflight，发现旧密钥立即失败并提示先运行 `./deploy-release.sh <sha> --services migration` 或由管理员重录 API key。发布后 release attestation 仍保留二次复验。
- 计划收口：`AI-SEC-012` 的仓库内执行链进一步收紧：migration worker 负责重加密，deploy-release 在 runtime 启动前 fail-fast 验结果，runtime provider 仍拒绝旧格式。状态仍保持 `Partial`，因为真实生产库还没有在发布窗口运行 migration 并用真实 SQL 结果证明全量迁移完成。
- 规则提炼：无新增长期规则，原因是“旧 `encv1:` 只能迁移重加密，runtime provider 不得兼容旧格式；生产库迁移必须由发布窗口验收”已经沉淀在 `AGENTS.md`、`资料/AICopilot业务规则.md`、部署文档和 `docs/AI架构治理清单.md`。本批只是把失败时机前移到 runtime 启动前。
- 验证命令：
  - `bash -n deploy/enterprise-ai/*.sh deploy/enterprise-ai/scripts/*.sh`
  - `REGISTRY=harbor.internal.example:80/enterprise-ai CLOUD_PLATFORM_URL=http://cloud.internal.example:81 ./deploy/enterprise-ai/local-release.sh --services httpapi,migration,web --ssh-target deploy@127.0.0.1 --remote-dir /srv/enterprise-ai/deploy --dry-run | rg "local deploy command|deploy-release|--services|check-(release|runner)-security-attestation|sync AICopilot"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests|FullyQualifiedName~MigrationWorkerSecretMigratorTests|FullyQualifiedName~SecretStringEncryptorTests|FullyQualifiedName~ModelSecretRuntimeBoundaryTests" --no-restore`
  - `rg -n "check_model_secret_migration_preflight|Expected legacy_count=0 and unprotected_count=0 before starting runtime services|Run ./deploy-release.sh|model-secret-preflight|runtime 启动前|aicopilot-migration" deploy/enterprise-ai/deploy-release.sh deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md" docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`
- 验证结果：shell 语法检查通过；release/runner 验收脚本 dry-run 通过；compose config 通过；`local-release.sh --dry-run` 输出确认会同步发布支持脚本并触发 `deploy-release.sh --services httpapi,migration,web`；`SecurityHardeningTests|MigrationWorkerSecretMigratorTests|SecretStringEncryptorTests|ModelSecretRuntimeBoundaryTests` 共 87 条通过；目标文件 `git diff --check` 通过；尾随空白检索无命中；静态检索确认 runtime 启动前密钥迁移 preflight、失败提示和文档口径均已落地。真实生产库 API key 迁移 SQL 未运行，原因是需要生产服务器、真实 PostgreSQL 和发布窗口。

## 2026-07-06 AI 灾备 workflow 接入 runner 机器侧验收

- 改动范围：AICopilot `.github/workflows/aicopilot-ai-read-diagnostics.yml`、`.github/workflows/aicopilot-deploy.yml`、`.github/workflows/aicopilot-enable-direct-cloud-readonly-db.yml`、`.github/workflows/aicopilot-enable-real-cloud-ai-read.yml`、`.github/workflows/aicopilot-image.yml`、`.github/workflows/aicopilot-provision-cloud-readonly-db-role.yml`、`deploy/enterprise-ai/README.md`、`AICopilot 项目部署与维护指南.md`、`docs/AI架构治理清单.md`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实 GitHub environment secrets、runner 服务器、Vault/OIDC 配置、生产 `.env` 或真实发布状态。
- 改动原因：`AI-SEC-010` 的仓库内门禁此前只要求 workflow 保持 `self-hosted + iiot-linux-prod`、`contents: read`、非 root 和禁用 hosted runner；新建的 `check-runner-security-attestation.sh` 还只在文档/服务器脚本层可执行。为了避免灾备 workflow 绕过 runner 机器侧验收，production/secrets 相关 workflow 也必须执行该脚本。
- 影响面：灾备 image/deploy/provision workflow 在 checkout 后调用仓库内 `deploy/enterprise-ai/scripts/check-runner-security-attestation.sh`；只在服务器上操作且不 checkout 的 diagnostics、enable-direct-cloud-readonly-db、enable-real-cloud-ai-read workflow 调用部署目录已同步的 `scripts/check-runner-security-attestation.sh`。安全测试新增断言：所有触达 `environment: production` 或 `secrets.` 的 `aicopilot-*.yml` 都必须包含该脚本调用。
- 计划收口：`AI-SEC-010` 的仓库内证据从“文档和本地脚本”推进为“灾备 workflow 自身执行脚本 + SecurityHardeningTests 锁定”。状态仍保持 `Partial`，因为真实 runner 文件系统权限、GitHub environment secret 权限、required reviewers、OIDC/Vault 或等价短期凭据还需要平台侧单独验收。
- 规则提炼：无新增长期规则，原因是“灾备 workflow 使用非 root self-hosted runner，runner/OIDC/Vault 是外部基础设施边界”已经沉淀在 `AICopilot 项目部署与维护指南.md`、`deploy/enterprise-ai/README.md` 和 `docs/AI架构治理清单.md`。本批只是把既有脚本接入 workflow。
- 验证命令：
  - `ruby -e 'require "yaml"; ARGV.each { |path| YAML.load_file(path); puts path }' .github/workflows/aicopilot-*.yml`
  - `rg -n "check-runner-security-attestation.sh|runs-on: \\[self-hosted, iiot-linux-prod\\]|contents: read|Self-hosted runner must not run as root" .github/workflows src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `git diff --check -- .github/workflows/aicopilot-ai-read-diagnostics.yml .github/workflows/aicopilot-deploy.yml .github/workflows/aicopilot-enable-direct-cloud-readonly-db.yml .github/workflows/aicopilot-enable-real-cloud-ai-read.yml .github/workflows/aicopilot-image.yml .github/workflows/aicopilot-provision-cloud-readonly-db-role.yml deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md" docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`
- 验证结果：全部 `aicopilot-*.yml` 通过 Ruby YAML 解析；静态检索确认 6 个 production/secrets 相关 workflow 都保留 `contents: read`、`self-hosted + iiot-linux-prod`、非 root 检查并调用 `check-runner-security-attestation.sh`；`SecurityHardeningTests` 74 条通过；目标文件 `git diff --check` 通过。真实 runner 用户、runner 文件系统、Docker Root Dir、GitHub environment secrets、required reviewers、Vault/OIDC 或短期凭据未验收，原因是这些需要外部基础设施权限和发布/运维窗口。

## 2026-07-06 AI 发布主链路自动执行安全验收

- 改动范围：AICopilot `deploy/enterprise-ai/deploy-release.sh`、`deploy/enterprise-ai/scripts/check-release-security-attestation.sh`、`deploy/enterprise-ai/README.md`、`AICopilot 项目部署与维护指南.md`、`docs/AI架构治理清单.md`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、生产数据库、GitHub runner、Vault/OIDC 配置或真实发布状态。
- 改动原因：上一批已提供 `check-release-security-attestation.sh`，但它仍主要靠文档提醒手工执行。`AI-SEC-002`、`AI-SEC-008`、`AI-SEC-012` 的真实环境验收需要在服务器发布链路里成为硬门禁，避免 Web 安全头、Cloud OIDC 状态接口、Web 非 root 容器或 API key 迁移验收被漏跑。同时复核脚本发现 `RETURN` trap 清理临时文件在 `set -u` 下存在后续函数返回时引用已离开作用域 local 变量的风险，真实服务器验收脚本自身不能不稳定。
- 影响面：`deploy-release.sh` 在 `probe_web` 和 `probe_web_security_headers` 后自动执行 `scripts/check-release-security-attestation.sh`，失败则退出且不写入 current release manifest；成功输出会写入 `current-release.summary.md` 的 `Release Security Attestation` 段。`check-release-security-attestation.sh` 新增 `/api/identity/cloud-oidc/status` HTTP 200 探针，并改为显式删除临时 header/SQL 结果文件，不再使用 `RETURN` trap。部署 README、项目部署指南和 AI 治理清单已同步改为“发布时自动运行，必要时手工复验”的口径。`SecurityHardeningTests` 锁定发布脚本必须调用 release attestation、失败退出、summary 记录验收输出、Cloud OIDC 状态接口探针，并禁止 release attestation 脚本回退到 `RETURN` trap。
- 计划收口：`AI-SEC-002`、`AI-SEC-008`、`AI-SEC-012` 的服务器侧验收入口已从“手工建议”推进为 `deploy-release.sh` 主链路硬门禁；状态仍不代表真实生产已经通过，因为本轮没有连接生产服务器、真实容器或生产 PostgreSQL。
- 规则提炼：无新增长期规则，原因是“HTTP-only 安全头、Web 非 root、`encv2:` 密钥迁移和真实环境验收不得伪造成完成”已经沉淀在 `AGENTS.md`、`资料/AICopilot业务规则.md`、部署文档和 `docs/AI架构治理清单.md`。本批只是把既有规则接入服务器发布主链路。
- 验证命令：
  - `bash -n deploy/enterprise-ai/*.sh deploy/enterprise-ai/scripts/*.sh`
  - `REGISTRY=harbor.internal.example:80/enterprise-ai CLOUD_PLATFORM_URL=http://cloud.internal.example:81 ./deploy/enterprise-ai/local-release.sh --services web --ssh-target deploy@127.0.0.1 --remote-dir /srv/enterprise-ai/deploy --dry-run | rg "check-(release|runner)-security-attestation|sync AICopilot deploy support files|local deploy command|deploy-release"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `rg -n "run_release_security_attestation|Release Security Attestation|check-release-security-attestation|release-security-attestation|自动运行.*check-release|Cloud OIDC 状态|/api/identity/cloud-oidc/status|summary 会包含|exit \"\\$attestation_status\"" deploy/enterprise-ai/deploy-release.sh deploy/enterprise-ai/scripts/check-release-security-attestation.sh deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md" docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`
  - `rg -n "RETURN" deploy/enterprise-ai/scripts/check-release-security-attestation.sh src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`
- 验证结果：shell 语法检查通过；release/runner 验收脚本 dry-run 通过；compose config 通过；`local-release.sh --dry-run` 输出确认 `check-release-security-attestation.sh` 和 `check-runner-security-attestation.sh` 会同步到服务器部署目录；`SecurityHardeningTests` 74 条通过；静态检索确认 `deploy-release.sh` 自动运行 release security attestation、失败退出并把结果写入 release summary，release attestation 覆盖 Cloud OIDC 状态接口，且只有测试中的 `NotContain("RETURN")` 命中 `RETURN`。真实 HTTP header curl、Web 容器非 root smoke、生产库 API key 迁移 SQL、真实 runner/OIDC/Vault 基础设施验收未运行，原因是这些需要生产服务器和发布窗口。

## 2026-07-06 AI runner 机器侧安全验收脚本补齐

- 改动范围：AICopilot `deploy/enterprise-ai/scripts/check-runner-security-attestation.sh`、`deploy/enterprise-ai/local-release.sh`、`deploy/enterprise-ai/README.md`、`AICopilot 项目部署与维护指南.md`、`docs/AI架构治理清单.md`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实 GitHub environment secrets、runner 服务器、Vault/OIDC 配置、生产 `.env`、生产数据库或真实发布状态。
- 改动原因：`AI-SEC-010` 仍有外部基础设施验收缺口。仓库内已有灾备 workflow least-privilege、`self-hosted + iiot-linux-prod`、非 root runner 和 hosted runner 禁用门禁，但 runner 机器本地事实缺少统一脚本入口。为避免把“文档要求”误读为“已完成”，补充 runner 机器侧验收脚本，并在文档中明确该脚本只能证明本机事实。
- 影响面：新增 `check-runner-security-attestation.sh`，检查当前执行用户非 root、预期 runner 工作根目录存在且可写、有 GitHub workspace 环境变量时 workspace 必须位于 `/data/github-runner/aicopilot` 下、Docker Root Dir 必须位于 `/data/docker` 下、部署目录必须为 `/srv/enterprise-ai/deploy`。脚本支持 `--dry-run`，并持续提示 GitHub production environment secrets、required reviewers、runner 服务账号权限和 OIDC/Vault 或等价短期凭据仍需平台侧单独留痕。`local-release.sh` 已把该脚本纳入同步和 chmod 清单。
- 计划收口：`docs/AI架构治理清单.md` 中 `AI-SEC-010` 继续保持 `Partial`。本批把 runner 机器侧验收入口从“人工口径”推进为“可执行脚本 + 文档 + 单测门禁”，但未连接真实 runner 机器，也未验收 GitHub environment secrets、required reviewers、OIDC/Vault 或等价短期凭据。
- 规则提炼：无新增长期规则，原因是 HTTP-only 部署红线、灾备 runner 必须非 root、runner/OIDC/Vault 属于外部基础设施验收边界，已沉淀在 `AICopilot 项目部署与维护指南.md`、`deploy/enterprise-ai/README.md` 和 `docs/AI架构治理清单.md`。本批只是补齐可执行验收脚本和测试门禁。
- 验证命令：
  - `chmod +x deploy/enterprise-ai/scripts/check-runner-security-attestation.sh`
  - `bash -n deploy/enterprise-ai/*.sh deploy/enterprise-ai/scripts/*.sh`
  - `./deploy/enterprise-ai/scripts/check-runner-security-attestation.sh --dry-run`
  - `REGISTRY=harbor.internal.example:80/enterprise-ai CLOUD_PLATFORM_URL=http://cloud.internal.example:81 ./deploy/enterprise-ai/local-release.sh --services web --ssh-target deploy@127.0.0.1 --remote-dir /srv/enterprise-ai/deploy --dry-run | rg "check-(release|runner)-security-attestation|sync AICopilot deploy support files|local deploy command|deploy-release"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `git diff --check -- deploy/enterprise-ai/scripts/check-runner-security-attestation.sh deploy/enterprise-ai/local-release.sh deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md" docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`
  - `rg -n "[ \t]+$" deploy/enterprise-ai/scripts/check-runner-security-attestation.sh deploy/enterprise-ai/local-release.sh deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md" docs/AI架构治理清单.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`
- 验证结果：脚本语法检查通过；runner 验收脚本 dry-run 通过；`local-release.sh --dry-run` 输出确认 `check-release-security-attestation.sh` 与 `check-runner-security-attestation.sh` 会同步到服务器部署目录并 chmod；`SecurityHardeningTests` 74 条通过；目标文件 `git diff --check` 通过；目标文件尾随空白检索无命中。真实 runner 用户、runner 文件系统、Docker Root Dir、GitHub environment secrets、required reviewers、Vault/OIDC 或短期凭据未验收，原因是这些需要外部基础设施权限和发布/运维窗口。

## 2026-07-06 AI 发布后安全验收脚本补齐

- 改动范围：AICopilot `deploy/enterprise-ai/scripts/check-release-security-attestation.sh`、`deploy/enterprise-ai/local-release.sh`、`deploy/enterprise-ai/README.md`、`AICopilot 项目部署与维护指南.md`、`docs/AI架构治理清单.md`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、生产业务代码、真实服务器、生产数据库、GitHub runner、Vault/OIDC 配置或 `.env`。
- 改动原因：当前剩余外部验收项分散在文档里：真实 HTTP header curl、`aicopilot-webui` 非 root 容器 smoke、`aigateway.language_models` / `rag.embedding_models` API key 迁移 SQL。为了继续推进完整 AI 修复计划，不能把这些项伪造成本地完成，但可以把它们集中成服务器侧可执行验收入口，降低后续发版时漏验或口径漂移的风险。
- 影响面：新增 `check-release-security-attestation.sh`，发布后在服务器执行。脚本要求 Web URL 必须是 HTTP，检查 `X-Content-Type-Options`、`X-Frame-Options`、`Referrer-Policy`、`Permissions-Policy`、`Content-Security-Policy`，并明确拒绝 `Strict-Transport-Security`；检查 `aicopilot-webui` 容器非 root 且 `/var/cache/nginx`、`/var/run` 可写；检查语言模型和 Embedding 模型 API key 的 `legacy_count=0`、`unprotected_count=0`。`local-release.sh` 已把该脚本纳入同步和 chmod 清单；部署 README、部署指南和 AI 治理清单改为优先引用该脚本。
- 计划收口：`AI-SEC-002`、`AI-SEC-008`、`AI-SEC-012` 的真实环境验收已有统一脚本入口；状态仍不能改成“真实环境完成”，因为本轮只跑了 dry-run 和本地门禁，没有连接生产服务器、真实容器或生产 PostgreSQL。
- 规则提炼：无新增长期规则，原因是 HTTP-only、安全头、非 root 容器、`encv2:` 密钥迁移和最终回复必须标明未运行真实发布验收的口径已经沉淀在 `AGENTS.md`、`资料/AICopilot业务规则.md`、部署文档和 `docs/AI架构治理清单.md`。本批只是把这些规则转成服务器侧统一验收脚本。
- 验证命令：
  - `chmod +x deploy/enterprise-ai/scripts/check-release-security-attestation.sh`
  - `bash -n deploy/enterprise-ai/*.sh deploy/enterprise-ai/scripts/*.sh`
  - `./deploy/enterprise-ai/scripts/check-release-security-attestation.sh --dry-run`
  - `if ./deploy/enterprise-ai/scripts/check-release-security-attestation.sh --dry-run --web-url https://aicopilot.internal.example:443/; then echo 'unexpected success' >&2; exit 1; else echo 'expected HTTPS rejection'; fi`
  - `REGISTRY=harbor.internal.example:80 CLOUD_PLATFORM_URL=http://cloud.internal.example:81 ./deploy/enterprise-ai/local-release.sh --services httpapi,migration,dataworker,ragworker,web --ssh-target deploy@aicopilot.internal.example --dry-run | rg "check-release-security-attestation|sync AICopilot deploy support files|Release tag"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore --logger "console;verbosity=minimal"`
- 验证结果：脚本语法检查通过；发布后安全验收脚本 dry-run 通过；HTTPS URL 负向 dry-run 正确拒绝；`local-release.sh --dry-run` 输出确认该脚本会同步到服务器并 chmod；`SecurityHardeningTests` 74 条通过。真实 HTTP header curl、容器 smoke、生产库迁移 SQL、runner/OIDC/Vault 基础设施验收未运行。

## 2026-07-06 AI 完整计划全量验证补强

- 改动范围：AICopilot `src/tests/AICopilot.BackendTests/AgentScopeLifecycleTests.cs`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、生产业务代码、真实服务器、生产数据库、GitHub runner 或 `.env`。
- 改动原因：继续执行完整 AI 修复计划，不能只依赖 filtered tests 证明大范围安全/架构改动。完整 `AICopilot.BackendTests` 首次运行暴露 `AgentScopeLifecycleTests` 失败：该测试只注册 `AddAiRuntime()`，但 `InMemoryModelEndpointPoolScheduler` 已因 endpoint pool 密钥保护要求依赖 `ISecretProtector`；测试容器缺少该依赖，且测试模型仍用明文 `fake-key`。
- 影响面：仅修正测试装配。`AgentScopeLifecycleTests` 注册测试版 `EndpointPoolSecretProtector`，并把测试模型 API key 改为 `encv2:` 受保护格式。生产组合根已经通过 Infrastructure 注册 `ISecretProtector`，本批没有改变 runtime 行为。
- 计划收口：完整后端测试、完整架构测试、solution build、前端 type-check、完整前端 unit tests、前端 build、本地 compose config、local-release dry-run 和模型 provider smoke dry-run 均已跑通。真实 HTTP header curl、容器 smoke、生产库迁移 SQL、runner/OIDC/Vault 基础设施验收仍属于发版/外部环境待验收项，不能因本地全量测试通过而标成完成。
- 规则提炼：无新增长期规则，原因是“大范围改动不得只用 filtered tests 声明完成”和“模型/endpoint pool API key 必须使用 `encv2:`”已沉淀在 `AGENTS.md` 与 `资料/AICopilot业务规则.md`。本批只是补齐测试容器依赖和全量验证证据。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~AgentScopeLifecycleTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore --logger "console;verbosity=minimal"`
  - `dotnet build AICopilot.slnx --no-restore`
  - `npm run type-check`（工作目录：`src/vues/AICopilot.Web`）
  - `npm run test:unit`（工作目录：`src/vues/AICopilot.Web`）
  - `npm run build`（工作目录：`src/vues/AICopilot.Web`）
  - `docker compose --env-file deploy/enterprise-ai/.env.example -f deploy/enterprise-ai/docker-compose.yaml config -q`
  - `REGISTRY=harbor.internal.example:80 CLOUD_PLATFORM_URL=http://cloud.internal.example:81 ./deploy/enterprise-ai/local-release.sh --services httpapi,migration,dataworker,ragworker,web --ssh-target deploy@aicopilot.internal.example --dry-run`
  - `./deploy/enterprise-ai/scripts/check-model-provider-openai.sh --base-url http://model.internal.example:40034/v1 --model smoke-model --api-key explicit-test-key --dry-run`
- 验证结果：失败用例定向重跑 1 条通过；完整 `AICopilot.BackendTests` 763 条通过；完整 `AICopilot.ArchitectureTests` 82 条通过；`dotnet build AICopilot.slnx --no-restore` 成功且 0 warning / 0 error；前端 `type-check` 通过；完整前端 unit tests 28 个文件、129 条通过；前端 build 成功，保留既有 `@vueuse/core` / Rolldown `INVALID_ANNOTATION` warning 但退出码为 0；compose config、local-release dry-run 和模型 provider smoke dry-run 通过。真实 HTTP header curl、容器 smoke、生产库迁移 SQL、runner/OIDC/Vault 基础设施验收未运行。

## 2026-07-06 AI 治理清单 Done 状态口径修正

- 改动范围：AICopilot `docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、业务代码、测试代码、部署脚本、真实服务器、生产数据库或 GitHub runner 配置。
- 改动原因：继续执行“完整 AI 修复计划必须根据源码和证据生成，严禁胡编、前后不一致”。复核治理清单时发现部分 `Done` 项的验收描述包含真实内网 `curl`、容器启动 smoke、生产数据库 SQL 或 runner/OIDC/Vault 基础设施动作；这些动作不能在当前仓库内证明，若不区分状态口径，会把“仓库内源码/门禁已完成”误读成“现场已发布验收完成”。同时执行前审计示例里写了不存在的根目录 `tests`，在 AICopilot 仓库会报 `tests: No such file or directory`，需要改成覆盖 `src` 下测试目录的可运行命令。
- 影响面：`docs/AI架构治理清单.md` 新增状态口径：表格 `Done` 只代表 AICopilot 仓库内源码、脚本、文档和本地测试/静态门禁完成，不代表真实服务器发布、线上 HTTP 探针、容器生产启动、生产数据库迁移或 GitHub/服务器基础设施改造完成。清单新增发版/外部环境待验收项，点名 `AI-SEC-002` 真实 HTTP header curl、`AI-SEC-008` 容器 smoke、`AI-SEC-010` runner/OIDC/Vault 基础设施、`AI-SEC-012` 生产库密钥迁移 SQL 和第 9 节发布门禁；执行前审计 `rg` 示例去掉不存在的根目录 `tests`，由 `src` 覆盖 `src/tests`。
- 计划收口：当前仓库内未完全关单项仍是 `AI-SEC-010` 和 `AI-SEC-012`；现场/外部待验收项单独列出，不再混进源码关单状态。后续发版时必须把第 9 节命令结果写入发布复盘或发布记录，不能仅凭仓库测试称为线上完成。
- 规则提炼：无新增长期规则，原因是“最终回复和复盘必须明确验证命令；大范围改动不得只用 filtered tests 称完成；部署前必须按部署文档验收”已存在于 `AGENTS.md`、`资料/AICopilot业务规则.md` 和部署文档。本批只是把治理清单状态口径修正到与既有规则一致。
- 验证命令：
  - `rg -n "状态口径|当前发版/外部环境待验收项|AI-SEC-002|AI-SEC-008|AI-SEC-010|AI-SEC-012|第 9 节" docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `rg -n --max-count 1 "Simulation|CloudAiRead|CloudReadOnly|production-records|TextToSql|sourceName|connection string|prompt|SQL 原文" src AGENTS.md 资料 docs`
  - `bash -n deploy/enterprise-ai/*.sh deploy/enterprise-ai/scripts/*.sh`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~CloudReadonlyChatBoundaryTests|FullyQualifiedName~CloudReadonlySimulationTests|FullyQualifiedName~CloudAiReadClientTests|FullyQualifiedName~SemanticAnalysisRunnerTests|FullyQualifiedName~DeviceLogFollowUpIntentRewriterTests|FullyQualifiedName~CloudReadOnlyTextToSqlFallbackRunnerTests|FullyQualifiedName~PromptGovernanceTests|FullyQualifiedName~AiEvalBehaviorGuardrailTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~ArchitectureBoundaryTests" --no-restore --logger "console;verbosity=minimal"`
  - `npm run test:unit -- tests/unit/frontendErrorHandling.spec.ts tests/unit/runtimeDetails.spec.ts tests/unit/apiClient.spec.ts tests/unit/chunkReducer.spec.ts tests/unit/authStore.spec.ts tests/unit/agentStateStores.spec.ts tests/unit/chatStoreSkills.spec.ts tests/unit/configViewUsability.spec.ts`
  - `git diff --check -- docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
- 验证结果：上述检索、shell 语法检查和格式检查通过；Cloud/Simulation/Text-to-SQL 后端定向测试 103 条通过；ArchitectureBoundaryTests 76 条通过；前端错误和运行详情相关 unit tests 8 个文件 55 条通过。真实 HTTP header curl、容器 smoke、生产库迁移 SQL、runner/OIDC/Vault 基础设施验收未运行，原因是本批只修正治理清单口径并复核仓库内门禁，没有生产发布授权或外部基础设施访问。

## 2026-07-06 AI-SEC-010 灾备 workflow runner 门禁补强

- 改动范围：AICopilot `src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`、`docs/AI架构治理清单.md`、`AICopilot 项目部署与维护指南.md`、`deploy/enterprise-ai/README.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实 GitHub environment secrets、runner 服务器、Vault/OIDC 配置或生产部署状态。
- 改动原因：继续复核 `AI-SEC-010`。现有灾备 workflow 已使用 `self-hosted + iiot-linux-prod`、`contents: read` 和非 root runner 校验，但原有测试只覆盖 `aicopilot-image` / `aicopilot-deploy` 两个入口，没有把 Cloud AiRead 诊断、启用真实只读、直连只读 DB、只读 DB 角色 provision 等同样触达 production/secrets 的 workflow 纳入统一门禁。
- 影响面：新增 `ProductionWorkflows_ShouldKeepLeastPrivilegeSelfHostedRunnerBoundary`，自动扫描所有 `aicopilot-*.yml` 中触达 `environment: production` 或 `secrets.` 的 workflow，要求它们保持 `permissions: contents: read`、`runs-on: [self-hosted, iiot-linux-prod]`、非 root runner 校验，并禁止 hosted runner、`id-token: write`、`contents/actions/pull-requests: write`、`permissions: write-all` 和真实内网 IP。`aicopilot-simulation-release-candidate.yml` 不触达 production/secrets，仍可作为普通 CI 使用 GitHub hosted runner。
- 计划收口：`docs/AI架构治理清单.md` 已更新 `AI-SEC-010` 的真实状态：仓库内短期 workflow 门禁已加固，但状态仍是 Partial，因为 runner 机器权限收敛、短期凭据、Vault/OIDC 或等价方案需要 GitHub/服务器侧基础设施验收，不能由 AICopilot 仓库代码伪造成 Done。部署指南和部署 README 已补充该边界。
- 规则提炼：无新增长期规则，原因是“灾备 workflow 使用非 root self-hosted runner、标准发布不依赖 GitHub workflow、OIDC/Vault 属于外部基础设施治理”已沉淀在 `AGENTS.md`、`AICopilot 项目部署与维护指南.md` 和 `deploy/enterprise-ai/README.md`。本批只是补齐测试门禁和清单状态。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore --logger "console;verbosity=minimal"`
  - `rg -n "ProductionWorkflows_ShouldKeepLeastPrivilegeSelfHostedRunnerBoundary|AI-SEC-010|runner 机器权限收敛|Vault/OIDC|contents: read|self-hosted, iiot-linux-prod|Self-hosted runner must not run as root" src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md "AICopilot 项目部署与维护指南.md" deploy/enterprise-ai/README.md .github/workflows`
  - `git diff --check -- src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md "AICopilot 项目部署与维护指南.md" deploy/enterprise-ai/README.md docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md "AICopilot 项目部署与维护指南.md" deploy/enterprise-ai/README.md docs/改动复盘与规则沉淀.md`
- 验证结果：`SecurityHardeningTests` 74 条通过。真实 runner 用户、文件系统权限、GitHub environment secret 权限、Vault/OIDC 或短期凭据未验收，原因是这些需要外部基础设施权限和发布/运维窗口，本轮只在 AICopilot 仓库内补齐可执行门禁和文档边界。

## 2026-07-06 AI-SEC-012 模型与 Embedding 密钥迁移补齐

- 改动范围：AICopilot `src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs`、`src/hosts/AICopilot.MigrationWorkApp/Worker.cs`、`src/tests/AICopilot.BackendTests/MigrationWorkerSecretMigratorTests.cs`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`、`docs/AI架构治理清单.md`、`deploy/enterprise-ai/README.md`、`AICopilot 项目部署与维护指南.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务写接口、真实服务器 `.env`、数据库数据或生产部署状态。
- 改动原因：继续执行 `AI-SEC-012`。原有 `SecretStringEncryptor` 已改为 `encv2:` AES-GCM，runtime `Decrypt` 会拒绝 `encv1:` 和明文；但 migration worker 只在 `MigrationWorkerAiGatewaySeeder` 的内置语言模型 seed 路径中处理 `LanguageModel.ApiKey`，没有独立覆盖所有已有 `LanguageModel`，也没有覆盖 `RagDbContext.EmbeddingModels`。这会导致生产库里已有 embedding API key 若仍是 `encv1:` 或明文，发布后 RAG/Embedding runtime 直接失败。
- 影响面：新增 `MigrationWorkerSecretMigrator` 在 migration worker 跑完 EF migrations 后、业务 seed 前执行，扫描 `aigateway.language_models` 和 `rag.embedding_models`。非空 `encv1:` 通过 `SecretStringEncryptor.ReEncryptLegacyCipher` 重加密为 `encv2:`；历史明文通过 `SecretStringEncryptor.Encrypt` 保护为 `encv2:`；已有 `encv2:` 和空值不动。语言模型密钥被迁移后重置 connectivity 状态，避免旧失败状态误导；Embedding 模型只更新密钥字段和既有配置字段。运行时 provider 仍拒绝 `encv1:` 和明文，没有恢复 CBC 兼容。
- 计划收口：`docs/AI架构治理清单.md` 已更新 `AI-SEC-012` 的真实状态：AI 仓库内可执行迁移路径覆盖 LanguageModel + EmbeddingModel，但状态仍是 Partial，因为真实生产库还没有在发布窗口运行 migration 并用 SQL 证明 `legacy_count=0`、`unprotected_count=0`。部署 README 和项目部署指南已补充 `./deploy-release.sh sha-<git-sha> --services migration` 以及验收 SQL。
- 规则提炼：无新增长期规则，原因是“模型、Embedding 和 endpoint pool API key 必须使用 `encv2:`；`encv1:` 只能在 migration worker 或一次性迁移命令中读取并重加密”已经沉淀在 `AGENTS.md` 和 `资料/AICopilot业务规则.md`。本批只是补齐代码执行面和发布验收步骤。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~MigrationWorkerSecretMigratorTests|FullyQualifiedName~SecretStringEncryptorTests|FullyQualifiedName~ModelSecretRuntimeBoundaryTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore --logger "console;verbosity=minimal"`
  - `rg -n "MigrationWorkerSecretMigrator|MigrateEmbeddingModelApiKeys|MigrateLanguageModelApiKeys|aigateway.language_models|rag.embedding_models|--services migration|legacy_count|unprotected_count|AI-SEC-012" src/hosts/AICopilot.MigrationWorkApp src/tests/AICopilot.BackendTests docs/AI架构治理清单.md deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md"`
  - `git diff --check -- src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs src/hosts/AICopilot.MigrationWorkApp/Worker.cs src/tests/AICopilot.BackendTests/MigrationWorkerSecretMigratorTests.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md" docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \t]+$" src/hosts/AICopilot.MigrationWorkApp/MigrationWorkerSecretMigrator.cs src/hosts/AICopilot.MigrationWorkApp/Worker.cs src/tests/AICopilot.BackendTests/MigrationWorkerSecretMigratorTests.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md" docs/改动复盘与规则沉淀.md`
- 验证结果：`MigrationWorkerSecretMigratorTests|SecretStringEncryptorTests|ModelSecretRuntimeBoundaryTests` 13 条通过；`SecurityHardeningTests` 74 条通过。真实服务器 migration 和 SQL 验收未运行，原因是本轮只在 AICopilot 仓库内补齐迁移代码、测试和发布窗口操作文档，没有生产发布授权，也没有访问真实服务器 `.env` 或数据库。

## 2026-07-06 AI-SEC-016 后端日志与错误边界关单

- 改动范围：AICopilot `src/infrastructure/AICopilot.Infrastructure/CloudRead/CloudAiReadHttpTransport.cs`、`src/services/AICopilot.RagService/Documents/DocumentIndexingService.cs`、`src/infrastructure/AICopilot.Infrastructure/Mcp/McpServerBootstrap.cs`、`McpRuntimeRegistrySynchronizer.cs`、`McpServerManager.cs`、`src/infrastructure/AICopilot.EntityFrameworkCore/Outbox/OutboxDispatcher.cs`、`src/infrastructure/AICopilot.Infrastructure/AiGateway/LanguageModelConnectivityTester.cs`、`PostgreSqlSessionExecutionLock.cs`、`src/services/AICopilot.Services.CrossCutting/Sql/SqlAllowlistColumnInspector.cs`、`src/services/AICopilot.DataAnalysisService/Semantics/SemanticQueryPlanner.cs`、`src/services/AICopilot.AiGatewayService/Tools/ToolInputSchemaValidator.cs`、`src/services/AICopilot.AiGatewayService/AgentTasks/AgentDynamicPlanner.cs`、`PlannerToolCatalog.cs`、`AgentDynamicPlannerResponseParser.cs`、`AgentTaskRuntime.cs`、`Runtime/AgentToolExecutionAuditBuilder.cs`、`src/infrastructure/AICopilot.Dapper/Security/AstSqlGuardrail.cs`、`src/services/AICopilot.DataAnalysisService/Plugins/DataAnalysisToolResultFormatter.cs`、`src/services/AICopilot.AiGatewayService/Workflows/Executors/SemanticAnalysisRunner.cs`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`、`ToolRegistryGovernanceTests.cs`、`DynamicPlannerContractTests.cs`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务写接口、真实服务器 `.env`、部署脚本、数据库或生产数据。
- 改动原因：收口 `AI-SEC-016` 的最后一批后端日志/错误边界。上一批已修 HttpApi catch-all、Dapper/DataAnalysis、provider/workflow/worker，但 CloudAiRead transport、RAG indexing、MCP runtime、outbox、model connectivity tester、SQL/JSON parser 用户错误和工具执行失败摘要仍可能把 raw exception 对象或 `ex.Message` 进入日志、接口响应、任务失败摘要或持久化失败原因。
- 影响面：失败行为保持非阻断和原有降级语义：CloudAiRead、RAG indexing、MCP runtime、outbox publish、模型连接测试、SQL/JSON parser、dynamic planner 和 tool input/schema 校验仍返回失败状态或安全提示。变化是错误内容统一为 `ErrorType`、`ErrorCode`、`FailureCode`、固定业务文案或 `OriginalMessage=hidden_by_security_policy`；`AgentTaskRuntime` 工具失败摘要改为结构化安全摘要，不再携带“脱敏后的原异常正文”；outbox/document indexing 不再保存 raw `ex.Message`。少量 `ex.Message` 仅保留为内部分类输入，例如 SQL 拒绝原因分类、Text-to-SQL repair 分类和 artifact finalized 错误码识别，输出仍是固定安全文案或 hash/code。
- 计划收口：`docs/AI架构治理清单.md` 已将 `AI-SEC-016` 从 Partial 改为 Done，并补充全量生产日志静态门禁和错误边界关单证据。当前剩余未完全关单项只保留 `AI-SEC-010` runner/OIDC/Vault 外部基础设施治理和 `AI-SEC-012` 真实生产 `encv1` 迁移/重录入发布窗口验收。
- 规则提炼：无新增长期规则，原因是本批继续落实既有“SQL/prompt/token/endpoint/连接串/原始 provider message 不得进入日志、审计、运行详情或用户可见错误”的安全口径；该口径已沉淀在 `AGENTS.md`、`资料/AICopilot业务规则.md` 和 `docs/AI架构治理清单.md`。新增的是项目内静态测试门禁，不需要改写工作区总规则。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~DynamicPlannerContractTests|FullyQualifiedName~ToolRegistryGovernanceTests|FullyQualifiedName~ModelSecretRuntimeBoundaryTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~CloudReadOnlyTextToSqlFallbackRunnerTests|FullyQualifiedName~SemanticQueryPlannerTests|FullyQualifiedName~TextToSqlReadOnlyTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~RagMcpAuditCommandTests|FullyQualifiedName~ModelApiKeyProtectionTests|FullyQualifiedName~PostFixClosureRegressionTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~CloudAiRead|FullyQualifiedName~McpRuntime|FullyQualifiedName~RagIndexing|FullyQualifiedName~Outbox|FullyQualifiedName~LanguageModel" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~Outbox|FullyQualifiedName~AgentTaskRunQueueWorker_ShouldStayBehindCoordinator|FullyQualifiedName~DddAggregateBoundaryTests" --no-restore --logger "console;verbosity=minimal"`
  - `rg -n "Log(Error|Warning|Information|Debug|Trace|Critical)\\([[:space:]]*ex," src/hosts src/infrastructure src/services src/core src/shared -U -g "!**/bin/**" -g "!**/obj/**"`
  - `rg -n "MarkAsFailed\\(ex\\.Message|MarkFailed\\(ex\\.Message|Failed\\(ex\\.Message|return .*ex\\.Message|\\$\".*\\{ex\\.Message\\}" src/hosts src/infrastructure src/services src/core src/shared -g "!**/bin/**" -g "!**/obj/**"`
  - `git diff --check -- src/infrastructure/AICopilot.Infrastructure/CloudRead/CloudAiReadHttpTransport.cs src/services/AICopilot.RagService/Documents/DocumentIndexingService.cs src/infrastructure/AICopilot.Infrastructure/Mcp/McpServerBootstrap.cs src/infrastructure/AICopilot.Infrastructure/Mcp/McpRuntimeRegistrySynchronizer.cs src/infrastructure/AICopilot.Infrastructure/Mcp/McpServerManager.cs src/infrastructure/AICopilot.EntityFrameworkCore/Outbox/OutboxDispatcher.cs src/infrastructure/AICopilot.Infrastructure/AiGateway/LanguageModelConnectivityTester.cs src/infrastructure/AICopilot.Infrastructure/AiGateway/PostgreSqlSessionExecutionLock.cs src/services/AICopilot.Services.CrossCutting/Sql/SqlAllowlistColumnInspector.cs src/services/AICopilot.DataAnalysisService/Semantics/SemanticQueryPlanner.cs src/services/AICopilot.AiGatewayService/Tools/ToolInputSchemaValidator.cs src/services/AICopilot.AiGatewayService/AgentTasks/AgentDynamicPlanner.cs src/services/AICopilot.AiGatewayService/AgentTasks/PlannerToolCatalog.cs src/services/AICopilot.AiGatewayService/AgentTasks/AgentDynamicPlannerResponseParser.cs src/services/AICopilot.AiGatewayService/AgentTasks/AgentTaskRuntime.cs src/services/AICopilot.AiGatewayService/AgentTasks/Runtime/AgentToolExecutionAuditBuilder.cs src/infrastructure/AICopilot.Dapper/Security/AstSqlGuardrail.cs src/services/AICopilot.DataAnalysisService/Plugins/DataAnalysisToolResultFormatter.cs src/services/AICopilot.AiGatewayService/Workflows/Executors/SemanticAnalysisRunner.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs src/tests/AICopilot.BackendTests/ToolRegistryGovernanceTests.cs src/tests/AICopilot.BackendTests/DynamicPlannerContractTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \\t]+$" src/infrastructure/AICopilot.Infrastructure/CloudRead/CloudAiReadHttpTransport.cs src/services/AICopilot.RagService/Documents/DocumentIndexingService.cs src/infrastructure/AICopilot.Infrastructure/Mcp/McpServerBootstrap.cs src/infrastructure/AICopilot.Infrastructure/Mcp/McpRuntimeRegistrySynchronizer.cs src/infrastructure/AICopilot.Infrastructure/Mcp/McpServerManager.cs src/infrastructure/AICopilot.EntityFrameworkCore/Outbox/OutboxDispatcher.cs src/infrastructure/AICopilot.Infrastructure/AiGateway/LanguageModelConnectivityTester.cs src/infrastructure/AICopilot.Infrastructure/AiGateway/PostgreSqlSessionExecutionLock.cs src/services/AICopilot.Services.CrossCutting/Sql/SqlAllowlistColumnInspector.cs src/services/AICopilot.DataAnalysisService/Semantics/SemanticQueryPlanner.cs src/services/AICopilot.AiGatewayService/Tools/ToolInputSchemaValidator.cs src/services/AICopilot.AiGatewayService/AgentTasks/AgentDynamicPlanner.cs src/services/AICopilot.AiGatewayService/AgentTasks/PlannerToolCatalog.cs src/services/AICopilot.AiGatewayService/AgentTasks/AgentDynamicPlannerResponseParser.cs src/services/AICopilot.AiGatewayService/AgentTasks/AgentTaskRuntime.cs src/services/AICopilot.AiGatewayService/AgentTasks/Runtime/AgentToolExecutionAuditBuilder.cs src/infrastructure/AICopilot.Dapper/Security/AstSqlGuardrail.cs src/services/AICopilot.DataAnalysisService/Plugins/DataAnalysisToolResultFormatter.cs src/services/AICopilot.AiGatewayService/Workflows/Executors/SemanticAnalysisRunner.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs src/tests/AICopilot.BackendTests/ToolRegistryGovernanceTests.cs src/tests/AICopilot.BackendTests/DynamicPlannerContractTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
- 验证结果：`SecurityHardeningTests` 72 条通过；`DynamicPlannerContractTests|ToolRegistryGovernanceTests|ModelSecretRuntimeBoundaryTests` 79 条通过；`CloudReadOnlyTextToSqlFallbackRunnerTests|SemanticQueryPlannerTests|TextToSqlReadOnlyTests` 54 条通过；`RagMcpAuditCommandTests|ModelApiKeyProtectionTests|PostFixClosureRegressionTests` 12 条通过；`CloudAiRead|McpRuntime|RagIndexing|Outbox|LanguageModel` 后端筛选 84 条通过；Architecture 定向筛选 17 条通过。生产源码 raw exception logging 多行检索无命中；直接返回/保存 `ex.Message` 风险形态检索无命中。未运行完整 `AICopilot.BackendTests`、完整 ArchitectureTests、前端测试、Docker 构建或真实服务器发布，原因是本批只收口 AI-SEC-016 后端日志/错误边界，不改前端、镜像、部署或真实服务器。

## 2026-07-06 AI provider/workflow/worker 日志脱敏

- 改动范围：AICopilot `src/infrastructure/AICopilot.AiRuntime/AgentRuntimeFactory.cs`、`src/services/AICopilot.AiGatewayService/AgentTasks/AgentTaskRunQueueWorker.cs`、`src/services/AICopilot.AiGatewayService/Workflows/AgentWorkflowPipeline.cs`、`src/services/AICopilot.AiGatewayService/Skills/AgentSkillRouterAutoSelector.cs`、`src/services/AICopilot.AiGatewayService/Workflows/Executors/IntentRoutingExecutor.cs`、`KnowledgeRetrievalExecutor.cs`、`DataAnalysisWidgetEmitter.cs`、`FreeFormDbaAnalysisRunner.cs`、`SemanticAnalysisRunner.cs`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、Cloud 写接口、真实服务器 `.env`、部署脚本或数据库。
- 改动原因：继续执行 `AI-SEC-016`。Dapper/DataAnalysis SQL 执行链已脱敏，但模型 provider fallback、endpoint pool fallback、Agent workflow branch fallback、Skill router、intent routing、knowledge retrieval、widget emit、freeform DBA、semantic analysis 和 run queue worker 仍存在 `logger.Log*(ex, ...)` 或用户文案拼接 `ex.Message` 的路径。模型 provider、Cloud AiRead、workflow 或 worker 异常可能携带 endpoint、prompt、token、SQL 或内部 provider message，不应进入日志或用户可见提示。
- 影响面：各 fallback/错误路径的业务行为保持不变：模型 provider 失败仍按 fallback policy 尝试下一个 provider，endpoint pool 不可用仍回退到模型配置，Skill router 失败仍返回无自动选择，intent routing 失败仍落 General.Chat，knowledge 失败仍返回空上下文，widget 构建失败仍不阻断主回答，freeform/semantic 查询失败仍走安全提示或 fallback，run queue worker 仍把队列项标为失败。变化只在日志和 worker-level failure message：日志改为 `ErrorType` / `FailureCode` + `OriginalMessage=hidden_by_security_policy`，不再附带原始 exception；Cloud AiRead `MissingRequiredParameter` 用户文案不再拼接 `ex.Message`；worker 兜底失败文案固定为安全文本。
- 计划收口：`docs/AI架构治理清单.md` 已补充 `AI-SEC-016` provider/workflow/worker 阶段证据；状态仍保持 Partial。当前全仓还存在 CloudAiRead transport、RAG indexing、MCP runtime、outbox、model connectivity tester、JSON/parser 用户错误和工具执行失败摘要等日志/错误面，需要继续专项审计，不能提前关单。
- 规则提炼：无新增长期规则，原因是本批继续落实既有“SQL/prompt/token/endpoint/连接串不得进日志、审计、运行详情或用户可见错误”的安全口径；该口径已存在于 `AGENTS.md`、`资料/AICopilot业务规则.md` 和 `docs/AI架构治理清单.md`。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~AgentRunQueueProductionOpsTests|FullyQualifiedName~SemanticAnalysisRunnerTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~ClaudeFollowupClosureTests|FullyQualifiedName~IntentRoutingFallbackClassifierTests|FullyQualifiedName~ToolRegistryGovernanceTests.AgentSkillRouterAutoSelector" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~AgentTaskRunQueueWorker_ShouldStayBehindCoordinator" --no-restore --logger "console;verbosity=minimal"`
  - `rg -n "logger\\.Log(Error|Warning|Debug)\\(\\s*ex|logger\\.Log(Error|Warning|Debug)\\([^\\n]*ex," src/infrastructure/AICopilot.AiRuntime src/services/AICopilot.AiGatewayService/AgentTasks/AgentTaskRunQueueWorker.cs src/services/AICopilot.AiGatewayService/Workflows src/services/AICopilot.AiGatewayService/Skills/AgentSkillRouterAutoSelector.cs -g "!**/bin/**" -g "!**/obj/**"`
  - `rg -n "logger\\.Log(Error|Warning|Debug)\\(\\s*ex|logger\\.Log(Error|Warning|Debug)\\([^\\n]*ex," src -g "!**/bin/**" -g "!**/obj/**"`
  - `rg -n "OriginalMessage=hidden_by_security_policy|FailureCode=\\{FailureCode\\}|Agent task run worker failed before runtime completed|缺少必要条件：\\{ex\\.Message\\}" src/infrastructure/AICopilot.AiRuntime src/services/AICopilot.AiGatewayService src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs -g "!**/bin/**" -g "!**/obj/**"`
  - `git diff --check -- src/infrastructure/AICopilot.AiRuntime/AgentRuntimeFactory.cs src/services/AICopilot.AiGatewayService/AgentTasks/AgentTaskRunQueueWorker.cs src/services/AICopilot.AiGatewayService/Workflows/AgentWorkflowPipeline.cs src/services/AICopilot.AiGatewayService/Skills/AgentSkillRouterAutoSelector.cs src/services/AICopilot.AiGatewayService/Workflows/Executors/IntentRoutingExecutor.cs src/services/AICopilot.AiGatewayService/Workflows/Executors/KnowledgeRetrievalExecutor.cs src/services/AICopilot.AiGatewayService/Workflows/Executors/DataAnalysisWidgetEmitter.cs src/services/AICopilot.AiGatewayService/Workflows/Executors/FreeFormDbaAnalysisRunner.cs src/services/AICopilot.AiGatewayService/Workflows/Executors/SemanticAnalysisRunner.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \\t]+$" src/infrastructure/AICopilot.AiRuntime/AgentRuntimeFactory.cs src/services/AICopilot.AiGatewayService/AgentTasks/AgentTaskRunQueueWorker.cs src/services/AICopilot.AiGatewayService/Workflows/AgentWorkflowPipeline.cs src/services/AICopilot.AiGatewayService/Skills/AgentSkillRouterAutoSelector.cs src/services/AICopilot.AiGatewayService/Workflows/Executors/IntentRoutingExecutor.cs src/services/AICopilot.AiGatewayService/Workflows/Executors/KnowledgeRetrievalExecutor.cs src/services/AICopilot.AiGatewayService/Workflows/Executors/DataAnalysisWidgetEmitter.cs src/services/AICopilot.AiGatewayService/Workflows/Executors/FreeFormDbaAnalysisRunner.cs src/services/AICopilot.AiGatewayService/Workflows/Executors/SemanticAnalysisRunner.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
- 验证结果：`SecurityHardeningTests` 70 条通过；`AgentRunQueueProductionOpsTests|SemanticAnalysisRunnerTests` 17 条通过；`ClaudeFollowupClosureTests|IntentRoutingFallbackClassifierTests|ToolRegistryGovernanceTests.AgentSkillRouterAutoSelector` 20 条通过；`AgentTaskRunQueueWorker_ShouldStayBehindCoordinator` 1 条通过；目标 provider/workflow/worker 范围 raw exception logging 负向检索无命中；全仓 raw exception logging 检索仍命中 CloudAiRead transport、RAG indexing、MCP runtime、outbox、model connectivity tester 等剩余专项项；脱敏标记、failure code 和固定 worker failure message 可检索，Cloud AiRead `MissingRequiredParameter` raw `ex.Message` 用户文案无命中。未运行完整 `AICopilot.BackendTests`、完整 ArchitectureTests、前端测试、Docker 构建或真实服务器发布，原因是本批只收口 provider/workflow/worker 日志脱敏；`AI-SEC-016` 仍有上述全链路日志专项未完成。

## 2026-07-06 AI Dapper/DataAnalysis SQL 日志与工具结果脱敏

- 改动范围：AICopilot `src/infrastructure/AICopilot.Dapper/DapperDatabaseConnector.cs`、`src/services/AICopilot.DataAnalysisService/Plugins/DataAnalysisPlugin.cs`、`src/services/AICopilot.DataAnalysisService/Plugins/DataAnalysisToolResultFormatter.cs`、`src/tests/AICopilot.BackendTests/TextToSqlReadOnlyTests.cs`、`src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、Cloud 写接口、真实服务器 `.env`、部署脚本或数据库。
- 改动原因：继续执行 `AI-SEC-016`。HttpApi catch-all 已脱敏，但 Dapper SQL guard reject / execution failure 仍会把 raw guard reason 或原始 `Exception` 对象交给 logger；DataAnalysis tool facade 会记录 `ex` 或把 `ex.Message` 原样返回给工具结果。SQL parser/provider 异常可能携带 SQL 片段、Host、Password、token、endpoint 或连接串，不应进入日志或用户可见结果。
- 影响面：`DapperDatabaseConnector` 现在在 guard reject 和执行失败时只记录 `SqlLength`、`SqlSha256`、`ReasonCode`、`ErrorType` 和 `OriginalMessage=hidden_by_security_policy`，不把原始异常对象交给 logger；内部抛出的 guard exception 语义保留，供现有分类/调用链继续判断。`DataAnalysisPlugin` 的表名、表结构和 SQL 执行失败日志不再记录原始异常对象，SQL 拒绝路径只记录 reason code；`DataAnalysisToolResultFormatter` 仅透出已知安全配置错误，SQL 拒绝原因映射为固定安全文案，避免 raw SQL、连接串或 provider message 返回给用户。Cloud 只读边界、SQL guard、只读事务、audit hash-only 和查询执行行为不变。
- 计划收口：`docs/AI架构治理清单.md` 已把 `AI-SEC-016` 阶段证据更新为 HttpApi catch-all、Dapper SQL 执行链和 DataAnalysis tool facade 已脱敏；状态仍保持 Partial，因为 provider、workflow、worker 等更广业务日志还需要继续做 SQL/prompt/token/endpoint 全链路专项审计，不能提前关单。
- 规则提炼：无新增长期规则，原因是本批落实既有“SQL/prompt/token/endpoint/连接串不得进日志、审计、运行详情或用户可见错误”的安全口径；该口径已存在于 `AGENTS.md`、`资料/AICopilot业务规则.md` 和 `docs/AI架构治理清单.md`。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~TextToSqlReadOnlyTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~DataAnalysisReadOnlyGuardrailTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~TextToSqlReadOnlyTests|FullyQualifiedName~SecurityHardeningTests|FullyQualifiedName~DataAnalysisReadOnlyGuardrailTests" --no-restore --logger "console;verbosity=minimal"`
  - `rg -n "logger\\.LogError\\(\\s*ex|logger\\.LogWarning\\(\\s*ex|SQL 执行被拦截: \\{Message\\}|原因: \\{ex\\.Message\\}|SQL security guard rejected query against \\{DatabaseName\\}: \\{Reason\\}" src/infrastructure/AICopilot.Dapper src/services/AICopilot.DataAnalysisService -g "!**/bin/**" -g "!**/obj/**"`
  - `rg -n "OriginalMessage=hidden_by_security_policy|ClassifySqlRejection|ClassifyGuardrailFailure|AI-SEC-016 阶段证据" src/infrastructure/AICopilot.Dapper src/services/AICopilot.DataAnalysisService src/tests/AICopilot.BackendTests docs/AI架构治理清单.md`
  - `git diff --check -- src/infrastructure/AICopilot.Dapper/DapperDatabaseConnector.cs src/services/AICopilot.DataAnalysisService/Plugins/DataAnalysisPlugin.cs src/services/AICopilot.DataAnalysisService/Plugins/DataAnalysisToolResultFormatter.cs src/tests/AICopilot.BackendTests/TextToSqlReadOnlyTests.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \\t]+$" src/infrastructure/AICopilot.Dapper/DapperDatabaseConnector.cs src/services/AICopilot.DataAnalysisService/Plugins/DataAnalysisPlugin.cs src/services/AICopilot.DataAnalysisService/Plugins/DataAnalysisToolResultFormatter.cs src/tests/AICopilot.BackendTests/TextToSqlReadOnlyTests.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
- 验证结果：`TextToSqlReadOnlyTests` 8 条通过；`SecurityHardeningTests` 69 条通过；`DataAnalysisReadOnlyGuardrailTests` 3 条通过；三组相关测试串行合并复跑 80 条通过；Dapper/DataAnalysis 目标目录 raw exception logging / raw `ex.Message` SQL 拒绝模式检索无命中；脱敏标记、分类函数和 `AI-SEC-016` 阶段证据可检索；目标 tracked 文件 `git diff --check` 通过；新增计划文档和目标 tracked 文件尾随空白检索无命中。未运行完整 `AICopilot.BackendTests`、ArchitectureTests、前端测试、Docker 构建或真实服务器发布，原因是本批只收口 Dapper/DataAnalysis SQL 日志和工具结果脱敏；`AI-SEC-016` 仍有 provider/workflow/worker 全链路日志专项未完成。

## 2026-07-06 AI 完整修复计划 HTTP-only 口径重生成

- 改动范围：AICopilot `docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、AICopilot 运行代码、部署脚本、真实服务器 `.env`、数据库、Cloud 业务数据或 Cloud 写接口。
- 改动原因：用户明确要求重新生成 AI 完整修复计划，并修正上一版把 HTTPS/TLS/HSTS/443/certificate 纳入第一轮红线的问题。当前生产约束是内网 HTTP 部署，证书方案未批准，AI 端计划必须把 HTTP-only 写成红线，同时继续保留除 HSTS 外的安全头、端口收敛、同源代理、CORS 白名单、强 secret、非 root 容器、敏感信息脱敏、Cloud 只读边界和部署 preflight。
- 影响面：`docs/AI架构治理清单.md` 标题改为“AI 完整修复计划（HTTP-only 修正版）”，继续作为 AI 端治理清单和执行计划入口；补充当前剩余未完全关单项 `AI-SEC-010`、`AI-SEC-012`、`AI-SEC-016`，避免把 runner/OIDC/Vault、真实 `encv1` 迁移验收和全链路业务日志脱敏伪造成已完成；新增“源码归属和执行面”，把后续执行拆到 deploy、HttpApi、密钥运行时、CloudRead、DataAnalysis/Text-to-SQL、Agent workflow/MCP/Tool/Approval、前端和测试目录。
- 计划收口：HTTP-only 红线仍保持不变：不得把 HTTPS redirection、HSTS、nginx 443 listener、证书申请/续期或 OIDC HTTPS metadata 强制校验列为当前修复门槛；不做 CloudPlatform 或 EdgeClient 改动；外部依赖只能记录，不能在 AI 计划里伪造成完成。
- 规则提炼：无新增长期规则，原因是 HTTP-only 部署红线已经沉淀在 `AGENTS.md`、`资料/AICopilot业务规则.md`、部署指南和 `deploy/enterprise-ai/README.md`；本批只是把执行计划标题、剩余项和源码归属补齐，保证后续按源码和状态推进。
- 验证命令：
  - `rg -n "AI 完整修复计划|HTTP-only|不得把 HTTPS|证书来源|AI-SEC-010|AI-SEC-012|AI-SEC-016|源码归属|src/infrastructure/AICopilot.Dapper|不做 HTTPS" docs/AI架构治理清单.md AGENTS.md 资料/AICopilot业务规则.md`
  - `rg -n "^# |^## 0|^## 1|^## 2|^## 2\\.1|^## 3|^## 10|^## 11|^## 12" docs/AI架构治理清单.md`
  - `git diff --check -- docs/改动复盘与规则沉淀.md`
  - `rg -n "[ \\t]+$" docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
- 验证结果：HTTP-only 红线、禁止 HTTPS/HSTS/443/certificate 夹带、三个剩余未完成项、源码归属和不纳入项均可检索；章节结构可检索；滚动复盘 `git diff --check` 通过；新增计划文档和滚动复盘尾随空白检索无命中。未运行 build/test，原因是本轮只重生成 AICopilot 计划/治理文档，没有改运行代码。

## 2026-07-06 AI 前端登录态与 Cloud OIDC 状态失败可见化

- 改动范围：AICopilot Web `src/stores/authStore.ts`、`tests/unit/authStore.spec.ts`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、Cloud 写接口、后端 API、真实服务器 `.env` 或部署脚本。
- 改动原因：继续执行 AI-SEC-017。路由守卫会在已登录状态下调用 `authStore.ensureCurrentUser()` 解析 `/identity/me`；该请求失败时原先只让路由守卫 `console.error` 并跳转 `/login`，`authStore` 本身不写错误原因、不清理坏 token，用户可能只看到普通登录页，甚至在保留坏 token 时反复触发 profile 解析。登录页加载 Cloud OIDC 状态失败时也只写控制台并把按钮显示为“Cloud 登录未启用”，容易把状态接口故障误导成未配置。
- 影响面：`ensureCurrentUser()` 失败时现在会通过 `clearAuth()` 清理本地 token、当前用户和当前聊天会话，并写入登录页既有 `authStore.errorMessage`。401/403 会使用后端 ProblemDetails code 映射到“账号禁用 / 用户不存在 / 登录态失效”等安全友好文案；非授权类异常使用通用“无法获取当前用户信息，请重新登录。”，不会暴露 raw endpoint、provider message 或内部细节。`ensureCloudOidcStatus()` 失败时写入“无法获取 Cloud 登录状态，请稍后重试或使用本地 AI 账号登录。”，仍允许本地账号登录；用户点击 Cloud 登录时不会把该状态接口失败覆盖成“Cloud 登录尚未配置”。登录成功、Cloud OIDC 完成、初始化状态检查和全局 401 unauthorized handler 行为不变。
- 计划收口：`docs/AI架构治理清单.md` 已把 AI-SEC-017 标记为 Done，并补充关单证据。当前源码下，用户操作或状态加载失败已进入会话、页面、登录或 dialog 错误栏；纯解析 fallback 只做安全降级展示，chunk reload guard 只处理 stale bundle 自动刷新；新增 `frontendErrorHandling.spec.ts` 固化前端源码不得再出现裸 `catch {}`。
- 规则提炼：无新增长期规则，原因是本批落实既有前端规则“用户操作/认证状态失败必须有可见反馈，后端错误 code 优先映射为安全文案”；规则已存在于 `src/vues/AICopilot.Web/AGENTS.md`、`AGENTS.md` 和 `资料/AICopilot业务规则.md`。
- 验证命令：
  - `npm run test:unit -- authStore.spec.ts`（工作目录：`src/vues/AICopilot.Web`）
  - `npm run test:unit -- frontendErrorHandling.spec.ts`（工作目录：`src/vues/AICopilot.Web`）
  - `npm run test:unit`（工作目录：`src/vues/AICopilot.Web`）
  - `npm run type-check`（工作目录：`src/vues/AICopilot.Web`）
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore --logger "console;verbosity=minimal"`
  - `git diff --check -- docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md src/vues/AICopilot.Web/src/stores/authStore.ts src/vues/AICopilot.Web/src/views/ConfigView.vue src/vues/AICopilot.Web/tests/unit/authStore.spec.ts src/vues/AICopilot.Web/tests/unit/frontendErrorHandling.spec.ts`
  - `rg -n "[ \\t]+$" docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md src/vues/AICopilot.Web/src/stores/authStore.ts src/vues/AICopilot.Web/src/views/ConfigView.vue src/vues/AICopilot.Web/tests/unit/authStore.spec.ts src/vues/AICopilot.Web/tests/unit/frontendErrorHandling.spec.ts`
  - `rg -n "catch\\s*\\{" src/vues/AICopilot.Web/src src/vues/AICopilot.Web/tests -g "!**/node_modules/**"`
- 验证结果：`authStore.spec.ts` 4 条通过；`frontendErrorHandling.spec.ts` 1 条通过；完整前端单元测试 28 个文件、129 条测试通过；前端 `type-check` 通过；后端 `SecurityHardeningTests` 68 条通过；`git diff --check` 通过；尾随空白检索无命中；前端源码/测试 `catch {}` 检索无命中。未运行前端 smoke、build、Docker 构建或真实服务器发布，原因是本批只改登录态和 Cloud OIDC 状态解析失败的错误流转、前端裸 catch 门禁和单元测试，没有改后端接口、路由结构或部署产物。

## 2026-07-06 AI 前端目录、任务、产物与配置失败可见化

- 改动范围：AICopilot Web `src/stores/agentCatalogStore.ts`、`src/stores/agentTaskStore.ts`、`src/stores/artifactWorkspaceStore.ts`、`src/stores/chatStore.ts`、`src/views/ConfigView.vue`、`tests/unit/agentStateStores.spec.ts`、`tests/unit/chatStoreSkills.spec.ts`、`tests/unit/configViewUsability.spec.ts`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、Cloud 写接口、后端 API、真实服务器 `.env` 或部署脚本。
- 改动原因：继续执行 AI-SEC-017。`agentCatalogStore` 和 `agentTaskStore` 的目录、工具、知识库、任务时间线、审批和审计摘要加载失败时原先只写 `console.error` 并清空数据；产物预览/图表预览失败只清空预览，产物下载和附件上传失败会抛给组件但不进入统一错误栏；配置页本地 Skill、工具目录和 Cloud 只读状态刷新失败可能只形成未处理 Promise 或控制台日志。聊天页/配置页无法区分“暂无数据”和“加载失败”，属于用户操作/状态加载失败没有可见反馈的缺口。
- 影响面：目录、任务和产物 store 不新增独立错误状态，只在加载或操作失败时把安全友好文案回调给 `chatStore`；`chatStore` 按请求归属会话写入既有 `chatErrorStore`，由 `ChatWindow` 现有 `role="alert"` 告警条展示。配置页本地刷新失败写入既有 `store.errorMessage`，由页面 `error-banner` 展示。异步请求失败时不会把错误栏切回旧会话。Skill、插件能力、知识库、任务时间线、审批记录、审计摘要、产物工作区、产物预览、图表预览、产物下载、附件上传和配置页 agent settings 刷新失败时会保留安全兜底文案；成功路径、选择状态、会话状态和 Cloud 只读边界不变。
- 计划收口：`docs/AI架构治理清单.md` 已更新 AI-SEC-017 说明：agent catalog / task timeline / artifact preview / artifact download / attachment upload / config agent settings refresh 等加载和操作失败已进入当前会话或页面错误栏。AI-SEC-017 仍保持 Partial，因为 toast/用户可见错误全量审计和剩余页面级 catch 路径还需要继续逐项核对，不能提前关单。
- 规则提炼：无新增长期规则，原因是本批落实既有前端规则“错误状态统一走 `chatErrorStore`，用户操作失败要有可见反馈，新增错误路径不能只写控制台”；规则已存在于 `src/vues/AICopilot.Web/AGENTS.md` 和 `docs/AI架构治理清单.md`。
- 验证命令：
  - `npm run test:unit -- agentStateStores.spec.ts`（工作目录：`src/vues/AICopilot.Web`）
  - `npm run test:unit -- chatStoreSkills.spec.ts agentStateStores.spec.ts`（工作目录：`src/vues/AICopilot.Web`）
  - `npm run test:unit -- configViewUsability.spec.ts`（工作目录：`src/vues/AICopilot.Web`）
  - `npm run type-check`（工作目录：`src/vues/AICopilot.Web`）
  - `npm run test:unit`（工作目录：`src/vues/AICopilot.Web`）
  - `git diff --check -- docs/改动复盘与规则沉淀.md src/vues/AICopilot.Web/src/stores/agentCatalogStore.ts src/vues/AICopilot.Web/src/stores/agentTaskStore.ts src/vues/AICopilot.Web/src/stores/artifactWorkspaceStore.ts src/vues/AICopilot.Web/src/stores/chatStore.ts src/vues/AICopilot.Web/src/views/ConfigView.vue src/vues/AICopilot.Web/tests/unit/agentStateStores.spec.ts src/vues/AICopilot.Web/tests/unit/chatStoreSkills.spec.ts src/vues/AICopilot.Web/tests/unit/configViewUsability.spec.ts`
  - `rg -n "[ \\t]+$" docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md src/vues/AICopilot.Web/src/stores/agentCatalogStore.ts src/vues/AICopilot.Web/src/stores/agentTaskStore.ts src/vues/AICopilot.Web/src/stores/artifactWorkspaceStore.ts src/vues/AICopilot.Web/src/stores/chatStore.ts src/vues/AICopilot.Web/src/views/ConfigView.vue src/vues/AICopilot.Web/tests/unit/agentStateStores.spec.ts src/vues/AICopilot.Web/tests/unit/chatStoreSkills.spec.ts src/vues/AICopilot.Web/tests/unit/configViewUsability.spec.ts`
- 验证结果：`agentStateStores.spec.ts` 6 条通过；`chatStoreSkills.spec.ts` + `agentStateStores.spec.ts` 共 22 条通过；`configViewUsability.spec.ts` 5 条通过；前端 `type-check` 通过；完整前端单元测试 26 个文件、124 条测试通过。未运行前端 smoke、build、Docker 构建或真实服务器发布，原因是本批只改 Pinia store/配置页错误流转和单元测试，没有改 DOM 结构、路由、后端接口或部署产物。

## 2026-07-06 AI HttpApi catch-all 日志脱敏

- 改动范围：AICopilot HttpApi `UseCaseExceptionHandler`、后端 `SecurityHardeningTests`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、Cloud 写接口、前端、真实服务器 `.env` 或部署脚本。
- 改动原因：执行 AI-SEC-016 的后端日志脱敏收口。catch-all 响应已返回安全 `internal_server_error` 和 traceId，但日志仍使用 `logger.LogError(exception, ...)`，.NET 默认会把异常 message/stack 写入日志；如果异常里带 provider endpoint、token、SQL 或连接串，就会进入服务端日志。
- 影响面：`UseCaseExceptionHandler` catch-all 现在不再把原始 `Exception` 对象传给 logger，只记录 `TraceIdentifier`、异常类型、inner 异常类型和固定 `hidden_by_security_policy` 标记。HTTP 响应仍保持 `internal_server_error`、安全 detail 和 traceId，不改变前端错误契约。
- 计划收口：`docs/AI架构治理清单.md` 已更新 AI-SEC-016 说明：catch-all 响应和日志已脱敏；AI-SEC-016 仍保持 Partial，因为 SQL/prompt/token/endpoint 全链路业务日志脱敏还需要独立专项审计。
- 规则提炼：无新增长期规则，原因是本批落实既有“后端未知异常不得泄露 SQL、prompt、token、endpoint、连接串或内部 provider 消息”的安全口径；该口径已存在于 `docs/AI架构治理清单.md`、`AGENTS.md` 和 `资料/AICopilot业务规则.md`。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore --logger "console;verbosity=minimal"`
  - `rg -n "logger\\.LogError\\(\\s*exception|hidden_by_security_policy|UseCaseExceptionHandler_ShouldNotLogRawExceptionMessageForCatchAll|internal_server_error|AI-SEC-016" src/hosts/AICopilot.HttpApi/Infrastructure/UseCaseExceptionHandler.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `git diff --check -- src/hosts/AICopilot.HttpApi/Infrastructure/UseCaseExceptionHandler.cs src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
- 验证结果：SecurityHardeningTests 68 条通过；静态检索确认 `UseCaseExceptionHandler` 不再匹配 `logger.LogError(exception, ...)`，命中 `hidden_by_security_policy` 和 catch-all 日志脱敏测试；目标文件 `git diff --check` 通过。未运行完整 BackendTests、完整日志链路审计或真实服务器日志验证，原因是本批只收口 HttpApi catch-all 日志脱敏，AI-SEC-016 仍保留全链路业务日志脱敏专项。

## 2026-07-06 AI 前端工具目录空响应兜底

- 改动范围：AICopilot Web `src/stores/agentCatalogStore.ts`、`tests/unit/chatStoreSkills.spec.ts`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、Cloud 写接口、后端 API、真实服务器 `.env` 或部署脚本。
- 改动原因：执行 AI-SEC-017 的前端错误/空响应兜底。AI-SEC-018 定向 smoke 暴露 `agentCatalogStore.loadPluginTools()` 在工具目录响应缺 `tools` 字段时直接执行 `catalog.tools.map`，导致控制台出现 `Cannot read properties of undefined (reading 'map')`。这类后端空响应或 mock 漂移不应让前端抛运行时异常。
- 影响面：`loadPluginTools()` 现在把非数组 `catalog.tools` 归一为空数组，再过滤已选工具；缺字段时不会抛异常、不会污染控制台，也不会保留已选但已不可用的工具。真实有效工具目录行为不变。
- 计划收口：`docs/AI架构治理清单.md` 已更新 AI-SEC-017 说明：agent tool catalog 缺 `tools` 已归一为空数组；AI-SEC-017 仍保持 Partial，因为 toast/用户可见错误统一仍未全量完成。
- 规则提炼：无新增长期规则，原因是本批落实既有前端规则“新增错误路径展示后端诊断且解析失败有安全兜底，不丢会话状态”；规则已存在于 `src/vues/AICopilot.Web/AGENTS.md` 和 `资料/AICopilot业务规则.md`。
- 验证命令：
  - `npm run test:unit -- chatStoreSkills.spec.ts`（工作目录：`src/vues/AICopilot.Web`）
  - `npm run type-check`（工作目录：`src/vues/AICopilot.Web`）
  - `npm run test:smoke -- --project=desktop -g "chat stream renders widgets and approval card"`（工作目录：`src/vues/AICopilot.Web`）
  - `git diff --check -- docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md src/vues/AICopilot.Web/src/stores/agentCatalogStore.ts src/vues/AICopilot.Web/tests/unit/chatStoreSkills.spec.ts`
- 验证结果：`chatStoreSkills.spec.ts` 13 条通过；前端 `type-check` 通过；定向 smoke `chat stream renders widgets and approval card` desktop 1 条通过，且不再出现缺 `tools` 字段导致的 `.map` 控制台错误；目标文件 `git diff --check` 通过。未运行完整前端单元测试、完整 smoke、build 或真实服务器发布，原因是本批只修 agent tool catalog 空响应兜底。

## 2026-07-06 AI 运行详情安全摘要收口

- 改动范围：AICopilot Web `src/protocol/runtimeDetails.ts`、`tests/unit/runtimeDetails.spec.ts`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、Cloud 写接口、后端执行链路、真实服务器 `.env` 或部署脚本。
- 改动原因：执行 AI-SEC-018。现有运行详情已经通过折叠 `<details>` 和工具参数/结果摘要避免展示 raw tool payload，但 `ChatRunStatus.summary` / `error.message` 仍可能把上游 raw endpoint、SQL、token、sourceName、tableName 等错误文本带入运行详情展开区域，需要把状态摘要也纳入脱敏规则。
- 影响面：`buildRuntimeDetails` 现在对状态 summary/error message 做敏感文本检测；命中 SQL、连接串、password、secret、token、authorization、endpoint、sourceName、tableName、databaseName、schema/view、URL 或 `/internal/` 路径时，失败状态显示“运行失败，原始错误未在详情中展开”，非失败状态显示“运行状态已记录，原始详情未在详情中展开”。安全的正常摘要、模型 badge、查询次数、返回行数、Widget 类型和工具安全参数摘要保持不变。
- 计划收口：`docs/AI架构治理清单.md` 已把 AI-SEC-018 标记为 Done，并补充关单证据：运行详情默认折叠、smoke 断言默认隐藏、工具参数/结果/状态错误原文和内部字段均只输出安全摘要。
- 规则提炼：无新增长期规则，原因是本批落实既有 `AGENTS.md`、`资料/AICopilot业务规则.md` 和 `src/vues/AICopilot.Web/AGENTS.md` 中“运行详情默认折叠且不得展开 SQL、连接串、token、endpoint、内部字段、原始结果行或未脱敏错误原文”的规则。
- 验证命令：
  - `npm run test:unit -- runtimeDetails.spec.ts`（工作目录：`src/vues/AICopilot.Web`）
  - `npm run type-check`（工作目录：`src/vues/AICopilot.Web`）
  - `rg -n "AI-SEC-018|Runtime Details Folding|运行详情|原始错误未在详情中展开|sourceName|tableName|endpoint|PreviousSqlForRepair" docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md AGENTS.md 资料/AICopilot业务规则.md src/vues/AICopilot.Web/AGENTS.md src/vues/AICopilot.Web/src/protocol/runtimeDetails.ts src/vues/AICopilot.Web/tests/unit/runtimeDetails.spec.ts src/vues/AICopilot.Web/tests/smoke/acceptance.spec.ts`
  - `npm run test:smoke -- --project=desktop -g "chat stream renders widgets and approval card"`（工作目录：`src/vues/AICopilot.Web`）
  - `git diff --check -- docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md src/vues/AICopilot.Web/src/protocol/runtimeDetails.ts src/vues/AICopilot.Web/tests/unit/runtimeDetails.spec.ts`
- 验证结果：`runtimeDetails.spec.ts` 5 条通过；前端 `type-check` 通过；定向 smoke `chat stream renders widgets and approval card` desktop 1 条通过，覆盖运行详情默认折叠和展开后结构化展示可见；静态检索命中 AI-SEC-018、Runtime Details Folding、运行详情、状态错误脱敏文案、sourceName/tableName/endpoint 防泄漏断言和既有规则；目标文件 `git diff --check` 通过。smoke 期间仍出现既有 `agentCatalogStore` 缺失 `tools` 字段时 `.map` 读取告警，属于 AI-SEC-017 前端错误/空响应兜底后续项，不影响本批运行详情关单。

## 2026-07-06 AI Cloud 只读与 Text-to-SQL 治理清单关单

- 改动范围：AICopilot `CloudReadonlySimulationTests`、`CloudReadOnlyTextToSqlFallbackRunnerTests`、`docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、Cloud 写接口、生产数据库、真实服务器 `.env`、部署脚本或运行代码。
- 改动原因：继续执行 AI 完整修复计划，把治理清单第五批和第六批从“计划项”收口到源码证据和定向测试证据。重点补齐两个可回归的边界：CloudReadonly Real 模式下 Cloud AiRead 失败不得降级 Simulation；CloudReadOnly Text-to-SQL repair 次数必须默认 3 次、硬上限 5 次。
- 影响面：`RealCloudReadonlyDataProvider` 行为不变，本批新增测试证明 Real provider 只调用 Cloud AiRead，失败时返回 `cloud_ai_read_unavailable`，不会返回 Simulation marker 或模拟数据；`CloudReadOnlyTextToSqlOptions` 行为不变，本批新增测试证明 repair attempts 会按默认值和硬上限 clamp。治理清单把 AI-SEC-019 到 AI-SEC-027 标记为 Done，并逐项写明对应源码/测试证据。
- 计划收口：AI-SEC-019/020/021/022/023 已由 `ArchitectureBoundaryTests`、`CloudReadonlyChatBoundaryTests`、`CloudReadonlySimulationTests`、`CloudAiReadClientTests`、`SemanticAnalysisRunnerTests`、`DeviceLogFollowUpIntentRewriterTests` 覆盖；AI-SEC-024/025/026/027 已由 `CloudReadOnlyTextToSqlFallbackRunnerTests`、`ArchitectureBoundaryTests`、`AiEvalBehaviorGuardrailTests` 和 `PromptGovernanceTests` 覆盖。
- 规则提炼：无新增长期规则，原因是本批把已沉淀的 Cloud 只读、Simulation 不冒充真实数据、Cloud AiRead 高频路径、DeviceLog 追问重查、Text-to-SQL governed schema、SQL/hash-only 审计和最终回答脱敏规则补成测试/清单证据；长期规则已经存在于 `AGENTS.md` 和 `资料/AICopilot业务规则.md`。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~CloudReadonlySimulationTests|FullyQualifiedName~CloudReadOnlyTextToSqlFallbackRunnerTests|FullyQualifiedName~CloudAiReadClientTests|FullyQualifiedName~SemanticAnalysisRunnerTests|FullyQualifiedName~DeviceLogFollowUpIntentRewriterTests|FullyQualifiedName~AiEvalBehaviorGuardrailTests|FullyQualifiedName~PromptGovernanceTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~CloudReadonlyChatBoundaryTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~ArchitectureBoundaryTests" --no-restore --logger "console;verbosity=minimal"`
  - `rg -n "AI-SEC-019|AI-SEC-020|AI-SEC-021|AI-SEC-022|AI-SEC-023|AI-SEC-024|AI-SEC-025|AI-SEC-026|AI-SEC-027|Cloud AiRead|Simulation|PreviousSqlForRepair|SqlHash|不能暴露 SQL" docs/AI架构治理清单.md AGENTS.md 资料/AICopilot业务规则.md src/tests/AICopilot.BackendTests src/tests/AICopilot.ArchitectureTests`
  - `git diff --check -- docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md src/tests/AICopilot.BackendTests/CloudReadonlySimulationTests.cs src/tests/AICopilot.BackendTests/CloudReadOnlyTextToSqlFallbackRunnerTests.cs`
- 验证结果：CloudReadonly/CloudAiRead/SemanticAnalysis/DeviceLog/AiEval/PromptGovernance/Text-to-SQL 组合后端专项 95 条通过；CloudReadonlyChatBoundaryTests 8 条通过；ArchitectureBoundaryTests 76 条通过；静态检索命中 AI-SEC-019 到 AI-SEC-027、Cloud AiRead、Simulation、PreviousSqlForRepair、SqlHash、不能暴露 SQL 等规则和测试证据；目标文件 `git diff --check` 通过。未运行完整 BackendTests、完整 ArchitectureTests、前端 type-check/build、Docker 构建或真实服务器发布，原因是本批只补清单关单证据和两个定向测试，不改运行代码和部署产物。

## 2026-07-06 AI 模型密钥 runtime 边界收口

- 改动范围：AICopilot AiRuntime endpoint pool scheduler、模型密钥 runtime 边界测试、endpoint pool 测试 helper、endpoint pool 相关后端测试、`docs/AI架构治理清单.md`、项目规则、业务规则和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实模型配置、真实环境变量、数据库数据或服务器状态。
- 改动原因：执行 AI-SEC-013/014。OpenAI/Anthropic/Embedding provider 已经通过 `ISecretProtector.Unprotect` 拒绝明文，但 endpoint pool 的 `ApiKey` / `ApiKeyEnvironmentVariable` 覆盖支路原先只透传配置值，没有明确要求配置值也是 `encv2:`，容易让运行时 provider 收到明文或让生产配置在启用 pool 后才失败。
- 影响面：`InMemoryModelEndpointPoolScheduler` 现在依赖 `ISecretProtector.IsProtected` 校验 endpoint pool 覆盖密钥；endpoint `ApiKey` 和 `ApiKeyEnvironmentVariable` 读出的环境变量值都必须是 `encv2:` 密文，否则在选 endpoint 时 fail-fast，错误信息只包含 endpoint id 和配置来源，不包含密钥值。scheduler 不解密、不记录密钥，只把受保护密文交给最终 OpenAI/Anthropic provider 统一解密。模型/Embedding 创建、更新、连接测试、DTO 和审计摘要继续保持脱敏。
- 计划收口：`docs/AI架构治理清单.md` 已把 AI-SEC-013 和 AI-SEC-014 标记为 Done。AI-SEC-012 仍保持 Partial，因为旧 `encv1:` 真实库迁移需要发布窗口和生产数据验收。
- 规则提炼：形成长期规则，已写入 `AGENTS.md` 和 `资料/AICopilot业务规则.md`：模型、Embedding 和 endpoint pool 覆盖 API key 都必须使用 `encv2:` AES-GCM；endpoint pool 的配置值和环境变量值也必须存密文，scheduler 只校验受保护格式，不接受明文、不解密、不记录。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~ModelSecretRuntimeBoundaryTests|FullyQualifiedName~SecretStringEncryptorTests|FullyQualifiedName~ModelApiKeyProtectionTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~ModelEndpointPoolScheduler|FullyQualifiedName~ModelProviderReliabilityTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~RagMcpAuditCommandTests|FullyQualifiedName~ModelSecretContractTests|FullyQualifiedName~Phase25RuntimeSmokeTests.UpdateLanguageModel_WithBlankApiKey_ShouldPreserveExistingSecret" --no-restore --logger "console;verbosity=minimal"`
  - `rg -n "RequireProtectedEndpointApiKey|ApiKeyEnvironmentVariable|secretProtector\\.IsProtected|secretProtector\\.Unprotect|ProtectApiKey|ApiKeyPreview = \"\\*\\*\\*\\*\\*\\*\"|redacted|Stored secret must be encrypted|CipherMode\\.CBC|encv2" src/infrastructure/AICopilot.AiRuntime src/infrastructure/AICopilot.Infrastructure src/infrastructure/AICopilot.Embedding src/services/AICopilot.AiGatewayService src/services/AICopilot.RagService src/tests/AICopilot.BackendTests/ModelSecretRuntimeBoundaryTests.cs src/tests/AICopilot.BackendTests/ModelApiKeyProtectionTests.cs src/tests/AICopilot.BackendTests/EndpointPoolSecretProtector.cs -g '!**/bin/**' -g '!**/obj/**'`
  - `rg -n "sk-|encv2:|dummy-key|ApiKeyPreview|apiKey=.*redacted|provided and redacted|changed and redacted|SecretStringEncryptor\\.Decrypt\\(|CipherMode\\.CBC" src/services src/infrastructure src/hosts src/shared -g '!**/bin/**' -g '!**/obj/**'`
  - `git diff --check -- src/infrastructure/AICopilot.AiRuntime/ModelEndpointPoolScheduler.cs src/tests/AICopilot.BackendTests/ModelSecretRuntimeBoundaryTests.cs src/tests/AICopilot.BackendTests/EndpointPoolSecretProtector.cs src/tests/AICopilot.BackendTests/EnterpriseDataGovernanceP0Tests.cs src/tests/AICopilot.BackendTests/EnterpriseDataGovernanceP1Tests.cs`
- 验证结果：密钥 runtime / AES-GCM / 持久化保护定向测试 13 条通过；endpoint pool / model reliability 定向测试 14 条通过；RAG 审计、模型密钥 DTO 契约和既有语言模型密钥保留测试 7 条通过；源码检索确认 OpenAI/Anthropic/Embedding provider 走 `Unprotect`，endpoint pool 走 `IsProtected`，DTO 只暴露 `ApiKeyPreview="******"`，审计摘要使用 redacted 文案；生产源码中未命中 `CipherMode.CBC`，`dummy-key` 仅保留在 migration seed 常量并会被 `SecretStringEncryptor.Encrypt` 包装；目标文件 `git diff --check` 通过。未运行完整 BackendTests 或真实生产库 `encv1:` 迁移验收，原因是本批只收口 runtime provider 和审计/DTO 密钥边界，旧密文迁移仍需发布窗口验证。

## 2026-07-06 AI CORS 同源代理门禁收口

- 改动范围：AICopilot HttpApi CORS 配置、HttpApi 启动管道、HttpApi appsettings、Web/API 同源代理门禁测试、`docs/AI架构治理清单.md`、项目规则、业务规则和本滚动复盘。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、Cloud OIDC Provider、Cloud 业务数据、生产数据库或前端交互页面。
- 改动原因：执行 AI-SEC-003，补齐 HTTP-only 前提下的 CORS/同源代理红线。上一批已确保 nginx 有 HTTP 兼容安全头，但后端仍缺少显式 CORS 边界；需要把“Web 到 API 走 `/api` 同源反代、后端默认不开放跨源、如需直连只能白名单精确 origin”变成可验证代码和测试。
- 影响面：生产 Web 构建仍默认 `VITE_API_BASE_URL=/api`，nginx 继续通过 `location /api/` 代理到 `${AICOPILOT_HTTPAPI_HTTP}/api/`；HttpApi 新增 `AICopilotExplicitOrigins` CORS policy，默认 `Cors:AllowedOrigins=[]`，即不对浏览器跨源开放；如后续确需浏览器直连后端，只能配置精确 http/https origin，拒绝 `*`、空值、带 path/query/fragment 的 origin 和任意放行。未引入 HTTPS、HSTS、443 或证书流程。
- 计划收口：`docs/AI架构治理清单.md` 已把 AI-SEC-003 标记为 Done。第一批 HTTP-only 部署安全红线中，AI 端可执行项 001/002/003/004/005 均已进入 Done；真实线上 header/curl 验收仍留到发布窗口执行。
- 规则提炼：形成长期规则，已写入 `AGENTS.md` 和 `资料/AICopilot业务规则.md`：AICopilot Web 到 HttpApi 的标准生产路径必须走 nginx 同源 `/api/` 反代；HttpApi CORS 默认不开放跨源；确需浏览器直连后端时只允许精确 origin，禁止 wildcard、通配子域、带 path/query/fragment 的 origin 或运行时任意放行。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore --logger "console;verbosity=minimal"`
  - `rg -n "AllowAnyOrigin|Access-Control-Allow-Origin \"\\*\"|SetIsOriginAllowed\\(_ => true\\)|VITE_API_BASE_URL=http|proxy_pass \\$\\{AICOPILOT_HTTPAPI_HTTP\\}/api/|HttpApiCorsConfiguration|AICopilotExplicitOrigins|Cors:AllowedOrigins" src/hosts/AICopilot.HttpApi src/vues/AICopilot.Web deploy/enterprise-ai docs/AI架构治理清单.md AGENTS.md 资料/AICopilot业务规则.md -g '!**/bin/**' -g '!**/obj/**' -g '!**/node_modules/**'`
  - `git diff --check -- src/hosts/AICopilot.HttpApi/HttpApiCorsConfiguration.cs src/hosts/AICopilot.HttpApi/DependencyInjection.cs src/hosts/AICopilot.HttpApi/Program.cs src/hosts/AICopilot.HttpApi/appsettings.json src/hosts/AICopilot.HttpApi/appsettings.Development.json src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs AGENTS.md 资料/AICopilot业务规则.md docs/AI架构治理清单.md`
- 验证结果：SecurityHardeningTests 67 条通过；源码检索命中 `HttpApiCorsConfiguration`、`AICopilotExplicitOrigins` 和 `Cors:AllowedOrigins`，未命中 `AllowAnyOrigin`、`Access-Control-Allow-Origin "*"`、`SetIsOriginAllowed(_ => true)` 或 `VITE_API_BASE_URL=http`；目标文件 `git diff --check` 通过。未运行完整 BackendTests、前端 build、nginx 容器 smoke 或真实服务器 `curl -I`，原因是本批只收口 CORS/同源代理源码门禁，真实响应头验收需发布窗口和目标内网地址。

## 2026-07-06 AI 部署 secret/root/preflight 治理收口

- 改动范围：AICopilot `deploy/enterprise-ai/.env.example`、`docker-compose.yaml`、`deploy-release.sh`、`local-release.sh`、`build-and-push.sh`、`mirror-base-images.sh`、模型 provider smoke 脚本、灾备 workflow、部署 README、项目部署维护指南、`docs/AI架构治理清单.md`、`docs/frontend-integration-contract-package-2026-05-17.md`、项目规则、业务规则、`SecurityHardeningTests` 和 Architecture 边界测试。未修改 CloudPlatform、EdgeClient、真实服务器 `.env`、GitHub secrets、Cloud 业务数据或生产数据库。
- 改动原因：继续执行 AI 修复计划的 HTTP-only 前提，把 CC 漏项里的 AI 端部署安全项落成可验证门禁：`.env.example` 不再携带真实内网 IP/弱 secret/dummy key，发布前 preflight fail-fast 校验必填 secret、HTTP-only URL、OIDC HTTP 配置、弱占位和 `.env` 权限；标准本地发布不再默认 root SSH；workflow 不再给 Cloud 地址兜真实 IP 默认值；新增的 `internal_server_error` 错误码补入前端契约。
- 影响面：`.env.example` 只保留 `internal.example` 模板地址和空 secret，真实 Harbor、Cloud、AICopilot、模型、token 和密码必须来自服务器 `.env`、GitHub secrets 或命令行环境变量；`deploy-release.sh` 加载真实 `.env` 后会先执行环境校验，拒绝模板占位、弱值、缺失 secret、非 HTTP URL、OIDC HTTP 组合不完整和权限过宽的 `.env`；`local-release.sh` 要求 `REGISTRY` 和 `CLOUD_PLATFORM_URL` 显式提供，`root@` 仅允许 `ALLOW_ROOT_SSH_DEPLOY=true` 应急；模型 smoke 脚本不再默认私有 IP、模型名或 dummy key，必须显式传入。
- 计划收口：`docs/AI架构治理清单.md` 已把 AI-SEC-004、005、006、009 标记为 Done，把 AI-SEC-010 标记为 Partial。runner 权限收敛和 OIDC/Vault 仍属于外部基础设施任务，不能伪造成 AICopilot 已完成。
- 规则提炼：形成长期部署治理规则，已写入 `AGENTS.md` 和 `资料/AICopilot业务规则.md`：部署模板、文档示例、workflow 默认值和脚本默认值不得携带真实内网 IP、弱 secret、`CHANGE_ME`、`dummy-key` 或默认 `root@` 发布目标；真实生产值只能来自服务器 `.env`、GitHub secrets 或命令行环境变量；root SSH 只允许显式应急。错误码契约已同步写入 `docs/frontend-integration-contract-package-2026-05-17.md`。
- 验证命令：
  - `rg -n "(10\\.[0-9]{1,3}\\.|172\\.(1[6-9]|2[0-9]|3[01])\\.|192\\.168\\.|CHANGE_ME|dummy-key|<tag>|root@10|http://10)" deploy/enterprise-ai/.env.example deploy/enterprise-ai/README.md "AICopilot 项目部署与维护指南.md" .github deploy/enterprise-ai/scripts/check-model-provider-openai.sh deploy/enterprise-ai/build-and-push.sh deploy/enterprise-ai/mirror-base-images.sh deploy/enterprise-ai/Dockerfile.backend-runtime deploy/enterprise-ai/docker-compose.yaml`
  - `rg -n "validate_deploy_environment|ensure_no_template_placeholders|ALLOW_ROOT_SSH_DEPLOY|REGISTRY is required|CLOUD_PLATFORM_URL is required|internal.example|sha-replace-with-release-tag|internal_server_error" deploy .github docs AGENTS.md 资料 src/tests src/shared src/hosts -g '!**/bin/**' -g '!**/obj/**'`
  - `bash -n deploy/enterprise-ai/deploy-release.sh deploy/enterprise-ai/local-release.sh deploy/enterprise-ai/build-and-push.sh deploy/enterprise-ai/mirror-base-images.sh deploy/enterprise-ai/scripts/check-model-provider-openai.sh`
  - `git diff --check -- AGENTS.md "AICopilot 项目部署与维护指南.md" deploy/enterprise-ai/README.md deploy/enterprise-ai/deploy-release.sh deploy/enterprise-ai/local-release.sh deploy/enterprise-ai/build-and-push.sh deploy/enterprise-ai/mirror-base-images.sh deploy/enterprise-ai/scripts/check-model-provider-openai.sh deploy/enterprise-ai/.env.example deploy/enterprise-ai/Dockerfile.backend-runtime deploy/enterprise-ai/docker-compose.yaml .github/workflows/aicopilot-deploy.yml .github/workflows/aicopilot-ai-read-diagnostics.yml .github/workflows/aicopilot-enable-real-cloud-ai-read.yml .github/workflows/aicopilot-image.yml "资料/AICopilot业务规则.md" docs/AI架构治理清单.md docs/frontend-integration-contract-package-2026-05-17.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs src/tests/AICopilot.ArchitectureTests/ArchitectureBoundaryTests.cs`
  - `docker compose --env-file deploy/enterprise-ai/.env.example -f deploy/enterprise-ai/docker-compose.yaml config -q`
  - `deploy/enterprise-ai/scripts/check-model-provider-openai.sh --base-url http://model.internal.example:40034/v1 --model smoke-model --api-key explicit-test-key --dry-run`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests|FullyQualifiedName~ErrorCodeCatalogTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~ArchitectureBoundaryTests" --no-restore --logger "console;verbosity=minimal"`
- 验证结果：负向检索未命中真实 `真实内网地址段`、`CHANGE_ME`、`dummy-key`、`<tag>`、`root@10` 或 `http://10`；正向检索命中环境校验、root SSH 应急开关、必填 `REGISTRY`/`CLOUD_PLATFORM_URL`、模板占位、release tag 占位和 `internal_server_error` 契约；脚本语法通过；目标文件 `git diff --check` 通过；compose config 解析通过；模型 smoke 脚本显式参数 dry-run 通过；后端 `SecurityHardeningTests|ErrorCodeCatalogTests` 67 条通过；ArchitectureBoundaryTests 76 条通过。未运行完整 BackendTests、完整 ArchitectureTests、前端 build、Docker 镜像构建或真实服务器发布，原因是本批只收口 AI 部署模板/preflight/契约门禁，生产发布仍需真实 `.env`、Harbor、服务器账号和发布窗口。

## 2026-07-06 AI 安全治理第一批代码落地

- 改动范围：AICopilot Web nginx 模板、Web Dockerfile、后端 runtime Dockerfile、`deploy/enterprise-ai/deploy-release.sh`、`mirror-base-images.sh`、`SecretStringEncryptor`、migration worker seed 密钥迁移、`UseCaseExceptionHandler`、前端 API/stream/router/chunk/store 错误处理、`SecurityHardeningTests`、密钥保护相关测试、部署文档、项目规则、业务规则和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 写接口、真实 `.env`、数据库数据或服务器状态。
- 改动原因：执行 `docs/AI架构治理清单.md` 的 AICopilot 安全治理计划，先落内网 HTTP-only 可执行项：HTTP 兼容安全响应头、发布后 header preflight、非 root 容器、API key AES-GCM、未知异常安全 ProblemDetails、前端裸 catch 诊断。
- 影响面：Web 入口仍是 HTTP 8000/外部 82，不引入 HTTPS/HSTS/443/证书；部署完成后会校验 `X-Content-Type-Options`、`X-Frame-Options`、CSP、Referrer-Policy、Permissions-Policy，且出现 HSTS 会失败；后端应用镜像不再在应用 Dockerfile 中 `USER root` / `apt-get`，运行依赖由 Harbor hardened dotnet base 提供；Web runtime 切到 `nginx` 用户；新写入模型/embedding key 为 `encv2:` AES-GCM；旧 `encv1:` 只允许 migration worker 重加密，运行时 `Decrypt` 拒绝；未知后端异常返回脱敏 `internal_server_error` + `traceId`；前端不再保留裸 `catch {}`。
- 计划收口：`docs/AI架构治理清单.md` 已把 AI-SEC-001、002、007、008、011、015 标记为 Done，把 AI-SEC-005、012、016、017 标记为 Partial。未完成项仍保持 Planned/Partial，不能视为整份安全治理完成。
- 规则提炼：新增长期密钥保护规则，已写入 `AGENTS.md` 和 `资料/AICopilot业务规则.md`：模型和 Embedding API key 必须使用 `encv2:` AES-GCM；旧 `encv1:` 只能迁移重加密，不能作为 runtime provider 正常兼容格式。HTTP-only 部署红线沿用上一条复盘已沉淀规则。
- 验证命令：
  - `rg -n "HTTP-only|Strict-Transport-Security|listen 443|ssl_certificate|UseHttpsRedirection|X-Content-Type-Options|Content-Security-Policy|probe_web_security_headers" AGENTS.md 资料/AICopilot业务规则.md "AICopilot 项目部署与维护指南.md" deploy/enterprise-ai/README.md deploy/enterprise-ai/deploy-release.sh src/vues/AICopilot.Web/nginx.conf.template docs/AI架构治理清单.md`
  - `rg -n "USER root|CipherMode\\.CBC|catch \\{" deploy/enterprise-ai/*.sh deploy/enterprise-ai/Dockerfile.backend-runtime src/hosts src/infrastructure src/services src/shared src/core src/vues -g "!**/bin/**" -g "!**/obj/**"`
  - `rg -n "encv2|LegacyCipherPrefix|ReEncryptLegacyCipher|AesGcm|internal_server_error|UseCaseExceptionHandler|FrontendSource_ShouldNotContainBareCatchBlocks" AGENTS.md 资料/AICopilot业务规则.md src tests docs/AI架构治理清单.md`
  - `bash -n deploy/enterprise-ai/deploy-release.sh deploy/enterprise-ai/mirror-base-images.sh deploy/enterprise-ai/build-and-push.sh deploy/enterprise-ai/local-release.sh`
  - `git diff --check -- AGENTS.md "AICopilot 项目部署与维护指南.md" deploy/enterprise-ai/README.md deploy/enterprise-ai/deploy-release.sh deploy/enterprise-ai/mirror-base-images.sh deploy/enterprise-ai/Dockerfile.backend-runtime "资料/AICopilot业务规则.md" docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md src`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests|FullyQualifiedName~SecretStringEncryptorTests|FullyQualifiedName~ModelSecretRuntimeBoundaryTests" --no-restore`
  - `npm run type-check`（工作目录：`src/vues/AICopilot.Web`）
- 验证结果：HTTP-only 红线、安全头、header preflight、`encv2`、旧密文迁移、catch-all ProblemDetails 和前端裸 catch 门禁均可检索；生产源码/脚本负向检索未命中 `USER root`、`CipherMode.CBC` 或裸 `catch {`；`bash -n` 通过；目标文件 `git diff --check` 通过；后端定向测试 75 条通过；前端 `vue-tsc --build` 通过。未运行完整 BackendTests、完整前端 build、Docker 镜像构建、容器 smoke 或真实服务器发布，原因是本轮只做本地代码/文档落地，生产发布仍需先提交并在发布窗口重新生成 Harbor hardened base image。

## 2026-07-06 AI 安全治理计划改为 HTTP-only 内网部署红线

- 改动范围：AICopilot 项目规则 `AGENTS.md`、业务规则 `资料/AICopilot业务规则.md`、部署入口 `AICopilot 项目部署与维护指南.md`、`deploy/enterprise-ai/README.md`、新增治理清单 `docs/AI架构治理清单.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、AICopilot 运行代码、部署脚本、真实 `.env`、数据库、Cloud 业务数据或 Cloud 写接口。
- 改动原因：上一版 AI 修复计划把 HTTPS/TLS/HSTS/443/certificate 作为第一轮安全红线，但当前生产约束明确为内网 HTTP 部署，证书方案未获批准，不能把需要证书的 HTTPS 项夹带进 AI 端整改。
- 影响面：AICopilot 当前治理口径改为 HTTP-only。后续安全整改不得强制引入 HTTPS redirection、HSTS、nginx 443 listener、证书申请/续期或 OIDC HTTPS metadata 校验；安全加固转向内网隔离、端口收敛、同源代理、CORS 白名单、强 secret、短期 token、非 root 容器、敏感信息脱敏、Cloud 只读边界、除 HSTS 外的安全响应头和部署 preflight。
- 计划收口：新增并扩展 `docs/AI架构治理清单.md` 为本轮 AI 完整修复计划，包含执行边界、总体优先级、执行前审计、七个修复批次、分层测试门禁、线上发布验收、外部依赖、不纳入项和关单格式；明确 HTTPS/TLS/HSTS/nginx 443/certificate 不纳入当前 AI 修复。
- 规则提炼：形成长期部署红线，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md`、`AICopilot 项目部署与维护指南.md` 和 `deploy/enterprise-ai/README.md`：AICopilot 当前内网生产部署必须保持 HTTP-only，任何 HTTPS 切换必须由用户单独批准传输层方案和证书来源。
- 验证命令：
  - `rg -n "HTTP-only|当前内网.*HTTP|不得把 HTTPS|Strict-Transport-Security|HSTS|listen 443|UseHttpsRedirection|ssl_certificate|证书" AGENTS.md 资料/AICopilot业务规则.md "AICopilot 项目部署与维护指南.md" deploy/enterprise-ai/README.md docs/AI架构治理清单.md`
  - `rg -n "AI架构治理清单|AI-SEC-001|AI-SEC-006|production-records|Cloud 只读|Simulation|Text-to-SQL" docs/AI架构治理清单.md AGENTS.md 资料/AICopilot业务规则.md`
  - `rg -n "执行前审计|第一批|第二批|第三批|第四批|第五批|第六批|第七批|HTTP-only|AI-SEC-027|外部依赖|关单格式" docs/AI架构治理清单.md`
  - `git diff --check -- AGENTS.md "AICopilot 项目部署与维护指南.md" deploy/enterprise-ai/README.md "资料/AICopilot业务规则.md" docs/AI架构治理清单.md docs/改动复盘与规则沉淀.md`
- 验证结果：HTTP-only 红线、HTTPS/HSTS/443/certificate 禁止口径、AI-SEC 分项、执行前审计、七个批次、Cloud 只读、Simulation、Text-to-SQL、外部依赖和关单格式均可检索；目标文件 `git diff --check` 无空白格式错误。未运行 build/test，原因是本轮仅改 AICopilot 文档和治理清单，未改运行代码。

## 2026-07-06 生产模型 API 服务器直连诊断与恢复复测

- 诊断范围：只读检查 AICopilot 生产服务器 `<aicopilot-prod-host>` 到私有模型端点 `<model-gateway-host>:40034` 的网络连通性、当前 compose/release 状态、线上模型配置和 httpapi 日志。未修改服务器 `.env`、数据库数据、compose、镜像、Cloud 业务数据或模型服务。
- 诊断原因：页面出现 `chat_stream_failed`，需要绕开 AICopilot 应用代码，从服务器侧直接验证私有 OpenAI-compatible 模型 API，避免把模型端点不可达误判为前端或业务编排问题。
- 初始证据：服务器 `/srv/enterprise-ai/deploy` 当前 release 为 `sha-e3ee1cd1ef0850c882d49b4cbf31f49b2ff9dce0`，线上发布目录尚未包含本地新增的 `scripts/check-model-provider-openai.sh`；因此使用 SSH 在服务器上直接执行等价 `curl`。初始 `curl` 到 `http://<model-gateway-host>:40034/v1/chat/completions` 返回 `curl_exit=7` / `Connection refused`，`nc -vz <model-gateway-host> 40034` 同样为 `Connection refused`；`ping <model-gateway-host>` 0% 丢包，说明模型宿主可达但目标端口未接受连接。二次复测结果仍为 `ping` 成功、`curl` / `nc` 到 `40034` 被拒绝。
- 恢复复测：后续再次从 `<aicopilot-prod-host>` 直连 `http://<model-gateway-host>:40034/v1/chat/completions` 已返回 HTTP 200，响应包含 OpenAI-compatible `choices` 字段，`nc -vz <model-gateway-host> 40034` 成功。说明模型端口已恢复接受连接，但线上 AICopilot release 仍为 `sha-e3ee1cd1ef0850c882d49b4cbf31f49b2ff9dce0`，服务器发布目录仍缺少 `scripts/check-model-provider-openai.sh`。
- 配置核对：线上 `aigateway.language_models` 只有启用的 `MiniMax Private|MiniMax-M3-AWQ-INT4|OpenAICompatible|http://<model-gateway-host>:40034/v1|32768|4096`。httpapi 近 96 小时日志存在 `POST http://<model-gateway-host>:40034/v1/chat/completions` 和 `System.Net.Http.HttpRequestException: Connection refused (<model-gateway-host>:40034)`，与服务器直连诊断一致。
- 结论：初始生产失败首先归因于 AICopilot 服务器到私有模型 API 的 TCP 连接被拒绝；模型只有一个不是问题，AICopilot 模型配置也没有指向其他端点。后续模型端口恢复后，服务器侧模型直连已通过。当前剩余验收项是登录 AICopilot 页面后发起真实聊天，确认不再出现 `chat_stream_failed`；AICopilot 侧仍应发布已新增的模型 smoke preflight 和错误分类改造，让后续部署前先阻断端点不可达问题。
- 规则提炼：无新增长期规则；已有 `deploy/enterprise-ai/README.md` 和 `AICopilot 项目部署与维护指南.md` 已规定模型 provider 必须用服务器侧 smoke 脚本独立验证，本次只是执行该口径的生产诊断。
- 验证命令：
  - `ssh <ssh-user>@<aicopilot-prod-host> 'cd /srv/enterprise-ai/deploy && docker compose --env-file .env -f docker-compose.yaml ps'`
  - `ssh <ssh-user>@<aicopilot-prod-host> 'cd /srv/enterprise-ai/deploy && ./scripts/check-model-provider-openai.sh --env-file .env'`
  - `ssh <ssh-user>@<aicopilot-prod-host> 'curl --max-time 20 --header "Authorization: Bearer dummy-key" --header "Content-Type: application/json" --data "{\"model\":\"MiniMax-M3-AWQ-INT4\",\"messages\":[{\"role\":\"user\",\"content\":\"Return exactly OK.\"}],\"max_tokens\":16,\"temperature\":0}" "http://<model-gateway-host>:40034/v1/chat/completions"'`
  - `ssh <ssh-user>@<aicopilot-prod-host> 'nc -vz -w 5 <model-gateway-host> 40034'`
  - `ssh <ssh-user>@<aicopilot-prod-host> 'ping -c 2 -W 2 <model-gateway-host>'`
  - `ssh <ssh-user>@<aicopilot-prod-host> 'cd /srv/enterprise-ai/deploy && docker compose --env-file .env -f docker-compose.yaml logs --since=96h --tail=2000 aicopilot-httpapi | grep -Ei "<model-gateway-host>|40034|Connection refused|MiniMax"'`
  - `printf '%s\n' 'select provider, name, protocol_type, base_url, is_enabled, usage, max_tokens, max_output_tokens from aigateway.language_models order by provider, name;' | ssh <ssh-user>@<aicopilot-prod-host> 'cd /srv/enterprise-ai/deploy && docker compose --env-file .env -f docker-compose.yaml exec -T postgres sh -lc "PGPASSWORD=\"$POSTGRES_PASSWORD\" psql -U \"$POSTGRES_USER\" -d \"$POSTGRES_DB\" -At"'`
- 验证结果：生产 compose 服务均运行；Web 首页 `http://<aicopilot-prod-host>:82` 返回 200；服务器发布目录缺少 smoke 脚本，说明脚本尚未部署；服务器直连模型 API 返回 `Connection refused`；`ping` 模型宿主成功；线上 DB 模型配置与私有 MiniMax 端点一致；httpapi 日志中的模型调用失败与服务器直连错误一致。
- 恢复复测结果：服务器直连模型 API 返回 HTTP 200，`nc` 到 `<model-gateway-host>:40034` 成功；Chrome 打开 `http://<aicopilot-prod-host>:82/chat` 被重定向到 `/login`，当前浏览器没有 AICopilot 登录态，未擅自点击 Cloud 登录或处理账号授权，因此页面聊天验收待登录后执行。
- 本地改造验证：`bash -n deploy/enterprise-ai/scripts/check-model-provider-openai.sh deploy/enterprise-ai/deploy-release.sh deploy/enterprise-ai/local-release.sh` 通过；`check-model-provider-openai.sh --dry-run` 和 `--env-file deploy/enterprise-ai/.env.example --dry-run` 通过；模型 smoke / DDD 相关 ArchitectureTests 9 条通过；`ChatErrorContractTests|IntentRoutingFallbackClassifierTests|SemanticQueryPlannerTests` 后端定向测试 52 条通过；完整 BackendTests 744 条通过；`dotnet build AICopilot.slnx --no-restore` 成功且 0 warning / 0 error；`pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`、`pwsh ./scripts/Test-ArchitectureBoundaries.ps1`、`pwsh ./scripts/Test-TextEncoding.ps1` 均通过；`docker compose --env-file deploy/enterprise-ai/.env.example -f deploy/enterprise-ai/docker-compose.yaml config -q` 通过；完整 ArchitectureTests 82 条通过；`git diff --check` 通过。
- 发布前置判断：`./deploy/enterprise-ai/local-release.sh --services httpapi,dataworker,ragworker,web,migration --ssh-target <ssh-user>@<aicopilot-prod-host> --dry-run` 能打印构建、同步 smoke 脚本和 SSH deploy 命令，但 dry-run 明确跳过 clean/pushed HEAD enforcement。当前工作区仍有大量未提交改动，HEAD 仍是线上旧 release `e3ee1cd1ef0850c882d49b4cbf31f49b2ff9dce0`；按 `local-release.sh` 和部署文档，正式发布前必须先提交并推送这批 AICopilot 改动，不能用旧 `sha-e3ee...` 代表新代码发布。

## 2026-07-06 AgentTask 审计/运行/计划/审批查询/上传/工作区与 artifact 编排门禁收口

- 改动范围：AICopilot AiGateway `AgentTaskAuditQueries`、`AgentTaskCommands`、`AgentApprovalQueryHandlers`、`Uploads/UploadRecords`、`Workspaces/ArtifactWorkspaceQueryHandlers`、`ArtifactVersioningQueryHandlers`、`ArtifactVersioningCommandHandlers`、`ArtifactWorkspaceP9QueryHandlers`、`ArtifactWorkspaceP9CommandHandlers`、新增 `AgentTaskAuditQueryCoordinator`、新增 `AgentTaskDtoQueryService`、新增 `PlanAgentTaskCoordinator`、新增 `AgentApprovalQueryCoordinator`、新增 `UploadRecordCoordinator`、新增 `ArtifactWorkspaceQueryCoordinator`、新增 `ArtifactVersioningQueryCoordinator`、新增 `ArtifactVersioningCommandCoordinator`、新增 `ArtifactWorkspaceP9Coordinator`、DI 注册、后端治理测试、Architecture 边界测试、DDD 聚合根边界专题文档和本滚动复盘。未修改 `IIoT.CloudPlatform`、`IIoT.EdgeClient`、Cloud 业务数据、Cloud 写接口、数据库 migration、模型配置或部署脚本。
- 改动原因：上一轮已把 tool execution audit 查询收口到独立 coordinator，但 Agent task audit summary、run attempt 查询、run queue 查询、approval query、UploadRecord 命令、workspace/artifact 查询、artifact 版本查询/命令和 P9 artifact 预览/命令仍有 handler 直接拼 repository/store/file store/audit/identity 权限。这样 handler 承担跨对象查询/命令编排，后续容易重新扩散 DDD 边界。
- 影响面：AgentTask audit/run queue/DTO/plan/approval query、UploadRecord、基础 workspace 查询/下载、artifact 内容/版本/diff/版本下载、artifact 内容更新/版本恢复、P9 artifact 预览/revision comment/regenerate/final approval submit 现在均由明确 coordinator 承接；对应 MediatR handler 只保留入口转发。新增 run queue 查询隔离/分页、AgentTask DTO 查询映射、approval query 映射、Session 上传行为、workspace 查询/下载后端测试，复跑 artifact versioning、P9 行为测试和 explicit audit save 白名单测试。
- DDD 边界：本批不新增或降级聚合根；继续保持队列、投影、审计、worker 状态和执行过程记录不进入 `IAggregateRoot<>` 或泛型 repository 白名单。新增 coordinator 是应用层协调器，不是领域聚合根，也不改变 `AgentTaskRunAttempt`、`AgentTaskRunQueueItem`、`ToolExecutionRecord`、`ApprovalRequest`、`UploadRecord`、`ArtifactWorkspace` 的持久化边界。
- 安全边界：本批只调整 AICopilot 查询层编排，不新增 Cloud 写能力，不放宽 MCP/Tool/Agent workflow 写 Cloud 禁令，不新增错误码，不改变私有模型 API 配置或调用参数。
- 规则提炼：形成长期技术契约，已写入 `docs/DDD聚合根边界.md` 并由 `src/tests/AICopilot.ArchitectureTests/ArchitectureBoundaryTests.cs` 执行化：AiGateway MediatR handler 直接注入 3 个及以上 repository/store/file store/query service 的当前债务清单为空，不允许新增；跨聚合或跨 store 编排必须进入明确命名的应用层 coordinator/query service。Artifact versioning 和 P9 artifact 的权限校验、file store 操作、版本归档、审计和状态机更新必须分别留在 `ArtifactVersioningQueryCoordinator`、`ArtifactVersioningCommandCoordinator`、`ArtifactWorkspaceP9Coordinator`。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~AgentTaskRunAttemptsQuery_ShouldPageAndIsolateByOwner|FullyQualifiedName~AgentTaskRunQueueQuery_ShouldPageAndIsolateByOwner" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~AgentAuditSummary_ShouldMergeToolRecordsAndFailureSummary" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~AgentTaskQueries_ShouldMapWorkspaceApprovalAndQueueViaDtoService|FullyQualifiedName~RetryAgentTaskCommand_ShouldResetFailedStep_AndEnqueueRetry|FullyQualifiedName~RetryAgentTaskCommand_ShouldRejectTerminalTasks|FullyQualifiedName~CancelAgentTaskCommand_ShouldCancelActiveAttemptAndPendingApprovals|FullyQualifiedName~AgentRunQueueEnqueue_ShouldPreventDuplicateActiveItems|FullyQualifiedName~RetryAgentTaskCommand_ShouldApplyBackoffLimitAndAudit|FullyQualifiedName~CancelAgentTaskCommand_ShouldKeepTerminalTaskUnchanged" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~PlanAgentTask_ShouldCreateDraft_WhenCloudReadonlyToolIsDisabled_AndRejectOnConfirm|FullyQualifiedName~PlanAgentTask_ShouldUseAutoSelectedCloudReadonlySkill|FullyQualifiedName~PlanAgentTask_ShouldCreateDraftWhenAutoSkillSelectorCannotMatch|FullyQualifiedName~PlanAgentTaskCommand_ShouldCreatePlanDraftWithoutExecutableToolPreparation|FullyQualifiedName~PlanAgentTaskCommand_ShouldNotCreateArtifactWorkspaceForPlanDraft" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~AgentApprovalQueries_ShouldFilterAndMapViaCoordinator" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~UploadRecordCommand_ShouldPersistSessionUploadViaCoordinator|FullyQualifiedName~AgentPlanRuntimeAndUpload_ShouldRecheckRagKnowledgeBasePermissions" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~ArtifactWorkspaceQueries_ShouldMapAndDownloadViaCoordinator" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~AgentArtifactVersioningTests|FullyQualifiedName~EnterpriseArtifactWorkspaceP9Tests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~ExplicitAuditSaves_ShouldStayInsideDocumentedWhitelist" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~AgentTaskAuditQueries_ShouldStayBehindCoordinator|FullyQualifiedName~AgentTaskToolExecutionQuery_ShouldStayBehindCoordinator" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~AgentApprovalQueries_ShouldStayBehindCoordinator|FullyQualifiedName~AiGatewayHandlers_ShouldNotAddMultiPersistenceDependencyDebt" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~UploadRecordCommand_ShouldStayBehindCoordinator|FullyQualifiedName~AiGatewayHandlers_ShouldNotAddMultiPersistenceDependencyDebt" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~ArtifactWorkspaceQueries_ShouldStayBehindCoordinator|FullyQualifiedName~AiGatewayHandlers_ShouldNotAddMultiPersistenceDependencyDebt" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~ArtifactVersioningQueries_ShouldStayBehindCoordinator|FullyQualifiedName~ArtifactVersioningCommands_ShouldStayBehindCoordinator|FullyQualifiedName~ArtifactWorkspaceP9_ShouldStayBehindCoordinator|FullyQualifiedName~AiGatewayHandlers_ShouldNotAddMultiPersistenceDependencyDebt" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~PlanAgentTask_ShouldStayBehindCoordinator|FullyQualifiedName~AgentTaskLifecycleRunRetryCancel_ShouldStayBehindCoordinator|FullyQualifiedName~AgentTaskQueries_ShouldKeepDtoCompositionBehindService|FullyQualifiedName~AiGatewayHandlers_ShouldNotAddMultiPersistenceDependencyDebt" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~PlanAgentTask_ShouldStayBehindCoordinator|FullyQualifiedName~ArtifactVersioningQueries_ShouldStayBehindCoordinator|FullyQualifiedName~ArtifactVersioningCommands_ShouldStayBehindCoordinator|FullyQualifiedName~ArtifactWorkspaceP9_ShouldStayBehindCoordinator|FullyQualifiedName~ArtifactWorkspaceQueries_ShouldStayBehindCoordinator|FullyQualifiedName~UploadRecordCommand_ShouldStayBehindCoordinator|FullyQualifiedName~AgentApprovalQueries_ShouldStayBehindCoordinator|FullyQualifiedName~AgentTaskAuditQueries_ShouldStayBehindCoordinator|FullyQualifiedName~AgentTaskToolExecutionQuery_ShouldStayBehindCoordinator|FullyQualifiedName~AgentTaskQueries_ShouldKeepDtoCompositionBehindService|FullyQualifiedName~AiGatewayHandlers_ShouldNotAddMultiPersistenceDependencyDebt|FullyQualifiedName~SessionTimelineQuery_ShouldStayBehindCoordinator|FullyQualifiedName~AgentTaskRunQueueWorker_ShouldStayBehindCoordinator|FullyQualifiedName~AgentTaskRuntimeEvents_ShouldStayBehindRecorder|FullyQualifiedName~ArtifactWorkspaceSubmitFinalize_ShouldStayBehindCoordinator|FullyQualifiedName~AgentApprovalDecision_ShouldStayBehindCoordinator|FullyQualifiedName~AgentTaskLifecycleRunRetryCancel_ShouldStayBehindCoordinator|FullyQualifiedName~DddAggregateBoundaryTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet build AICopilot.slnx --no-restore`
  - `git diff --check`
  - `rg -n "AgentTaskAuditQueryCoordinator|AgentTaskDtoQueryService|PlanAgentTaskCoordinator|AgentApprovalQueryCoordinator|UploadRecordCoordinator|ArtifactWorkspaceQueryCoordinator|ArtifactVersioningQueryCoordinator|ArtifactVersioningCommandCoordinator|ArtifactWorkspaceP9Coordinator|AiGatewayHandlers_ShouldNotAddMultiPersistenceDependencyDebt|handler 多持久化依赖债务|债务清单为空|ArtifactWorkspaceP9_ShouldStayBehindCoordinator" docs/改动复盘与规则沉淀.md docs/DDD聚合根边界.md src/services/AICopilot.AiGatewayService src/tests/AICopilot.ArchitectureTests src/tests/AICopilot.BackendTests -g '!**/bin/**' -g '!**/obj/**'`
- 验证结果：后端 run attempt / run queue 查询隔离分页、audit summary 合并/脱敏、AgentTask DTO 查询、lifecycle/run queue、PlanDraft 源码契约、approval query 映射、UploadRecord Session 上传、RAG upload 权限源码契约和 workspace 查询/下载定向测试 19 条通过、0 失败；artifact versioning / P9 行为测试 7 条通过、0 失败；explicit audit save 白名单测试 1 条通过、0 失败；组合 DDD/coordinator/handler 多依赖债务架构测试 23 条通过、0 失败；`dotnet build AICopilot.slnx --no-restore` 成功且 0 warning / 0 error；`git diff --check` 通过；规则/文档检索通过。

## 2026-07-03 模型不可达诊断、真实查询 fallback 与 AgentTask/Workspace 应用层协调器收口

- 改动范围：AICopilot AiGateway 流式错误分类、意图路由 fallback、DataAnalysis 设备日志语义补全、AgentTask Run/Retry/Cancel lifecycle coordinator、AgentApproval decision coordinator、ArtifactWorkspace lifecycle coordinator、Agent runtime event recorder、AgentTask run queue worker coordinator、session timeline query coordinator、AgentTask tool execution query coordinator、后端/架构测试、enterprise-ai 部署脚本、部署 README、项目部署维护指南、DDD 聚合根边界专题文档和本滚动复盘。未修改 `IIoT.CloudPlatform`、`IIoT.EdgeClient`、Cloud 业务数据、Cloud 写接口、数据库 migration 或生产模型配置数据。
- 改动原因：生产页面真实提示词“替我查询下模切设备最近的一些信息并帮我整理分类成表格图表”失败时只暴露 `chat_stream_failed`，无法区分模型网络不可达和业务编排错误；同时服务器到私有模型 API 的连通性缺少绕开 AICopilot 应用的独立验收入口，后续容易继续把网络/模型端点问题误判成代码问题。
- 影响面：`AgentStreamRuntime` 会识别 SDK 包装后的 `HttpRequestException` / timeout / cancellation 链路，返回稳定 `model_provider_unavailable` 或 `model_request_timeout`，不泄漏 provider endpoint 或 key；路由模型不可用时，宽泛“最近设备信息/整理/表格/图表”类提示词确定性 fallback 到只读 `Analysis.DeviceLog.Latest`；“信息”不再被泛化成 INFO 日志级别，只有“信息日志/INFO级别”等明确表达才补 `level=INFO`。
- 部署诊断：新增 `deploy/enterprise-ai/scripts/check-model-provider-openai.sh`，从服务器直接 POST OpenAI-compatible `/chat/completions`，默认目标为当前私有 MiniMax `http://<model-gateway-host>:40034/v1` / `MiniMax-M3-AWQ-INT4` / dummy key；`AICOPILOT_MODEL_SMOKE_ENABLED=true` 时，`deploy-release.sh` 会在重启服务前执行模型 smoke preflight，`local-release.sh` 同步该脚本到服务器。
- DDD 边界：本批没有新增或降级聚合根，但把用户侧 `RunAgentTaskCommandHandler` / `RetryAgentTaskCommandHandler` / `CancelAgentTaskCommandHandler` 的跨对象编排收口到 `AgentTaskLifecycleCoordinator`，把 `ApproveAgentApprovalCommandHandler` / `RejectAgentApprovalCommandHandler` 的审批决策编排收口到 `AgentApprovalDecisionCoordinator`，把 `SubmitFinalReviewCommandHandler` / `FinalizeArtifactWorkspaceCommandHandler` 的 workspace 终审提交与最终确认编排收口到 `ArtifactWorkspaceLifecycleCoordinator`，把 `AgentTaskRuntime` 内的 tool execution audit、Agent tool audit 和 timeline staging 收口到 `AgentRuntimeEventRecorder`，把 data worker 队列执行领取、stale lease 恢复、queue item 完成/失败回写和 run attempt 解析收口到 `AgentTaskRunQueueWorkerCoordinator`，把 session timeline 查询组装收口到 `SessionTimelineQueryCoordinator`，并把 AgentTask tool execution audit 查询收口到 `AgentTaskToolExecutionQueryCoordinator`；handler/runtime/worker 不再直接拼 `IAgentTaskRunQueue`、`IAgentTaskRunAttemptStore`、pending approval 取消、run queue audit、审批权限校验、plan confirmation、workspace/task/approval/file store、`ToolExecutionRecord`、tool audit、timeline 和 audit。`DddAggregateBoundaryTests` 继续确认“队列、投影、审计、worker 状态、执行过程记录不再作为聚合根”的门禁有效。
- 安全边界：本批只新增模型连通性探针和只读设备日志 fallback，不新增 Cloud 写能力，不放宽 MCP/Tool/Agent workflow 写 Cloud 禁令，不新增错误码，不向前端或日志暴露 API key、endpoint 原始异常或内部 provider 消息。
- 规则提炼：形成长期验收口径，已写入 `AICopilot 项目部署与维护指南.md`、`deploy/enterprise-ai/README.md`、`docs/DDD聚合根边界.md`，并由 `src/tests/AICopilot.ArchitectureTests/ArchitectureBoundaryTests.cs` 执行化：模型 API 故障必须能通过服务器侧独立 smoke 与 AICopilot 应用错误分离；发布前可启用模型 smoke 作为 preflight；AgentTask 用户侧 Run/Retry/Cancel 生命周期编排必须留在 `AgentTaskLifecycleCoordinator`，审批 Approve/Reject 决策编排必须留在 `AgentApprovalDecisionCoordinator`，workspace SubmitFinalReview/Finalize 编排必须留在 `ArtifactWorkspaceLifecycleCoordinator`，runtime tool execution audit / timeline / Agent tool audit 必须留在 `AgentRuntimeEventRecorder`，data worker queue item 领取、stale lease 和完成/失败回写必须留在 `AgentTaskRunQueueWorkerCoordinator`，session timeline 查询组装必须留在 `SessionTimelineQueryCoordinator`，tool execution audit 查询必须留在 `AgentTaskToolExecutionQueryCoordinator`。
- 验证命令：
  - `bash -n deploy/enterprise-ai/scripts/check-model-provider-openai.sh deploy/enterprise-ai/deploy-release.sh deploy/enterprise-ai/local-release.sh`
  - `deploy/enterprise-ai/scripts/check-model-provider-openai.sh --dry-run`
  - `deploy/enterprise-ai/scripts/check-model-provider-openai.sh --env-file deploy/enterprise-ai/.env.example --dry-run`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~ChatErrorContractTests|FullyQualifiedName~IntentRoutingFallbackClassifierTests|FullyQualifiedName~SemanticQueryPlannerTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~DddAggregateBoundaryTests|FullyQualifiedName~ModelProviderSmokeCheck_ShouldStayAvailableAsServerSideDiagnostic|FullyQualifiedName~CloudReadOnlyReadonlyGrantSources_ShouldStayAlignedWithGovernedSchema" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~AgentRunQueueProductionOpsTests|FullyQualifiedName~ToolRegistryGovernanceTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~AgentTaskLifecycleRunRetryCancel_ShouldStayBehindCoordinator|FullyQualifiedName~DddAggregateBoundaryTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~AgentApprovalResumeQueue_ShouldEnqueueAfterToolApproval|FullyQualifiedName~AgentApprovalPermissionHardeningTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~AgentApprovalDecision_ShouldStayBehindCoordinator|FullyQualifiedName~AgentTaskLifecycleRunRetryCancel_ShouldStayBehindCoordinator|FullyQualifiedName~DddAggregateBoundaryTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~ArtifactWorkspaceSubmitFinalize_ShouldStayBehindCoordinator|FullyQualifiedName~AgentApprovalDecision_ShouldStayBehindCoordinator|FullyQualifiedName~AgentTaskLifecycleRunRetryCancel_ShouldStayBehindCoordinator|FullyQualifiedName~DddAggregateBoundaryTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~AgentApprovalPermissionHardeningTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~AgentTaskRuntime_ShouldCreateRunAttempt_AndLinkExecutionRecord|FullyQualifiedName~AgentTaskRuntime_ShouldRejectToolOutsidePersistedSkill|FullyQualifiedName~AgentTaskRuntime_ShouldRejectDisabledTool_AndWriteRejectedExecutionRecord|FullyQualifiedName~AgentTaskRuntime_ShouldUseRegistryApprovalRequirement_BeforeExecutingStep|FullyQualifiedName~AgentTaskRuntime_ShouldRedactFailedExecutionRecord|FullyQualifiedName~AgentTaskRuntime_ShouldExecuteApprovedMcpTool_AndWriteRedactedExecutionRecord" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~AgentTaskRuntimeEvents_ShouldStayBehindRecorder|FullyQualifiedName~ArtifactWorkspaceSubmitFinalize_ShouldStayBehindCoordinator|FullyQualifiedName~AgentApprovalDecision_ShouldStayBehindCoordinator|FullyQualifiedName~AgentTaskLifecycleRunRetryCancel_ShouldStayBehindCoordinator|FullyQualifiedName~DddAggregateBoundaryTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~AgentRunQueueWorkerPositive_ShouldLeaseRunAndCompleteQueueItem|FullyQualifiedName~AgentRunQueueLeaseExpired_ShouldFailStartedLeaseConservatively|FullyQualifiedName~DataWorker_ShouldFailStaleStartedLeaseAndAudit" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~AgentTaskRunQueueWorker_ShouldStayBehindCoordinator|FullyQualifiedName~AgentTaskRuntimeEvents_ShouldStayBehindRecorder|FullyQualifiedName~ArtifactWorkspaceSubmitFinalize_ShouldStayBehindCoordinator|FullyQualifiedName~AgentApprovalDecision_ShouldStayBehindCoordinator|FullyQualifiedName~AgentTaskLifecycleRunRetryCancel_ShouldStayBehindCoordinator|FullyQualifiedName~DddAggregateBoundaryTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~GetSessionTimeline_ShouldExposeRagStepSummaryWithoutRawOutput|FullyQualifiedName~GetSessionTimeline_ShouldResolveApprovalCurrentStateFromAggregate" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~ToolExecutionRecordQuery_ShouldPageFilterRedactAndIsolateByTaskOwner" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter "FullyQualifiedName~AgentTaskToolExecutionQuery_ShouldStayBehindCoordinator|FullyQualifiedName~SessionTimelineQuery_ShouldStayBehindCoordinator|FullyQualifiedName~AgentTaskRunQueueWorker_ShouldStayBehindCoordinator|FullyQualifiedName~AgentTaskRuntimeEvents_ShouldStayBehindRecorder|FullyQualifiedName~ArtifactWorkspaceSubmitFinalize_ShouldStayBehindCoordinator|FullyQualifiedName~AgentApprovalDecision_ShouldStayBehindCoordinator|FullyQualifiedName~AgentTaskLifecycleRunRetryCancel_ShouldStayBehindCoordinator|FullyQualifiedName~DddAggregateBoundaryTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet build AICopilot.slnx --no-restore`
  - `git diff --check`
  - `rg -n "AgentTaskLifecycleCoordinator|AgentApprovalDecisionCoordinator|ArtifactWorkspaceLifecycleCoordinator|AgentRuntimeEventRecorder|AgentTaskRunQueueWorkerCoordinator|SessionTimelineQueryCoordinator|AgentTaskToolExecutionQueryCoordinator|check-model-provider-openai|AICOPILOT_MODEL_SMOKE|model_provider_unavailable|DDD聚合根边界|Run/Retry/Cancel|Approve.+Reject|SubmitFinalReview|Finalize|ToolExecutionRecord|stale lease|timeline 查询|tool execution audit 查询" docs/改动复盘与规则沉淀.md docs/DDD聚合根边界.md "AICopilot 项目部署与维护指南.md" deploy/enterprise-ai/README.md deploy/enterprise-ai/.env.example src/services/AICopilot.AiGatewayService src/tests/AICopilot.ArchitectureTests src/tests/AICopilot.BackendTests -g '!**/bin/**' -g '!**/obj/**'`
- 验证结果：脚本语法检查通过；模型 smoke 脚本默认参数 dry-run 和 `.env.example` dry-run 均通过且不输出 key；后端模型/fallback/语义定向测试 52 条通过、0 失败；架构模型 smoke/DDD 定向测试 8 条通过、0 失败；AgentTask lifecycle 后端定向测试 68 条通过、0 失败；AgentTask lifecycle + DDD 架构定向测试 7 条通过、0 失败；审批恢复队列和审批权限硬化后端定向测试 4 条通过、0 失败；审批/lifecycle/DDD 架构定向测试 8 条通过、0 失败；workspace submit/finalize/lifecycle/DDD 架构定向测试 9 条通过、0 失败；workspace 终审提交、审批权限和 finalize 后端定向测试 3 条通过、0 失败；runtime tool execution audit/审批等待/失败脱敏后端定向测试 6 条通过、0 失败；runtime event recorder/workspace/approval/lifecycle/DDD 架构定向测试 10 条通过、0 失败；worker queue item 执行、stale lease 和审计后端定向测试 3 条通过、0 失败；worker/runtime/workspace/approval/lifecycle/DDD 架构定向测试 11 条通过、0 失败；timeline RAG 摘要和审批状态后端定向测试 2 条通过、0 失败；tool execution audit 查询分页/过滤/脱敏/隔离后端定向测试 1 条通过、0 失败；tool execution query/timeline/worker/runtime/workspace/approval/lifecycle/DDD 架构定向测试 13 条通过、0 失败；`dotnet build AICopilot.slnx --no-restore` 成功且 0 warning / 0 error；`git diff --check` 通过；规则/文档检索通过。

## 2026-07-02 DDD 聚合根边界收紧

- 改动范围：AICopilot AiGateway/Core 聚合类型标记、EF Core 仓储注册、队列/worker/session timeline/tool audit 专用 store、AgentTask 运行队列与审计查询调用点、会话消息投影调用点、后端/架构测试、DDD 聚合根边界专题文档、项目规则和本滚动复盘。未修改 `IIoT.CloudPlatform`、`IIoT.EdgeClient`、Cloud 业务数据、Cloud 写接口、部署脚本或数据库 migration。
- 改动原因：AI 项目聚合根边界被队列、投影、审计、worker 状态和执行过程记录冲散，`AgentTaskRunQueueItem`、`AgentTaskRunAttempt`、`AgentWorkerHeartbeat`、`MessageEvent`、`ToolExecutionRecord` 被当成 `IAggregateRoot<>` 和泛型 repository 使用，后续容易继续扩散。
- 影响面：上述 5 个类型不再实现 `IAggregateRoot<>`，也不再注册 `IRepository<>` / `IReadRepository<>`；对应访问收口为 `IAgentTaskRunQueueStore`、`IAgentTaskRunAttemptStore`、`IAgentWorkerHeartbeatStore`、`IMessageTimelineProjectionStore`、`IToolExecutionAuditStore`。真聚合根仍保留白名单；`DataSourcePermissionGrant` 暂保留为待评估聚合根，后续再判断是否下沉到 `BusinessDatabase` 子实体。
- 关键实现：新增 `DddAggregateBoundaryTests`，用白名单校验所有 `IAggregateRoot<>`、仓储注册和 DbSet 分类；当前 `KnownArchitectureDebt` 为空，测试会阻止上述 5 类回到聚合根或泛型 repository。后端测试 fake repository 同步改为专用 store fake，保证应用层测试也沿新边界编写。
- 补充修复：完整 BackendTests 暴露多轮语义验收中 Final Agent 会在设备日志上下文较大时追加 `token_budget_exceeded` 错误。本次在 `DataAnalysisFinalContextFormatter` 增加 final prompt compact 输出，并在 `FinalAgentBuildExecutor` 预算失败时先裁历史、再压缩 DataAnalysis context，保留 `semantic_summary/query_execution/display_blocks` 必要证据，避免已流出 Widget 后最终回答被预算误挡。
- 安全边界：本批只收紧 AICopilot 内部 DDD/持久化访问边界，不新增 Cloud 写能力，不放宽 MCP/Tool/Agent workflow 写 Cloud 禁令，不改变 CloudReadOnly、SQL guard、业务数据读取范围或审批边界。
- 规则提炼：形成长期规则，已写入 `docs/DDD聚合根边界.md`、`AGENTS.md`、`资料/AICopilot业务规则.md`，并由 `src/tests/AICopilot.ArchitectureTests/DddAggregateBoundaryTests.cs` 执行化：队列、投影、审计、worker 状态和执行过程记录不得实现 `IAggregateRoot<>`，不得进入泛型 repository 白名单；新增聚合根必须先更新白名单、分类说明和架构测试。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~AgentRunQueueProductionOpsTests|FullyQualifiedName~ToolRegistryGovernanceTests" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~SessionPolicySemanticsTests|FullyQualifiedName~AcceptanceBaselineTests.ChineseStructuredAcceptanceChat_ShouldCoverAllBusinessDomains|FullyQualifiedName~Phase25RuntimeSmokeTests.SemanticDeviceAndLogChat_ShouldQueryRealReadOnlyBusinessDatabase|FullyQualifiedName~Phase25RuntimeSmokeTests.SemanticChat_ShouldBlockRecipeDataAndSupportCapacityProductionDataDomains" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~AiEvalBehaviorGuardrailTests.FinalPromptEval|FullyQualifiedName~SessionPolicySemanticsTests|FullyQualifiedName~AcceptanceBaselineTests.ChineseStructuredAcceptanceChat_ShouldCoverAllBusinessDomains|FullyQualifiedName~Phase25RuntimeSmokeTests.SemanticDeviceAndLogChat_ShouldQueryRealReadOnlyBusinessDatabase|FullyQualifiedName~Phase25RuntimeSmokeTests.SemanticChat_ShouldBlockRecipeDataAndSupportCapacityProductionDataDomains" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --logger "trx;LogFileName=backend-full-after-ddd-boundary-final.trx" --logger "console;verbosity=minimal"`
  - `dotnet build AICopilot.slnx --no-restore`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore --logger "console;verbosity=minimal"`
  - `rg "I(Read)?Repository<(AgentTaskRunQueueItem|AgentTaskRunAttempt|AgentWorkerHeartbeat|MessageEvent|ToolExecutionRecord)>|IRepository<(AgentTaskRunQueueItem|AgentTaskRunAttempt|AgentWorkerHeartbeat|MessageEvent|ToolExecutionRecord)>|IReadRepository<(AgentTaskRunQueueItem|AgentTaskRunAttempt|AgentWorkerHeartbeat|MessageEvent|ToolExecutionRecord)>" -n src/services src/infrastructure src/tests`
  - `rg -n "IAggregateRoot" src/core/AICopilot.Core.AiGateway/Aggregates src/core/AICopilot.Core.DataAnalysis/Aggregates src/core/AICopilot.Core.Rag/Aggregates src/core/AICopilot.Core.McpServer/Aggregates`
  - `rg -n "DDD 聚合根边界|聚合根债务清单为空|AgentWorkerHeartbeat|MessageEvent|ToolExecutionRecord|AgentTaskRunQueueItem|AgentTaskRunAttempt" AGENTS.md 资料/AICopilot业务规则.md docs/DDD聚合根边界.md src/tests/AICopilot.ArchitectureTests/DddAggregateBoundaryTests.cs`
- 验证结果：队列/工具治理定向后端测试 68 条通过；语义验收与 SessionPolicy 定向后端测试 14 条通过；FinalPromptEval 与相关回归组合 21 条通过；完整 BackendTests 735 条通过、0 失败、0 跳过，结果文件为 `src/tests/AICopilot.BackendTests/TestResults/backend-full-after-ddd-boundary-final.trx`；solution build 成功且 0 warning / 0 error；ArchitectureTests 64 条通过；五个降级类型的泛型 repository 检索无匹配；聚合根列表和规则/文档检索通过。

## 2026-07-02 Cloud AiRead 新契约对齐

- 改动范围：AICopilot CloudAiRead client、只读端点 allowlist、CloudAiRead DTO/mapper/query parameter builder、DataAnalysis 语义运行器高频路由、业务语义提示、后端/架构测试、部署/前端契约文档、CloudReadOnly 对齐文档、项目规则和本滚动复盘。未修改 `IIoT.CloudPlatform`、`IIoT.EdgeClient`、Cloud 业务数据、Cloud 写接口、Cloud migration、数据库授权 SQL 或 Text-to-SQL 生成/执行器。
- 改动原因：Cloud 已移除旧 AI 专用过站 route，并将生产数据统一收口到 `GET /api/v1/ai/read/production-records`，同时新增 `GET /api/v1/ai/read/capacity/hourly`，设备日志补 `preset` / `minLevel`。AICopilot 仍停留在旧契约会导致启用 CloudAiRead 后请求已删除路径，或继续依赖 Direct DB 高频链路形成双轨。
- 影响面：AICopilot 运行时不再调用旧 AI pass-stations route；生产数据读取统一使用 `production-records`，按 `typeKey` / `processId` / `deviceId` 收窄范围，并映射 Cloud 返回的 `fieldSchema` / `fields` 支撑新工序和插件字段通用加载；小时产能接入 `capacity/hourly`；日志查询支持 `preset` 与 `minLevel`，并校验 `level` 与 `minLevel` 互斥、`preset` 与显式时间范围互斥；语义层遇到高频设备日志、产能和生产数据时，CloudAiRead 启用则优先走 Cloud AiRead API，非高频或未覆盖链路仍保留治理白名单内的 Direct DB / Text-to-SQL 低频探索兜底。
- 关键实现：`CloudAiReadEndpointPolicy` allowlist 加入 `production-records` 与 `capacity/hourly` 并移除动态 pass-stations 前缀；`CloudAiReadClient.QuerySemanticAsync` 生产数据改调 `GetProductionRecordsAsync`，小时产能按语义文本选择 `GetCapacityHourlyAsync`，`deviceCode` 只先查设备解析为唯一 `deviceId`，不会作为高频业务端点参数透传；`CloudAiReadDocumentAdapter` 支持生产记录字段 schema、小时产能行和日志 `deviceName`；`SemanticAnalysisRunner` 只对 `DeviceLog`、`Capacity`、`ProductionData` 三类高频目标在 CloudAiRead 启用时前置 API 路由，`Device` 等非高频语义仍保持现有 Direct DB 路径。
- 安全边界：本批不新增 Cloud 写能力，不放宽 CloudReadOnly SQL guard、BusinessQuery safety schema、readonly role 授权、Text-to-SQL prompt schema 或修复重试策略；不保留旧 route、不加兼容 adapter、不做新旧双轨；`scenarioId`、`from`、`to`、`pilotWindowId` 等 AICopilot 内部元数据仍不得透传 Cloud。
- 规则提炼：形成长期规则，已写入 `AGENTS.md` 与 `资料/AICopilot业务规则.md`：高频设备日志、小时/汇总产能和生产数据优先走 Cloud AiRead 正式只读 API；DataAnalysis `CloudReadOnly` Direct DB / Text-to-SQL 只作为低频探索、治理白名单内补充分析或未覆盖只读链路兜底，不得压过已批准的高频 Cloud AiRead 端点。接口契约同步写入 `docs/frontend-integration-contract-package-2026-05-17.md`、`资料/CloudReadOnlyAlignment.md`、`AICopilot 项目部署与维护指南.md` 和历史 readiness 说明。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~CloudAiReadClientTests"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~CloudAiReadClientTests|FullyQualifiedName~SemanticAnalysisRunnerTests|FullyQualifiedName~ToolRegistryGovernanceTests|FullyQualifiedName~CloudReadonlySimulationTests|FullyQualifiedName~FrontendIntegrationContractTests"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore --filter "FullyQualifiedName~ArchitectureBoundaryTests"`
  - `dotnet build AICopilot.slnx --no-restore`
  - `git diff --check`
  - `rg -n "pass-stations|passStationTypeKey|DefaultPassStation|GetPassStation|PassStation" src/services src/infrastructure src/hosts src/core src/shared --glob '!**/Migrations/**'`
  - `rg -n "production-records|capacity/hourly|minLevel|fieldSchema|fields|高频设备日志|Cloud AiRead 正式只读" AGENTS.md 资料/AICopilot业务规则.md 资料/CloudReadOnlyAlignment.md docs/frontend-integration-contract-package-2026-05-17.md "AICopilot 项目部署与维护指南.md"`
- 验证结果：定向 `CloudAiReadClientTests` 35 条通过；后端定向 `CloudAiReadClientTests + SemanticAnalysisRunnerTests + ToolRegistryGovernanceTests + CloudReadonlySimulationTests + FrontendIntegrationContractTests` 116 条通过；架构边界 `ArchitectureBoundaryTests` 58 条通过；`dotnet build AICopilot.slnx --no-restore` 0 warning / 0 error；`git diff --check` 通过；旧 pass-stations / PassStation 运行代码检索无匹配；新契约和高频 Cloud AiRead 规则检索通过。

## 2026-07-01 AICopilot 前端 smoke 基线收口

- 改动范围：AICopilot 前端 Config 页工具目录加载时机、Config 页工具名空值防护、前端 smoke 验收断言、`configViewUsability` 单元测试和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、Cloud 写接口、后端 DataAnalysis 执行、DeviceLog SQL guard、Final Agent prompt 或部署配置。
- 改动原因：DeviceLog 五个体验改造批次完成后，完整 `npm run test:smoke` 仍有 4 个非 DeviceLog 基线失败：Chat smoke 仍按旧 Plan-first / “添加=高级选项”口径断言，Config smoke 仍按旧“三阶段固定槽位首屏可见”口径断言；同时 Config 页初始渲染会在 mock 缺 `tools` 字段时把 `toolSummaries` 置为 `undefined`，触发 `toolSummaries.value.find` 运行时告警。
- 影响面：Config 页首屏继续只展示主模型连接和 Cloud 只读状态，三阶段 Agent 槽位仍收进“高级设置”；工具目录不再随首屏 `refreshAllAgentSettings()` 预加载，只在高级设置打开或高级区刷新时加载；工具目录响应缺 `tools` 时归一为空数组，Skill 工具名无法映射时回退显示 tool code。Chat smoke 同步到当前 Chat-first 设计：首屏聊天模式 active，用户手动切到计划模式后再打开“高级选项”验证计划类型和插件能力。
- 安全边界：本批只调整前端加载时机、防御性渲染和 smoke 断言，不新增业务数据读取，不扩大工具目录暴露范围，不改变 Chat/Plan 执行语义，不把内部工具目录、运行队列、模型池或 workspace settings 接回首屏。
- 规则提炼：无新增长期规则，原因是本批属于既有前端产品口径和验收脚本漂移修复；“内部操作默认不预加载/不暴露、三阶段配置收进高级设置、会话模式切换清理错误、运行详情折叠展示”等约束已在 `src/vues/AICopilot.Web/AGENTS.md`、`资料/AICopilot业务规则.md` 和历史核心记录中覆盖。
- 验证命令：
  - `npm run test:unit -- tests/unit/configViewUsability.spec.ts tests/unit/chatWindowUsability.spec.ts`
  - `npm run test:smoke -- --grep "chat shell does not preload internal operations|config renders fixed agent slots"`
  - `npm run type-check`
  - `npm run test:unit`
  - `npm run build`
  - `npm run test:smoke`
  - `git diff --check`
  - `rg -n "AICopilot 前端 smoke|工具目录|Chat-first|高级设置|DeviceLog Answer Presentation|Runtime Details Folding|Chat Run Status|DataAnalysis Widget Rendering" docs/改动复盘与规则沉淀.md src/vues/AICopilot.Web/AGENTS.md 资料/AICopilot业务规则.md src/vues/AICopilot.Web/src/views/ConfigView.vue src/vues/AICopilot.Web/tests/smoke/acceptance.spec.ts src/vues/AICopilot.Web/tests/unit/configViewUsability.spec.ts`
- 验证结果：定向 `configViewUsability + chatWindowUsability` 单元测试 2 个文件、12 条通过；定向 smoke 4 条通过；前端 `type-check` 通过；完整前端单元测试 26 个测试文件、115 条通过；production build 成功，保留既有 `@vueuse/core` 在 Rolldown 下的 `INVALID_ANNOTATION` warning 但退出码为 0；完整 smoke 22 条中 19 通过、3 按既有配置跳过、0 失败；`git diff --check` 通过；规则/复盘检索通过。

## 2026-07-01 DeviceLog 消息视觉收窄第 5 批

- 改动范围：AICopilot 前端消息文本渲染、DeviceLog 固定段落解析、DeviceLog 结果卡组件、Widget 指标/图表/表格密度样式、聊天流 smoke mock/断言、AICopilot 业务规则、AICopilot.Web 前端规则和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、Cloud 写接口、后端 DataAnalysis 执行、Final Agent prompt、SQL guard、数据源 mapping 或部署配置。
- 改动原因：执行 DeviceLog 数据分析体验改造 Batch 5，在 Batch 1-4 已完成运行状态、结构化 Widget、最终回答格式和运行详情折叠后，把 DeviceLog 最终文本从报告式 Markdown 收窄成正式数据分析结果卡，提升结论、指标、证据、原因、建议、边界和查询范围的阅读层级。
- 影响面：新增 `deviceLogAnswerSections.ts` 只在识别到固定 DeviceLog 段落结构时输出结构化视图；新增 `DeviceLogAnswerCard.vue` 和 `MessageTextBlock.vue`，普通文本仍回退到原 Markdown；`MessageItem.vue` 不再直接承担 Markdown/DeviceLog 判断；`StatsWidget.vue`、`ChartWidget.vue`、`DataTableWidget.vue`、`WidgetRenderer.vue` 收紧留白、圆角和高度，表格长文本允许换行且保持滚动高度。
- 关键实现：DeviceLog 结果卡突出“结论”，用紧凑卡片承载关键指标和关键记录，用较安静的列展示可能原因和建议动作，用虚线弱化但仍可见的边界块展示“不能直接执行的动作”，查询范围作为底部范围行；解析器支持 `结论/关键指标/关键记录/可能原因/根因分析/建议动作/不能直接执行的动作/查询范围/查询范围说明`，但要求段落信号足够完整，避免普通回答误套结果页。
- 安全边界：本批只重排已有回答文本和已有 Widget payload，不新增指标、不改写结论、不补未查询数据、不隐藏不可执行边界、不把运行详情或 Widget 当权威状态源；非 DeviceLog 或段落不完整文本继续走原 Markdown。
- 规则提炼：形成长期规则，已写入 `资料/AICopilot业务规则.md` 和 `src/vues/AICopilot.Web/AGENTS.md`：DeviceLog 固定段落最终回答可以结构化渲染，但只能重排已有回答文本；不得新增指标、补未查询数据、改写模型结论或强行套普通回答。
- 验证命令：
  - `npm run test:unit -- tests/unit/deviceLogAnswerSections.spec.ts`
  - `npm run type-check`
  - `npm run test:unit`
  - `npm run build`
  - `git diff --check`
  - `command -v npx >/dev/null 2>&1`
  - `npm run test:smoke -- --grep "chat stream renders widgets and approval card"`
  - `node --input-type=module <<'EOF' ... EOF`（使用 Playwright 检查 1440×1000 和 1024×768 的结构化回答卡、运行详情默认折叠和横向溢出，并输出截图到 `src/vues/AICopilot.Web/output/playwright/device-log-answer-desktop.png` / `device-log-answer-narrow.png`）
  - `rg -n "DeviceLog Answer Presentation|DeviceLog 固定段落最终回答|deviceLogAnswerSections|DeviceLogAnswerCard|device-log-answer|消息视觉收窄第 5 批" src/vues/AICopilot.Web/AGENTS.md 资料/AICopilot业务规则.md docs/改动复盘与规则沉淀.md src/vues/AICopilot.Web/src src/vues/AICopilot.Web/tests`
- 验证结果：新增 `deviceLogAnswerSections.spec.ts` 3 通过、0 失败；前端 `type-check` 通过；完整前端单元测试 26 个测试文件、114 条测试通过；production build 成功，保留既有 `@vueuse/core` 在 Rolldown 下的 `INVALID_ANNOTATION` warning 但退出码为 0；`git diff --check` 通过；定向 smoke `chat stream renders widgets and approval card` 桌面 1 通过、移动 1 跳过；Playwright 视觉检查在 1440×1000 和 1024×768 下均无横向溢出，结构化回答卡和边界段落可见，运行详情默认折叠；完整 `npm run test:smoke` 当前状态仍未全绿：15 通过、4 失败、3 跳过，失败点仍为既有聊天壳“计划模式按钮应有 active class”和 ConfigView agent slot 可见性断言，且 ConfigView mock 期间继续出现 `toolSummaries.value.find` 读取 undefined 的运行时告警；规则检索通过。

## 2026-07-01 DeviceLog 数据分析运行详情折叠第 4 批

- 改动范围：AICopilot 前端消息渲染、运行详情安全摘要组装、工具调用摘要组件、前端单元测试、聊天流 smoke 断言、AICopilot 业务规则、AICopilot.Web 前端规则和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、Cloud 写接口、DataAnalysis 后端执行、SQL guard 白名单、数据源 mapping 或部署配置。
- 改动原因：执行 DeviceLog 数据分析体验改造 Batch 4，在 Batch 1 已有运行状态、Batch 2 已有结构化展示、Batch 3 已有最终回答格式的基础上，把模型、状态、过程、意图、工具和 Widget 摘要收敛进 assistant 消息默认折叠的运行详情，避免主回答继续混入调试信息。
- 影响面：`MessageItem.vue` 只保留主回答、Widget 和审批卡主内容；新增 `MessageRuntimeDetailsPanel.vue` 渲染折叠运行详情；新增 `runtimeDetails.ts` 从本轮 message chunks、metadata 和 `chatRunStatus` 组装安全摘要；`FunctionCallItem.vue` 的非 mini 展示从原始参数/结果改为安全执行摘要；聊天流 smoke 增加运行详情默认折叠和展开后结构化展示摘要断言。
- 关键实现：运行详情展示状态耗时、查询/返回行数、模型 metadata、AgentEvent 安全文案、意图置信度、工具名/状态、白名单业务过滤条件、返回行数/截断状态和 Widget 类型摘要；保持“回答模型：未知”的历史兼容展示，避免破坏既有聊天壳验收；原 Agent Run 使用的 `RuntimeDetailsPanel.vue` 保持无 diff。
- 安全边界：本批不新增查询、不扩大读取范围、不改变审批或 AgentTask 权威状态；运行详情不作为审批、工具执行、Cloud 查询或 Widget 权威来源；工具参数和结果不展开 SQL 原文、连接串、密码、token、sourceName、表/视图名、endpoint、内部字段、原始结果行或未脱敏错误原文。
- 规则提炼：形成长期规则，已写入 `资料/AICopilot业务规则.md` 和 `src/vues/AICopilot.Web/AGENTS.md`：运行详情只能基于本轮 stream/history chunks、消息 metadata 和按会话隔离运行状态生成默认折叠的安全摘要；不得展开内部或敏感工具载荷。
- 验证命令：
  - `npm run test:unit -- tests/unit/runtimeDetails.spec.ts`
  - `npm run type-check`
  - `npm run test:unit`
  - `npm run build`
  - `command -v npx >/dev/null 2>&1`
  - `npm run test:smoke -- --grep "chat stream renders widgets and approval card|inline agent run restores"`
  - `npm run test:smoke -- --grep "chat stream renders widgets and approval card"`
  - `npm run test:smoke`
  - `git diff --check`
  - `rg -n "Runtime Details Folding|运行详情只能基于|运行详情不得展开|MessageRuntimeDetailsPanel|runtimeDetails|原始工具结果|chat stream renders widgets" src/vues/AICopilot.Web/AGENTS.md 资料/AICopilot业务规则.md docs/改动复盘与规则沉淀.md src/vues/AICopilot.Web/src src/vues/AICopilot.Web/tests`
- 验证结果：新增 `runtimeDetails.spec.ts` 4 通过、0 失败；前端 `type-check` 通过；完整前端单元测试 25 个测试文件、111 条测试通过；production build 成功，保留既有 `@vueuse/core` 在 Rolldown 下的 `INVALID_ANNOTATION` warning 但退出码为 0；`git diff --check` 通过；规则检索通过；定向 smoke `chat stream renders widgets and approval card|inline agent run restores` 桌面 2 通过、移动 2 跳过；补充运行详情断言后定向 smoke `chat stream renders widgets and approval card` 桌面 1 通过、移动 1 跳过。完整 `npm run test:smoke` 本轮未全绿：15 通过、4 失败、3 跳过；失败点为既有聊天壳断言“计划模式按钮应有 active class”在桌面/移动未满足，以及 ConfigView agent slot 可见性断言在桌面/移动未满足，同时 ConfigView mock 期间出现 `toolSummaries.value.find` 读取 undefined 的运行时告警；这些失败不在本批运行详情改动链路内，未在本批扩 scope 修复。

## 2026-07-01 DeviceLog 最终回答格式第 3 批

- 改动范围：AICopilot Final Agent DataAnalysis 回答要求、DataAnalysis `answer_contract`、后端 Fake AI 测试桩、AiEval 后端测试、AICopilot 业务规则和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、Cloud 写接口、SQL guard 白名单、数据源 mapping、前端运行状态或部署配置。
- 改动原因：执行 DeviceLog 数据分析体验改造 Batch 3，在 Batch 2 已提供 `display_blocks` 的基础上，收口最终文字回答格式，避免模型或测试桩继续只复述 `semantic_summary`，无法稳定呈现可能原因、建议动作和不能执行的动作边界。
- 影响面：`FinalAgentBuildExecutor` 的 DataAnalysis 要求明确 DeviceLog `display_blocks` 回答顺序为“结论、关键指标、关键记录、可能原因、建议动作、不能直接执行的动作、查询范围”；`DataAnalysisFinalContextFormatter` 的 `answer_contract` 同步携带默认段落和 DeviceLog 展示段落；`FakeAiProviderHost` 在测试中优先从 DeviceLog `display_blocks` 生成固定段落，非 DeviceLog 或无展示块时仍走旧 `semantic_summary` 路径。
- 关键实现：测试桩从 `MetricStripBlock` 取关键指标，从 `EvidenceTableBlock` 取前 1-3 条证据，从 `issue_category_ranking` / `level_distribution` 给出“可能原因”且标注为 AI 推断分析；建议动作只输出现场人工排查建议；“不能直接执行的动作”明确 AICopilot 不会重启设备、修改参数、下发配方、补录/删除日志或写 Cloud。
- 安全边界：本批不新增查询、不扩大 CloudReadOnly 读取范围、不改 SQL guard；最终回答仍只基于本轮 context 事实，继续拒绝 SQL/连接串/sourceName 等内部细节，建议动作不得表达为已执行动作。
- 规则提炼：形成长期规则，已写入 `资料/AICopilot业务规则.md`：DeviceLog 使用 `display_blocks` 时最终回答必须按固定段落顺序组织；可能原因必须标注为 AI 推断分析，建议动作只能是人工排查建议，不能写成已执行的控制、下发、写入或修复动作。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~AiEvalBehaviorGuardrailTests|FullyQualifiedName~AiEvalPromptGuardrailTests"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~SemanticAnalysisRunnerTests|FullyQualifiedName~SemanticSummaryBuilderTests|FullyQualifiedName~AiEvalBehaviorGuardrailTests|FullyQualifiedName~AiEvalPromptGuardrailTests"`
  - `dotnet build AICopilot.slnx --no-restore`
  - `git diff --check`
  - `rg -n "DeviceLog 最终回答|DeviceLog display_blocks|可能原因必须标注为 AI 推断分析|不能直接执行的动作|device_log_display_sections" 资料/AICopilot业务规则.md docs/改动复盘与规则沉淀.md src/services src/tests`
- 验证结果：`AiEvalBehaviorGuardrailTests|AiEvalPromptGuardrailTests` 组合 15 通过、0 失败；`SemanticAnalysisRunnerTests|SemanticSummaryBuilderTests|AiEvalBehaviorGuardrailTests|AiEvalPromptGuardrailTests` 组合 30 通过、0 失败；第一次并行 build 与 test 同跑时 build 最终成功但出现测试 DLL 被占用重试 warning，测试结束后单独重跑 `dotnet build AICopilot.slnx --no-restore` 成功且 0 warning/0 error；`git diff --check` 通过；规则检索通过。

## 2026-07-01 DeviceLog 数据分析结构化展示第 2 批

- 改动范围：AICopilot DataAnalysis 语义 DeviceLog 成功路径、最终 DataAnalysis context、Widget chunk 序列化、前端 Widget normalizer、WidgetRenderer 兼容层、DeviceLog 摘要 profile、后端/前端单元测试、AICopilot 业务规则、AICopilot.Web 前端规则和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、Cloud 写接口、SQL guard 白名单、部署脚本或生产配置。
- 改动原因：执行 DeviceLog 数据分析体验改造 Batch 2，在 Batch 1 已有运行状态的基础上，把查询结果补齐为结构化指标、图表和证据表，避免最终回答仍只能依赖长文本摘要。
- 影响面：`DataAnalysisFinalContextFormatter.FormatSemantic` 对 DeviceLog 追加 `display_blocks`，保留既有 `query_execution`、`semantic_summary` 和 `business_data_preview`；`SemanticAnalysisRunner` 仅在 DeviceLog 语义查询成功后，从本轮已返回行派生并发出 `StatsCard`、3 个 `Chart` 和 `DataTable` Widget chunk；自由 Text-to-SQL Widget chunk 改用统一 serializer 输出稳定 `type/title/description/data`；前端 `WidgetRenderer` 通过 `widgetNormalizer` 兼容 `type` / `Type` / `widget_type` 与 `data` / `Data`。
- 关键实现：新增 `DeviceLogSemanticDisplayBuilder`，只从 `SemanticQueryPlan`、`SemanticSummaryDto` 和 `normalizedRows` 派生日志总数、ERROR/WARN 数、最新时间、级别分布、按小时分布、问题关键词分类和前 20 条证据表；新增 `DataAnalysisWidgetPayloadSerializer`，避免后端 Widget chunk 缺少前端可识别的 `type`；Final Agent prompt 增加 `display_blocks` 使用要求，明确展示块不能被描述为控制动作。
- 安全边界：本批不新增数据库查询、不绕过 CloudReadOnly Direct DB / Cloud AiRead / fallback 的既有只读链路，不扩大 SQL guard 和表列白名单；展示块会清洗疑似 SQL、连接串、密码、内部 source/table 字段和 prompt injection 文本；前端只渲染后端 Widget 数据，不自行编造 DeviceLog 图表或返回行数。
- 规则提炼：形成长期规则，已写入 `资料/AICopilot业务规则.md` 和 `src/vues/AICopilot.Web/AGENTS.md`：DeviceLog 展示块和 Widget 只能从本轮只读查询返回行、`query_execution` 和 `semantic_summary` 派生；不得由模型编造、Markdown 解析、前端假数据或任意图表配置生成。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SemanticAnalysisRunnerTests|FullyQualifiedName~AiEvalBehaviorGuardrailTests"`
  - `npm run test:unit -- widgetNormalizer.spec.ts`
  - `npm run type-check`
  - `npm run test:unit`
  - `npm run build`
  - `dotnet build AICopilot.slnx --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~SemanticAnalysisRunnerTests|FullyQualifiedName~AiEvalBehaviorGuardrailTests|FullyQualifiedName~SemanticSummaryBuilderTests"`
  - `git diff --check`
  - `rg -n "DeviceLog 数据分析展示块|DataAnalysis Widget|widgetNormalizer|display_blocks|前端假数据|任意图表配置" 资料/AICopilot业务规则.md src/vues/AICopilot.Web/AGENTS.md docs/改动复盘与规则沉淀.md src/services src/vues/AICopilot.Web/src src/tests src/vues/AICopilot.Web/tests`
- 验证结果：后端 `SemanticAnalysisRunnerTests|AiEvalBehaviorGuardrailTests` 组合 21 通过、0 失败；补跑 `SemanticAnalysisRunnerTests|AiEvalBehaviorGuardrailTests|SemanticSummaryBuilderTests` 组合 27 通过、0 失败；新增前端 `widgetNormalizer.spec.ts` 3 通过、0 失败；`npm run type-check` 通过；完整前端单元测试 24 个测试文件、107 条测试通过；前端 production build 成功，保留既有 `@vueuse/core` 在 Rolldown 下的 `INVALID_ANNOTATION` warning 但退出码为 0；solution build 成功且 0 warning/0 error；`git diff --check` 通过；规则检索通过。

## 2026-07-01 DeviceLog 数据分析运行状态前置第 1 批

- 改动范围：AICopilot 前端 `src/vues/AICopilot.Web` 的会话级运行状态模型、Chat/Plan 流式入口状态接线、assistant 消息状态条组件、前端单元测试、AICopilot 业务规则、AICopilot.Web 前端规则和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、Cloud 写接口、DataAnalysis 后端执行、SQL guard、部署脚本或生产配置。
- 改动原因：执行 DeviceLog 数据分析体验改造 Batch 1，解决用户发送 DeviceLog 查询后页面长时间无反馈、看起来像卡住的问题；本批只做运行状态前置，不做图表、证据表、最终回答格式大改或 Artifact 平台。
- 影响面：`sendMessage` 和 `planAgentTask` 在创建 assistant 占位消息后立即创建 `understanding` 状态；后续只根据本轮 stream chunk 推进 `querying`、`answering`、`completed` 或 `failed`。状态按 `sessionId + messageKey` 绑定，切换会话不会串状态；删除会话和全局 reset 会清理状态。`FunctionResult` 只从真实结构化 result 中读取 `rowCount` / `returnedRowCount` / `rows.length` / `items.length` 等事实，拿不到时不显示返回行数。
- 关键实现：新增 `chatRunStatusStore`，用 `SessionScopedState.chatRunStatus` 作为会话状态入口；新增 `ChatRunStatusStrip.vue` 在 assistant 消息顶部显示“运行中/完成/失败”、耗时、阶段摘要、查询次数和返回行数；`MessageItem.vue` 只按当前消息读取状态，不在组件内解析 SSE。`approval_pending` 这类会移除占位消息的情况会清掉对应运行状态，避免孤立状态条。
- 规则提炼：形成长期规则，已写入 `资料/AICopilot业务规则.md` 和 `src/vues/AICopilot.Web/AGENTS.md`：AI 对话中任何可能超过 1 秒的工具调用、DataAnalysis 或 Cloud 只读查询必须有用户可见、按会话隔离的运行状态；状态只能来自本轮执行事实，不能伪造进度、查询次数、返回行数或查询成功。
- 验证命令：
  - `npm run type-check`
  - `npm run test:unit`
  - `command -v npx`
  - `bash "$PWCLI" open http://127.0.0.1:5174/ && bash "$PWCLI" snapshot`
  - `npm run build`
  - `git diff --check`
- 验证结果：前端 `type-check` 通过；前端单元测试 23 个测试文件、104 条测试全部通过；Vite dev server 在 `http://127.0.0.1:5174/` 成功启动，Playwright 打开页面并渲染到登录页，因本地后端初始化状态不可达仅能验证前端壳加载，不能触发真实登录后的聊天流视觉验收；production build 成功，保留 `@vueuse/core` 在 Rolldown 下的 `INVALID_ANNOTATION` warning 但构建退出码为 0；`git diff --check` 通过。

## 2026-07-01 DeviceLog 追问 scope 优先级修复

- 改动范围：AICopilot 意图路由追问重写器、DeviceLog 追问重写测试和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、Cloud 写接口、数据库授权 SQL、部署脚本或生产配置。
- 改动原因：CC 审核指出上一轮追问重写只显式覆盖当前追问的日志级别，未显式覆盖当前追问的新设备/工序范围；当上一轮是“模切设备”，追问“那涂布设备的警告呢”时，拼接 queryText 可能继续优先继承旧“模切”范围，影响用户体验。
- 影响面：仍保持 `Analysis.DeviceLog.*` intent 不拆不重命名；仍使用确定性规则，不引入 LLM 判断追问；仍只生成只读语义 filter，不新增 Cloud 写路径，不新增设备编码精确解析。
- 关键实现：`DeviceLogFollowUpIntentRewriter` 从当前追问中提取 `xxx设备/xxx工序/xxx机台` 范围；当前追问有 scope 时显式写入 `processName contains <scope>` filter，使当前 scope 优先于历史 scope；当前追问没有 scope 时保持从上一轮 DeviceLog seed question 继承范围。追问判定同时支持“那涂布设备呢”这类 scope-only 短追问，但仍要求存在上一轮 DeviceLog seed question，避免无上下文误判。
- 规则提炼：无新增长期规则，原因是本次属于既有长期规则“DataAnalysis 追问其他日志级别、设备、工序或时间窗口时必须重新查询，并基于本轮执行事实回答”的缺陷修复；规则已存在于工作区 `../docs/总规则.md`、项目 `AGENTS.md` 和 `资料/AICopilot业务规则.md`。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~DeviceLogFollowUpIntentRewriterTests|FullyQualifiedName~SemanticQueryPlannerTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~AiEvalBehaviorGuardrailTests|FullyQualifiedName~SemanticAnalysisRunnerTests" --no-restore --logger "console;verbosity=minimal"`
  - `dotnet build AICopilot.slnx --no-restore`
  - `git diff --check`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --logger "console;verbosity=minimal" --logger "trx;LogFileName=artifacts/test-results/backend-full-device-log-scope-followup.trx"`
- 验证结果：DeviceLogFollowUpIntentRewriter + SemanticQueryPlanner 组合 44 通过、0 失败；AiEvalBehaviorGuardrail + SemanticAnalysisRunner 组合 19 通过、0 失败；solution build 成功且 0 warning/0 error；`git diff --check` 通过；完整 BackendTests 723 通过、0 失败、0 跳过，耗时 4 m 45 s，结果文件为 `src/tests/AICopilot.BackendTests/TestResults/artifacts/test-results/backend-full-device-log-scope-followup.trx`。

## 2026-07-01 DeviceLog 查询语义与追问重查闭环

- 改动范围：AICopilot DataAnalysis 语义补全、DeviceLog 语义定义、真实/模拟物理 mapping、RealSource 模板、语义 SQL 生成、意图路由追问重写、最终 DataAnalysis 上下文、Final Agent 回答要求、CloudReadOnly Text-to-SQL governed schema 元数据、后端/架构测试、项目规则和工作区总规则。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、Cloud 写接口、数据库授权 SQL 或部署发布脚本。
- 改动原因：线上已确认 Cloud PostgreSQL readonly 数据可查，最近 1 天模切日志真实存在 `ERROR=608`、`WARN=173`、`INFO=594`，但 AICopilot 仍出现 Error-only、大小写不匹配、缺时间窗口、缺工序/设备过滤、追问不重查和最终回答基于旧文本推断的问题。根因不是模型上下文长度，而是语义补全、路由追问和最终证据边界没有形成闭环。
- 影响面：`Analysis.DeviceLog.*` intent 保持不拆不重命名；CloudReadOnly 仍只读，仍走白名单表/列、参数化 SQL、AST guard、BusinessQuery safety policy 和 hash-only 审计；本次只扩大 DeviceLog 语义可表达范围，不放宽 Cloud 写边界或数据源权限。
- 关键实现：`SemanticIntentQueryCompleter` 将日志级别统一为数据库真实值 `ERROR/WARN/INFO`，支持 `ERROR,WARN` 多级别 `IN` 查询，解析 `最近1天/24小时/7天` 等相对时间，识别“模切设备/涂布工序”类自然语言范围为 `processName contains`；DeviceLog 定义、mapping、RealSource 和 readiness contract 暴露 `deviceName/processName`，真实 SQL 复用既有 `device_logs -> devices -> mfg_processes` join，PostgreSQL contains 使用参数化 `ILIKE`。
- 追问闭环：新增 `DeviceLogFollowUpIntentRewriter`，在路由模型返回后识别“警告信息呢/那正常信息呢”等追问，继承上一轮 DeviceLog 的工序/时间范围，但用当前追问的日志级别覆盖 filter，强制生成或覆盖 `Analysis.DeviceLog.*` 意图，确保 DataAnalysis 分支重新查询，而不是让 Final Agent 从上一轮回答里猜。
- 证据边界：`DataAnalysisFinalContextFormatter` 新增 `query_execution`，记录本轮语义 target/kind、filters、timeRange、limit、returnedRowCount 和 evidence boundary；Final Agent prompt 要求回答“是否有错误/警告/正常信息”前必须核对 `query_execution.filters`，未覆盖的级别或时间范围不得回答为有/没有。
- Text-to-SQL 元数据：`CloudReadOnlyGovernedSchema` 新增 `AllowedColumnValueHints`，LLM fallback 输入为 `device_logs.level` 提供 `ERROR/WARN/INFO` uppercase 枚举提示；仍不提供连接串、凭据、样例数据、参数值、查询结果或非白名单对象。
- 规则提炼：形成长期规则，已写入工作区 `../docs/总规则.md`、项目 `AGENTS.md` 和 `资料/AICopilot业务规则.md`：DeviceLog 必须使用真实日志级别枚举；多级别异常分析必须显式查询覆盖级别；DataAnalysis 追问必须重新查询；最终回答必须基于本轮 `query_execution` 证据，不得用上一轮文本推断未查询数据。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter FullyQualifiedName~SemanticQueryPlannerTests --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SemanticQueryPlannerTests|FullyQualifiedName~SemanticDefinitionTests|FullyQualifiedName~ConfiguredSemanticPhysicalMappingProviderTests|FullyQualifiedName~SemanticSqlGenerationTests" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~DeviceLogFollowUpIntentRewriterTests|FullyQualifiedName~SemanticQueryPlannerTests" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~DeviceLogFollowUpIntentRewriterTests|FullyQualifiedName~AiEvalBehaviorGuardrailTests|FullyQualifiedName~SemanticAnalysisRunnerTests" --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter FullyQualifiedName~CloudReadOnlyLlmTextToSqlGeneratorTests --no-restore`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --filter FullyQualifiedName~ArchitectureBoundaryTests --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SemanticQueryPlannerTests|FullyQualifiedName~SemanticDefinitionTests|FullyQualifiedName~ConfiguredSemanticPhysicalMappingProviderTests|FullyQualifiedName~SemanticSqlGenerationTests|FullyQualifiedName~DeviceLogFollowUpIntentRewriterTests|FullyQualifiedName~AiEvalBehaviorGuardrailTests|FullyQualifiedName~SemanticAnalysisRunnerTests|FullyQualifiedName~CloudReadOnlyLlmTextToSqlGeneratorTests" --no-restore`
  - `dotnet build AICopilot.slnx --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --logger "console;verbosity=minimal" --logger "trx;LogFileName=artifacts/test-results/backend-full-device-log.trx"`
- 验证结果：Planner 单项从 32 通过扩展到 38 通过；DeviceLog 定义/mapping/SQL 组合 54 通过、0 失败；追问重写 + Planner 41 通过、0 失败；追问/上下文/语义执行组合 21 通过、0 失败；CloudReadOnly LLM generator 2 通过、0 失败；ArchitectureBoundaryTests 58 通过、0 失败；最终聚合后端专项 77 通过、0 失败；solution build 成功且 0 warning/0 error；完整 BackendTests 720 通过、0 失败、0 跳过，耗时 4 m 22 s，结果文件为 `src/tests/AICopilot.BackendTests/TestResults/artifacts/test-results/backend-full-device-log.trx`。

## 2026-06-30 CloudReadOnly PostgreSQL 权限漂移修复闭环

- 改动范围：AICopilot CloudReadOnly readonly role 授权 SQL、授权 apply/check 脚本、手动 provisioning workflow、服务器 `deploy-release.sh` preflight、本机 `local-release.sh` 部署文件同步、`CloudReadOnlyGovernedSchema` 元数据、LLM Text-to-SQL 输入、prompt version、权限错误分类、语义分析错误展示、架构/后端测试、项目规则和部署文档。未修改 CloudPlatform 业务代码、Cloud 业务数据、Cloud 写接口或 EdgeClient。
- 改动原因：线上 AICopilot 已能直连 Cloud PostgreSQL，但 readonly role 缺少 `mfg_processes` 的 `SELECT` 权限，导致“模切设备最近 1 天日志”类请求被包装成数据源不可用。根因不是网络或 OIDC 权限，而是治理白名单、授权脚本、探针和部署前检查没有形成同一权威源，历史 workflow 内联 GRANT 清单容易漂移。
- 影响面：CloudReadOnly 仍只允许读取 `devices`、`mfg_processes`、`device_logs`、`hourly_capacity`、`pass_station_records` 五张白名单表，不新增任何 Cloud 写权限；SQL guard 和 BusinessQuery safety policy 仍使用表/列白名单；部署启用 direct DB 时会先验证 readonly role 权限，失败即停止部署。
- 线上止血：已在 `<aicopilot-prod-host>` 的 Cloud PostgreSQL 容器中对 `aicopilot_cloud_readonly` 执行表级 `GRANT SELECT ON TABLE public.mfg_processes`，随后验证五张白名单表 `SELECT` 全部为 true，五张表 `INSERT/UPDATE/DELETE` 全部为 false，`public` schema `CREATE` 为 false。
- 关键实现：新增 `deploy/enterprise-ai/cloud-readonly/apply-readonly-grants.sql` 和 `check-readonly-grants.sql` 作为唯一授权/探针 SQL；新增 `deploy/enterprise-ai/scripts/apply-cloud-readonly-grants.sh` 和 `check-cloud-readonly-grants.sh`，支持 dry-run、解析服务器 `.env`、定位 Cloud PostgreSQL 容器并 fail-fast 校验；`aicopilot-provision-cloud-readonly-db-role` workflow 改为 checkout 后读取同一 SQL 文件，禁止继续维护内联 GRANT 清单；`deploy-release.sh` 在 direct DB 启用时先执行 readonly preflight；`local-release.sh` 发布前同步 `cloud-readonly/` 和脚本到服务器，并在远端解包后显式 `chmod +x` 部署支撑脚本，避免 Git filemode 或 macOS xattr 造成清理脚本不可执行。
- LLM 元数据：`CloudReadOnlyGovernedSchema` 新增列类型和 join hints，`CloudReadOnlyLlmTextToSqlGenerator` 只把治理 schema 中的 `columns`、`columnTypes`、`joinHints` 提供给模型，prompt version 升到 6，要求生成 join 时优先使用治理元数据，不猜测 uuid/integer/bigint 关系。
- 权限诊断：CloudReadOnly 执行异常如果是 `permission denied`，只暴露治理白名单内的表名，例如 `mfg_processes`；连接串、role、密码、SQL 原文和非白名单对象不会进入用户可见诊断。语义分析层对该类错误返回“CloudReadOnly 只读权限不足”而不是泛化为数据源不可用。
- 规则提炼：形成长期规则，已写入 `AGENTS.md`、`资料/AICopilot业务规则.md`、`AICopilot 项目部署与维护指南.md` 和 `deploy/enterprise-ai/README.md`：Cloud PostgreSQL readonly role 授权的权威载体只能是 `deploy/enterprise-ai/cloud-readonly/*.sql`；生产只允许显式表级白名单 `GRANT SELECT`，不得使用 `GRANT SELECT ON ALL TABLES`、默认权限、未来表自动授权或列级/表级混用；新增治理表/列/join 必须同步 governed schema、授权 SQL、探针、preflight、RealSource、架构测试和文档。
- 验证命令：
  - `bash -n deploy/enterprise-ai/scripts/apply-cloud-readonly-grants.sh deploy/enterprise-ai/scripts/check-cloud-readonly-grants.sh scripts/Provision-AICopilotCloudReadOnlyDbRole.sh`
  - `./deploy/enterprise-ai/scripts/apply-cloud-readonly-grants.sh --env-file "$tmp_env" --dry-run`
  - `./deploy/enterprise-ai/scripts/check-cloud-readonly-grants.sh --env-file "$tmp_env" --dry-run`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore --filter "FullyQualifiedName~CloudReadOnlyReadonlyGrantSources_ShouldStayAlignedWithGovernedSchema" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~CloudReadOnlyLlmTextToSqlGeneratorTests|FullyQualifiedName~PromptGovernanceTests" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore --filter "FullyQualifiedName~CloudReadOnlyTextToSql_ShouldExposeOnlyGovernedSchemaAndKeepRepairSqlInMemory|FullyQualifiedName~CloudReadOnlyReadonlyGrantSources_ShouldStayAlignedWithGovernedSchema" --logger "console;verbosity=minimal"`
  - `bash -n deploy/enterprise-ai/deploy-release.sh deploy/enterprise-ai/local-release.sh deploy/enterprise-ai/scripts/apply-cloud-readonly-grants.sh deploy/enterprise-ai/scripts/check-cloud-readonly-grants.sh`
  - `./deploy/enterprise-ai/local-release.sh --services httpapi --ssh-target <ssh-user>@<aicopilot-prod-host> --dry-run`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~CloudReadOnlyTextToSqlRepairClassifierTests|FullyQualifiedName~SemanticAnalysisRunnerTests" --logger "console;verbosity=minimal"`
  - `dotnet build AICopilot.slnx --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~CloudReadOnly|FullyQualifiedName~CloudReadonly|FullyQualifiedName~DataAnalysisReadOnlyGuardrailTests|FullyQualifiedName~ConfiguredSemanticPhysicalMappingProviderTests|FullyQualifiedName~PromptGovernanceTests|FullyQualifiedName~SecurityHardeningTests" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore --logger "console;verbosity=minimal"`
  - `pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh ./scripts/Test-ArchitectureBoundaries.ps1`
  - `pwsh ./scripts/Test-TextEncoding.ps1`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --logger "console;verbosity=minimal"`
  - `git diff --check`
- 验证结果：Shell 语法检查通过；CloudReadOnly apply/check 脚本 dry-run 通过；local-release dry-run 会同步 deploy 文件并打印服务器命令；授权源架构测试 1 通过、0 失败；LLM generator/prompt 测试 17 通过、0 失败；CloudReadOnly 架构组合测试 2 通过、0 失败；权限分类和语义分析组合测试 23 通过、0 失败；后端 CloudReadOnly/Security 组合回归 161 通过、0 失败；完整 ArchitectureTests 58 通过、0 失败；solution build 成功且 0 warning/0 error；Baseline/Architecture/TextEncoding 三个脚本通过；第一次完整 BackendTests 受并发跨项目 AppHost 测试影响出现 708 通过、1 个 AppHost 启动超时，失败用例单独重跑 1 通过、0 失败；第二次完整 BackendTests 709 通过、0 失败；`git diff --check` 通过。首次生产发布已拉起新镜像并通过 Web probe，但暴露 `post-release-cleanup.sh` 远端执行权限问题，已通过 `local-release.sh` 远端 `chmod +x` 修正；第二次生产发布暴露服务器缺少 Harbor API 凭据时 post-release cleanup 误判为失败，已改为无凭据默认跳过 Harbor retention/GC、显式 required 时才阻断；最终生产发布使用 `DEPLOY_SSH_TARGET=<ssh-user>@<aicopilot-prod-host> ./deploy/enterprise-ai/local-release.sh --services httpapi,migration,dataworker,ragworker,web`，已部署 release `sha-1fcb23445d86ea6d60628799d5eeaea4496b9c53`，服务器 `current-release.env` 五个镜像均指向该 tag，Web probe `http://127.0.0.1:82/` 返回 200，Cloud OIDC 状态接口返回 enabled，post-release cleanup 成功完成且在缺少 Harbor API 凭据时按规则跳过 Harbor retention/GC；线上用 `aicopilot_cloud_readonly` 只读账号验证 `device_logs -> devices -> mfg_processes` join，最近 1 天模切日志计数为 `ERROR=608`、`WARN=173`、`INFO=594`，确认 `mfg_processes` 权限漂移已修复。

## 2026-06-30 AICopilot 本机标准发布流程重写

- 改动范围：AICopilot `deploy/enterprise-ai/build-and-push.sh`、新增 `local-release.sh`、`aicopilot-image` / `aicopilot-deploy` workflow、部署指南、`deploy/enterprise-ai/README.md`、`SecurityHardeningTests`、项目规则 `AGENTS.md`、工作区上传部署总览/总规则/历史核心记录。未修改 AICopilot 业务运行代码、Cloud 业务数据、Cloud 写接口、真实 `.env` 或服务器 `deploy-release.sh`。
- 改动原因：旧日常发布依赖 self-hosted runner image workflow，出现构建阶段卡住和等待超时问题；用户明确要求 Cloud/AICopilot 统一改为“先 push GitHub -> 本机构建镜像 -> 推 Harbor -> 本机 SSH 触发服务器 deploy-release.sh -> 服务器 pull/restart/health/cleanup”。
- 影响面：AICopilot 日常发布入口改为本机 `deploy/enterprise-ai/local-release.sh --services <services>` 或显式 `--all`；`aicopilot-image` / `aicopilot-deploy` 只保留带确认词的灾备入口；服务器端 `deploy-release.sh` 继续作为唯一部署入口；本机构建不会在部署健康检查通过前删除当前生产 tag。
- 关键实现：`build-and-push.sh` 增加显式 `--services httpapi,migration,dataworker,ragworker,web`、`--all`、`--dry-run`、服务去重、portable timeout、Harbor 登录检查和固定输出 `Release tag` / `Deploy services input` / `AICOPILOT_*_IMAGE` / `artifacts/deploy/aicopilot-built-services.txt`；新增 `local-release.sh` 校验工作区干净、HEAD 已推送、SSH target 显式传入，并执行 `DEPLOY_GIT_SHA=<sha> DEPLOY_TRIGGERED_BY=local ./deploy-release.sh sha-<sha> --services <services>`；workflow push 日常触发被移除并要求 `EMERGENCY_AICOPILOT_IMAGE_BUILD` / `EMERGENCY_AICOPILOT_DEPLOY`。
- 规则提炼：形成长期部署规则，已写入工作区 `docs/总规则.md`、`docs/上传部署总览.md`、`docs/历史核心记录.md` 和 AICopilot `AGENTS.md` / 部署指南 / `deploy/enterprise-ai/README.md`：AICopilot 生产发布不得等待 GitHub image/deploy workflow，本机构建、Harbor 检查和 SSH deploy 必须 fail-fast；超时后停止诊断 Docker buildx、Harbor tag、服务器 compose/logs 和 release 状态，不得继续 watch 或无限等待。
- 验证命令：
  - `bash -n deploy/enterprise-ai/build-and-push.sh deploy/enterprise-ai/local-release.sh deploy/enterprise-ai/deploy-release.sh`
  - `ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f); puts "YAML ok: #{f}" }' .github/workflows/aicopilot-image.yml .github/workflows/aicopilot-deploy.yml`
  - `./deploy/enterprise-ai/build-and-push.sh --dry-run --services httpapi,httpapi,web`
  - `if ./deploy/enterprise-ai/build-and-push.sh --dry-run --services nope; then exit 1; else echo 'AICopilot invalid service rejected'; fi`
  - `./deploy/enterprise-ai/local-release.sh --dry-run --services httpapi --ssh-target <ssh-user>@<aicopilot-prod-host>`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter SecurityHardeningTests --logger "console;verbosity=minimal"`
  - `rg` 检查旧标准入口描述和 fail-fast 规则。
- 验证结果：Shell 语法检查通过；AICopilot workflow YAML 解析通过；dry-run 服务去重、非法服务拒绝、本机 SSH 命令打印通过；`SecurityHardeningTests` 64 通过、0 失败；检索确认旧 `aicopilot-image` 日常标准入口描述已替换为灾备口径。

## 2026-06-30 AICopilot 后端镜像 Harbor runtime base 修正

- 改动范围：AICopilot `aicopilot-image` workflow、`deploy/enterprise-ai/Dockerfile.backend-runtime`、`build-and-push.sh`、`mirror-base-images.sh`、AICopilot 部署指南、`deploy/enterprise-ai/README.md`、`SecurityHardeningTests`、项目规则 `AGENTS.md` 和本滚动复盘。未修改 CloudPlatform、EdgeClient、Cloud 业务数据、AICopilot 业务运行代码或真实 `.env`。
- 改动原因：执行提交部署时，push 触发的 `aicopilot-image` run `28418679071` 在 `Publish backend images` 失败，根因是后端 Docker runtime base 默认仍为 `mcr.microsoft.com/dotnet/aspnet:10.0-noble`，内网 runner 直接拉 MCR blob 失败；这也不符合生产基础镜像必须从 Harbor mirror 获取的部署规则。
- 影响面：只影响 AICopilot 后端镜像构建链路。生产构建默认改用 `<aicopilot-prod-host>:80/enterprise-ai/base-dotnet-aspnet:10.0-noble` / `$image_prefix/base-dotnet-aspnet:10.0-noble`，应急构建同样默认走 `BASE_IMAGE_PREFIX`；`mirror-base-images.sh` 新增 MCR 源镜像到 Harbor 的同步项。运行时服务、CloudReadOnly 只读边界和部署 `.env` secret 注入逻辑不变。
- 关键实现：`aicopilot-image.yml` 的 `RUNTIME_BASE_IMAGE` 默认改为 Harbor `base-dotnet-aspnet:10.0-noble`；`Dockerfile.backend-runtime` 默认 ARG 改为 Harbor runtime base；`build-and-push.sh` 默认 `DOTNET_RUNTIME_BASE_IMAGE` 改为 `$BASE_IMAGE_PREFIX/base-dotnet-aspnet:10.0-noble`；`mirror-base-images.sh` 新增 `mcr.microsoft.com/dotnet/aspnet:10.0-noble -> base-dotnet-aspnet:10.0-noble`；部署文档补充 Docker Hub/MCR 均不是生产依赖源；安全测试锁住 workflow、Dockerfile 和应急脚本不得默认回退到 MCR。
- 规则提炼：形成长期规则，已写入 `AGENTS.md` 和 AICopilot 部署文档：AICopilot 生产 Docker 构建必须使用 Harbor mirror 的基础镜像，包括 .NET ASP.NET runtime、Node、Nginx、PostgreSQL、RabbitMQ 和 Qdrant；workflow、Dockerfile 和应急构建脚本不得默认从 Docker Hub 或 MCR 拉生产基础镜像。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter SecurityHardeningTests --logger "console;verbosity=minimal"`
  - `bash -n deploy/enterprise-ai/build-and-push.sh && bash -n deploy/enterprise-ai/mirror-base-images.sh && bash -n deploy/enterprise-ai/deploy-release.sh`
  - `ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f); puts "YAML ok: #{f}" }' .github/workflows/aicopilot-image.yml .github/workflows/aicopilot-deploy.yml`
  - `pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh ./scripts/Test-ArchitectureBoundaries.ps1`
  - `pwsh ./scripts/Test-TextEncoding.ps1`
  - `rg -n "mcr.microsoft.com/dotnet/aspnet|base-dotnet-aspnet|RUNTIME_BASE_IMAGE|DOTNET_RUNTIME_BASE_IMAGE|Docker Hub 不作为生产依赖源|MCR 也不得" .github deploy "AICopilot 项目部署与维护指南.md" AGENTS.md docs/改动复盘与规则沉淀.md src/tests/AICopilot.BackendTests/SecurityHardeningTests.cs`
  - `git diff --check`
- 验证结果：`SecurityHardeningTests` 64 通过、0 失败；`build-and-push.sh`、`mirror-base-images.sh`、`deploy-release.sh` 语法检查通过；`aicopilot-image.yml` 和 `aicopilot-deploy.yml` YAML 解析通过；Baseline/Architecture/TextEncoding 三个脚本通过；检索确认 MCR 只作为 mirror 源和复盘/测试说明出现，生产 workflow、Dockerfile 和应急构建默认都指向 Harbor runtime base；`git diff --check` 通过。

## 2026-06-30 CloudReadOnly Text-to-SQL 规则与架构测试补充

- 改动范围：AICopilot 项目规则 `AGENTS.md`、业务规则 `资料/AICopilot业务规则.md`、架构测试 `ArchitectureBoundaryTests` 和本滚动复盘。未修改运行代码、CloudPlatform、EdgeClient、Cloud 写接口、Cloud 业务数据或部署发布配置。
- 改动原因：收口 CC 对上一轮 CloudReadOnly LLM Text-to-SQL 改造的 P1/P2 补充意见：LLM prompt 暴露物理 schema 不是 bug，但必须形成规则边界；`PreviousSqlForRepair` 只在内存回传给 LLM 的约束需要架构测试锁住；完整 BackendTests 通过数需要标注测试环境依赖。
- 影响面：无运行行为变化。CloudReadOnly fallback 仍走 LLM 结构化生成、双层 SQL guard 和 hash-only 审计；`SemanticIntentQueryCompleter` 仍按当前策略只闭环 DeviceLog 语义补全，本次不扩大补全范围。
- 关键实现：规则明确 CloudReadOnly Text-to-SQL LLM prompt 可见的物理 schema 只能来自 `CloudReadOnlyGovernedSchema` 治理白名单，最多包含批准表名、列名和必要业务描述，不得暴露连接串、凭据、role/权限细节、样例数据、查询结果、参数值、非白名单表字段或系统/敏感字段；新增架构测试 `CloudReadOnlyTextToSql_ShouldExposeOnlyGovernedSchemaAndKeepRepairSqlInMemory`，锁住 `PreviousSqlForRepair` 只能出现在 generation request、fallback runner 和 LLM generator 三个生产文件中，禁止进入 audit、result、repair attempt 原文字段或其他持久化/状态路径。
- 复盘口径补充：上一条记录中的完整 `AICopilot.BackendTests` 707 通过，是在本机具备该测试套件所需 PostgreSQL、RabbitMQ/AppHost 等依赖的环境下执行；未具备这些依赖的本机或 CI 环境不得复用该“完整全绿”口径。
- 规则提炼：形成长期规则，已写入 `AGENTS.md` 和 `资料/AICopilot业务规则.md`：CloudReadOnly Text-to-SQL LLM prompt 只能接收治理白名单 schema 元数据和安全修复上下文，不能接收连接串、凭据、数据样本、参数值、查询结果、非白名单对象或系统/敏感字段。
- 验证命令：
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore --filter "FullyQualifiedName~CloudReadOnlyTextToSql_ShouldExposeOnlyGovernedSchemaAndKeepRepairSqlInMemory" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore --logger "console;verbosity=minimal"`
  - `pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh ./scripts/Test-ArchitectureBoundaries.ps1`
  - `pwsh ./scripts/Test-TextEncoding.ps1`
  - `rg -n "LLM prompt 可见的物理 schema|PreviousSqlForRepair|CloudReadOnlyTextToSql_ShouldExposeOnlyGovernedSchemaAndKeepRepairSqlInMemory|PostgreSQL、RabbitMQ/AppHost" AGENTS.md 资料/AICopilot业务规则.md docs/改动复盘与规则沉淀.md src/tests/AICopilot.ArchitectureTests/ArchitectureBoundaryTests.cs`
  - `git diff --check`
- 验证结果：新增架构测试 1 通过、0 失败；完整 `AICopilot.ArchitectureTests` 57 通过、0 失败；Baseline/Architecture/TextEncoding 三个脚本通过；规则检索通过；`git diff --check` 通过。

## 2026-06-30 CloudReadOnly LLM Text-to-SQL fallback 修正闭环

- 改动范围：AICopilot contracts、AiGateway LLM Text-to-SQL generator、CloudReadOnly fallback runner、内置 conversation template、model endpoint pool scope、DataAnalysis/CloudReadOnly 双层 SQL safety、Agent business query 传递链、后端测试、项目规则 `AGENTS.md` 与 `资料/AICopilot业务规则.md`。未修改 CloudPlatform、EdgeClient、Cloud 写接口、Cloud 业务数据或部署发布配置。
- 改动原因：收口 CloudReadOnly fallback 审核问题。上一版生产 fallback 退化为规则 SQL 模板，且重试次数、查询 timeout/MaxRows、列白名单双层覆盖和复盘口径不够严谨；本轮按“LLM 看到错误后重新生成 SQL，而不是替换成写死模板”的目标修正。
- 影响面：强语义 intent 和 Direct DB SQL 仍优先，`Analysis.*` intent 未拆分或重命名；CloudReadOnly fallback 只在强语义规划、SQL 生成、guard 或执行失败后进入；Agent `query_business_database_readonly` 显式选择 CloudReadOnly 时仍走同一 governed runner。Legacy `DataSourceSelectionMode.TextToSql` / Business Text-to-SQL draft runtime 仍只服务 SimulationBusiness。
- 关键实现：删除生产 `CloudReadOnlyRuleBasedTextToSqlGenerator` 路径，新增 `CloudReadOnlyLlmTextToSqlGenerator` 和内置模板 `cloud_readonly_text_to_sql`，通过 `ConfiguredAgentRuntimeFactory -> CreateSessionAsync -> RunStructuredAsync<T>` 做无工具、temperature 0、结构化 JSON 生成；新增 `CloudReadOnlyTextToSqlOptions`，默认修复重试 3 次、硬上限 5 次，并配置化 `MaxRows`、`CommandTimeoutSeconds` 和 `MaxOutputTokens`；`CloudReadOnlyTextToSqlFallbackRunner` 支持参数化 SQL，把参数传给只读 executor；上一轮失败 SQL 只在当前调用内以内存字段 `PreviousSqlForRepair` 回传给 LLM，不进入 audit/state/result；timeout、权限、凭据、非只读、系统表、敏感字段、多语句和写 SQL 默认不可修复、不重试。
- 安全边界：CloudReadOnly fallback 必须满足已启用、只读、`ReadOnlyCredentialVerified=true`、共享 schema 白名单、SELECT-only guard、系统表/敏感字段/多语句/写动词拒绝、只读 connector 执行和 hash-only 审计；新增 `SqlAllowlistColumnInspector` 基于 `SqlParserCS` AST 覆盖 projection、where、join on/using、group by、having、order by、函数参数和 LATERAL 派生子查询，`CloudReadOnlySemanticSqlGuard` 与 `BusinessReadonlyQuerySafetyPolicy` 双层执行表/列白名单。
- 保留/删除清单：保留 `CloudReadOnlyGovernedSchema`、`SemanticIntentQueryCompleter`、metadata whitelist、`HasExecutableGovernedSchema`、hash-only repair/audit、AgentTaskRuntime 到 AgentRuntimeBusinessQueryToolService 的传递链；删除生产规则 SQL 模板 fallback；测试 fake generator 仅作为单测桩保留。
- P2 清理：测试假连接串和 AiEval 测试资产中的旧 secret 假口令已改为 `Password=fake-test-only`，保留脱敏测试覆盖但降低安全扫描误报概率。
- 规则提炼：形成长期规则，已写入 `AGENTS.md` 和 `资料/AICopilot业务规则.md`：CloudReadOnly fallback 必须是 LLM 结构化生成，不能用生产规则 SQL 模板替代；必须双层表/列白名单；timeout 等边界错误不可修复不重试；上一轮失败 SQL 只允许当前调用内存回传给 LLM，不能持久化。
- 验证命令：
  - `dotnet restore AICopilot.slnx`
  - `dotnet build AICopilot.slnx --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~CloudReadOnlyLlmTextToSqlGeneratorTests|FullyQualifiedName~PromptGovernanceTests" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~CloudReadOnlyTextToSqlFallbackRunnerTests|FullyQualifiedName~CloudReadOnlyTextToSqlRepairClassifierTests|FullyQualifiedName~CloudReadOnlyLlmTextToSqlGeneratorTests" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~CloudReadOnlySemanticSqlGuardTests|FullyQualifiedName~DataAnalysisReadOnlyGuardrailTests|FullyQualifiedName~SemanticAnalysisRunnerTests.RunAsync_ShouldAllowCloudReadOnlySemanticSql_WithPostgresLateralJoin" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~Semantic|FullyQualifiedName~CloudReadOnly|FullyQualifiedName~CloudReadonly|FullyQualifiedName~SqlGuard|FullyQualifiedName~TextToSql|FullyQualifiedName~DataAnalysisReadOnlyGuardrailTests|FullyQualifiedName~AgentRuntimeBusinessQueryToolServiceTests|FullyQualifiedName~PromptGovernanceTests" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.AiEvalTests/AICopilot.AiEvalTests.csproj --no-restore --filter "FullyQualifiedName~GoldenCaseTests" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore --logger "console;verbosity=minimal"`
  - `pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh ./scripts/Test-ArchitectureBoundaries.ps1`
  - `pwsh ./scripts/Test-TextEncoding.ps1`
  - `git diff --check`
  - `rg -n "CloudReadOnly Text-to-SQL|LLM 结构化生成|规则 SQL 模板|PreviousSqlForRepair|BusinessQuery safety policy|hash-only" AGENTS.md 资料/AICopilot业务规则.md docs/改动复盘与规则沉淀.md src/services src/tests`
- 验证结果：`CloudReadOnlyLlmTextToSqlGeneratorTests|PromptGovernanceTests` 17 通过、0 失败；`CloudReadOnlyTextToSqlFallbackRunnerTests|CloudReadOnlyTextToSqlRepairClassifierTests|CloudReadOnlyLlmTextToSqlGeneratorTests` 20 通过、0 失败；`CloudReadOnlySemanticSqlGuardTests|DataAnalysisReadOnlyGuardrailTests|SemanticAnalysisRunnerTests.RunAsync_ShouldAllowCloudReadOnlySemanticSql_WithPostgresLateralJoin` 7 通过、0 失败；Cloud/Semantic/TextToSql/DataAnalysis/Agent business query/PromptGovernance 组合回归 203 通过、0 失败；AiEval `GoldenCaseTests` 6 通过、0 失败；完整 `AICopilot.BackendTests` 707 通过、0 失败（本机具备该测试套件所需 PostgreSQL、RabbitMQ/AppHost 等依赖；未具备依赖的环境不得复用该全绿口径）；`AICopilot.ArchitectureTests` 56 通过、0 失败；`dotnet build AICopilot.slnx --no-restore` 成功且 0 warning/0 error；Baseline/Architecture/TextEncoding 三个脚本通过；`git diff --check` 通过；规则检索通过。

## 2026-06-29 管道收口与全量验证闭环

- 改动范围：AICopilot 前端错误契约文档、AiEval 语义预览测试期望、后端测试夹具与 stream 授权集成测试、PR CI workflow、项目规则 `AGENTS.md`、`资料/AICopilot业务规则.md`、滚动复盘。未修改 CloudPlatform、EdgeClient、业务 handler、事务拥有者或 `AgentWorkflowPipeline` 行为。
- 改动原因：收口前一轮 MediatR 管道和 Direct DB 架构清理的交付缺口。前序验证只跑 filtered 子集，遗漏 `request_validation_failed` 未同步前端错误契约的问题；同时需要把 stream 权限种子和 CI 全量验证口径补齐，避免再次以局部测试结果声明完成。
- 影响面：运行行为只受既有管道改动影响，本批不新增架构能力。文档契约新增 `request_validation_failed` 错误码说明；AiEval 测试期望从“设备状态”对齐为当前规则口径“最新日志级别”；CI 新增 Linux `backend-full-tests` job，执行完整 `AICopilot.BackendTests` 且不带 `--filter`。原 simulation filtered suites 保留为专项检查，但不再作为全量验证依据。
- 关键实现：`docs/frontend-integration-contract-package-2026-05-17.md` 在 App Problem Codes 表补入 `request_validation_failed`；`AiEvalBehaviorGuardrailTests` 对语义预览键名改为 `最新日志级别`，与 `SemanticAnalysisPresentation` 的字段描述和 Direct DB 设备 `status` 口径一致；新增 `CloudSemanticSimulationAICopilotAppFixture`，只开启 CloudReadonly Simulation 种子但保留默认 RagWorker/DataWorker，修复 Phase25/Acceptance 依赖 `device_master_cloud_sim_view` 却误用关闭 RagWorker fixture 的问题；`IdentityAccessManagementTests.ChatStream_ShouldReturnForbiddenBeforeSse_WhenUserLacksChatPermission` 覆盖无 `AiGateway.Chat` 权限时 `/api/aigateway/chat` 返回干净 403 且不是 SSE 200 断流；`.github/workflows/aicopilot-simulation-release-candidate.yml` 增加 `backend-full-tests` job，在 `ubuntu-latest` 上运行 `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --logger "console;verbosity=minimal"`。
- Stream 权限核对：当前公开 stream 权限为 `ChatStreamRequest` / `ApprovalDecisionStreamRequest` -> `AiGateway.Chat`，`PlanAgentTaskStreamRequest` -> `AiGateway.PlanAgentTask`；`User` 默认角色和 fresh database seed 均包含两项权限。新增 HTTP 集成测试已确认缺少 `AiGateway.Chat` 的用户在 chat stream 入口得到 HTTP 403，且响应不是 `text/event-stream`。
- 本机全量 BackendTests 分类：收口过程中曾暴露 AiEval 语义预览字段口径未更新、AppHost 启动抖动、Phase25/Acceptance 缺少 `device_master_cloud_sim_view` 测试视图三类问题。最终通过错误契约补齐、AiEval 期望修正和专用 Cloud semantic simulation fixture 修复后，Phase25/Acceptance 专项 17 通过、0 失败；新增 stream 403 集成测试 1 通过、0 失败；最终完整 BackendTests 672 通过、0 失败。
- 规则提炼：形成长期规则，已写入 `AGENTS.md` 和 `资料/AICopilot业务规则.md`：新增、删除或重命名后端错误码必须同批更新前端错误契约并运行 `ErrorCodeCatalogTests`；大范围架构、管道、权限、工作流或契约改动不得只以 filtered tests 作为完成依据；新增或接线 `IStreamPipelineBehavior` 后必须核对所有公开 stream 权限、测试种子角色和无权限 401/403 行为。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-build --no-restore --filter "FullyQualifiedName~ErrorCodeCatalogTests" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~AiEvalBehaviorGuardrailTests.DataAnalysisFinalContext_ShouldExposeOnlyBusinessSafeSemanticPreview" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-build --no-restore --filter "FullyQualifiedName~FreshDatabaseSeedTests" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~Phase25RuntimeSmokeTests|FullyQualifiedName~AcceptanceBaselineTests" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~IdentityAccessManagementTests.ChatStream_ShouldReturnForbiddenBeforeSse_WhenUserLacksChatPermission" --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore --logger "console;verbosity=minimal"`
  - `pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh ./scripts/Test-ArchitectureBoundaries.ps1`
  - `pwsh ./scripts/Test-TextEncoding.ps1`
  - `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/aicopilot-simulation-release-candidate.yml"); puts "YAML ok"'`
  - `rg -n "Backend full test suite|AICopilot\.BackendTests\.csproj|--filter" .github/workflows/aicopilot-simulation-release-candidate.yml`
- 验证结果：`ErrorCodeCatalogTests` 1 通过、0 失败；AiEval 目标测试 1 通过、0 失败；`FreshDatabaseSeedTests` 1 通过、0 失败；Phase25/Acceptance 专项 17 通过、0 失败；stream 403 集成测试 1 通过、0 失败；最终完整 BackendTests 672 通过、0 失败；ArchitectureTests 56 通过、0 失败；Baseline/Architecture/TextEncoding 三个脚本通过；workflow YAML 解析通过；CI workflow 检索确认存在不带 filter 的 full BackendTests job。

## 2026-06-29 代码质量 LINQ 与 CA1851 门禁第 9 批

- 改动范围：AICopilot 根 `.editorconfig`、架构测试、项目规则 `AGENTS.md`、`资料/AICopilot业务规则.md`、滚动复盘。
- 改动原因：执行 AICopilot 架构清理计划中的代码质量规则，把“默认 LINQ + IQueryable 下推，但热路径允许 for/foreach”的口径固化成规则，并先以 CA1851 warning 监控重复枚举风险，避免直接升 error 造成基线红。
- 影响面：不修改业务 handler、不改变运行行为、不引入新 NuGet 包、不启用整类 Performance analyzer，也不打开 `TreatWarningsAsErrors`。当前只新增根 `.editorconfig`，将 CA1851 设为 warning；后续如要升 error，必须先清理基线并单独验证。
- 关键实现：新增 `.editorconfig`，设置 `dotnet_diagnostic.CA1851.severity = warning`；`AGENTS.md` 和 `资料/AICopilot业务规则.md` 记录：简单转换/过滤/投影/分组默认 LINQ，`IQueryable` 必须下推过滤/投影/排序/分页；性能热路径、状态机、解析器、流式枚举、数组/`Span<T>` 紧循环允许 `for`/`foreach`；重点拦重复枚举、先物化再过滤、N+1、O(n²)、重复扫描和错误数据结构。架构测试新增门禁，锁住 CA1851 warning 和规则文案。
- 规则提炼：形成长期规则，已写入 `AGENTS.md` 和 `资料/AICopilot业务规则.md`；工程质量不绝对禁止 `for`/`foreach`，而是要求 LINQ 表达简单意图、IQueryable 下推、热路径选择合适数据结构和复杂度。
- 验证命令：
  - `rg -n "\.ToList\(\)\.Where|\.ToArray\(\)\.Where|\.Count\(\).*foreach|foreach.*\.Count\(" src -g "*.cs"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore`
  - `dotnet build AICopilot.slnx --no-restore`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-build --no-restore`
  - `pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh ./scripts/Test-ArchitectureBoundaries.ps1`
  - `pwsh ./scripts/Test-TextEncoding.ps1`
  - `git diff --check`
  - `rg 'CA1851|默认优先使用 LINQ|IQueryable|重复枚举|N\+1|先物化再过滤' .editorconfig AGENTS.md 资料/AICopilot业务规则.md docs/改动复盘与规则沉淀.md src/tests/AICopilot.ArchitectureTests/ArchitectureBoundaryTests.cs -n`
- 验证结果：未发现 `.ToList().Where` / `.ToArray().Where` 类明显先物化再过滤模式；ArchitectureTests 56 通过、0 失败；solution build 成功且 0 warning/0 error；Baseline/Architecture/TextEncoding 三个脚本通过；`git diff --check` 通过；规则检索通过。

## 2026-06-29 Direct DB 工序名与探针修正第 8 批

- 改动范围：AICopilot DataAnalysis 语义物理映射、CloudReadOnly SQL guard、BusinessQuery safety schema、Cloud readonly role provision workflow、RealSource 模板、设备语义展示口径、部署说明、架构测试、后端映射/SQL/guard/summary 测试、项目规则 `AGENTS.md` 和 `资料/AICopilot业务规则.md`。
- 改动原因：执行 AICopilot 架构清理计划第 8 批，修正上次审计提出的三个演示/运行问题：`processName`/`lineName` 直接显示 `process_id` UUID；设备 `status` 实际来自最新日志级别但展示成设备状态；Cloud readonly role workflow 的 `count(*) >= 0` 探针恒真，无法暴露真实行数。
- 影响面：不修改 CloudPlatform、不写 Cloud 业务数据、不新增 Cloud 写权限、不放宽 SQL guard。Direct DB 只读范围从 4 张表扩到 5 张表，新增 `mfg_processes` 仅用于 SELECT 工序名称；设备状态字段不改契约名，只改展示标签和摘要文案为“最新日志级别”，避免破坏现有语义定义和前端字段。
- 关键实现：Direct DB 默认映射和 `appsettings.RealSource.template.json` 为设备、产能、过站分支 join `mfg_processes mp`，`lineName`/`processName` 改为 `mp.process_name`；`CloudReadOnlySemanticSqlGuard`、`CloudReadOnlyBusinessQuerySchema`、只读 role GRANT 和权限校验同步加入 `mfg_processes`。Provision workflow 将四个 `count(*) >= 0` 恒真探针改为五张表真实 `count(*)::bigint` 行数输出，并继续校验 read-only role、SELECT 权限、写权限缺失和 schema create 缺失。设备语义摘要和字段展示把 `status` 文案改为“最新日志级别”。
- 规则提炼：形成长期规则，已写入 `AGENTS.md` 和 `资料/AICopilot业务规则.md`：Direct DB 工序名只能通过只读 `mfg_processes.process_name` join；新增 join 表必须同步 SQL guard、BusinessQuery safety schema、只读 role 授权 workflow、RealSource 模板和测试；Direct DB 设备 `status` 当前表示最新 `device_logs.level`，对用户展示必须称为“最新日志级别”，不得暗示实时在线/运行状态。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~ConfiguredSemanticPhysicalMappingProviderTests|FullyQualifiedName~SemanticSqlGenerationTests|FullyQualifiedName~SemanticAnalysisRunnerTests|FullyQualifiedName~DataAnalysisReadOnlyGuardrailTests|FullyQualifiedName~SemanticSummaryBuilderTests|FullyQualifiedName~PromptGovernanceTests"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore`
  - `ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f); puts "YAML ok: #{f}" }' .github/workflows/aicopilot-provision-cloud-readonly-db-role.yml`
  - `ruby -rjson -e 'ARGV.each { |f| JSON.parse(File.read(f)); puts "JSON ok: #{f}" }' src/hosts/AICopilot.HttpApi/appsettings.RealSource.template.json`
  - `dotnet build AICopilot.slnx --no-restore`
  - `pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh ./scripts/Test-ArchitectureBoundaries.ps1`
  - `pwsh ./scripts/Test-TextEncoding.ps1`
- 验证结果：相关 BackendTests 43 通过、0 失败；ArchitectureTests 55 通过、0 失败；workflow YAML 和 RealSource JSON 解析通过；solution build 成功且 0 warning/0 error；Baseline/Architecture/TextEncoding 三个脚本通过。

## 2026-06-29 旧路线清理与 Direct DB 优先第 7 批

- 改动范围：AICopilot AiGateway semantic analysis 分流、CloudReadonly/AiRead/Simulation 说明文档、AiRead secret 脚本收口、架构测试、后端语义分析测试、项目规则 `AGENTS.md` 和 `资料/AICopilot业务规则.md`。
- 改动原因：执行 AICopilot 架构清理计划第 7 批，落实“Simulation 保留但收窄、AiRead 封存不删、CloudReadOnly Direct DB 是内部真实查询优先路线”。此前 `SemanticAnalysisRunner` 只要 `CloudAiReadClient.IsEnabled` 就先走 AiRead，会压过已配置的 Direct DB 映射；同时既有 `scripts/Set-AICopilotCloudAiReadSecret.sh` 已处于修改状态，需要正式收口。
- 影响面：不修改 CloudPlatform、不写 Cloud 业务数据、不删除 AiRead client 或 workflow，不删除 Simulation 资产。内部语义查询在存在 `CloudReadOnly` Direct DB 映射时优先走 business database connector；只有没有内部映射且 AiRead 启用并支持目标时，才保留 AiRead fallback。Simulation 继续只作为显式离线演示/测试模式，Real/Direct DB/AiRead 不得 fallback 到 Simulation。
- 关键实现：`SemanticAnalysisRunner` 将 AiRead 分流移动到“未找到语义物理映射”之后；新增后端测试证明 `CloudAiReadClient.IsEnabled=true` 时仍优先调用 Direct DB connector；架构测试锁住 `semanticPhysicalMappingProvider.TryGetMapping` 必须早于 `RunCloudAiReadAsync`。AiRead secret 脚本补注释说明当前 `gh secret set` 无 `--body` 时从 stdin 读取，把既有脏改纳入正式意图并通过 `bash -n`。旧 CloudReadonly readiness 文档标注历史口径，Simulation 文档明确不得作为真实链路 fallback。
- 规则提炼：形成长期规则，已写入 `AGENTS.md` 和 `资料/AICopilot业务规则.md`：内部真实 Cloud 语义查询优先走 DataAnalysis `CloudReadOnly` Direct DB 映射；Cloud AiRead 封存为未来外部系统只读 API 接入口，不能在内部映射存在时压过 Direct DB；Simulation 只允许显式离线演示/测试，默认关闭，不得在真实数据为空、失败或未配置时补位。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~SemanticAnalysisRunnerTests|FullyQualifiedName~CloudReadonlySimulationTests|FullyQualifiedName~CloudAiReadClientTests"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore`
  - `bash -n scripts/Set-AICopilotCloudAiReadSecret.sh`
  - `dotnet build AICopilot.slnx --no-restore`
  - `pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh ./scripts/Test-ArchitectureBoundaries.ps1`
  - `pwsh ./scripts/Test-TextEncoding.ps1`
- 验证结果：SemanticAnalysisRunner/CloudReadonlySimulation/CloudAiReadClient 相关 BackendTests 45 通过、0 失败；ArchitectureTests 55 通过、0 失败；AiRead secret 脚本语法通过；solution build 成功且 0 warning/0 error；Baseline/Architecture/TextEncoding 三个脚本通过。

## 2026-06-29 Agent workflow 拓扑声明第 6 批

- 改动范围：AICopilot AiGateway agent workflow 主干、工作流拓扑声明、工作流回归测试、项目规则 `AGENTS.md`、滚动复盘。
- 改动原因：执行 AICopilot 架构清理计划第 6 批第一步，落实“AI 工作流规整但不能粗暴重构”的要求。现有 `AgentWorkflowPipeline` 已经通过 `Task.WhenAll` 并行执行 Tools、Knowledge、DataAnalysis、BusinessPolicy 四个分支；本批把阶段和并行分支顺序显式声明到 `AgentWorkflowTopology`，方便后续扩展和门禁审查，同时不改变 executor 契约、不改业务 handler、不把并行分支拍平成串行。
- 影响面：不修改 CloudPlatform、不写 Cloud 业务数据、不改变 DataAnalysis、RAG、Tool、BusinessPolicy 分支的执行逻辑，不改变 PlanDraft 只做 routing + capability discovery 的出口语义。`AgentWorkflowPipeline` 继续使用现有 concrete executors、`AgentWorkflowSink`、`RunBranchSafelyAsync` 和 `Task.WhenAll`；只是从拓扑读取四个分支并集中分派到既有 executor。
- 关键实现：新增 `AgentWorkflowTopology`，声明 IntentRouting、CapabilityDiscovery、ParallelFanOut、ContextAggregation、FinalAgentBuild、FinalAgentRun 阶段，以及 Tools、Knowledge、DataAnalysis、BusinessPolicy 四个并行分支顺序；`RunIntentWorkflowAsync` 改为基于 `AgentWorkflowTopology.ParallelBranches` 创建 branch tasks，并通过 `ExecuteBranchAsync` switch 调用既有 executor。`ClaudeFollowupClosureTests` 新增门禁，锁住拓扑四分支、`Task.WhenAll(branchTasks)`、`AgentWorkflowSink` 和“不在主流程里顺序 await ExecuteBranchAsync”。
- 规则提炼：形成长期规则，已写入 `AGENTS.md`：Agent workflow 的阶段和并行分支必须由 `AgentWorkflowTopology` 显式声明；四个核心分支必须保持 `Task.WhenAll` + `AgentWorkflowSink` fan-out/fan-in 模式，不得为了“管道化”拍平成串行或为新能力另起孤立链路。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~ClaudeFollowupClosureTests"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore`
  - `dotnet build AICopilot.slnx --no-restore`
  - `pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh ./scripts/Test-ArchitectureBoundaries.ps1`
  - `pwsh ./scripts/Test-TextEncoding.ps1`
  - `git diff --check`
  - `rg "AgentWorkflowTopology|Task.WhenAll|Agent workflow 的阶段|ParallelFanOut" AGENTS.md docs/改动复盘与规则沉淀.md src/services/AICopilot.AiGatewayService src/tests/AICopilot.BackendTests -n`
- 验证结果：`ClaudeFollowupClosureTests` 15 通过、0 失败；ArchitectureTests 55 通过、0 失败；solution build 成功且 0 warning/0 error；Baseline/Architecture/TextEncoding 三个脚本通过；`git diff --check` 通过；规则与源码检索通过。

## 2026-06-29 事务审计边界门禁第 5 批

- 改动范围：AICopilot 架构测试、项目规则 `AGENTS.md`、滚动复盘。
- 改动原因：执行 AICopilot 架构清理计划第 5 批前半，落实 CC 提醒的“事务拥有者冲突”风险。现有 `AuditTransactionCoordinator` 和 `EfTransactionalExecutionService` 都会显式开启 EF transaction，不能再在 MediatR pipeline 外层粗暴新增 TransactionBehavior，否则会产生嵌套事务和 EF execution strategy 冲突。
- 影响面：不修改业务 handler、不迁移事务/审计调用点、不新增 TransactionBehavior、不改变 repository 保存、Identity 用户/角色事务或审计写入运行路径。此批只把边界写进门禁和规则，保证后续迁移必须先经过显式设计。
- 关键实现：架构测试新增 2 条门禁：MediatR CrossCutting behaviors 不得触碰 `BeginTransactionAsync`、`UseTransactionAsync`、`ITransactionalExecutionService`、`AuditTransactionCoordinator`、`IAuditLogWriter`、`AuditDbContext`、`SaveChangesAsync`；事务拥有者必须继续分离，repository 保存业务+审计原子性由 `AuditTransactionCoordinator` 拥有，Identity 用户/角色事务由 `EfTransactionalExecutionService` 拥有，统一管道注册不得出现 Transaction/Audit behavior。
- 规则提炼：形成长期规则，已写入 `AGENTS.md`：事务/审计拥有者必须显式且唯一；repository 保存的业务+审计原子性由 `AuditTransactionCoordinator` 拥有，Identity 用户/角色事务由 `ITransactionalExecutionService`/`EfTransactionalExecutionService` 拥有；MediatR behavior 不得开启事务、保存审计、调用 `SaveChangesAsync` 或包裹 stream/handler 形成隐式事务边界。
- 验证命令：
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-build --no-restore --filter "FullyQualifiedName~AuditTransactionBoundaryTests|FullyQualifiedName~SecurityHardeningTests"`
  - `pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh ./scripts/Test-ArchitectureBoundaries.ps1`
  - `pwsh ./scripts/Test-TextEncoding.ps1`
  - `git diff --check`
- 验证结果：ArchitectureTests 55 通过、0 失败；事务/审计相关 BackendTests 71 通过、0 失败；Baseline/Architecture/TextEncoding 三个脚本通过；`git diff --check` 通过。

## 2026-06-29 MediatR Telemetry 与轻量 Validation 管道第 4 批

- 改动范围：AICopilot CrossCutting MediatR telemetry/validation behaviors、AiGateway stream 请求输入 validators、ServiceDefaults OpenTelemetry source、架构测试、后端行为测试、项目规则 `AGENTS.md`。
- 改动原因：执行 AICopilot 架构清理计划第 4 批，在已统一的 MediatR 管道入口中补齐低风险横切能力；Telemetry 复用现有 OpenTelemetry/structured logging，Validation 采用轻量 `IRequestValidator<TRequest>` 机制并立即给真实 stream 请求挂校验，避免空壳管道。
- 影响面：不修改 CloudPlatform、不写 Cloud 业务数据、不改变事务/审计运行路径、不重构业务 handler、不引入 FluentValidation 新包。普通请求和 stream 请求管道顺序均固定为 Telemetry -> Validation -> Authorization。Telemetry 只记录 request type、kind、耗时、状态和异常类型，不记录 prompt、SQL、token、连接串或业务数据明细。Validation 当前只覆盖 chat/approval/agent-plan stream 的空 SessionId、空 message/callId/decision/goal 这类基础输入。
- 关键实现：新增 `TelemetryBehavior` / `TelemetryStreamBehavior`、`ValidationBehavior` / `ValidationStreamBehavior`、`IRequestValidator<TRequest>`、`RequestValidationException` 和 `AICopilot.MediatR` ActivitySource；`AddAICopilotMediatRPipeline` 统一注册 Telemetry、Validation、Authorization 三段普通/stream 管道；ServiceDefaults tracing 加入 `AICopilot.MediatR` source。AiGateway 注册 `ChatStreamRequestValidator`、`ApprovalDecisionStreamRequestValidator`、`PlanAgentTaskStreamRequestValidator`。架构测试锁住行为注册顺序和 OTel source；后端测试覆盖 validation 拒绝前不进 stream handler、telemetry 不缓冲 stream、实际 chat stream validator 拒绝空消息。
- 规则提炼：形成长期规则，已写入 `AGENTS.md`：MediatR 管道顺序固定为 Telemetry -> Validation -> Authorization；新增 Validation behavior 必须同步真实 validator 和测试；telemetry 必须复用现有 OTel/structured logging 且不得记录 prompt、SQL、token、连接串、API key 或业务数据明细。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~AuthorizationPipelineBehaviorTests"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-build --no-restore`
  - `dotnet build AICopilot.slnx --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-build --no-restore --filter "FullyQualifiedName~Identity|FullyQualifiedName~Authorization|FullyQualifiedName~SecurityHardening|FullyQualifiedName~Worker|FullyQualifiedName~Rag|FullyQualifiedName~AuthorizationPipelineBehaviorTests"`
  - `pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh ./scripts/Test-ArchitectureBoundaries.ps1`
  - `pwsh ./scripts/Test-TextEncoding.ps1`
  - `git diff --check`
- 验证结果：新增/扩展授权管道行为测试 6 通过、0 失败；ArchitectureTests 53 通过、0 失败；solution build 成功且 0 warning/0 error；宽授权/安全/worker/RAG 过滤 167 通过、0 失败；Baseline/Architecture/TextEncoding 三个脚本通过；`git diff --check` 通过。

## 2026-06-29 MediatR stream 细粒度授权接线第 3 批

- 改动范围：AICopilot CrossCutting 授权行为、统一 MediatR 管道入口、架构测试、后端授权行为特征测试、项目规则 `AGENTS.md`。
- 改动原因：执行 AICopilot 架构清理计划第 3 批，修复既有 stream 请求 `[AuthorizeRequirement]` 已声明但没有 `IStreamPipelineBehavior` 接线的问题。控制器 `[Authorize]` 继续负责认证，stream 细粒度权限进入统一管道，不再依赖 handler 局部会话归属检查替代角色权限。
- 影响面：不修改 CloudPlatform、不写 Cloud 业务数据、不改变 Agent workflow 编排、不引入事务/审计 stream behavior、不缓冲 SSE/stream。普通 `AuthorizationBehavior` 与新增 `AuthorizationStreamBehavior` 共用 `AuthorizationRequirementEvaluator`，保持既有未登录、用户不存在、缺权限异常契约；stream behavior 在进入 handler 前校验权限，然后逐项透传 `IAsyncEnumerable`。
- 关键实现：新增 `AuthorizationRequirementEvaluator` 提取 `AuthorizeRequirement` 读取、用户状态校验、权限缺失计算和异常构造；`AuthorizationBehavior` 调 evaluator 后继续执行普通 `next`；新增 `AuthorizationStreamBehavior` 实现 `IStreamPipelineBehavior`，授权成功后使用 `await foreach` + `WithCancellation` 转发 `next()`。统一入口 `AddAICopilotMediatRPipeline` 同时注册普通和 stream 授权行为。`AuthorizationPipelineBehaviorTests` 覆盖缺权限时不调用 stream `next`、有权限时不缓冲流、无权限要求请求在缺少用户态服务时仍可透传。
- 规则提炼：形成长期规则，已写入 `AGENTS.md`：Stream 细粒度权限必须由 `AuthorizationStreamBehavior` 在进入 stream handler 前校验；stream behavior 只能逐项透传 `IAsyncEnumerable`，不得预读、缓存或把 SSE/stream 转成一次性结果。
- 验证命令：
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-restore --filter "FullyQualifiedName~AuthorizationPipelineBehaviorTests"`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-build --no-restore --filter "FullyQualifiedName~Identity|FullyQualifiedName~Authorization|FullyQualifiedName~SecurityHardening|FullyQualifiedName~Worker|FullyQualifiedName~Rag|FullyQualifiedName~AuthorizationPipelineBehaviorTests"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-build --no-restore --filter "FullyQualifiedName~RagWorkerStartupTests"`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-build --no-restore --filter "FullyQualifiedName~IdentityAccessManagementTests.UserRole_ShouldKeepChatAccess_AndForbiddenResponsesShouldIncludeMissingPermissions"`
  - `dotnet build AICopilot.slnx --no-restore`
  - `pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh ./scripts/Test-ArchitectureBoundaries.ps1`
  - `pwsh ./scripts/Test-TextEncoding.ps1`
  - `git diff --check`
  - `rg "AuthorizationStreamBehavior|逐项透传|AddAICopilotMediatRPipeline|MediatR 横切行为" AGENTS.md docs/改动复盘与规则沉淀.md src -n`
- 验证结果：新增授权行为测试 3 通过、0 失败；ArchitectureTests 52 通过、0 失败；宽授权/安全/worker/RAG 过滤重跑 164 通过、0 失败；`RagWorkerStartupTests` 单测通过；Identity fixture 单测通过；solution build 成功且 0 warning/0 error；Baseline/Architecture/TextEncoding 三个脚本通过；`git diff --check` 通过；规则检索通过。宽过滤首次运行曾出现 14 个 `AICopilotAppFixture` 分布式应用启动 2 分钟超时失败；隔离 `RagWorkerStartupTests` 和失败的 Identity 单测均通过，随后不并发重跑同一宽过滤通过，判定为本机 AppHost 冷启动/资源争用抖动，不是本批逻辑回归。

## 2026-06-29 MediatR 统一管道入口第 2 批

- 改动范围：AICopilot CrossCutting 统一管道注册、HttpApi/DataWorker/RagWorker 组合根、IdentityService handler 注册、架构测试、项目规则 `AGENTS.md`。
- 改动原因：执行 AICopilot 架构清理计划第 2 批，把既有 `AuthorizationBehavior` 从 IdentityService 的隐式注册抽到统一 `AddAICopilotMediatRPipeline` 入口；先锁地基，不提前实现 stream authorization、Validation、Telemetry、Transaction 或 Audit behavior，也不重构业务 handler。
- 影响面：不修改 CloudPlatform、不写 Cloud 业务数据、不改变 Agent workflow 编排、不改变事务/审计运行路径、不改变已有权限判断语义。`HttpApi`、`DataWorker`、`RagWorker` 显式调用统一管道入口；service 模块继续只注册本模块 MediatR handlers 和业务服务。为支持 worker host 统一挂管道，`AuthorizationBehavior` 改为只有请求类型真的声明 `AuthorizeRequirement` 时才解析 `ICurrentUser` / `IIdentityAccessService`；无权限要求的内部请求不因缺少登录用户态服务失败。
- 关键实现：新增 `AICopilot.Services.CrossCutting.DependencyInjection.AddAICopilotMediatRPipeline`，通过 `TryAddEnumerable` 注册普通 `IPipelineBehavior<,>` 的既有 `AuthorizationBehavior<,>`；从 `AICopilot.IdentityService.DependencyInjection` 移除 `cfg.AddBehavior(...)`；`DataWorker`、`RagWorker` 增加 CrossCutting 显式项目引用。架构测试新增 3 条门禁：MediatR 横切行为只能通过统一入口注册；使用 MediatR handler 的 host 必须先调用统一入口再注册 service 模块；`AuthorizationBehavior` 必须在确认请求存在权限要求后再解析用户态身份服务。
- 规则提炼：形成长期规则，已写入 `AGENTS.md`：MediatR 横切行为只能通过 `AddAICopilotMediatRPipeline` 统一注册；service 模块不能分散注册 authorization、validation、telemetry、transaction 或 audit behavior；使用 MediatR handler 的 host 组合根必须先挂统一管道，worker host 不能因为统一管道强依赖登录用户态。
- 验证命令：
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore`
  - `dotnet build AICopilot.slnx --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-build --no-restore --filter "FullyQualifiedName~Identity|FullyQualifiedName~Authorization|FullyQualifiedName~SecurityHardening|FullyQualifiedName~Worker|FullyQualifiedName~Rag"`
  - `pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh ./scripts/Test-ArchitectureBoundaries.ps1`
  - `pwsh ./scripts/Test-TextEncoding.ps1`
  - `git diff --check`
- 验证结果：ArchitectureTests 52 通过、0 失败；solution build 成功且 0 warning/0 error；相关 BackendTests 161 通过、0 失败；Baseline/Architecture/TextEncoding 三个脚本通过；`git diff --check` 通过。既有脏文件 `scripts/Set-AICopilotCloudAiReadSecret.sh` 仍未擅自还原，留待 AiRead 封存批次收口；全量 BackendTests 既有 Simulation 视图缺失失败本批未重跑、未扩大失败类型。

## 2026-06-29 MediatR stream 权限与 HTTP 授权门禁第 1 批

- 改动范围：AICopilot 架构测试、HttpApi `IdentityController` 匿名入口元数据、项目规则 `AGENTS.md`。
- 改动原因：执行 AICopilot 架构清理计划第 1 批，只补当前已经成立且可立即全绿的门禁；先锁住 stream 请求权限标记、HTTP Controller 授权显式性、stream 不承载事务/审计边界、CloudReadOnly direct DB 只读 guard 与 Simulation/Real 互斥红线，不提前改业务 handler、工作流或事务实现。
- 影响面：不修改 CloudPlatform、不写 Cloud 业务数据、不改变 DataAnalysis `CloudReadOnly` SQL 执行逻辑、不改变 Agent workflow 编排、不改变事务/审计运行路径。`IdentityController` 的登录、OIDC 状态/挑战/完成和初始化状态端点显式标注 `[AllowAnonymous]`，用于表达既有匿名语义；其他控制器继续保持 `[Authorize]` 或方法级授权。
- 关键实现：`ArchitectureBoundaryTests` 新增 4 条门禁：公开 `IStreamRequest` 必须声明 `AuthorizeRequirement`；HttpApi Controller 必须类级或方法级显式 `[Authorize]` / `[AllowAnonymous]`；禁止 `IStreamPipelineBehavior` 与 Transaction/Audit 边界绑定；CloudReadOnly semantic guard、BusinessQuerySafetySchema 和 migration seeder 必须保留四张白名单表、敏感字段阻断、通配投影阻断、只读凭据验证和 Simulation/Real 互斥。同步在 `AGENTS.md` 沉淀 HTTP 授权显式性和 stream 管道边界规则。
- 规则提炼：形成长期规则，已写入 `AGENTS.md`：HTTP API Controller 必须默认授权或显式匿名；MediatR `IStreamRequest` 与普通 `IRequest` 是两条管道，公开 stream 请求必须声明权限要求，后续不得把事务或审计边界套到 SSE/stream 长连接上。
- 第 0 批基线：改动前已读取工作区总规则、项目规则、AI 业务规则、历史核心记录、滚动复盘、测试脚本和近期 git 历史；`git status` 显示既有脏文件 `scripts/Set-AICopilotCloudAiReadSecret.sh`，本批未擅自还原，留待 AiRead 封存批次收口；后端全集基线为 656 通过、6 失败，6 个失败均为既有 `device_master_cloud_sim_view` 缺失类 Simulation smoke/acceptance 失败。
- 验证命令：
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-restore`
  - `pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh ./scripts/Test-ArchitectureBoundaries.ps1`
  - `pwsh ./scripts/Test-TextEncoding.ps1`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-build --no-restore --filter "FullyQualifiedName~CloudReadOnly|FullyQualifiedName~CloudReadonly|FullyQualifiedName~Identity|FullyQualifiedName~Authorization|FullyQualifiedName~SecurityHardening"`
  - `git diff --check`
- 验证结果：ArchitectureTests 49 通过、0 失败；Baseline/Architecture/TextEncoding 三个脚本通过；相关 BackendTests 145 通过、0 失败；`git diff --check` 通过。全量 BackendTests 改前基线仍有 6 个既有 Simulation 视图缺失失败，本批未扩大失败类型。

## 2026-06-29 线上 Agent artifact workspace 可写路径修复

- 改动范围：AICopilot 后端运行镜像、服务器 compose、`.env.example`、direct Cloud readonly DB 启用 workflow、部署说明、部署模板安全测试和项目规则。
- 改动原因：线上 Plan 确认执行进入 Agent 链路后失败，错误为 `Access to the path '/app/AICopilot' is denied.`；根因是容器以非 root 用户运行，而 `ArtifactWorkspace:RootPath` 未在生产 compose 显式配置，运行时回落到容器内不可写默认路径。
- 影响面：只影响 AICopilot 容器内文件上传根目录和 Agent artifact workspace 根目录；不修改 CloudPlatform、不写 Cloud 业务数据、不改变 DataAnalysis `CloudReadOnly` 只读 SQL guard、白名单表或真实 Cloud DB 连接边界。
- 关键实现：后端 runtime image 预创建并授权 `/var/lib/aicopilot/storage` 与 `/var/lib/aicopilot/artifact-workspaces`；compose 挂载 `enterprise-ai-aicopilot-data` 命名卷到后端服务并注入 `FileStorage__RootPath` / `ArtifactWorkspace__RootPath`；direct enable workflow 在重建后输出两个路径及可写状态；部署说明记录非 root 容器不得依赖 `/app` 或 `LocalApplicationData` 默认路径。
- 规则提炼：形成长期规则，已写入 `AGENTS.md`：容器部署必须显式配置并挂载可写的 `FileStorage:RootPath` 和 `ArtifactWorkspace:RootPath`，不得依赖容器内 `LocalApplicationData` 默认路径或 `/app` 目录写入运行产物。
- 验证命令：
  - `bash -n deploy/enterprise-ai/deploy-release.sh`
  - `ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f); puts "YAML ok: #{f}" }' .github/workflows/aicopilot-enable-direct-cloud-readonly-db.yml`
  - `docker compose --env-file deploy/enterprise-ai/.env.example -f deploy/enterprise-ai/docker-compose.yaml config -q`
  - `docker build --platform linux/amd64 --build-arg APP_DLL=dummy.dll --tag aicopilot-backend-runtime-smoke:local --file deploy/enterprise-ai/Dockerfile.backend-runtime <temp-dir-with-dummy-dll>`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~SecurityHardeningTests" --no-restore`
  - `pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh ./scripts/Test-ArchitectureBoundaries.ps1`
  - `git diff --check`
- 线上验证：
  - GitHub `aicopilot-image` run `28347697727` 成功，发布 `sha-07d841249b0bf47cd525e8dd812b8d7dc950a615` 后端/前端镜像。
  - GitHub `aicopilot-deploy` run `28347966929` 成功，按 built-services `httpapi,migration,dataworker,ragworker,web` 部署。
  - GitHub `aicopilot-enable-direct-cloud-readonly-db` run `28348126615` 成功；容器环境为 `DataAnalysis__CloudReadOnly__Enabled=true`、`DataAnalysis__CloudReadOnly__ConnectionString_CONFIGURED=yes`、`DataAnalysis__CloudReadOnly__ReadOnlyCredentialVerified=true`、`CloudAiRead__Enabled=false`，并确认 `FileStorage__RootPath_WRITABLE=yes`、`ArtifactWorkspace__RootPath_WRITABLE=yes`。
  - 线上页面 `http://<aicopilot-prod-host>:82` 返回 200，`/api/identity/initialization-status` 返回 200 且 `isInitialized=true`，`/api/identity/cloud-oidc/status` 返回 200 且 `isEnabled=true`。
  - Playwright 通过 Cloud OIDC 登录后执行“列出 LINE-A 当前设备状态，生成关键指标和记录摘要”：Plan 生成、确认执行、XLSX 工具审批、最终输出确认均通过；后端 task 状态为 `Completed`，workspace 状态为 `Finalized`，5 个步骤全部 `Completed`，产物树包含 `chart-data.json`、`report.html`、`report.md`、`report.xlsx` 及 `final/` 输出；未出现 `/app/AICopilot` 权限错误、`agent_task_failed` 或 Simulation 标识。
- 验证结果：脚本语法、workflow YAML、compose config、本地 Dockerfile runtime 烟测、64 个 `SecurityHardeningTests`、Baseline/Architecture 门禁、diff whitespace 检查和线上端到端执行均通过。`SecurityHardeningTests` 同步修正了已过期的 image workflow 断言：当前生产构建是 `dotnet publish` 后用 `docker buildx build` 推送 Harbor，不再使用旧 `/t:PublishContainer` 路径。

## 2026-06-29 真实 Cloud DB 只读 Text-to-SQL 接通

- 改动范围：AICopilot DataAnalysis 只读治理、语义映射、语义 SQL 生成、AiGateway runner、Migration worker 数据源注册、服务器 compose/workflow、跨 compose 只读 Docker 网络、本机 secret 脚本、部署说明和相关测试。
- 改动原因：内部 AI 项目开发验证需要直接读取真实 Cloud PostgreSQL 做 Text-to-SQL 效果测试，不走 Simulation，不依赖 Cloud AiRead 插件链路，不写 Cloud 业务数据。
- 影响面：新增 DataAnalysis `CloudReadOnly` 真实只读数据源注册路径；真实 Cloud 查询只允许 `devices`、`device_logs`、`hourly_capacity`、`pass_station_records` 四类白名单表；拒绝敏感字段、通配投影、非白名单表和写 SQL；旧 Simulation 默认链路保持关闭条件和显式标识。
- 关键实现：本机 `scripts/Set-AICopilotCloudReadOnlyDbSecret.sh` 从 Mac 读取只读连接串并写入 GitHub production secret；`scripts/Provision-AICopilotCloudReadOnlyDbRole.sh` 可生成随机密码、写入 GitHub secrets 并触发受控 workflow；`aicopilot-provision-cloud-readonly-db-role` 需要确认词，只创建/轮换专用 readonly role，并可随后启用 AICopilot direct DB。历史内联授权清单已被 2026-06-30 的 `deploy/enterprise-ai/cloud-readonly/*.sql` 权威载体取代；当前只读授权范围以 `apply-readonly-grants.sql` 和 `check-readonly-grants.sql` 为准。`aicopilot-enable-direct-cloud-readonly-db` workflow 写服务器 `.env`、备份并重建 AICopilot 服务；部署脚本创建外部 Docker 网络 `enterprise-ai-cloud-readonly`，直连 DB 模式启用时把 Cloud compose 的 `deploy/postgres` 容器接入并设置别名 `cloud-postgres`；migration worker 只注册 AICopilot 自身的 verified `CloudReadOnly` BusinessDatabase，不修改 CloudPlatform。
- 规则提炼：形成长期规则，已写入 `AGENTS.md` 和 `资料/AICopilot业务规则.md`：内部开发可通过 DataAnalysis `CloudReadOnly` 只读数据源直连真实 Cloud PostgreSQL 做 Text-to-SQL 验证，但必须使用已验证只读账号、白名单表字段和只读 SQL guard，不得写 Cloud 业务数据，也不得用 Simulation 冒充真实结果；创建或轮换 Cloud PostgreSQL 只读账号只能通过显式确认的受控自动化执行，只能创建/更新专用 readonly role，不得授予写权限、schema create 权限、superuser、createdb、createrole 或 replication。
- 验证命令：
  - `bash -n scripts/Provision-AICopilotCloudReadOnlyDbRole.sh`
  - `bash -n scripts/Set-AICopilotCloudReadOnlyDbSecret.sh`
  - `bash -n deploy/enterprise-ai/deploy-release.sh`
  - `bash -n scripts/Set-AICopilotCloudAiReadSecret.sh`
  - `ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f); puts "YAML ok: #{f}" }' .github/workflows/aicopilot-provision-cloud-readonly-db-role.yml .github/workflows/aicopilot-enable-direct-cloud-readonly-db.yml .github/workflows/aicopilot-deploy.yml .github/workflows/aicopilot-enable-real-cloud-ai-read.yml`
  - `docker compose --env-file deploy/enterprise-ai/.env.example -f deploy/enterprise-ai/docker-compose.yaml config -q`
  - `dotnet build AICopilot.slnx --no-restore`
  - `dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --filter "FullyQualifiedName~ConfiguredSemanticPhysicalMappingProviderTests|FullyQualifiedName~SemanticSqlGenerationTests|FullyQualifiedName~SemanticAnalysisRunnerTests|FullyQualifiedName~MigrationWorkerCloudReadOnlySeederTests|FullyQualifiedName~DataAnalysisReadOnlyGuardrailTests" --no-restore`
  - `dotnet test src/tests/AICopilot.ArchitectureTests/AICopilot.ArchitectureTests.csproj --no-build --no-restore`
  - `pwsh ./scripts/Test-AICopilotBaselineFreezeScope.ps1`
  - `pwsh ./scripts/Test-ArchitectureBoundaries.ps1`
  - `pwsh ./scripts/Test-TextEncoding.ps1`
- 验证结果：脚本语法、workflow YAML、compose config、解决方案构建、26 个重点 BackendTests、45 个 ArchitectureTests、Baseline/Architecture/TextEncoding 门禁全部通过。已只读核对 CloudPlatform 部署拓扑：Cloud Postgres 未发布宿主端口，仅在 Cloud compose 内网；已补 AICopilot 部署侧外部只读网络和 `cloud-postgres` 别名。`dotnet test src/tests/AICopilot.BackendTests/AICopilot.BackendTests.csproj --no-build --no-restore` 全集为 655 通过、7 失败；失败项为本机 Simulation 视图 `device_master_cloud_sim_view` 缺失导致的既有 smoke/acceptance 路径 6 项，以及 `SecurityHardeningTests` 对旧 image workflow `/t:PublishContainer` 字符串的既有断言 1 项，不在本轮直连 Cloud DB 新路径内。

## 2026-06-29 改动收口规则同步

- 改动范围：`AGENTS.md`、`资料/AICopilot业务规则.md`、`docs/改动复盘与规则沉淀.md`、AICopilot 架构测试和基线冻结脚本。
- 改动原因：将“代码改完必须复盘、提炼规则、更新规则入口”的强制机制扩展到 AICopilot，避免后续只靠全局规则记忆。
- 影响面：仅规则、文档和仓库卫生测试；不修改 AI 业务代码、接口、数据库、前端页面或部署脚本行为。
- 改前阅读：已阅读工作区总规则、历史核心记录、AICopilot 项目入口、AI 业务规则和现有架构/基线门禁。
- 规则提炼:
  - AICopilot 代码改动前必须阅读规则、源码、测试和近期 git/GitHub 历史。
  - AICopilot 代码改动完成前必须更新本滚动复盘文档，并判断是否沉淀长期规则。
  - AICopilot 文档门禁必须保护滚动复盘、规则沉淀、无新增长期规则、最终回复三要素和 Cloud 只读边界。
- 验证命令：
  - `rg "改动复盘|规则沉淀|无新增长期规则|最终回复必须列出|已验收功能默认冻结" AGENTS.md docs IIoT.CloudPlatform AICopilot`
  - `dotnet test AICopilot/src/tests/AICopilot.ArchitectureTests`
  - `pwsh AICopilot/scripts/Test-AICopilotBaselineFreezeScope.ps1`
- 验证结果：`rg` 检索通过；`AICopilot.ArchitectureTests` 通过 45 个测试；`Test-AICopilotBaselineFreezeScope.ps1` 通过。新增脚本门禁曾暴露 PowerShell 字符串中的 Markdown 反引号匹配问题，已改为稳定短语后通过。
