# AICopilot 交付收口与上线准备

更新时间：2026-04-24

## 交付结论

当前批次目标是交付收口，不继续扩展新能力。已通过的验收报告保存在 `资料/acceptance-closure-latest.md`。上线前必须重新执行完整验收脚本，并以最新报告为准。

配套材料：

- `资料/评审拆包清单.md`：按能力组拆解当前大变更，供代码评审使用。
- `资料/PR评审说明草稿.md`：可直接整理进 PR 描述。
- `资料/预发上线执行手册.md`：预发部署、验收、回滚和观察清单。

## 变更审查分组

- 配置治理：正式配置默认使用 Redis 保存审批上下文，开发配置仍允许 Memory；生产环境必须通过环境变量或部署系统提供 Redis 连接串、JWT Secret、数据库连接串和模型密钥。
- 身份权限：身份、角色、审计相关改动需要重点检查错误码、审计脱敏、启动初始化和管理员引导流程。
- AI Gateway：重点检查 SSE Error chunk 契约、会话历史成对持久化、模型/模板缺失错误码、Token 预算和 Agent 生命周期。
- 审批与安全：重点检查现场在岗声明、审批幂等、重复 callId、防重复工具执行、审批审计和高风险工具闸门。
- Redis 上下文：重点检查 `IFinalAgentContextStore` 的 Provider 配置、Redis 连接串、跨实例恢复和上下文过期策略。
- SQL Guardrail：重点检查 AST 只读校验、SQL Server/PostgreSQL 方言兼容、大结果截断和只读数据库约束。
- 前端聊天体验：重点检查 Error chunk 提示、流结束 loading 复位、审批卡片状态、重复审批提示和会话历史刷新。
- 测试与脚本：重点检查 `scripts/Run-AcceptanceClosure.ps1`、Docker/Aspire smoke、migration schema 验证和前端 build。

## 上线前检查

- 执行完整验收：`powershell -ExecutionPolicy Bypass -File .\scripts\Run-AcceptanceClosure.ps1 -ReportPath .\资料\acceptance-closure-latest.md`。
- 确认报告中以下项目均为 `PASSED`：HttpApi/AppHost/BackendTests/Frontend build、diff whitespace、focused unit tests、Docker、migration/Redis verification、3 条 runtime smoke。
- 确认 migration `20260423064237_Phase44OnsiteAttestationColumns` 已在隔离环境应用，并验证 `sessions` 的 onsite 字段为 UTC 安全类型。
- 确认生产环境提供 `ConnectionStrings__final-agent-context-redis`，并保持 `AiGateway__FinalAgentContextStore__Provider=Redis`。
- 确认开发环境可以继续使用 `AiGateway__FinalAgentContextStore__Provider=Memory`，但预发和生产不得回退到 Memory。
- 确认没有把开发 JWT Secret、测试数据库密码、测试模型密钥或本地连接串写入生产部署模板。

## 回滚策略

- 应用回滚：优先回滚 HttpApi、MigrationWorkApp、RagWorker 和 Web 前端镜像到上一稳定版本。
- 数据库回滚：除非明确接受数据丢失，不执行 drop database、删除 PostgreSQL volume 或手工清表。若必须回滚 schema，先备份数据库，再评估 EF migration down 脚本影响。
- Redis 回滚：Redis 只保存审批恢复上下文，回滚应用前可保留 Redis 数据；若版本不兼容导致审批恢复失败，清理 Redis 会让未完成审批失效，但不应影响已落库会话历史。
- 前端回滚：若仅 UI 出现问题，优先单独回滚 Web 资产，不回滚后端和数据库。

## PR 评审说明

- 本 PR 体量较大是因为同时完成了稳定性止血、数据一致性、安全治理和验收闭环，不应继续夹带新功能。
- 架构修复包括：SSE 异常兜底、审批幂等、Redis 审批上下文、`IChatClientProvider` 抽象、SQL AST Guardrail、Agent 依赖生命周期收口。
- 验收补丁包括：migration/Redis 验证、3 条运行时 smoke、前端 build、验收脚本和交付报告。
- 非本批目标包括：Anthropic/Azure 新 Provider、内部插件 MCP 化、WebSocket 双向流、Workflow 配置化、生产发布执行。

## 已知非阻断项

- 前端构建存在 Vite 大 chunk 警告，不阻断本批交付；后续可单独做代码分割优化。
- `src/Hosts/AICopilot.AppHost/appsettings.json` 中的 `pg-password` 仅用于本地 Aspire 参数，不应作为生产配置来源。
- 当前交付报告是自动化验收记录；正式上线前仍需要在目标预发环境执行一次相同脚本并归档结果。
